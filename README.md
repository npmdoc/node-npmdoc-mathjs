# api documentation for  [mathjs (v3.11.2)](http://mathjs.org)  [![npm package](https://img.shields.io/npm/v/npmdoc-mathjs.svg?style=flat-square)](https://www.npmjs.org/package/npmdoc-mathjs) [![travis-ci.org build-status](https://api.travis-ci.org/npmdoc/node-npmdoc-mathjs.svg)](https://travis-ci.org/npmdoc/node-npmdoc-mathjs)
#### Math.js is an extensive math library for JavaScript and Node.js. It features a flexible expression parser with support for symbolic computation, comes with a large set of built-in functions and constants, and offers an integrated solution to work with dif

[![NPM](https://nodei.co/npm/mathjs.png?downloads=true)](https://www.npmjs.com/package/mathjs)

[![apidoc](https://npmdoc.github.io/node-npmdoc-mathjs/build/screenCapture.buildNpmdoc.browser._2Fhome_2Ftravis_2Fbuild_2Fnpmdoc_2Fnode-npmdoc-mathjs_2Ftmp_2Fbuild_2Fapidoc.html.png)](https://npmdoc.github.io/node-npmdoc-mathjs/build..beta..travis-ci.org/apidoc.html)

![npmPackageListing](https://npmdoc.github.io/node-npmdoc-mathjs/build/screenCapture.npmPackageListing.svg)

![npmPackageDependencyTree](https://npmdoc.github.io/node-npmdoc-mathjs/build/screenCapture.npmPackageDependencyTree.svg)



# package.json

```json

{
    "author": {
        "name": "Jos de Jong",
        "email": "wjosdejong@gmail.com",
        "url": "https://github.com/josdejong"
    },
    "bin": {
        "mathjs": "./bin/cli.js"
    },
    "bugs": {
        "url": "https://github.com/josdejong/mathjs/issues"
    },
    "contributors": [
        {
            "name": "Adam Kelly",
            "url": "https://github.com/adamisntdead"
        },
        {
            "name": "Andy Pan",
            "url": "https://github.com/andy0130tw"
        },
        {
            "name": "Bart Kiers",
            "url": "https://github.com/bkiers"
        },
        {
            "name": "Brett Jurgens",
            "url": "https://github.com/brettjurgens"
        },
        {
            "name": "Bryan Cuccioli",
            "url": "https://github.com/bcuccioli"
        },
        {
            "name": "Daniel Levin",
            "url": "https://github.com/daniel-levin"
        },
        {
            "name": "David Simons",
            "url": "https://github.com/SwamWithTurtles"
        },
        {
            "name": "Devan Patel",
            "url": "https://github.com/devanp92"
        },
        {
            "name": "devdevdata222",
            "url": "https://github.com/devdevdata222"
        },
        {
            "name": "Dzmitry Litskalau",
            "url": "https://github.com/litmit"
        },
        {
            "name": "Ekin Koc",
            "url": "https://github.com/eknkc"
        },
        {
            "name": "Elijah Insua",
            "url": "https://github.com/tmpvar"
        },
        {
            "name": "Eric Mansfield",
            "url": "https://github.com/ericman314"
        },
        {
            "name": "Fabrice Colas",
            "url": "https://github.com/fabricecolas"
        },
        {
            "name": "Favian Contreras",
            "url": "https://github.com/BigFav"
        },
        {
            "name": "Finn Pauls",
            "url": "https://github.com/finnp"
        },
        {
            "name": "Ganga Christopher",
            "url": "https://github.com/gangachris"
        },
        {
            "name": "Harry Sarson",
            "url": "https://github.com/HarrySarson"
        },
        {
            "name": "Holman Gao",
            "url": "https://github.com/golmansax"
        },
        {
            "name": "Guillermo Indalecio Fernández",
            "url": "https://github.com/guillermobox"
        },
        {
            "name": "hamadu",
            "url": "https://github.com/hamadu"
        },
        {
            "name": "Harsh Gupta",
            "url": "https://github.com/hgupta9"
        },
        {
            "name": "Huseyn Guliyev",
            "url": "https://github.com/husayt"
        },
        {
            "name": "Jim Garrison",
            "url": "https://github.com/garrison"
        },
        {
            "name": "Kenan Yildirim",
            "url": "https://github.com/KenanY"
        },
        {
            "name": "Keunhong Park",
            "url": "https://github.com/void42"
        },
        {
            "name": "Kunal Vyas",
            "url": "https://github.com/kv-kunalvyas"
        },
        {
            "name": "Marcus Andre",
            "url": "https://github.com/marcusandre"
        },
        {
            "name": "Horatiu Lazu",
            "url": "https://github.com/MathBunny"
        },
        {
            "name": "Mathias Polligkeit",
            "url": "https://github.com/woylie"
        },
        {
            "name": "Max Bruckner",
            "url": "https://github.com/FSMaxB"
        },
        {
            "name": "Maxence Dalmais",
            "url": "https://github.com/maxired"
        },
        {
            "name": "Mitchel Kelonye",
            "url": "https://github.com/kelonye"
        },
        {
            "name": "mtraynham",
            "url": "https://github.com/mtraynham"
        },
        {
            "name": "Niels Heisterkamp",
            "url": "https://github.com/nheisterkamp"
        },
        {
            "name": "Owen Versteeg",
            "url": "https://github.com/owenversteeg"
        },
        {
            "name": "Pavel Panchekha",
            "url": "https://github.com/pavpanchekha"
        },
        {
            "name": "Pat Grasso",
            "url": "https://github.com/patgrasso"
        },
        {
            "name": "Pete Corey",
            "url": "https://github.com/pcorey"
        },
        {
            "name": "Retsam",
            "url": "https://github.com/Retsam"
        },
        {
            "name": "Robert Eisele",
            "url": "https://github.com/infusion"
        },
        {
            "name": "Rogelio J. Baucells",
            "url": "https://github.com/rjbaucells"
        },
        {
            "name": "Ryan Seys",
            "url": "https://github.com/ryanseys"
        },
        {
            "name": "Sebastien Piquemal",
            "url": "https://github.com/sebpiq"
        },
        {
            "name": "Sebastian Ruhleder",
            "url": "https://github.com/ruhleder"
        },
        {
            "name": "Sergey Romanov",
            "url": "https://github.com/saromanov"
        },
        {
            "name": "Stu Blair",
            "url": "https://github.com/stu-blair"
        },
        {
            "name": "Thomas Brierley",
            "url": "https://github.com/ThomasBrierley"
        },
        {
            "name": "Todd Morse",
            "url": "https://github.com/morsecodist"
        },
        {
            "name": "Waldir Pimenta",
            "url": "https://github.com/waldyrious"
        },
        {
            "name": "Zach Zibrat",
            "url": "https://github.com/palimpsests"
        }
    ],
    "dependencies": {
        "complex.js": "2.0.1",
        "decimal.js": "7.1.1",
        "fraction.js": "4.0.0",
        "seed-random": "2.2.0",
        "tiny-emitter": "1.0.2",
        "typed-function": "0.10.5"
    },
    "description": "Math.js is an extensive math library for JavaScript and Node.js. It features a flexible expression parser with support for symbolic computation, comes with a large set of built-in functions and constants, and offers an integrated solution to work with dif",
    "devDependencies": {
        "glob": "7.1.1",
        "gulp": "3.9.1",
        "gulp-util": "3.0.8",
        "istanbul": "0.4.5",
        "mkdirp": "0.5.1",
        "mocha": "3.2.0",
        "q": "1.4.1",
        "tar": "2.2.1",
        "uglify-js": "2.7.5",
        "underscore": "1.8.3",
        "webpack": "2.2.1"
    },
    "directories": {},
    "dist": {
        "shasum": "d2ca723d7e9254cc58d1421a5a61d8cfac8ff642",
        "tarball": "https://registry.npmjs.org/mathjs/-/mathjs-3.11.2.tgz"
    },
    "engines": {
        "node": ">= 0.1"
    },
    "gitHead": "6bd493fc6787b5a317ffafdd1b3afc1af752c23a",
    "homepage": "http://mathjs.org",
    "keywords": [
        "math",
        "mathematics",
        "functions",
        "numeric",
        "algebra",
        "parser",
        "expression",
        "number",
        "bignumber",
        "complex",
        "fraction",
        "matrix",
        "unit"
    ],
    "license": "Apache-2.0",
    "main": "./index",
    "maintainers": [
        {
            "name": "josdejong",
            "email": "wjosdejong@gmail.com"
        }
    ],
    "name": "mathjs",
    "optionalDependencies": {},
    "readme": "ERROR: No README data found!",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/josdejong/mathjs.git"
    },
    "scripts": {
        "build": "gulp",
        "coverage": "istanbul cover _mocha -- test --recursive; echo \"\nCoverage report is available at ./coverage/lcov-report/index.html\"",
        "docs": "gulp docs",
        "test": "mocha test --recursive",
        "watch": "gulp watch"
    },
    "version": "3.11.2"
}
```



# <a name="apidoc.tableOfContents"></a>[table of contents](#apidoc.tableOfContents)

#### [module mathjs](#apidoc.module.mathjs)
1.  boolean <span class="apidocSignatureSpan">mathjs.</span>false
1.  boolean <span class="apidocSignatureSpan">mathjs.</span>true
1.  [function <span class="apidocSignatureSpan">mathjs.</span>abs (arg0)](#apidoc.element.mathjs.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>acos (arg0)](#apidoc.element.mathjs.acos)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>acosh (arg0)](#apidoc.element.mathjs.acosh)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>acot (arg0)](#apidoc.element.mathjs.acot)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>acoth (arg0)](#apidoc.element.mathjs.acoth)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>acsc (arg0)](#apidoc.element.mathjs.acsc)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>acsch (arg0)](#apidoc.element.mathjs.acsch)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>add (arg0, arg1, arg2)](#apidoc.element.mathjs.add)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>and (arg0, arg1)](#apidoc.element.mathjs.and)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>arg (arg0)](#apidoc.element.mathjs.arg)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>asec (arg0)](#apidoc.element.mathjs.asec)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>asech (arg0)](#apidoc.element.mathjs.asech)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>asin (arg0)](#apidoc.element.mathjs.asin)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>asinh (arg0)](#apidoc.element.mathjs.asinh)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>atan (arg0)](#apidoc.element.mathjs.atan)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>atan2 (arg0, arg1)](#apidoc.element.mathjs.atan2)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>atanh (arg0)](#apidoc.element.mathjs.atanh)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>bellNumbers (arg0)](#apidoc.element.mathjs.bellNumbers)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>bignumber (arg0)](#apidoc.element.mathjs.bignumber)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>bitAnd (arg0, arg1)](#apidoc.element.mathjs.bitAnd)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>bitNot (arg0)](#apidoc.element.mathjs.bitNot)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>bitOr (arg0, arg1)](#apidoc.element.mathjs.bitOr)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>bitXor (arg0, arg1)](#apidoc.element.mathjs.bitXor)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>boolean (arg0)](#apidoc.element.mathjs.boolean)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>catalan (arg0)](#apidoc.element.mathjs.catalan)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>cbrt (arg0, arg1)](#apidoc.element.mathjs.cbrt)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>ceil (arg0)](#apidoc.element.mathjs.ceil)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>chain (arg0)](#apidoc.element.mathjs.chain)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>clone (arg0)](#apidoc.element.mathjs.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>combinations (arg0, arg1)](#apidoc.element.mathjs.combinations)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>compare (arg0, arg1)](#apidoc.element.mathjs.compare)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>compile (arg0)](#apidoc.element.mathjs.compile)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>complex (arg0, arg1)](#apidoc.element.mathjs.complex)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>composition (arg0, arg1)](#apidoc.element.mathjs.composition)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>concat (arg0)](#apidoc.element.mathjs.concat)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>config (options)](#apidoc.element.mathjs.config)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>conj (arg0)](#apidoc.element.mathjs.conj)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>cos (arg0)](#apidoc.element.mathjs.cos)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>cosh (arg0)](#apidoc.element.mathjs.cosh)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>cot (arg0)](#apidoc.element.mathjs.cot)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>coth (arg0)](#apidoc.element.mathjs.coth)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>create (config)](#apidoc.element.mathjs.create)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>createUnit (arg0, arg1, arg2)](#apidoc.element.mathjs.createUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>cross (arg0, arg1)](#apidoc.element.mathjs.cross)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>csc (arg0)](#apidoc.element.mathjs.csc)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>csch (arg0)](#apidoc.element.mathjs.csch)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>cube (arg0)](#apidoc.element.mathjs.cube)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>deepEqual (arg0, arg1)](#apidoc.element.mathjs.deepEqual)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>derivative (arg0, arg1, arg2)](#apidoc.element.mathjs.derivative)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>det (arg0)](#apidoc.element.mathjs.det)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>diag (arg0, arg1, arg2)](#apidoc.element.mathjs.diag)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>distance (arg0, arg1, arg2)](#apidoc.element.mathjs.distance)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>divide (arg0, arg1)](#apidoc.element.mathjs.divide)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>dot (arg0, arg1)](#apidoc.element.mathjs.dot)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>dotDivide (arg0, arg1)](#apidoc.element.mathjs.dotDivide)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>dotMultiply (arg0, arg1)](#apidoc.element.mathjs.dotMultiply)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>dotPow (arg0, arg1)](#apidoc.element.mathjs.dotPow)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>emit ()](#apidoc.element.mathjs.emit)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>equal (arg0, arg1)](#apidoc.element.mathjs.equal)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>erf (arg0)](#apidoc.element.mathjs.erf)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>error.ArgumentsError (fn, count, min, max)](#apidoc.element.mathjs.error.ArgumentsError)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>error.ArgumentsError.prototype.constructor ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>error.DimensionError (actual, expected, relation)](#apidoc.element.mathjs.error.DimensionError)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>error.IndexError (index, min, max)](#apidoc.element.mathjs.error.IndexError)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>eval (arg0, arg1)](#apidoc.element.mathjs.eval)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>exp (arg0)](#apidoc.element.mathjs.exp)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>expression.Parser ()](#apidoc.element.mathjs.expression.Parser)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>expression.parse (expr, options)](#apidoc.element.mathjs.expression.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>eye (arg0, arg1, arg2)](#apidoc.element.mathjs.eye)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>factorial (arg0)](#apidoc.element.mathjs.factorial)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>filter (arg0, arg1)](#apidoc.element.mathjs.filter)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>fix (arg0)](#apidoc.element.mathjs.fix)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>flatten (arg0)](#apidoc.element.mathjs.flatten)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>floor (arg0)](#apidoc.element.mathjs.floor)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>forEach (arg0, arg1)](#apidoc.element.mathjs.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>format (arg0, arg1)](#apidoc.element.mathjs.format)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>fraction (arg0, arg1)](#apidoc.element.mathjs.fraction)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>gamma (arg0)](#apidoc.element.mathjs.gamma)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>gcd (arg0, arg1, arg2)](#apidoc.element.mathjs.gcd)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>help (arg0)](#apidoc.element.mathjs.help)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>hypot (arg0)](#apidoc.element.mathjs.hypot)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>im (arg0)](#apidoc.element.mathjs.im)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>import (object, options)](#apidoc.element.mathjs.import)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>index (arg0)](#apidoc.element.mathjs.index)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>intersect (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.intersect)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>inv (arg0)](#apidoc.element.mathjs.inv)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isInteger (arg0)](#apidoc.element.mathjs.isInteger)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isNaN (arg0)](#apidoc.element.mathjs.isNaN)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isNegative (arg0)](#apidoc.element.mathjs.isNegative)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isNumeric (arg0)](#apidoc.element.mathjs.isNumeric)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isPositive (arg0)](#apidoc.element.mathjs.isPositive)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isPrime (arg0)](#apidoc.element.mathjs.isPrime)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>isZero (arg0)](#apidoc.element.mathjs.isZero)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>kldivergence (arg0, arg1)](#apidoc.element.mathjs.kldivergence)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>kron (arg0, arg1)](#apidoc.element.mathjs.kron)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>larger (arg0, arg1)](#apidoc.element.mathjs.larger)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>largerEq (arg0, arg1)](#apidoc.element.mathjs.largerEq)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>lcm (arg0, arg1, arg2)](#apidoc.element.mathjs.lcm)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>leftShift (arg0, arg1)](#apidoc.element.mathjs.leftShift)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>log (arg0, arg1)](#apidoc.element.mathjs.log)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>log10 (arg0)](#apidoc.element.mathjs.log10)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>lsolve (arg0, arg1)](#apidoc.element.mathjs.lsolve)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>lup (arg0)](#apidoc.element.mathjs.lup)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>lusolve (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.lusolve)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>mad (arg0)](#apidoc.element.mathjs.mad)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>map (arg0, arg1)](#apidoc.element.mathjs.map)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>matrix (arg0, arg1, arg2)](#apidoc.element.mathjs.matrix)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>max (arg0, arg1)](#apidoc.element.mathjs.max)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>mean (arg0, arg1)](#apidoc.element.mathjs.mean)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>median (arg0, arg1)](#apidoc.element.mathjs.median)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>min (arg0, arg1)](#apidoc.element.mathjs.min)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>mod (arg0, arg1)](#apidoc.element.mathjs.mod)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>mode (arg0)](#apidoc.element.mathjs.mode)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>multinomial (arg0)](#apidoc.element.mathjs.multinomial)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>multiply (arg0, arg1, arg2)](#apidoc.element.mathjs.multiply)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>norm (arg0, arg1)](#apidoc.element.mathjs.norm)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>not (arg0)](#apidoc.element.mathjs.not)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>nthRoot (arg0, arg1)](#apidoc.element.mathjs.nthRoot)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>number (arg0, arg1)](#apidoc.element.mathjs.number)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>off ()](#apidoc.element.mathjs.off)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>on ()](#apidoc.element.mathjs.on)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>once ()](#apidoc.element.mathjs.once)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>ones (arg0, arg1)](#apidoc.element.mathjs.ones)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>or (arg0, arg1)](#apidoc.element.mathjs.or)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>parse (arg0, arg1)](#apidoc.element.mathjs.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>parser ()](#apidoc.element.mathjs.parser)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>partitionSelect (arg0, arg1, arg2)](#apidoc.element.mathjs.partitionSelect)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>permutations (arg0, arg1)](#apidoc.element.mathjs.permutations)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>pow (arg0, arg1)](#apidoc.element.mathjs.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>print (arg0, arg1, arg2)](#apidoc.element.mathjs.print)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>prod (arg0, arg1)](#apidoc.element.mathjs.prod)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>quantileSeq (data, probOrN, sorted)](#apidoc.element.mathjs.quantileSeq)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>range (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.range)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>re (arg0)](#apidoc.element.mathjs.re)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>reshape (arg0, arg1)](#apidoc.element.mathjs.reshape)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>resize (x, size, defaultValue)](#apidoc.element.mathjs.resize)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>rightArithShift (arg0, arg1)](#apidoc.element.mathjs.rightArithShift)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>rightLogShift (arg0, arg1)](#apidoc.element.mathjs.rightLogShift)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>round (arg0, arg1)](#apidoc.element.mathjs.round)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sec (arg0)](#apidoc.element.mathjs.sec)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sech (arg0)](#apidoc.element.mathjs.sech)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sign (arg0)](#apidoc.element.mathjs.sign)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>simplify (arg0, arg1)](#apidoc.element.mathjs.simplify)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sin (arg0)](#apidoc.element.mathjs.sin)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sinh (arg0)](#apidoc.element.mathjs.sinh)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>size (arg0)](#apidoc.element.mathjs.size)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>slu (arg0, arg1, arg2)](#apidoc.element.mathjs.slu)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>smaller (arg0, arg1)](#apidoc.element.mathjs.smaller)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>smallerEq (arg0, arg1)](#apidoc.element.mathjs.smallerEq)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sort (arg0, arg1)](#apidoc.element.mathjs.sort)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sparse (arg0, arg1)](#apidoc.element.mathjs.sparse)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>splitUnit (arg0, arg1)](#apidoc.element.mathjs.splitUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sqrt (arg0)](#apidoc.element.mathjs.sqrt)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>square (arg0)](#apidoc.element.mathjs.square)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>squeeze (arg0)](#apidoc.element.mathjs.squeeze)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>std (arg0, arg1)](#apidoc.element.mathjs.std)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>stirlingS2 (arg0, arg1)](#apidoc.element.mathjs.stirlingS2)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>string (arg0)](#apidoc.element.mathjs.string)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>subset (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.subset)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>subtract (arg0, arg1)](#apidoc.element.mathjs.subtract)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>sum (arg0, arg1)](#apidoc.element.mathjs.sum)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>tan (arg0)](#apidoc.element.mathjs.tan)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>tanh (arg0)](#apidoc.element.mathjs.tanh)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>to (arg0, arg1)](#apidoc.element.mathjs.to)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>trace (arg0)](#apidoc.element.mathjs.trace)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>transpose (arg0)](#apidoc.element.mathjs.transpose)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.BigNumber (v)](#apidoc.element.mathjs.type.BigNumber)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Chain (value)](#apidoc.element.mathjs.type.Chain)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Complex (a, b)](#apidoc.element.mathjs.type.Complex)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.DenseMatrix (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.FibonacciHeap ()](#apidoc.element.mathjs.type.FibonacciHeap)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Fraction (a, b)](#apidoc.element.mathjs.type.Fraction)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Help (doc)](#apidoc.element.mathjs.type.Help)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.ImmutableDenseMatrix (data, datatype)](#apidoc.element.mathjs.type.ImmutableDenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Index (ranges)](#apidoc.element.mathjs.type.Index)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Matrix ()](#apidoc.element.mathjs.type.Matrix)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Range (start, end, step)](#apidoc.element.mathjs.type.Range)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.ResultSet (entries)](#apidoc.element.mathjs.type.ResultSet)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Spa ()](#apidoc.element.mathjs.type.Spa)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.SparseMatrix (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>type.Unit (value, name)](#apidoc.element.mathjs.type.Unit)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>typed (arg0, arg1)](#apidoc.element.mathjs.typed)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>typeof (arg0)](#apidoc.element.mathjs.typeof)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>unaryMinus (arg0)](#apidoc.element.mathjs.unaryMinus)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>unaryPlus (arg0)](#apidoc.element.mathjs.unaryPlus)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>unequal (arg0, arg1)](#apidoc.element.mathjs.unequal)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>unit (arg0, arg1)](#apidoc.element.mathjs.unit)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>usolve (arg0, arg1)](#apidoc.element.mathjs.usolve)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>var (arg0, arg1)](#apidoc.element.mathjs.var)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>xgcd (arg0, arg1)](#apidoc.element.mathjs.xgcd)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>xor (arg0, arg1)](#apidoc.element.mathjs.xor)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>zeros (arg0, arg1)](#apidoc.element.mathjs.zeros)
1.  number <span class="apidocSignatureSpan">mathjs.</span>E
1.  number <span class="apidocSignatureSpan">mathjs.</span>Infinity
1.  number <span class="apidocSignatureSpan">mathjs.</span>LN10
1.  number <span class="apidocSignatureSpan">mathjs.</span>LN2
1.  number <span class="apidocSignatureSpan">mathjs.</span>LOG10E
1.  number <span class="apidocSignatureSpan">mathjs.</span>LOG2E
1.  number <span class="apidocSignatureSpan">mathjs.</span>NaN
1.  number <span class="apidocSignatureSpan">mathjs.</span>PI
1.  number <span class="apidocSignatureSpan">mathjs.</span>SQRT1_2
1.  number <span class="apidocSignatureSpan">mathjs.</span>SQRT2
1.  number <span class="apidocSignatureSpan">mathjs.</span>e
1.  number <span class="apidocSignatureSpan">mathjs.</span>efimovFactor
1.  number <span class="apidocSignatureSpan">mathjs.</span>fineStructure
1.  number <span class="apidocSignatureSpan">mathjs.</span>phi
1.  number <span class="apidocSignatureSpan">mathjs.</span>pi
1.  number <span class="apidocSignatureSpan">mathjs.</span>sackurTetrode
1.  number <span class="apidocSignatureSpan">mathjs.</span>tau
1.  number <span class="apidocSignatureSpan">mathjs.</span>weakMixingAngle
1.  object <span class="apidocSignatureSpan">mathjs.</span>algebra
1.  object <span class="apidocSignatureSpan">mathjs.</span>atomicMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>avogadro
1.  object <span class="apidocSignatureSpan">mathjs.</span>bohrMagneton
1.  object <span class="apidocSignatureSpan">mathjs.</span>bohrRadius
1.  object <span class="apidocSignatureSpan">mathjs.</span>boltzmann
1.  object <span class="apidocSignatureSpan">mathjs.</span>classicalElectronRadius
1.  object <span class="apidocSignatureSpan">mathjs.</span>conductanceQuantum
1.  object <span class="apidocSignatureSpan">mathjs.</span>constants
1.  object <span class="apidocSignatureSpan">mathjs.</span>core
1.  object <span class="apidocSignatureSpan">mathjs.</span>coulomb
1.  object <span class="apidocSignatureSpan">mathjs.</span>derivative.signatures
1.  object <span class="apidocSignatureSpan">mathjs.</span>deuteronMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>electricConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>electronMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>elementaryCharge
1.  object <span class="apidocSignatureSpan">mathjs.</span>error
1.  object <span class="apidocSignatureSpan">mathjs.</span>error.ArgumentsError.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>error.DimensionError.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>error.IndexError.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.Parser.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.AccessorNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.ArrayNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.AssignmentNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.BlockNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.ConditionalNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.ConstantNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.FunctionAssignmentNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.FunctionNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.IndexNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.Node.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.ObjectNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.OperatorNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.ParenthesisNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.RangeNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.node.SymbolNode.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>expression.transform
1.  object <span class="apidocSignatureSpan">mathjs.</span>faraday
1.  object <span class="apidocSignatureSpan">mathjs.</span>fermiCoupling
1.  object <span class="apidocSignatureSpan">mathjs.</span>firstRadiation
1.  object <span class="apidocSignatureSpan">mathjs.</span>gasConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>gravitationConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>gravity
1.  object <span class="apidocSignatureSpan">mathjs.</span>hartreeEnergy
1.  object <span class="apidocSignatureSpan">mathjs.</span>i
1.  object <span class="apidocSignatureSpan">mathjs.</span>inverseConductanceQuantum
1.  object <span class="apidocSignatureSpan">mathjs.</span>json
1.  object <span class="apidocSignatureSpan">mathjs.</span>klitzing
1.  object <span class="apidocSignatureSpan">mathjs.</span>loschmidt
1.  object <span class="apidocSignatureSpan">mathjs.</span>magneticConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>magneticFluxQuantum
1.  object <span class="apidocSignatureSpan">mathjs.</span>molarMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>molarMassC12
1.  object <span class="apidocSignatureSpan">mathjs.</span>molarPlanckConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>molarVolume
1.  object <span class="apidocSignatureSpan">mathjs.</span>neutronMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>nuclearMagneton
1.  object <span class="apidocSignatureSpan">mathjs.</span>null
1.  object <span class="apidocSignatureSpan">mathjs.</span>planckCharge
1.  object <span class="apidocSignatureSpan">mathjs.</span>planckConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>planckLength
1.  object <span class="apidocSignatureSpan">mathjs.</span>planckMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>planckTemperature
1.  object <span class="apidocSignatureSpan">mathjs.</span>planckTime
1.  object <span class="apidocSignatureSpan">mathjs.</span>protonMass
1.  object <span class="apidocSignatureSpan">mathjs.</span>quantumOfCirculation
1.  object <span class="apidocSignatureSpan">mathjs.</span>reducedPlanckConstant
1.  object <span class="apidocSignatureSpan">mathjs.</span>rydberg
1.  object <span class="apidocSignatureSpan">mathjs.</span>secondRadiation
1.  object <span class="apidocSignatureSpan">mathjs.</span>speedOfLight
1.  object <span class="apidocSignatureSpan">mathjs.</span>stefanBoltzmann
1.  object <span class="apidocSignatureSpan">mathjs.</span>thomsonCrossSection
1.  object <span class="apidocSignatureSpan">mathjs.</span>type
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.BigNumber.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Chain.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Complex.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.DenseMatrix.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.FibonacciHeap.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Fraction.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Help.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.ImmutableDenseMatrix.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Index.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Matrix.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Range.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.ResultSet.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Spa.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.SparseMatrix.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>type.Unit.prototype
1.  object <span class="apidocSignatureSpan">mathjs.</span>typed.signatures
1.  object <span class="apidocSignatureSpan">mathjs.</span>uninitialized
1.  object <span class="apidocSignatureSpan">mathjs.</span>vacuumImpedance
1.  object <span class="apidocSignatureSpan">mathjs.</span>wienDisplacement
1.  string <span class="apidocSignatureSpan">mathjs.</span>version

#### [module mathjs.constants](#apidoc.module.mathjs.constants)
1.  boolean <span class="apidocSignatureSpan">mathjs.constants.</span>lazy
1.  boolean <span class="apidocSignatureSpan">mathjs.constants.</span>math
1.  [function <span class="apidocSignatureSpan">mathjs.constants.</span>factory (type, config, load, typed, math)](#apidoc.element.mathjs.constants.factory)

#### [module mathjs.core](#apidoc.module.mathjs.core)
1.  [function <span class="apidocSignatureSpan">mathjs.core.</span>create (options)](#apidoc.element.mathjs.core.create)

#### [module mathjs.derivative](#apidoc.module.mathjs.derivative)
1.  boolean <span class="apidocSignatureSpan">mathjs.derivative.</span>_simplify
1.  [function <span class="apidocSignatureSpan">mathjs.</span>derivative (arg0, arg1, arg2)](#apidoc.element.mathjs.derivative.derivative)
1.  [function <span class="apidocSignatureSpan">mathjs.derivative.</span>toTex (deriv)](#apidoc.element.mathjs.derivative.toTex)
1.  object <span class="apidocSignatureSpan">mathjs.derivative.</span>signatures

#### [module mathjs.derivative.signatures](#apidoc.module.mathjs.derivative.signatures)

#### [module mathjs.error](#apidoc.module.mathjs.error)
1.  [function <span class="apidocSignatureSpan">mathjs.error.</span>ArgumentsError (fn, count, min, max)](#apidoc.element.mathjs.error.ArgumentsError)
1.  [function <span class="apidocSignatureSpan">mathjs.error.</span>DimensionError (actual, expected, relation)](#apidoc.element.mathjs.error.DimensionError)
1.  [function <span class="apidocSignatureSpan">mathjs.error.</span>IndexError (index, min, max)](#apidoc.element.mathjs.error.IndexError)

#### [module mathjs.error.ArgumentsError](#apidoc.module.mathjs.error.ArgumentsError)
1.  [function <span class="apidocSignatureSpan">mathjs.error.</span>ArgumentsError (fn, count, min, max)](#apidoc.element.mathjs.error.ArgumentsError.ArgumentsError)

#### [module mathjs.error.ArgumentsError.prototype](#apidoc.module.mathjs.error.ArgumentsError.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.</span>isArgumentsError
1.  [function <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor)
1.  string <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.</span>name

#### [module mathjs.error.ArgumentsError.prototype.constructor](#apidoc.module.mathjs.error.ArgumentsError.prototype.constructor)
1.  [function <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor.constructor)
1.  [function <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.constructor.</span>captureStackTrace ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor.captureStackTrace)
1.  number <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.constructor.</span>stackTraceLimit

#### [module mathjs.error.DimensionError](#apidoc.module.mathjs.error.DimensionError)
1.  [function <span class="apidocSignatureSpan">mathjs.error.</span>DimensionError (actual, expected, relation)](#apidoc.element.mathjs.error.DimensionError.DimensionError)

#### [module mathjs.error.DimensionError.prototype](#apidoc.module.mathjs.error.DimensionError.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.error.DimensionError.prototype.</span>isDimensionError
1.  [function <span class="apidocSignatureSpan">mathjs.error.DimensionError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.DimensionError.prototype.constructor)
1.  string <span class="apidocSignatureSpan">mathjs.error.DimensionError.prototype.</span>name

#### [module mathjs.error.IndexError](#apidoc.module.mathjs.error.IndexError)
1.  [function <span class="apidocSignatureSpan">mathjs.error.</span>IndexError (index, min, max)](#apidoc.element.mathjs.error.IndexError.IndexError)

#### [module mathjs.error.IndexError.prototype](#apidoc.module.mathjs.error.IndexError.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.error.IndexError.prototype.</span>isIndexError
1.  [function <span class="apidocSignatureSpan">mathjs.error.IndexError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.IndexError.prototype.constructor)
1.  string <span class="apidocSignatureSpan">mathjs.error.IndexError.prototype.</span>name

#### [module mathjs.expression](#apidoc.module.mathjs.expression)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.</span>Parser ()](#apidoc.element.mathjs.expression.Parser)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.</span>parse (expr, options)](#apidoc.element.mathjs.expression.parse)
1.  object <span class="apidocSignatureSpan">mathjs.expression.</span>docs
1.  object <span class="apidocSignatureSpan">mathjs.expression.</span>node
1.  object <span class="apidocSignatureSpan">mathjs.expression.</span>transform

#### [module mathjs.expression.Parser](#apidoc.module.mathjs.expression.Parser)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.</span>Parser ()](#apidoc.element.mathjs.expression.Parser.Parser)

#### [module mathjs.expression.Parser.prototype](#apidoc.module.mathjs.expression.Parser.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>isParser
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>clear ()](#apidoc.element.mathjs.expression.Parser.prototype.clear)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>compile (expr)](#apidoc.element.mathjs.expression.Parser.prototype.compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>eval (expr)](#apidoc.element.mathjs.expression.Parser.prototype.eval)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>get (name)](#apidoc.element.mathjs.expression.Parser.prototype.get)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>getAll ()](#apidoc.element.mathjs.expression.Parser.prototype.getAll)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>parse (expr)](#apidoc.element.mathjs.expression.Parser.prototype.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>remove (name)](#apidoc.element.mathjs.expression.Parser.prototype.remove)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>set (name, value)](#apidoc.element.mathjs.expression.Parser.prototype.set)
1.  string <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>type

#### [module mathjs.expression.node](#apidoc.module.mathjs.expression.node)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>AccessorNode (object, index)](#apidoc.element.mathjs.expression.node.AccessorNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ArrayNode (items)](#apidoc.element.mathjs.expression.node.ArrayNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>AssignmentNode (object, index, value)](#apidoc.element.mathjs.expression.node.AssignmentNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>BlockNode (blocks)](#apidoc.element.mathjs.expression.node.BlockNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ConditionalNode (condition, trueExpr, falseExpr)](#apidoc.element.mathjs.expression.node.ConditionalNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ConstantNode (value, valueType)](#apidoc.element.mathjs.expression.node.ConstantNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>FunctionAssignmentNode (name, params, expr)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>FunctionNode (fn, args)](#apidoc.element.mathjs.expression.node.FunctionNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>IndexNode (dimensions, dotNotation)](#apidoc.element.mathjs.expression.node.IndexNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>Node ()](#apidoc.element.mathjs.expression.node.Node)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ObjectNode (properties)](#apidoc.element.mathjs.expression.node.ObjectNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>OperatorNode (op, fn, args, implicit)](#apidoc.element.mathjs.expression.node.OperatorNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ParenthesisNode (content)](#apidoc.element.mathjs.expression.node.ParenthesisNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>RangeNode (start, end, step)](#apidoc.element.mathjs.expression.node.RangeNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>SymbolNode (name)](#apidoc.element.mathjs.expression.node.SymbolNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.</span>UpdateNode ()](#apidoc.element.mathjs.expression.node.UpdateNode)

#### [module mathjs.expression.node.AccessorNode.prototype](#apidoc.module.mathjs.expression.node.AccessorNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>isAccessorNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.AccessorNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>type

#### [module mathjs.expression.node.ArrayNode.prototype](#apidoc.module.mathjs.expression.node.ArrayNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>isArrayNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ArrayNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>type

#### [module mathjs.expression.node.AssignmentNode.prototype](#apidoc.module.mathjs.expression.node.AssignmentNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>isAssignmentNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>type

#### [module mathjs.expression.node.BlockNode.prototype](#apidoc.module.mathjs.expression.node.BlockNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>isBlockNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.BlockNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.BlockNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.BlockNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.BlockNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.BlockNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.BlockNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>type

#### [module mathjs.expression.node.ConditionalNode.prototype](#apidoc.module.mathjs.expression.node.ConditionalNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>isConditionalNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>type

#### [module mathjs.expression.node.ConstantNode.prototype](#apidoc.module.mathjs.expression.node.ConstantNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>isConstantNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ConstantNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>type

#### [module mathjs.expression.node.FunctionAssignmentNode.prototype](#apidoc.module.mathjs.expression.node.FunctionAssignmentNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>isFunctionAssignmentNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>type

#### [module mathjs.expression.node.FunctionNode.prototype](#apidoc.module.mathjs.expression.node.FunctionNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>isFunctionNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_compileScope (defs, args)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._compileScope)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_getUniqueArgumentsName (defs)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._getUniqueArgumentsName)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>getIdentifier ()](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.getIdentifier)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>toString (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>toTex (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.toTex)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>type

#### [module mathjs.expression.node.IndexNode.prototype](#apidoc.module.mathjs.expression.node.IndexNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>isIndexNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.IndexNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.IndexNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.IndexNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.IndexNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>getObjectProperty ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.getObjectProperty)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>isObjectProperty ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.isObjectProperty)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.IndexNode.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>needsSize ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.needsSize)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>type

#### [module mathjs.expression.node.Node.prototype](#apidoc.module.mathjs.expression.node.Node.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>isNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.Node.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_ifNode (node)](#apidoc.element.mathjs.expression.node.Node.prototype._ifNode)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_toString ()](#apidoc.element.mathjs.expression.node.Node.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.Node.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.Node.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>cloneDeep ()](#apidoc.element.mathjs.expression.node.Node.prototype.cloneDeep)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>compile ()](#apidoc.element.mathjs.expression.node.Node.prototype.compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>equals (other)](#apidoc.element.mathjs.expression.node.Node.prototype.equals)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>eval (scope)](#apidoc.element.mathjs.expression.node.Node.prototype.eval)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>filter (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.filter)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>find ()](#apidoc.element.mathjs.expression.node.Node.prototype.find)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>getContent ()](#apidoc.element.mathjs.expression.node.Node.prototype.getContent)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>getIdentifier ()](#apidoc.element.mathjs.expression.node.Node.prototype.getIdentifier)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>match ()](#apidoc.element.mathjs.expression.node.Node.prototype.match)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>toString (options)](#apidoc.element.mathjs.expression.node.Node.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>toTex (options)](#apidoc.element.mathjs.expression.node.Node.prototype.toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>transform (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.transform)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>traverse (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.traverse)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>comment
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>type

#### [module mathjs.expression.node.ObjectNode.prototype](#apidoc.module.mathjs.expression.node.ObjectNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>isObjectNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ObjectNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>type

#### [module mathjs.expression.node.OperatorNode.prototype](#apidoc.module.mathjs.expression.node.OperatorNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>isOperatorNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>getIdentifier ()](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.getIdentifier)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>type

#### [module mathjs.expression.node.ParenthesisNode.prototype](#apidoc.module.mathjs.expression.node.ParenthesisNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>isParenthesisNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>getContent ()](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.getContent)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>type

#### [module mathjs.expression.node.RangeNode.prototype](#apidoc.module.mathjs.expression.node.RangeNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>isRangeNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.RangeNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.RangeNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.RangeNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.RangeNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.RangeNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.RangeNode.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>needsEnd ()](#apidoc.element.mathjs.expression.node.RangeNode.prototype.needsEnd)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>type

#### [module mathjs.expression.node.SymbolNode.prototype](#apidoc.module.mathjs.expression.node.SymbolNode.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>isSymbolNode
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype._compile)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype._toString)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype._toTex)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.SymbolNode.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype.map)
1.  string <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>type

#### [module mathjs.expression.parse](#apidoc.module.mathjs.expression.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.</span>parse (expr, options)](#apidoc.element.mathjs.expression.parse.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isAlpha (c, cPrev, cNext)](#apidoc.element.mathjs.expression.parse.isAlpha)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isDecimalMark (c, cNext)](#apidoc.element.mathjs.expression.parse.isDecimalMark)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isDigit (c)](#apidoc.element.mathjs.expression.parse.isDigit)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isDigitDot (c)](#apidoc.element.mathjs.expression.parse.isDigitDot)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isValidLatinOrGreek (c)](#apidoc.element.mathjs.expression.parse.isValidLatinOrGreek)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isValidMathSymbol (high, low)](#apidoc.element.mathjs.expression.parse.isValidMathSymbol)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isWhitespace (c, nestingLevel)](#apidoc.element.mathjs.expression.parse.isWhitespace)

#### [module mathjs.expression.transform](#apidoc.module.mathjs.expression.transform)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>concat (arg0)](#apidoc.element.mathjs.expression.transform.concat)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>filter (args, math, scope)](#apidoc.element.mathjs.expression.transform.filter)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>forEach (arg0, arg1)](#apidoc.element.mathjs.expression.transform.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>index ()](#apidoc.element.mathjs.expression.transform.index)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>map (arg0, arg1)](#apidoc.element.mathjs.expression.transform.map)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>max (arg0)](#apidoc.element.mathjs.expression.transform.max)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>mean (arg0)](#apidoc.element.mathjs.expression.transform.mean)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>min (arg0)](#apidoc.element.mathjs.expression.transform.min)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>range (arg0)](#apidoc.element.mathjs.expression.transform.range)
1.  [function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>subset (arg0)](#apidoc.element.mathjs.expression.transform.subset)

#### [module mathjs.json](#apidoc.module.mathjs.json)
1.  [function <span class="apidocSignatureSpan">mathjs.json.</span>reviver (key, value)](#apidoc.element.mathjs.json.reviver)

#### [module mathjs.type](#apidoc.module.mathjs.type)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>BigNumber (v)](#apidoc.element.mathjs.type.BigNumber)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Chain (value)](#apidoc.element.mathjs.type.Chain)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Complex (a, b)](#apidoc.element.mathjs.type.Complex)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>DenseMatrix (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>FibonacciHeap ()](#apidoc.element.mathjs.type.FibonacciHeap)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Fraction (a, b)](#apidoc.element.mathjs.type.Fraction)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Help (doc)](#apidoc.element.mathjs.type.Help)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>ImmutableDenseMatrix (data, datatype)](#apidoc.element.mathjs.type.ImmutableDenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Index (ranges)](#apidoc.element.mathjs.type.Index)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Matrix ()](#apidoc.element.mathjs.type.Matrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Range (start, end, step)](#apidoc.element.mathjs.type.Range)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>ResultSet (entries)](#apidoc.element.mathjs.type.ResultSet)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Spa ()](#apidoc.element.mathjs.type.Spa)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>SparseMatrix (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Unit (value, name)](#apidoc.element.mathjs.type.Unit)

#### [module mathjs.type.BigNumber](#apidoc.module.mathjs.type.BigNumber)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>crypto
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>BigNumber (v)](#apidoc.element.mathjs.type.BigNumber.BigNumber)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>abs (x)](#apidoc.element.mathjs.type.BigNumber.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>acos (x)](#apidoc.element.mathjs.type.BigNumber.acos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>acosh (x)](#apidoc.element.mathjs.type.BigNumber.acosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>add (x, y)](#apidoc.element.mathjs.type.BigNumber.add)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>asin (x)](#apidoc.element.mathjs.type.BigNumber.asin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>asinh (x)](#apidoc.element.mathjs.type.BigNumber.asinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>atan (x)](#apidoc.element.mathjs.type.BigNumber.atan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>atan2 (y, x)](#apidoc.element.mathjs.type.BigNumber.atan2)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>atanh (x)](#apidoc.element.mathjs.type.BigNumber.atanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>cbrt (x)](#apidoc.element.mathjs.type.BigNumber.cbrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ceil (x)](#apidoc.element.mathjs.type.BigNumber.ceil)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>clone (obj)](#apidoc.element.mathjs.type.BigNumber.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>config (obj)](#apidoc.element.mathjs.type.BigNumber.config)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>cos (x)](#apidoc.element.mathjs.type.BigNumber.cos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>cosh (x)](#apidoc.element.mathjs.type.BigNumber.cosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>div (x, y)](#apidoc.element.mathjs.type.BigNumber.div)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>exp (x)](#apidoc.element.mathjs.type.BigNumber.exp)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>floor (x)](#apidoc.element.mathjs.type.BigNumber.floor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>fromJSON (json)](#apidoc.element.mathjs.type.BigNumber.fromJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>hypot ()](#apidoc.element.mathjs.type.BigNumber.hypot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ln (x)](#apidoc.element.mathjs.type.BigNumber.ln)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>log (x, y)](#apidoc.element.mathjs.type.BigNumber.log)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>log10 (x)](#apidoc.element.mathjs.type.BigNumber.log10)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>log2 (x)](#apidoc.element.mathjs.type.BigNumber.log2)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>max ()](#apidoc.element.mathjs.type.BigNumber.max)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>min ()](#apidoc.element.mathjs.type.BigNumber.min)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>mod (x, y)](#apidoc.element.mathjs.type.BigNumber.mod)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>mul (x, y)](#apidoc.element.mathjs.type.BigNumber.mul)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>pow (x, y)](#apidoc.element.mathjs.type.BigNumber.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>random (sd)](#apidoc.element.mathjs.type.BigNumber.random)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>round (x)](#apidoc.element.mathjs.type.BigNumber.round)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>set (obj)](#apidoc.element.mathjs.type.BigNumber.set)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sign (x)](#apidoc.element.mathjs.type.BigNumber.sign)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sin (x)](#apidoc.element.mathjs.type.BigNumber.sin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sinh (x)](#apidoc.element.mathjs.type.BigNumber.sinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sqrt (x)](#apidoc.element.mathjs.type.BigNumber.sqrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sub (x, y)](#apidoc.element.mathjs.type.BigNumber.sub)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>tan (x)](#apidoc.element.mathjs.type.BigNumber.tan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>tanh (x)](#apidoc.element.mathjs.type.BigNumber.tanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>trunc (x)](#apidoc.element.mathjs.type.BigNumber.trunc)
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>EUCLID
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_CEIL
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_DOWN
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_FLOOR
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_HALF_CEIL
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_HALF_DOWN
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_HALF_EVEN
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_HALF_FLOOR
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_HALF_UP
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ROUND_UP
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>maxE
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>minE
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>modulo
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>precision
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>rounding
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>toExpNeg
1.  number <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>toExpPos

#### [module mathjs.type.BigNumber.prototype](#apidoc.module.mathjs.type.BigNumber.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isBigNumber
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>abs ()](#apidoc.element.mathjs.type.BigNumber.prototype.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>absoluteValue ()](#apidoc.element.mathjs.type.BigNumber.prototype.absoluteValue)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>acos ()](#apidoc.element.mathjs.type.BigNumber.prototype.acos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>acosh ()](#apidoc.element.mathjs.type.BigNumber.prototype.acosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>add (y)](#apidoc.element.mathjs.type.BigNumber.prototype.add)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>asin ()](#apidoc.element.mathjs.type.BigNumber.prototype.asin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>asinh ()](#apidoc.element.mathjs.type.BigNumber.prototype.asinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>atan ()](#apidoc.element.mathjs.type.BigNumber.prototype.atan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>atanh ()](#apidoc.element.mathjs.type.BigNumber.prototype.atanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cbrt ()](#apidoc.element.mathjs.type.BigNumber.prototype.cbrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>ceil ()](#apidoc.element.mathjs.type.BigNumber.prototype.ceil)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cmp (y)](#apidoc.element.mathjs.type.BigNumber.prototype.cmp)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>comparedTo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.comparedTo)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cos ()](#apidoc.element.mathjs.type.BigNumber.prototype.cos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cosh ()](#apidoc.element.mathjs.type.BigNumber.prototype.cosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.cosine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cubeRoot ()](#apidoc.element.mathjs.type.BigNumber.prototype.cubeRoot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>decimalPlaces ()](#apidoc.element.mathjs.type.BigNumber.prototype.decimalPlaces)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>div (y)](#apidoc.element.mathjs.type.BigNumber.prototype.div)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>divToInt (y)](#apidoc.element.mathjs.type.BigNumber.prototype.divToInt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>dividedBy (y)](#apidoc.element.mathjs.type.BigNumber.prototype.dividedBy)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>dividedToIntegerBy (y)](#apidoc.element.mathjs.type.BigNumber.prototype.dividedToIntegerBy)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>dp ()](#apidoc.element.mathjs.type.BigNumber.prototype.dp)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>eq (y)](#apidoc.element.mathjs.type.BigNumber.prototype.eq)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>equals (y)](#apidoc.element.mathjs.type.BigNumber.prototype.equals)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>exp ()](#apidoc.element.mathjs.type.BigNumber.prototype.exp)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>floor ()](#apidoc.element.mathjs.type.BigNumber.prototype.floor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>greaterThan (y)](#apidoc.element.mathjs.type.BigNumber.prototype.greaterThan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>greaterThanOrEqualTo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.greaterThanOrEqualTo)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>gt (y)](#apidoc.element.mathjs.type.BigNumber.prototype.gt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>gte (y)](#apidoc.element.mathjs.type.BigNumber.prototype.gte)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>hyperbolicCosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicCosine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>hyperbolicSine ()](#apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicSine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>hyperbolicTangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicTangent)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseCosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseCosine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseHyperbolicCosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicCosine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseHyperbolicSine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicSine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseHyperbolicTangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicTangent)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseSine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseSine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseTangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseTangent)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isFinite ()](#apidoc.element.mathjs.type.BigNumber.prototype.isFinite)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isInt ()](#apidoc.element.mathjs.type.BigNumber.prototype.isInt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isInteger ()](#apidoc.element.mathjs.type.BigNumber.prototype.isInteger)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isNaN ()](#apidoc.element.mathjs.type.BigNumber.prototype.isNaN)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isNeg ()](#apidoc.element.mathjs.type.BigNumber.prototype.isNeg)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isNegative ()](#apidoc.element.mathjs.type.BigNumber.prototype.isNegative)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isPos ()](#apidoc.element.mathjs.type.BigNumber.prototype.isPos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isPositive ()](#apidoc.element.mathjs.type.BigNumber.prototype.isPositive)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isZero ()](#apidoc.element.mathjs.type.BigNumber.prototype.isZero)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lessThan (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lessThan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lessThanOrEqualTo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lessThanOrEqualTo)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>ln ()](#apidoc.element.mathjs.type.BigNumber.prototype.ln)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>log (base)](#apidoc.element.mathjs.type.BigNumber.prototype.log)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>logarithm (base)](#apidoc.element.mathjs.type.BigNumber.prototype.logarithm)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lt (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lte (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lte)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>minus (y)](#apidoc.element.mathjs.type.BigNumber.prototype.minus)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>mod (y)](#apidoc.element.mathjs.type.BigNumber.prototype.mod)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>modulo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.modulo)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>mul (y)](#apidoc.element.mathjs.type.BigNumber.prototype.mul)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>naturalExponential ()](#apidoc.element.mathjs.type.BigNumber.prototype.naturalExponential)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>naturalLogarithm ()](#apidoc.element.mathjs.type.BigNumber.prototype.naturalLogarithm)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>neg ()](#apidoc.element.mathjs.type.BigNumber.prototype.neg)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>negated ()](#apidoc.element.mathjs.type.BigNumber.prototype.negated)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>plus (y)](#apidoc.element.mathjs.type.BigNumber.prototype.plus)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>pow (y)](#apidoc.element.mathjs.type.BigNumber.prototype.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>precision (z)](#apidoc.element.mathjs.type.BigNumber.prototype.precision)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>round ()](#apidoc.element.mathjs.type.BigNumber.prototype.round)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sd (z)](#apidoc.element.mathjs.type.BigNumber.prototype.sd)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sin ()](#apidoc.element.mathjs.type.BigNumber.prototype.sin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sine ()](#apidoc.element.mathjs.type.BigNumber.prototype.sine)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sinh ()](#apidoc.element.mathjs.type.BigNumber.prototype.sinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sqrt ()](#apidoc.element.mathjs.type.BigNumber.prototype.sqrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>squareRoot ()](#apidoc.element.mathjs.type.BigNumber.prototype.squareRoot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sub (y)](#apidoc.element.mathjs.type.BigNumber.prototype.sub)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>tan ()](#apidoc.element.mathjs.type.BigNumber.prototype.tan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>tangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.tangent)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>tanh ()](#apidoc.element.mathjs.type.BigNumber.prototype.tanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>times (y)](#apidoc.element.mathjs.type.BigNumber.prototype.times)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toBinary (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toBinary)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toDP (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toDP)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toDecimalPlaces (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toDecimalPlaces)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toExponential (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toExponential)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toFixed (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toFixed)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toFraction (maxD)](#apidoc.element.mathjs.type.BigNumber.prototype.toFraction)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toHex (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toHex)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toHexadecimal (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toHexadecimal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.BigNumber.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toNearest (y, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toNearest)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toNumber ()](#apidoc.element.mathjs.type.BigNumber.prototype.toNumber)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toOctal (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toOctal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toPower (y)](#apidoc.element.mathjs.type.BigNumber.prototype.toPower)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toPrecision (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toPrecision)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toSD (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toSD)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toSignificantDigits (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toSignificantDigits)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toString ()](#apidoc.element.mathjs.type.BigNumber.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>trunc ()](#apidoc.element.mathjs.type.BigNumber.prototype.trunc)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>truncated ()](#apidoc.element.mathjs.type.BigNumber.prototype.truncated)
1.  [function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.BigNumber.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>type

#### [module mathjs.type.Chain](#apidoc.module.mathjs.type.Chain)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Chain (value)](#apidoc.element.mathjs.type.Chain.Chain)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.</span>createProxy (arg0, arg1)](#apidoc.element.mathjs.type.Chain.createProxy)

#### [module mathjs.type.Chain.prototype](#apidoc.module.mathjs.type.Chain.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isChain
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>abs ()](#apidoc.element.mathjs.type.Chain.prototype.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acos ()](#apidoc.element.mathjs.type.Chain.prototype.acos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acosh ()](#apidoc.element.mathjs.type.Chain.prototype.acosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acot ()](#apidoc.element.mathjs.type.Chain.prototype.acot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acoth ()](#apidoc.element.mathjs.type.Chain.prototype.acoth)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acsc ()](#apidoc.element.mathjs.type.Chain.prototype.acsc)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acsch ()](#apidoc.element.mathjs.type.Chain.prototype.acsch)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>add ()](#apidoc.element.mathjs.type.Chain.prototype.add)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>and ()](#apidoc.element.mathjs.type.Chain.prototype.and)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>arg ()](#apidoc.element.mathjs.type.Chain.prototype.arg)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asec ()](#apidoc.element.mathjs.type.Chain.prototype.asec)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asech ()](#apidoc.element.mathjs.type.Chain.prototype.asech)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asin ()](#apidoc.element.mathjs.type.Chain.prototype.asin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asinh ()](#apidoc.element.mathjs.type.Chain.prototype.asinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>atan ()](#apidoc.element.mathjs.type.Chain.prototype.atan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>atan2 ()](#apidoc.element.mathjs.type.Chain.prototype.atan2)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>atanh ()](#apidoc.element.mathjs.type.Chain.prototype.atanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bellNumbers ()](#apidoc.element.mathjs.type.Chain.prototype.bellNumbers)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bignumber ()](#apidoc.element.mathjs.type.Chain.prototype.bignumber)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitAnd ()](#apidoc.element.mathjs.type.Chain.prototype.bitAnd)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitNot ()](#apidoc.element.mathjs.type.Chain.prototype.bitNot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitOr ()](#apidoc.element.mathjs.type.Chain.prototype.bitOr)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitXor ()](#apidoc.element.mathjs.type.Chain.prototype.bitXor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>boolean ()](#apidoc.element.mathjs.type.Chain.prototype.boolean)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>catalan ()](#apidoc.element.mathjs.type.Chain.prototype.catalan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cbrt ()](#apidoc.element.mathjs.type.Chain.prototype.cbrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>ceil ()](#apidoc.element.mathjs.type.Chain.prototype.ceil)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>chain ()](#apidoc.element.mathjs.type.Chain.prototype.chain)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>clone ()](#apidoc.element.mathjs.type.Chain.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>combinations ()](#apidoc.element.mathjs.type.Chain.prototype.combinations)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>compare ()](#apidoc.element.mathjs.type.Chain.prototype.compare)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>compile ()](#apidoc.element.mathjs.type.Chain.prototype.compile)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>complex ()](#apidoc.element.mathjs.type.Chain.prototype.complex)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>composition ()](#apidoc.element.mathjs.type.Chain.prototype.composition)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>concat ()](#apidoc.element.mathjs.type.Chain.prototype.concat)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>config ()](#apidoc.element.mathjs.type.Chain.prototype.config)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>conj ()](#apidoc.element.mathjs.type.Chain.prototype.conj)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cos ()](#apidoc.element.mathjs.type.Chain.prototype.cos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cosh ()](#apidoc.element.mathjs.type.Chain.prototype.cosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cot ()](#apidoc.element.mathjs.type.Chain.prototype.cot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>coth ()](#apidoc.element.mathjs.type.Chain.prototype.coth)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>create ()](#apidoc.element.mathjs.type.Chain.prototype.create)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>createUnit ()](#apidoc.element.mathjs.type.Chain.prototype.createUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cross ()](#apidoc.element.mathjs.type.Chain.prototype.cross)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>csc ()](#apidoc.element.mathjs.type.Chain.prototype.csc)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>csch ()](#apidoc.element.mathjs.type.Chain.prototype.csch)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cube ()](#apidoc.element.mathjs.type.Chain.prototype.cube)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>deepEqual ()](#apidoc.element.mathjs.type.Chain.prototype.deepEqual)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>derivative ()](#apidoc.element.mathjs.type.Chain.prototype.derivative)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>det ()](#apidoc.element.mathjs.type.Chain.prototype.det)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>diag ()](#apidoc.element.mathjs.type.Chain.prototype.diag)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>distance ()](#apidoc.element.mathjs.type.Chain.prototype.distance)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>divide ()](#apidoc.element.mathjs.type.Chain.prototype.divide)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>done ()](#apidoc.element.mathjs.type.Chain.prototype.done)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dot ()](#apidoc.element.mathjs.type.Chain.prototype.dot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dotDivide ()](#apidoc.element.mathjs.type.Chain.prototype.dotDivide)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dotMultiply ()](#apidoc.element.mathjs.type.Chain.prototype.dotMultiply)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dotPow ()](#apidoc.element.mathjs.type.Chain.prototype.dotPow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>emit ()](#apidoc.element.mathjs.type.Chain.prototype.emit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>equal ()](#apidoc.element.mathjs.type.Chain.prototype.equal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>erf ()](#apidoc.element.mathjs.type.Chain.prototype.erf)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>eval ()](#apidoc.element.mathjs.type.Chain.prototype.eval)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>exp ()](#apidoc.element.mathjs.type.Chain.prototype.exp)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>eye ()](#apidoc.element.mathjs.type.Chain.prototype.eye)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>factorial ()](#apidoc.element.mathjs.type.Chain.prototype.factorial)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>filter ()](#apidoc.element.mathjs.type.Chain.prototype.filter)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>fix ()](#apidoc.element.mathjs.type.Chain.prototype.fix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>flatten ()](#apidoc.element.mathjs.type.Chain.prototype.flatten)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>floor ()](#apidoc.element.mathjs.type.Chain.prototype.floor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>forEach ()](#apidoc.element.mathjs.type.Chain.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>format ()](#apidoc.element.mathjs.type.Chain.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>fraction ()](#apidoc.element.mathjs.type.Chain.prototype.fraction)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>gamma ()](#apidoc.element.mathjs.type.Chain.prototype.gamma)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>gcd ()](#apidoc.element.mathjs.type.Chain.prototype.gcd)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>help ()](#apidoc.element.mathjs.type.Chain.prototype.help)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>hypot ()](#apidoc.element.mathjs.type.Chain.prototype.hypot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>im ()](#apidoc.element.mathjs.type.Chain.prototype.im)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>import ()](#apidoc.element.mathjs.type.Chain.prototype.import)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>index ()](#apidoc.element.mathjs.type.Chain.prototype.index)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>intersect ()](#apidoc.element.mathjs.type.Chain.prototype.intersect)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>inv ()](#apidoc.element.mathjs.type.Chain.prototype.inv)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isInteger ()](#apidoc.element.mathjs.type.Chain.prototype.isInteger)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isNaN ()](#apidoc.element.mathjs.type.Chain.prototype.isNaN)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isNegative ()](#apidoc.element.mathjs.type.Chain.prototype.isNegative)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isNumeric ()](#apidoc.element.mathjs.type.Chain.prototype.isNumeric)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isPositive ()](#apidoc.element.mathjs.type.Chain.prototype.isPositive)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isPrime ()](#apidoc.element.mathjs.type.Chain.prototype.isPrime)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isZero ()](#apidoc.element.mathjs.type.Chain.prototype.isZero)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>kldivergence ()](#apidoc.element.mathjs.type.Chain.prototype.kldivergence)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>kron ()](#apidoc.element.mathjs.type.Chain.prototype.kron)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>larger ()](#apidoc.element.mathjs.type.Chain.prototype.larger)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>largerEq ()](#apidoc.element.mathjs.type.Chain.prototype.largerEq)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lcm ()](#apidoc.element.mathjs.type.Chain.prototype.lcm)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>leftShift ()](#apidoc.element.mathjs.type.Chain.prototype.leftShift)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>log ()](#apidoc.element.mathjs.type.Chain.prototype.log)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>log10 ()](#apidoc.element.mathjs.type.Chain.prototype.log10)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lsolve ()](#apidoc.element.mathjs.type.Chain.prototype.lsolve)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lup ()](#apidoc.element.mathjs.type.Chain.prototype.lup)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lusolve ()](#apidoc.element.mathjs.type.Chain.prototype.lusolve)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mad ()](#apidoc.element.mathjs.type.Chain.prototype.mad)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>map ()](#apidoc.element.mathjs.type.Chain.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>matrix ()](#apidoc.element.mathjs.type.Chain.prototype.matrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>max ()](#apidoc.element.mathjs.type.Chain.prototype.max)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mean ()](#apidoc.element.mathjs.type.Chain.prototype.mean)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>median ()](#apidoc.element.mathjs.type.Chain.prototype.median)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>min ()](#apidoc.element.mathjs.type.Chain.prototype.min)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mod ()](#apidoc.element.mathjs.type.Chain.prototype.mod)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mode ()](#apidoc.element.mathjs.type.Chain.prototype.mode)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>multinomial ()](#apidoc.element.mathjs.type.Chain.prototype.multinomial)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>multiply ()](#apidoc.element.mathjs.type.Chain.prototype.multiply)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>norm ()](#apidoc.element.mathjs.type.Chain.prototype.norm)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>not ()](#apidoc.element.mathjs.type.Chain.prototype.not)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>nthRoot ()](#apidoc.element.mathjs.type.Chain.prototype.nthRoot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>number ()](#apidoc.element.mathjs.type.Chain.prototype.number)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>off ()](#apidoc.element.mathjs.type.Chain.prototype.off)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>on ()](#apidoc.element.mathjs.type.Chain.prototype.on)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>once ()](#apidoc.element.mathjs.type.Chain.prototype.once)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>ones ()](#apidoc.element.mathjs.type.Chain.prototype.ones)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>or ()](#apidoc.element.mathjs.type.Chain.prototype.or)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>parse ()](#apidoc.element.mathjs.type.Chain.prototype.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>parser ()](#apidoc.element.mathjs.type.Chain.prototype.parser)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>partitionSelect ()](#apidoc.element.mathjs.type.Chain.prototype.partitionSelect)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>permutations ()](#apidoc.element.mathjs.type.Chain.prototype.permutations)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>pow ()](#apidoc.element.mathjs.type.Chain.prototype.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>print ()](#apidoc.element.mathjs.type.Chain.prototype.print)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>prod ()](#apidoc.element.mathjs.type.Chain.prototype.prod)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>quantileSeq ()](#apidoc.element.mathjs.type.Chain.prototype.quantileSeq)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>range ()](#apidoc.element.mathjs.type.Chain.prototype.range)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>re ()](#apidoc.element.mathjs.type.Chain.prototype.re)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>reshape ()](#apidoc.element.mathjs.type.Chain.prototype.reshape)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>resize ()](#apidoc.element.mathjs.type.Chain.prototype.resize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>rightArithShift ()](#apidoc.element.mathjs.type.Chain.prototype.rightArithShift)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>rightLogShift ()](#apidoc.element.mathjs.type.Chain.prototype.rightLogShift)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>round ()](#apidoc.element.mathjs.type.Chain.prototype.round)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sec ()](#apidoc.element.mathjs.type.Chain.prototype.sec)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sech ()](#apidoc.element.mathjs.type.Chain.prototype.sech)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sign ()](#apidoc.element.mathjs.type.Chain.prototype.sign)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>simplify ()](#apidoc.element.mathjs.type.Chain.prototype.simplify)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sin ()](#apidoc.element.mathjs.type.Chain.prototype.sin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sinh ()](#apidoc.element.mathjs.type.Chain.prototype.sinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>size ()](#apidoc.element.mathjs.type.Chain.prototype.size)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>slu ()](#apidoc.element.mathjs.type.Chain.prototype.slu)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>smaller ()](#apidoc.element.mathjs.type.Chain.prototype.smaller)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>smallerEq ()](#apidoc.element.mathjs.type.Chain.prototype.smallerEq)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sort ()](#apidoc.element.mathjs.type.Chain.prototype.sort)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sparse ()](#apidoc.element.mathjs.type.Chain.prototype.sparse)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>splitUnit ()](#apidoc.element.mathjs.type.Chain.prototype.splitUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sqrt ()](#apidoc.element.mathjs.type.Chain.prototype.sqrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>square ()](#apidoc.element.mathjs.type.Chain.prototype.square)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>squeeze ()](#apidoc.element.mathjs.type.Chain.prototype.squeeze)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>std ()](#apidoc.element.mathjs.type.Chain.prototype.std)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>stirlingS2 ()](#apidoc.element.mathjs.type.Chain.prototype.stirlingS2)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>string ()](#apidoc.element.mathjs.type.Chain.prototype.string)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>subset ()](#apidoc.element.mathjs.type.Chain.prototype.subset)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>subtract ()](#apidoc.element.mathjs.type.Chain.prototype.subtract)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sum ()](#apidoc.element.mathjs.type.Chain.prototype.sum)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>tan ()](#apidoc.element.mathjs.type.Chain.prototype.tan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>tanh ()](#apidoc.element.mathjs.type.Chain.prototype.tanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>to ()](#apidoc.element.mathjs.type.Chain.prototype.to)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>toString ()](#apidoc.element.mathjs.type.Chain.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>trace ()](#apidoc.element.mathjs.type.Chain.prototype.trace)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>transpose ()](#apidoc.element.mathjs.type.Chain.prototype.transpose)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>typed ()](#apidoc.element.mathjs.type.Chain.prototype.typed)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>typeof ()](#apidoc.element.mathjs.type.Chain.prototype.typeof)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unaryMinus ()](#apidoc.element.mathjs.type.Chain.prototype.unaryMinus)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unaryPlus ()](#apidoc.element.mathjs.type.Chain.prototype.unaryPlus)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unequal ()](#apidoc.element.mathjs.type.Chain.prototype.unequal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unit ()](#apidoc.element.mathjs.type.Chain.prototype.unit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>usolve ()](#apidoc.element.mathjs.type.Chain.prototype.usolve)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Chain.prototype.valueOf)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>var ()](#apidoc.element.mathjs.type.Chain.prototype.var)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>xgcd ()](#apidoc.element.mathjs.type.Chain.prototype.xgcd)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>xor ()](#apidoc.element.mathjs.type.Chain.prototype.xor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>zeros ()](#apidoc.element.mathjs.type.Chain.prototype.zeros)
1.  string <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>type

#### [module mathjs.type.Complex](#apidoc.module.mathjs.type.Complex)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Complex (a, b)](#apidoc.element.mathjs.type.Complex.Complex)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.</span>fromJSON (json)](#apidoc.element.mathjs.type.Complex.fromJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.</span>fromPolar (args)](#apidoc.element.mathjs.type.Complex.fromPolar)
1.  number <span class="apidocSignatureSpan">mathjs.type.Complex.</span>EPSILON
1.  object <span class="apidocSignatureSpan">mathjs.type.Complex.</span>E
1.  object <span class="apidocSignatureSpan">mathjs.type.Complex.</span>I
1.  object <span class="apidocSignatureSpan">mathjs.type.Complex.</span>ONE
1.  object <span class="apidocSignatureSpan">mathjs.type.Complex.</span>PI
1.  object <span class="apidocSignatureSpan">mathjs.type.Complex.</span>ZERO

#### [module mathjs.type.Complex.prototype](#apidoc.module.mathjs.type.Complex.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>isComplex
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>abs ()](#apidoc.element.mathjs.type.Complex.prototype.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acos ()](#apidoc.element.mathjs.type.Complex.prototype.acos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acosh ()](#apidoc.element.mathjs.type.Complex.prototype.acosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acot ()](#apidoc.element.mathjs.type.Complex.prototype.acot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acoth ()](#apidoc.element.mathjs.type.Complex.prototype.acoth)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acsc ()](#apidoc.element.mathjs.type.Complex.prototype.acsc)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acsch ()](#apidoc.element.mathjs.type.Complex.prototype.acsch)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>add (a, b)](#apidoc.element.mathjs.type.Complex.prototype.add)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>arg ()](#apidoc.element.mathjs.type.Complex.prototype.arg)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asec ()](#apidoc.element.mathjs.type.Complex.prototype.asec)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asech ()](#apidoc.element.mathjs.type.Complex.prototype.asech)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asin ()](#apidoc.element.mathjs.type.Complex.prototype.asin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asinh ()](#apidoc.element.mathjs.type.Complex.prototype.asinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>atan ()](#apidoc.element.mathjs.type.Complex.prototype.atan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>atanh ()](#apidoc.element.mathjs.type.Complex.prototype.atanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>ceil (places)](#apidoc.element.mathjs.type.Complex.prototype.ceil)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>clone ()](#apidoc.element.mathjs.type.Complex.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>conjugate ()](#apidoc.element.mathjs.type.Complex.prototype.conjugate)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>cos ()](#apidoc.element.mathjs.type.Complex.prototype.cos)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>cosh ()](#apidoc.element.mathjs.type.Complex.prototype.cosh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>cot ()](#apidoc.element.mathjs.type.Complex.prototype.cot)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>coth ()](#apidoc.element.mathjs.type.Complex.prototype.coth)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>csc ()](#apidoc.element.mathjs.type.Complex.prototype.csc)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>csch ()](#apidoc.element.mathjs.type.Complex.prototype.csch)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>div (a, b)](#apidoc.element.mathjs.type.Complex.prototype.div)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>equals (a, b)](#apidoc.element.mathjs.type.Complex.prototype.equals)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>exp ()](#apidoc.element.mathjs.type.Complex.prototype.exp)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>floor (places)](#apidoc.element.mathjs.type.Complex.prototype.floor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>format (options)](#apidoc.element.mathjs.type.Complex.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>inverse ()](#apidoc.element.mathjs.type.Complex.prototype.inverse)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>isNaN ()](#apidoc.element.mathjs.type.Complex.prototype.isNaN)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>log ()](#apidoc.element.mathjs.type.Complex.prototype.log)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>mul (a, b)](#apidoc.element.mathjs.type.Complex.prototype.mul)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>neg ()](#apidoc.element.mathjs.type.Complex.prototype.neg)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>pow (a, b)](#apidoc.element.mathjs.type.Complex.prototype.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>round (places)](#apidoc.element.mathjs.type.Complex.prototype.round)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sec ()](#apidoc.element.mathjs.type.Complex.prototype.sec)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sech ()](#apidoc.element.mathjs.type.Complex.prototype.sech)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sign ()](#apidoc.element.mathjs.type.Complex.prototype.sign)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sin ()](#apidoc.element.mathjs.type.Complex.prototype.sin)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sinh ()](#apidoc.element.mathjs.type.Complex.prototype.sinh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sqrt ()](#apidoc.element.mathjs.type.Complex.prototype.sqrt)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sub (a, b)](#apidoc.element.mathjs.type.Complex.prototype.sub)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>tan ()](#apidoc.element.mathjs.type.Complex.prototype.tan)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>tanh ()](#apidoc.element.mathjs.type.Complex.prototype.tanh)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Complex.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toPolar ()](#apidoc.element.mathjs.type.Complex.prototype.toPolar)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toString ()](#apidoc.element.mathjs.type.Complex.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toVector ()](#apidoc.element.mathjs.type.Complex.prototype.toVector)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Complex.prototype.valueOf)
1.  number <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>im
1.  number <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>re
1.  string <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>type

#### [module mathjs.type.DenseMatrix](#apidoc.module.mathjs.type.DenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>DenseMatrix (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix.DenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.</span>_swapRows (i, j, data)](#apidoc.element.mathjs.type.DenseMatrix._swapRows)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.</span>diagonal (size, value, k, defaultValue, datatype)](#apidoc.element.mathjs.type.DenseMatrix.diagonal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.</span>fromJSON (json)](#apidoc.element.mathjs.type.DenseMatrix.fromJSON)

#### [module mathjs.type.DenseMatrix.prototype](#apidoc.module.mathjs.type.DenseMatrix.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>isDenseMatrix
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>create (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix.prototype.create)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>datatype ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.datatype)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>diagonal (k)](#apidoc.element.mathjs.type.DenseMatrix.prototype.diagonal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.DenseMatrix.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>format (options)](#apidoc.element.mathjs.type.DenseMatrix.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>get (index)](#apidoc.element.mathjs.type.DenseMatrix.prototype.get)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>map (callback)](#apidoc.element.mathjs.type.DenseMatrix.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>reshape (size, copy)](#apidoc.element.mathjs.type.DenseMatrix.prototype.reshape)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>resize (size, defaultValue, copy)](#apidoc.element.mathjs.type.DenseMatrix.prototype.resize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>set (index, value, defaultValue)](#apidoc.element.mathjs.type.DenseMatrix.prototype.set)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>size ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.size)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>storage ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.storage)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>subset (index, replacement, defaultValue)](#apidoc.element.mathjs.type.DenseMatrix.prototype.subset)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>swapRows (i, j)](#apidoc.element.mathjs.type.DenseMatrix.prototype.swapRows)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>toArray ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.toArray)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>toString ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>type

#### [module mathjs.type.FibonacciHeap](#apidoc.module.mathjs.type.FibonacciHeap)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>FibonacciHeap ()](#apidoc.element.mathjs.type.FibonacciHeap.FibonacciHeap)

#### [module mathjs.type.FibonacciHeap.prototype](#apidoc.module.mathjs.type.FibonacciHeap.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>isFibonacciHeap
1.  [function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>clear ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.clear)
1.  [function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>extractMinimum ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.extractMinimum)
1.  [function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>insert (key, value)](#apidoc.element.mathjs.type.FibonacciHeap.prototype.insert)
1.  [function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>isEmpty ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.isEmpty)
1.  [function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>remove (node)](#apidoc.element.mathjs.type.FibonacciHeap.prototype.remove)
1.  [function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>size ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.size)
1.  string <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>type

#### [module mathjs.type.Fraction](#apidoc.module.mathjs.type.Fraction)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Fraction (a, b)](#apidoc.element.mathjs.type.Fraction.Fraction)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>DivisionByZero ()](#apidoc.element.mathjs.type.Fraction.DivisionByZero)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>InvalidParameter ()](#apidoc.element.mathjs.type.Fraction.InvalidParameter)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>fromJSON (json)](#apidoc.element.mathjs.type.Fraction.fromJSON)
1.  number <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>REDUCE

#### [module mathjs.type.Fraction.prototype](#apidoc.module.mathjs.type.Fraction.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>isFraction
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>abs ()](#apidoc.element.mathjs.type.Fraction.prototype.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>add (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.add)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>ceil (places)](#apidoc.element.mathjs.type.Fraction.prototype.ceil)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>clone ()](#apidoc.element.mathjs.type.Fraction.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>compare (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.compare)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>div (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.div)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>divisible (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.divisible)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>equals (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.equals)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>floor (places)](#apidoc.element.mathjs.type.Fraction.prototype.floor)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>gcd (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.gcd)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>inverse ()](#apidoc.element.mathjs.type.Fraction.prototype.inverse)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>lcm (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.lcm)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>mod (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.mod)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>mul (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.mul)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>neg ()](#apidoc.element.mathjs.type.Fraction.prototype.neg)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>pow (m)](#apidoc.element.mathjs.type.Fraction.prototype.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>round (places)](#apidoc.element.mathjs.type.Fraction.prototype.round)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>sub (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.sub)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toContinued ()](#apidoc.element.mathjs.type.Fraction.prototype.toContinued)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toFraction (excludeWhole)](#apidoc.element.mathjs.type.Fraction.prototype.toFraction)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Fraction.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toLatex (excludeWhole)](#apidoc.element.mathjs.type.Fraction.prototype.toLatex)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toString ()](#apidoc.element.mathjs.type.Fraction.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Fraction.prototype.valueOf)
1.  number <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>d
1.  number <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>n
1.  number <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>s
1.  string <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>type

#### [module mathjs.type.Help](#apidoc.module.mathjs.type.Help)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Help (doc)](#apidoc.element.mathjs.type.Help.Help)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Help.</span>fromJSON (json)](#apidoc.element.mathjs.type.Help.fromJSON)

#### [module mathjs.type.Help.prototype](#apidoc.module.mathjs.type.Help.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>isHelp
1.  [function <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Help.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>toString ()](#apidoc.element.mathjs.type.Help.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Help.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>type

#### [module mathjs.type.ImmutableDenseMatrix](#apidoc.module.mathjs.type.ImmutableDenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>ImmutableDenseMatrix (data, datatype)](#apidoc.element.mathjs.type.ImmutableDenseMatrix.ImmutableDenseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.</span>fromJSON (json)](#apidoc.element.mathjs.type.ImmutableDenseMatrix.fromJSON)

#### [module mathjs.type.ImmutableDenseMatrix.prototype](#apidoc.module.mathjs.type.ImmutableDenseMatrix.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>isImmutableDenseMatrix
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>max ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.max)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>min ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.min)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>reshape ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.reshape)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>resize ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.resize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>set ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.set)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>subset (index)](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.subset)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>swapRows ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.swapRows)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.toJSON)
1.  object <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>_data
1.  object <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>_size
1.  string <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>type

#### [module mathjs.type.Index](#apidoc.module.mathjs.type.Index)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Index (ranges)](#apidoc.element.mathjs.type.Index.Index)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.</span>create (ranges)](#apidoc.element.mathjs.type.Index.create)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.</span>fromJSON (json)](#apidoc.element.mathjs.type.Index.fromJSON)

#### [module mathjs.type.Index.prototype](#apidoc.module.mathjs.type.Index.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>isIndex
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>clone ()](#apidoc.element.mathjs.type.Index.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>dimension (dim)](#apidoc.element.mathjs.type.Index.prototype.dimension)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.Index.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>getObjectProperty ()](#apidoc.element.mathjs.type.Index.prototype.getObjectProperty)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>isObjectProperty ()](#apidoc.element.mathjs.type.Index.prototype.isObjectProperty)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>isScalar ()](#apidoc.element.mathjs.type.Index.prototype.isScalar)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>max ()](#apidoc.element.mathjs.type.Index.prototype.max)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>min ()](#apidoc.element.mathjs.type.Index.prototype.min)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>size ()](#apidoc.element.mathjs.type.Index.prototype.size)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>toArray ()](#apidoc.element.mathjs.type.Index.prototype.toArray)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Index.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>toString ()](#apidoc.element.mathjs.type.Index.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Index.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>type

#### [module mathjs.type.Matrix](#apidoc.module.mathjs.type.Matrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Matrix ()](#apidoc.element.mathjs.type.Matrix.Matrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.</span>storage (format)](#apidoc.element.mathjs.type.Matrix.storage)
1.  object <span class="apidocSignatureSpan">mathjs.type.Matrix.</span>_storage

#### [module mathjs.type.Matrix.prototype](#apidoc.module.mathjs.type.Matrix.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>isMatrix
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.Matrix.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>create (data, datatype)](#apidoc.element.mathjs.type.Matrix.prototype.create)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>datatype ()](#apidoc.element.mathjs.type.Matrix.prototype.datatype)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.Matrix.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>format (options)](#apidoc.element.mathjs.type.Matrix.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>get (index)](#apidoc.element.mathjs.type.Matrix.prototype.get)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>map (callback, skipZeros)](#apidoc.element.mathjs.type.Matrix.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>reshape (size, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.reshape)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>resize (size, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.resize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>set (index, value, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.set)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>size ()](#apidoc.element.mathjs.type.Matrix.prototype.size)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>storage ()](#apidoc.element.mathjs.type.Matrix.prototype.storage)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>subset (index, replacement, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.subset)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>toArray ()](#apidoc.element.mathjs.type.Matrix.prototype.toArray)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>toString ()](#apidoc.element.mathjs.type.Matrix.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Matrix.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>type

#### [module mathjs.type.Range](#apidoc.module.mathjs.type.Range)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Range (start, end, step)](#apidoc.element.mathjs.type.Range.Range)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.</span>fromJSON (json)](#apidoc.element.mathjs.type.Range.fromJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.</span>parse (str)](#apidoc.element.mathjs.type.Range.parse)

#### [module mathjs.type.Range.prototype](#apidoc.module.mathjs.type.Range.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>isRange
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>clone ()](#apidoc.element.mathjs.type.Range.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.Range.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>format (options)](#apidoc.element.mathjs.type.Range.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>map (callback)](#apidoc.element.mathjs.type.Range.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>max ()](#apidoc.element.mathjs.type.Range.prototype.max)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>min ()](#apidoc.element.mathjs.type.Range.prototype.min)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>size ()](#apidoc.element.mathjs.type.Range.prototype.size)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>toArray ()](#apidoc.element.mathjs.type.Range.prototype.toArray)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Range.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>toString ()](#apidoc.element.mathjs.type.Range.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Range.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>type

#### [module mathjs.type.ResultSet](#apidoc.module.mathjs.type.ResultSet)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>ResultSet (entries)](#apidoc.element.mathjs.type.ResultSet.ResultSet)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ResultSet.</span>fromJSON (json)](#apidoc.element.mathjs.type.ResultSet.fromJSON)

#### [module mathjs.type.ResultSet.prototype](#apidoc.module.mathjs.type.ResultSet.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>isResultSet
1.  [function <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.ResultSet.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>toString ()](#apidoc.element.mathjs.type.ResultSet.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.ResultSet.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>type

#### [module mathjs.type.Spa](#apidoc.module.mathjs.type.Spa)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Spa ()](#apidoc.element.mathjs.type.Spa.Spa)

#### [module mathjs.type.Spa.prototype](#apidoc.module.mathjs.type.Spa.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>isSpa
1.  [function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>accumulate (i, v)](#apidoc.element.mathjs.type.Spa.prototype.accumulate)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>forEach (from, to, callback)](#apidoc.element.mathjs.type.Spa.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>get (i)](#apidoc.element.mathjs.type.Spa.prototype.get)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>set (i, v)](#apidoc.element.mathjs.type.Spa.prototype.set)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>swap (i, j)](#apidoc.element.mathjs.type.Spa.prototype.swap)
1.  string <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>type

#### [module mathjs.type.SparseMatrix](#apidoc.module.mathjs.type.SparseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>SparseMatrix (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix.SparseMatrix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>_forEachRow (j, values, index, ptr, callback)](#apidoc.element.mathjs.type.SparseMatrix._forEachRow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>_swapRows (x, y, columns, values, index, ptr)](#apidoc.element.mathjs.type.SparseMatrix._swapRows)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>diagonal (size, value, k, defaultValue, datatype)](#apidoc.element.mathjs.type.SparseMatrix.diagonal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>fromJSON (json)](#apidoc.element.mathjs.type.SparseMatrix.fromJSON)

#### [module mathjs.type.SparseMatrix.prototype](#apidoc.module.mathjs.type.SparseMatrix.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>isSparseMatrix
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>create (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix.prototype.create)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>datatype ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.datatype)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>density ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.density)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>diagonal (k)](#apidoc.element.mathjs.type.SparseMatrix.prototype.diagonal)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>forEach (callback, skipZeros)](#apidoc.element.mathjs.type.SparseMatrix.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>format (options)](#apidoc.element.mathjs.type.SparseMatrix.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>get (index)](#apidoc.element.mathjs.type.SparseMatrix.prototype.get)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>map (callback, skipZeros)](#apidoc.element.mathjs.type.SparseMatrix.prototype.map)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>reshape (size, copy)](#apidoc.element.mathjs.type.SparseMatrix.prototype.reshape)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>resize (size, defaultValue, copy)](#apidoc.element.mathjs.type.SparseMatrix.prototype.resize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>set (index, v, defaultValue)](#apidoc.element.mathjs.type.SparseMatrix.prototype.set)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>size ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.size)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>storage ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.storage)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>subset (index, replacement, defaultValue)](#apidoc.element.mathjs.type.SparseMatrix.prototype.subset)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>swapRows (i, j)](#apidoc.element.mathjs.type.SparseMatrix.prototype.swapRows)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>toArray ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.toArray)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>toString ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>type

#### [module mathjs.type.Unit](#apidoc.module.mathjs.type.Unit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.</span>Unit (value, name)](#apidoc.element.mathjs.type.Unit.Unit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>_getNumberConverter (type)](#apidoc.element.mathjs.type.Unit._getNumberConverter)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>createUnit (obj, options)](#apidoc.element.mathjs.type.Unit.createUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>createUnitSingle (name, obj, options)](#apidoc.element.mathjs.type.Unit.createUnitSingle)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>deleteUnit (name)](#apidoc.element.mathjs.type.Unit.deleteUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>fromJSON (json)](#apidoc.element.mathjs.type.Unit.fromJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>getUnitSystem ()](#apidoc.element.mathjs.type.Unit.getUnitSystem)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>isValuelessUnit (name)](#apidoc.element.mathjs.type.Unit.isValuelessUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>parse (str, options)](#apidoc.element.mathjs.type.Unit.parse)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>setUnitSystem (name)](#apidoc.element.mathjs.type.Unit.setUnitSystem)
1.  object <span class="apidocSignatureSpan">mathjs.type.Unit.</span>BASE_UNITS
1.  object <span class="apidocSignatureSpan">mathjs.type.Unit.</span>PREFIXES
1.  object <span class="apidocSignatureSpan">mathjs.type.Unit.</span>UNITS
1.  object <span class="apidocSignatureSpan">mathjs.type.Unit.</span>UNIT_SYSTEMS
1.  object <span class="apidocSignatureSpan">mathjs.type.Unit.</span>typeConverters

#### [module mathjs.type.Unit.prototype](#apidoc.module.mathjs.type.Unit.prototype)
1.  boolean <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>isUnit
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_bestPrefix ()](#apidoc.element.mathjs.type.Unit.prototype._bestPrefix)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_denormalize (value, prefixValue)](#apidoc.element.mathjs.type.Unit.prototype._denormalize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_isDerived ()](#apidoc.element.mathjs.type.Unit.prototype._isDerived)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_normalize (value)](#apidoc.element.mathjs.type.Unit.prototype._normalize)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>abs ()](#apidoc.element.mathjs.type.Unit.prototype.abs)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>clone ()](#apidoc.element.mathjs.type.Unit.prototype.clone)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>divide (other)](#apidoc.element.mathjs.type.Unit.prototype.divide)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>equalBase (other)](#apidoc.element.mathjs.type.Unit.prototype.equalBase)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>equals (other)](#apidoc.element.mathjs.type.Unit.prototype.equals)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>format (options)](#apidoc.element.mathjs.type.Unit.prototype.format)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>formatUnits ()](#apidoc.element.mathjs.type.Unit.prototype.formatUnits)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>hasBase (base)](#apidoc.element.mathjs.type.Unit.prototype.hasBase)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>multiply (other)](#apidoc.element.mathjs.type.Unit.prototype.multiply)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>pow (p)](#apidoc.element.mathjs.type.Unit.prototype.pow)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>simplifyUnitListLazy ()](#apidoc.element.mathjs.type.Unit.prototype.simplifyUnitListLazy)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>splitUnit (parts)](#apidoc.element.mathjs.type.Unit.prototype.splitUnit)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>to (valuelessUnit)](#apidoc.element.mathjs.type.Unit.prototype.to)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Unit.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toNumber (valuelessUnit)](#apidoc.element.mathjs.type.Unit.prototype.toNumber)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toNumeric (valuelessUnit)](#apidoc.element.mathjs.type.Unit.prototype.toNumeric)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toSI ()](#apidoc.element.mathjs.type.Unit.prototype.toSI)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toString ()](#apidoc.element.mathjs.type.Unit.prototype.toString)
1.  [function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Unit.prototype.valueOf)
1.  string <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>type

#### [module mathjs.typed](#apidoc.module.mathjs.typed)
1.  [function <span class="apidocSignatureSpan">mathjs.</span>typed (arg0, arg1)](#apidoc.element.mathjs.typed.typed)
1.  [function <span class="apidocSignatureSpan">mathjs.typed.</span>addConversion (conversion)](#apidoc.element.mathjs.typed.addConversion)
1.  [function <span class="apidocSignatureSpan">mathjs.typed.</span>addType (type)](#apidoc.element.mathjs.typed.addType)
1.  [function <span class="apidocSignatureSpan">mathjs.typed.</span>convert (value, type)](#apidoc.element.mathjs.typed.convert)
1.  [function <span class="apidocSignatureSpan">mathjs.typed.</span>create ()](#apidoc.element.mathjs.typed.create)
1.  [function <span class="apidocSignatureSpan">mathjs.typed.</span>find (fn, signature)](#apidoc.element.mathjs.typed.find)
1.  object <span class="apidocSignatureSpan">mathjs.typed.</span>config
1.  object <span class="apidocSignatureSpan">mathjs.typed.</span>conversions
1.  object <span class="apidocSignatureSpan">mathjs.typed.</span>ignore
1.  object <span class="apidocSignatureSpan">mathjs.typed.</span>signatures
1.  object <span class="apidocSignatureSpan">mathjs.typed.</span>types

#### [module mathjs.typed.signatures](#apidoc.module.mathjs.typed.signatures)
1.  [function <span class="apidocSignatureSpan">mathjs.typed.signatures.</span>Object (signatures)](#apidoc.element.mathjs.typed.signatures.Object)



# <a name="apidoc.module.mathjs"></a>[module mathjs](#apidoc.module.mathjs)

#### <a name="apidoc.element.mathjs.abs"></a>[function <span class="apidocSignatureSpan">mathjs.</span>abs (arg0)](#apidoc.element.mathjs.abs)
- description and source-code
```javascript
function abs(arg0) {
  "use strict";
  var name = 'abs';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.acos"></a>[function <span class="apidocSignatureSpan">mathjs.</span>acos (arg0)](#apidoc.element.mathjs.acos)
- description and source-code
```javascript
function acos(arg0) {
  "use strict";
  var name = 'acos';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.acosh"></a>[function <span class="apidocSignatureSpan">mathjs.</span>acosh (arg0)](#apidoc.element.mathjs.acosh)
- description and source-code
```javascript
function acosh(arg0) {
  "use strict";
  var name = 'acosh';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.acot"></a>[function <span class="apidocSignatureSpan">mathjs.</span>acot (arg0)](#apidoc.element.mathjs.acot)
- description and source-code
```javascript
function acot(arg0) {
  "use strict";
  var name = 'acot';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.acoth"></a>[function <span class="apidocSignatureSpan">mathjs.</span>acoth (arg0)](#apidoc.element.mathjs.acoth)
- description and source-code
```javascript
function acoth(arg0) {
  "use strict";
  var name = 'acoth';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.acsc"></a>[function <span class="apidocSignatureSpan">mathjs.</span>acsc (arg0)](#apidoc.element.mathjs.acsc)
- description and source-code
```javascript
function acsc(arg0) {
  "use strict";
  var name = 'acsc';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.acsch"></a>[function <span class="apidocSignatureSpan">mathjs.</span>acsch (arg0)](#apidoc.element.mathjs.acsch)
- description and source-code
```javascript
function acsch(arg0) {
  "use strict";
  var name = 'acsch';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.add"></a>[function <span class="apidocSignatureSpan">mathjs.</span>add (arg0, arg1, arg2)](#apidoc.element.mathjs.add)
- description and source-code
```javascript
function add(arg0, arg1, arg2) {
  "use strict";
  var name = 'add';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, BigNumber
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: BigNumber, number
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert9(arg1)); // signature: BigNumber, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert10(arg1)); // signature: BigNumber, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert11(arg1)); // signature: BigNumber, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert12(arg1)); // signature: BigNumber, null
      }
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert7(arg0), arg1); // signature: Fraction, Complex
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert9(arg0), arg1); // signature: Fraction, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (a ...
```
- example usage
```shell
...
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.and"></a>[function <span class="apidocSignatureSpan">mathjs.</span>and (arg0, arg1)](#apidoc.element.mathjs.and)
- description and source-code
```javascript
function and(arg0, arg1) {
  "use strict";
  var name = 'and';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(convert1(arg0), convert2(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.arg"></a>[function <span class="apidocSignatureSpan">mathjs.</span>arg (arg0)](#apidoc.element.mathjs.arg)
- description and source-code
```javascript
function arg(arg0) {
  "use strict";
  var name = 'arg';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.asec"></a>[function <span class="apidocSignatureSpan">mathjs.</span>asec (arg0)](#apidoc.element.mathjs.asec)
- description and source-code
```javascript
function asec(arg0) {
  "use strict";
  var name = 'asec';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.asech"></a>[function <span class="apidocSignatureSpan">mathjs.</span>asech (arg0)](#apidoc.element.mathjs.asech)
- description and source-code
```javascript
function asech(arg0) {
  "use strict";
  var name = 'asech';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.asin"></a>[function <span class="apidocSignatureSpan">mathjs.</span>asin (arg0)](#apidoc.element.mathjs.asin)
- description and source-code
```javascript
function asin(arg0) {
  "use strict";
  var name = 'asin';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.asinh"></a>[function <span class="apidocSignatureSpan">mathjs.</span>asinh (arg0)](#apidoc.element.mathjs.asinh)
- description and source-code
```javascript
function asinh(arg0) {
  "use strict";
  var name = 'asinh';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.atan"></a>[function <span class="apidocSignatureSpan">mathjs.</span>atan (arg0)](#apidoc.element.mathjs.atan)
- description and source-code
```javascript
function atan(arg0) {
  "use strict";
  var name = 'atan';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.atan2"></a>[function <span class="apidocSignatureSpan">mathjs.</span>atan2 (arg0, arg1)](#apidoc.element.mathjs.atan2)
- description and source-code
```javascript
function atan2(arg0, arg1) {
  "use strict";
  var name = 'atan2';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,Array,Matrix');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: BigNumber, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg ...
```
- example usage
```shell
...

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
...
```

#### <a name="apidoc.element.mathjs.atanh"></a>[function <span class="apidocSignatureSpan">mathjs.</span>atanh (arg0)](#apidoc.element.mathjs.atanh)
- description and source-code
```javascript
function atanh(arg0) {
  "use strict";
  var name = 'atanh';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.bellNumbers"></a>[function <span class="apidocSignatureSpan">mathjs.</span>bellNumbers (arg0)](#apidoc.element.mathjs.bellNumbers)
- description and source-code
```javascript
function bellNumbers(arg0) {
  "use strict";
  var name = 'bellNumbers';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature0(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.bignumber"></a>[function <span class="apidocSignatureSpan">mathjs.</span>bignumber (arg0)](#apidoc.element.mathjs.bignumber)
- description and source-code
```javascript
function bignumber(arg0) {
  "use strict";
  var name = 'bignumber';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature1(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature4(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature1(convert0(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature1(convert1(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Fraction,string,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.bitAnd"></a>[function <span class="apidocSignatureSpan">mathjs.</span>bitAnd (arg0, arg1)](#apidoc.element.mathjs.bitAnd)
- description and source-code
```javascript
function bitAnd(arg0, arg1) {
  "use strict";
  var name = 'bitAnd';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test6(arg0)) { // type: Array
    if (test6(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, Array
      }
    }
    if (test7(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) { ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.bitNot"></a>[function <span class="apidocSignatureSpan">mathjs.</span>bitNot (arg0)](#apidoc.element.mathjs.bitNot)
- description and source-code
```javascript
function bitNot(arg0) {
  "use strict";
  var name = 'bitNot';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature1(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.bitOr"></a>[function <span class="apidocSignatureSpan">mathjs.</span>bitOr (arg0, arg1)](#apidoc.element.mathjs.bitOr)
- description and source-code
```javascript
function bitOr(arg0, arg1) {
  "use strict";
  var name = 'bitOr';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test6(arg0)) { // type: Array
    if (test6(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, Array
      }
    }
    if (test7(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) { ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.bitXor"></a>[function <span class="apidocSignatureSpan">mathjs.</span>bitXor (arg0, arg1)](#apidoc.element.mathjs.bitXor)
- description and source-code
```javascript
function bitXor(arg0, arg1) {
  "use strict";
  var name = 'bitXor';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test6(arg0)) { // type: Array
    if (test6(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, Array
      }
    }
    if (test7(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) { ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.boolean"></a>[function <span class="apidocSignatureSpan">mathjs.</span>boolean (arg0)](#apidoc.element.mathjs.boolean)
- description and source-code
```javascript
function bool(arg0) {
  "use strict";
  var name = 'bool';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature1(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature3(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: boolean
    if (arguments.length === 1) {
      return signature5(arg0); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature1(convert1(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,string,Array,Matrix,boolean');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.catalan"></a>[function <span class="apidocSignatureSpan">mathjs.</span>catalan (arg0)](#apidoc.element.mathjs.catalan)
- description and source-code
```javascript
function catalan(arg0) {
  "use strict";
  var name = 'catalan';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature0(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.cbrt"></a>[function <span class="apidocSignatureSpan">mathjs.</span>cbrt (arg0, arg1)](#apidoc.element.mathjs.cbrt)
- description and source-code
```javascript
function cbrt(arg0, arg1) {
  "use strict";
  var name = 'cbrt';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (test1(arg1)) { // type: boolean
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'boolean');
  }
  if (test2(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (test1(arg1)) { // type: boolean
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'boolean');
  }
  if (test3(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (test1(arg1)) { // type: boolean
      if (arguments.length === 2) {
        return signature1(convert1(arg0), arg1); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'boolean');
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert2(arg0)); // signature: Fraction
    }
    if (test1(arg1)) { // type: boolean
      if (arguments.length === 2) {
        return signature1(convert3(arg0), arg1); // signature: Fraction, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'boolean');
  }
  if (test8(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert4(arg0)); // signature: string
    }
    if (test1(arg1)) { // type: boolean
      if (arguments.length === 2) {
        return signature1(convert5(arg0), arg1); // signature: string, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'boolean');
  }
  if (test1(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert6(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert7(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.ceil"></a>[function <span class="apidocSignatureSpan">mathjs.</span>ceil (arg0)](#apidoc.element.mathjs.ceil)
- description and source-code
```javascript
function ceil(arg0) {
  "use strict";
  var name = 'ceil';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.chain"></a>[function <span class="apidocSignatureSpan">mathjs.</span>chain (arg0)](#apidoc.element.mathjs.chain)
- description and source-code
```javascript
function chain(arg0) {
  "use strict";
  var name = 'chain';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  // type: any
  if (arguments.length === 1) {
    return signature1(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
...
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support
...
```

#### <a name="apidoc.element.mathjs.clone"></a>[function <span class="apidocSignatureSpan">mathjs.</span>clone (arg0)](#apidoc.element.mathjs.clone)
- description and source-code
```javascript
function clone(arg0) {
  "use strict";
  var name = 'clone';
  // type: any
  if (arguments.length === 1) {
    return signature0(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.combinations"></a>[function <span class="apidocSignatureSpan">mathjs.</span>combinations (arg0, arg1)](#apidoc.element.mathjs.combinations)
- description and source-code
```javascript
function combinations(arg0, arg1) {
  "use strict";
  var name = 'combinations';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'BigNumber');
  }
  if (test2(arg0)) { // type: Fraction (convert to BigNumber)
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert1(arg0), arg1); // signature: Fraction, BigNumber ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.compare"></a>[function <span class="apidocSignatureSpan">mathjs.</span>compare (arg0, arg1)](#apidoc.element.mathjs.compare)
- description and source-code
```javascript
function compare(arg0, arg1) {
  "use strict";
  var name = 'compare';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, Big ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.compile"></a>[function <span class="apidocSignatureSpan">mathjs.</span>compile (arg0)](#apidoc.element.mathjs.compile)
- description and source-code
```javascript
function compile(arg0) {
  "use strict";
  var name = 'compile';
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature0(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: number (convert to string)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: boolean (convert to string)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: null (convert to string)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.complex"></a>[function <span class="apidocSignatureSpan">mathjs.</span>complex (arg0, arg1)](#apidoc.element.mathjs.complex)
- description and source-code
```javascript
function complex(arg0, arg1) {
  "use strict";
  var name = 'complex';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature1(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature3(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature3(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature2(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature2(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature2(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test6(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature4(convert5(arg0)); // signature: BigNumber
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature3(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature3(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature3(arg0, convert6(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature3(arg0, convert7(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) { ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.composition"></a>[function <span class="apidocSignatureSpan">mathjs.</span>composition (arg0, arg1)](#apidoc.element.mathjs.composition)
- description and source-code
```javascript
function composition(arg0, arg1) {
  "use strict";
  var name = 'composition';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test2(arg0)) { // type: Fraction (convert to BigNumber)
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: Fraction, number
      }
      if (arguments.length > 2) {
        throw createE ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.concat"></a>[function <span class="apidocSignatureSpan">mathjs.</span>concat (arg0)](#apidoc.element.mathjs.concat)
- description and source-code
```javascript
function concat(arg0) {
  "use strict";
  var name = 'concat';
  if (test0(arg0)) { // type: ...string
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test0(arguments[i])) {
        varArgs.push(arguments[i]);
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'string');
      }
    }
    return signature0(varArgs); // signature: ...string
  }
  if (test1(arg0) || test2(arg0) || test3(arg0) || test4(arg0) || test5(arg0) || test6(arg0) || test6(arg0) || test7(arg0) || test7
(arg0)) { // type: ...Array|Matrix|number|BigNumber|Fraction|boolean|null
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test1(arguments[i]) || test2(arguments[i]) || test3(arguments[i]) || test4(arguments[i])) {
        varArgs.push(arguments[i]);
      }
      else if (test5(arguments[i])) {
        varArgs.push(convert0(arguments[i]));
      }
      else if (test6(arguments[i])) {
        varArgs.push(convert1(arguments[i]));
      }
      else if (test6(arguments[i])) {
        varArgs.push(convert2(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert3(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert4(arguments[i]));
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
      }
    }
    return signature1(varArgs); // signature: ...Array|Matrix|number|BigNumber|Fraction|boolean|null
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Array,Matrix,number,BigNumber');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.config"></a>[function <span class="apidocSignatureSpan">mathjs.</span>config (options)](#apidoc.element.mathjs.config)
- description and source-code
```javascript
function _config(options) {
  if (options) {
    var prev = object.clone(config);

    // validate some of the options
    validateOption(options, 'matrix', MATRIX);
    validateOption(options, 'number', NUMBER);

    // merge options
    object.deepExtend(config, options);

    var curr = object.clone(config);

    var changes = object.clone(options);

    // emit 'config' event
    math.emit('config', curr, prev, changes);

    return curr;
  }
  else {
    return object.clone(config);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.conj"></a>[function <span class="apidocSignatureSpan">mathjs.</span>conj (arg0)](#apidoc.element.mathjs.conj)
- description and source-code
```javascript
function conj(arg0) {
  "use strict";
  var name = 'conj';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.cos"></a>[function <span class="apidocSignatureSpan">mathjs.</span>cos (arg0)](#apidoc.element.mathjs.cos)
- description and source-code
```javascript
function cos(arg0) {
  "use strict";
  var name = 'cos';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.cosh"></a>[function <span class="apidocSignatureSpan">mathjs.</span>cosh (arg0)](#apidoc.element.mathjs.cosh)
- description and source-code
```javascript
function cosh(arg0) {
  "use strict";
  var name = 'cosh';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.cot"></a>[function <span class="apidocSignatureSpan">mathjs.</span>cot (arg0)](#apidoc.element.mathjs.cot)
- description and source-code
```javascript
function cot(arg0) {
  "use strict";
  var name = 'cot';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.coth"></a>[function <span class="apidocSignatureSpan">mathjs.</span>coth (arg0)](#apidoc.element.mathjs.coth)
- description and source-code
```javascript
function coth(arg0) {
  "use strict";
  var name = 'coth';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.create"></a>[function <span class="apidocSignatureSpan">mathjs.</span>create (config)](#apidoc.element.mathjs.create)
- description and source-code
```javascript
function create(config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.createUnit"></a>[function <span class="apidocSignatureSpan">mathjs.</span>createUnit (arg0, arg1, arg2)](#apidoc.element.mathjs.createUnit)
- description and source-code
```javascript
function createUnit(arg0, arg1, arg2) {
  "use strict";
  var name = 'createUnit';
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature0(arg0); // signature: string
    }
    if (test1(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: string, Unit
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: string, Unit, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: string, string
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: string, string, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test3(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: string, number
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(arg0, convert0(arg1), arg2); // signature: string, number, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test4(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: string, boolean
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(arg0, convert1(arg1), arg2); // signature: string, boolean, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test5(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: string, null
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(arg0, convert2(arg1), arg2); // signature: string, null, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test2(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: string, Object
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: string, Object, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Unit,string,Object');
  }
  if (test3(arg0)) { // type: number (convert to string)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: number
    }
    if (test1(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Unit
      }
      if (test2(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature2(convert0(arg0), arg1, arg2); // signature: number, Unit, Object
        } ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.cross"></a>[function <span class="apidocSignatureSpan">mathjs.</span>cross (arg0, arg1)](#apidoc.element.mathjs.cross)
- description and source-code
```javascript
function cross(arg0, arg1) {
  "use strict";
  var name = 'cross';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.csc"></a>[function <span class="apidocSignatureSpan">mathjs.</span>csc (arg0)](#apidoc.element.mathjs.csc)
- description and source-code
```javascript
function csc(arg0) {
  "use strict";
  var name = 'csc';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.csch"></a>[function <span class="apidocSignatureSpan">mathjs.</span>csch (arg0)](#apidoc.element.mathjs.csch)
- description and source-code
```javascript
function csch(arg0) {
  "use strict";
  var name = 'csch';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.cube"></a>[function <span class="apidocSignatureSpan">mathjs.</span>cube (arg0)](#apidoc.element.mathjs.cube)
- description and source-code
```javascript
function cube(arg0) {
  "use strict";
  var name = 'cube';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.deepEqual"></a>[function <span class="apidocSignatureSpan">mathjs.</span>deepEqual (arg0, arg1)](#apidoc.element.mathjs.deepEqual)
- description and source-code
```javascript
function deepEqual(arg0, arg1) {
  "use strict";
  var name = 'deepEqual';
  // type: any
  // type: any
  if (arguments.length === 2) {
    return signature0(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'any');
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.derivative"></a>[function <span class="apidocSignatureSpan">mathjs.</span>derivative (arg0, arg1, arg2)](#apidoc.element.mathjs.derivative)
- description and source-code
```javascript
function derivative(arg0, arg1, arg2) {
  "use strict";
  var name = 'derivative';
  if (test0(arg0)) { // type: string
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, string
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, arg1, arg2); // signature: string, string, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test2(arg1)) { // type: SymbolNode
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: string, SymbolNode
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature3(arg0, arg1, arg2); // signature: string, SymbolNode, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test3(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: string, number
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, convert0(arg1), arg2); // signature: string, number, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test4(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: string, boolean
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, convert1(arg1), arg2); // signature: string, boolean, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test5(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: string, null
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, convert2(arg1), arg2); // signature: string, null, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string,SymbolNode');
  }
  if (test6(arg0)) { // type: Node
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Node, string
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature5(arg0, arg1, arg2); // signature: Node, string, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test2(arg1)) { // type: SymbolNode
      if (arguments.length === 2) {
        return signature6(arg0, arg1); // signature: Node, SymbolNode
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature7(arg0, arg1, arg2); // signature: Node, SymbolNode, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (t ...
```
- example usage
```shell
...

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7
...
```

#### <a name="apidoc.element.mathjs.det"></a>[function <span class="apidocSignatureSpan">mathjs.</span>det (arg0)](#apidoc.element.mathjs.det)
- description and source-code
```javascript
function det(arg0) {
  "use strict";
  var name = 'det';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
  }
  // type: any
  if (arguments.length === 1) {
    return signature1(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.diag"></a>[function <span class="apidocSignatureSpan">mathjs.</span>diag (arg0, arg1, arg2)](#apidoc.element.mathjs.diag)
- description and source-code
```javascript
function diag(arg0, arg1, arg2) {
  "use strict";
  var name = 'diag';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
      if (test2(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: Array, number, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature2(arg0, arg1, convert0(arg2)); // signature: Array, number, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature2(arg0, arg1, convert1(arg2)); // signature: Array, number, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature2(arg0, arg1, convert2(arg2)); // signature: Array, number, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string');
    }
    if (test5(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, BigNumber
      }
      if (test2(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature4(arg0, arg1, arg2); // signature: Array, BigNumber, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, arg1, convert0(arg2)); // signature: Array, BigNumber, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, arg1, convert1(arg2)); // signature: Array, BigNumber, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, arg1, convert2(arg2)); // signature: Array, BigNumber, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string');
    }
    if (test2(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: Array, string
      }
      if (test2(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature2(arg0, convert3(arg1), arg2); // signature: Array, string, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature2(arg0, convert3(arg1), convert0(arg2)); // signature: Array, string, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to string)
        if (argume ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.distance"></a>[function <span class="apidocSignatureSpan">mathjs.</span>distance (arg0, arg1, arg2)](#apidoc.element.mathjs.distance)
- description and source-code
```javascript
function distance(arg0, arg1, arg2) {
  "use strict";
  var name = 'distance';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Array
      }
      if (test0(arg2)) { // type: Array
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: Array, Array, Array
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: Matrix (convert to Array)
        if (arguments.length === 3) {
          return signature2(arg0, arg1, convert0(arg2)); // signature: Array, Array, Matrix
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array');
    }
    if (test1(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Matrix
      }
      if (test0(arg2)) { // type: Array
        if (arguments.length === 3) {
          return signature2(arg0, convert0(arg1), arg2); // signature: Array, Matrix, Array
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: Matrix (convert to Array)
        if (arguments.length === 3) {
          return signature2(arg0, convert0(arg1), convert0(arg2)); // signature: Array, Matrix, Matrix
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test1(arg0)) { // type: Matrix (convert to Array)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: Matrix
    }
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: Matrix, Array
      }
      if (test0(arg2)) { // type: Array
        if (arguments.length === 3) {
          return signature2(convert0(arg0), arg1, arg2); // signature: Matrix, Array, Array
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: Matrix (convert to Array)
        if (arguments.length === 3) {
          return signature2(convert0(arg0), arg1, convert0(arg2)); // signature: Matrix, Array, Matrix
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array');
    }
    if (test1(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert0(arg1)); // signature: Matrix, Matrix
      }
      if (test0(arg2)) { // type: Array
        if (arguments.length === 3) {
          return signature2(convert0(arg0), convert0(arg1), arg2); // signature: Matrix, Matrix, Array
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: Matrix (convert to Array)
        if (arguments.length === 3) {
          return signature2(convert0(arg0), convert0(arg1), convert0(arg2)); // signature: Matrix, Matrix, Matrix
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test2(arg0)) { / ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.divide"></a>[function <span class="apidocSignatureSpan">mathjs.</span>divide (arg0, arg1)](#apidoc.element.mathjs.divide)
- description and source-code
```javascript
function divide(arg0, arg1) {
  "use strict";
  var name = 'divide';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, Unit
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      } ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.dot"></a>[function <span class="apidocSignatureSpan">mathjs.</span>dot (arg0, arg1)](#apidoc.element.mathjs.dot)
- description and source-code
```javascript
function dot(arg0, arg1) {
  "use strict";
  var name = 'dot';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.dotDivide"></a>[function <span class="apidocSignatureSpan">mathjs.</span>dotDivide (arg0, arg1)](#apidoc.element.mathjs.dotDivide)
- description and source-code
```javascript
function dotDivide(arg0, arg1) {
  "use strict";
  var name = 'dotDivide';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature2(arg0, arg1); // signature: Array, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature5(arg0, arg1); // signature: Matrix, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  // type: any
  if (test0(arg1)) { // type: Array
    if (arguments.length === 2) {
      return signature6(arg0, arg1); // signature: any, Array
    }
  }
  if (test1(arg1)) { // type: Matrix
    if (arguments.length === 2) {
      return signature7(arg0, arg1); // signature: any, Matrix
    }
  }
  // type: any
  if (arguments.length === 2) {
    return signature8(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix,any');
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.dotMultiply"></a>[function <span class="apidocSignatureSpan">mathjs.</span>dotMultiply (arg0, arg1)](#apidoc.element.mathjs.dotMultiply)
- description and source-code
```javascript
function dotMultiply(arg0, arg1) {
  "use strict";
  var name = 'dotMultiply';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature2(arg0, arg1); // signature: Array, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature5(arg0, arg1); // signature: Matrix, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  // type: any
  if (test0(arg1)) { // type: Array
    if (arguments.length === 2) {
      return signature6(arg0, arg1); // signature: any, Array
    }
  }
  if (test1(arg1)) { // type: Matrix
    if (arguments.length === 2) {
      return signature7(arg0, arg1); // signature: any, Matrix
    }
  }
  // type: any
  if (arguments.length === 2) {
    return signature8(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix,any');
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.dotPow"></a>[function <span class="apidocSignatureSpan">mathjs.</span>dotPow (arg0, arg1)](#apidoc.element.mathjs.dotPow)
- description and source-code
```javascript
function dotPow(arg0, arg1) {
  "use strict";
  var name = 'dotPow';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature2(arg0, arg1); // signature: Array, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature5(arg0, arg1); // signature: Matrix, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  // type: any
  if (test0(arg1)) { // type: Array
    if (arguments.length === 2) {
      return signature6(arg0, arg1); // signature: any, Array
    }
  }
  if (test1(arg1)) { // type: Matrix
    if (arguments.length === 2) {
      return signature7(arg0, arg1); // signature: any, Matrix
    }
  }
  // type: any
  if (arguments.length === 2) {
    return signature8(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix,any');
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.emit"></a>[function <span class="apidocSignatureSpan">mathjs.</span>emit ()](#apidoc.element.mathjs.emit)
- description and source-code
```javascript
emit = function () { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.equal"></a>[function <span class="apidocSignatureSpan">mathjs.</span>equal (arg0, arg1)](#apidoc.element.mathjs.equal)
- description and source-code
```javascript
function equal(arg0, arg1) {
  "use strict";
  var name = 'equal';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature2(arg0, arg1); // signature: Array, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature5(arg0, arg1); // signature: Matrix, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  // type: any
  if (test0(arg1)) { // type: Array
    if (arguments.length === 2) {
      return signature6(arg0, arg1); // signature: any, Array
    }
  }
  if (test1(arg1)) { // type: Matrix
    if (arguments.length === 2) {
      return signature7(arg0, arg1); // signature: any, Matrix
    }
  }
  // type: any
  if (arguments.length === 2) {
    return signature8(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix,any');
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.erf"></a>[function <span class="apidocSignatureSpan">mathjs.</span>erf (arg0)](#apidoc.element.mathjs.erf)
- description and source-code
```javascript
function erf(arg0) {
  "use strict";
  var name = 'erf';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature1(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.ArgumentsError"></a>[function <span class="apidocSignatureSpan">mathjs.</span>error.ArgumentsError (fn, count, min, max)](#apidoc.element.mathjs.error.ArgumentsError)
- description and source-code
```javascript
function ArgumentsError(fn, count, min, max) {
  if (!(this instanceof ArgumentsError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.fn = fn;
  this.count = count;
  this.min = min;
  this.max = max;

  this.message = 'Wrong number of arguments in function ' + fn +
      ' (' + count + ' provided, ' +
      min + ((max != undefined) ? ('-' + max) : '') + ' expected)';

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.ArgumentsError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">mathjs.</span>error.ArgumentsError.prototype.constructor ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.DimensionError"></a>[function <span class="apidocSignatureSpan">mathjs.</span>error.DimensionError (actual, expected, relation)](#apidoc.element.mathjs.error.DimensionError)
- description and source-code
```javascript
function DimensionError(actual, expected, relation) {
  if (!(this instanceof DimensionError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.actual   = actual;
  this.expected = expected;
  this.relation = relation;

  this.message = 'Dimension mismatch (' +
      (Array.isArray(actual) ? ('[' + actual.join(', ') + ']') : actual) +
      ' ' + (this.relation || '!=') + ' ' +
      (Array.isArray(expected) ? ('[' + expected.join(', ') + ']') : expected) +
      ')';

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.IndexError"></a>[function <span class="apidocSignatureSpan">mathjs.</span>error.IndexError (index, min, max)](#apidoc.element.mathjs.error.IndexError)
- description and source-code
```javascript
function IndexError(index, min, max) {
  if (!(this instanceof IndexError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.index = index;
  if (arguments.length < 3) {
    this.min = 0;
    this.max = min;
  }
  else {
    this.min = min;
    this.max = max;
  }

  if (this.min !== undefined && this.index < this.min) {
    this.message = 'Index out of range (' + this.index + ' < ' + this.min + ')';
  }
  else if (this.max !== undefined && this.index >= this.max) {
    this.message = 'Index out of range (' + this.index + ' > ' + (this.max - 1) + ')';
  }
  else {
    this.message = 'Index out of range (' + this.index + ')';
  }

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.eval"></a>[function <span class="apidocSignatureSpan">mathjs.</span>eval (arg0, arg1)](#apidoc.element.mathjs.eval)
- description and source-code
```javascript
function compile(arg0, arg1) {
  "use strict";
  var name = 'compile';
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature0(arg0); // signature: string
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: string, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test4(arg0)) { // type: number (convert to string)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: number
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test5(arg0)) { // type: boolean (convert to string)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature1(convert1(arg0), arg1); // signature: boolean, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test6(arg0)) { // type: null (convert to string)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature1(convert2(arg0), arg1); // signature: null, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Array,Matrix');
}
```
- example usage
```shell
...
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
...
```

#### <a name="apidoc.element.mathjs.exp"></a>[function <span class="apidocSignatureSpan">mathjs.</span>exp (arg0)](#apidoc.element.mathjs.exp)
- description and source-code
```javascript
function exp(arg0) {
  "use strict";
  var name = 'exp';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser"></a>[function <span class="apidocSignatureSpan">mathjs.</span>expression.Parser ()](#apidoc.element.mathjs.expression.Parser)
- description and source-code
```javascript
function Parser() {
  if (!(this instanceof Parser)) {
    throw new SyntaxError(
        'Constructor must be called with the new operator');
  }
  this.scope = {};
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse"></a>[function <span class="apidocSignatureSpan">mathjs.</span>expression.parse (expr, options)](#apidoc.element.mathjs.expression.parse)
- description and source-code
```javascript
function parse(expr, options) {
  if (arguments.length != 1 && arguments.length != 2) {
    throw new ArgumentsError('parse', arguments.length, 1, 2);
  }

  // pass extra nodes
  extra_nodes = (options && options.nodes) ? options.nodes : {};

  if (typeof expr === 'string') {
    // parse a single expression
    expression = expr;
    return parseStart();
  }
  else if (Array.isArray(expr) || expr instanceof type.Matrix) {
    // parse an array or matrix with expressions
    return deepMap(expr, function (elem) {
      if (typeof elem !== 'string') throw new TypeError('String expected');

      expression = elem;
      return parseStart();
    });
  }
  else {
    // oops
    throw new TypeError('String or matrix expected');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.eye"></a>[function <span class="apidocSignatureSpan">mathjs.</span>eye (arg0, arg1, arg2)](#apidoc.element.mathjs.eye)
- description and source-code
```javascript
function eye(arg0, arg1, arg2) {
  "use strict";
  var name = 'eye';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature1(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, number
      }
      if (test1(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature3(arg0, arg1, arg2); // signature: number, number, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test0(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, arg1, convert0(arg2)); // signature: number, number, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, arg1, convert1(arg2)); // signature: number, number, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, arg1, convert2(arg2)); // signature: number, number, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string');
    }
    if (test4(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, BigNumber
      }
      if (test1(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature3(arg0, arg1, arg2); // signature: number, BigNumber, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test0(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, arg1, convert0(arg2)); // signature: number, BigNumber, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, arg1, convert1(arg2)); // signature: number, BigNumber, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, arg1, convert2(arg2)); // signature: number, BigNumber, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string');
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, string
      }
      if (test1(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature3(arg0, convert3(arg1), arg2); // signature: number, string, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test0(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature3(arg0, convert3(arg1), convert0(arg2)); // signature: number, string, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        } ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.factorial"></a>[function <span class="apidocSignatureSpan">mathjs.</span>factorial (arg0)](#apidoc.element.mathjs.factorial)
- description and source-code
```javascript
function factorial(arg0) {
  "use strict";
  var name = 'factorial';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature1(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.filter"></a>[function <span class="apidocSignatureSpan">mathjs.</span>filter (arg0, arg1)](#apidoc.element.mathjs.filter)
- description and source-code
```javascript
function filter(arg0, arg1) {
  "use strict";
  var name = 'filter';
  if (test0(arg0)) { // type: Array
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: RegExp
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, RegExp
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function,RegExp');
  }
  if (test3(arg0)) { // type: Matrix
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: RegExp
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, RegExp
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function,RegExp');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.fix"></a>[function <span class="apidocSignatureSpan">mathjs.</span>fix (arg0)](#apidoc.element.mathjs.fix)
- description and source-code
```javascript
function fix(arg0) {
  "use strict";
  var name = 'fix';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.flatten"></a>[function <span class="apidocSignatureSpan">mathjs.</span>flatten (arg0)](#apidoc.element.mathjs.flatten)
- description and source-code
```javascript
function flatten(arg0) {
  "use strict";
  var name = 'flatten';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.floor"></a>[function <span class="apidocSignatureSpan">mathjs.</span>floor (arg0)](#apidoc.element.mathjs.floor)
- description and source-code
```javascript
function floor(arg0) {
  "use strict";
  var name = 'floor';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.</span>forEach (arg0, arg1)](#apidoc.element.mathjs.forEach)
- description and source-code
```javascript
function forEach(arg0, arg1) {
  "use strict";
  var name = 'forEach';
  if (test0(arg0)) { // type: Array
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  if (test2(arg0)) { // type: Matrix
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.format"></a>[function <span class="apidocSignatureSpan">mathjs.</span>format (arg0, arg1)](#apidoc.element.mathjs.format)
- description and source-code
```javascript
function format(arg0, arg1) {
  "use strict";
  var name = 'format';
  // type: any
  if (arguments.length === 1) {
    return signature0(arg0); // signature: any
  }
  if (test0(arg1)) { // type: number
    if (arguments.length === 2) {
      return signature0(arg0, arg1); // signature: any, number
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test1(arg1)) { // type: function
    if (arguments.length === 2) {
      return signature0(arg0, arg1); // signature: any, function
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test2(arg1)) { // type: string (convert to number)
    if (arguments.length === 2) {
      return signature0(arg0, convert0(arg1)); // signature: any, string
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test3(arg1)) { // type: boolean (convert to number)
    if (arguments.length === 2) {
      return signature0(arg0, convert1(arg1)); // signature: any, boolean
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test4(arg1)) { // type: null (convert to number)
    if (arguments.length === 2) {
      return signature0(arg0, convert2(arg1)); // signature: any, null
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test5(arg1)) { // type: Object
    if (arguments.length === 2) {
      return signature0(arg0, arg1); // signature: any, Object
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  throw createError(name, arguments.length, 1, arguments[1], 'number,function,Object');
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.fraction"></a>[function <span class="apidocSignatureSpan">mathjs.</span>fraction (arg0, arg1)](#apidoc.element.mathjs.fraction)
- description and source-code
```javascript
function fraction(arg0, arg1) {
  "use strict";
  var name = 'fraction';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number');
  }
  if (test4(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature4(arg0); // signature: string
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: string, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert0(arg1)); // signature: string, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: string, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert2(arg1)); // signature: string, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number');
  }
  if (test6(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(convert1(arg0), arg1); // signature: bo ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.gamma"></a>[function <span class="apidocSignatureSpan">mathjs.</span>gamma (arg0)](#apidoc.element.mathjs.gamma)
- description and source-code
```javascript
function gamma(arg0) {
  "use strict";
  var name = 'gamma';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.gcd"></a>[function <span class="apidocSignatureSpan">mathjs.</span>gcd (arg0, arg1, arg2)](#apidoc.element.mathjs.gcd)
- description and source-code
```javascript
function gcd(arg0, arg1, arg2) {
  "use strict";
  var name = 'gcd';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, arg1, varArgs); // signature: number, number, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test3(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, arg1, varArgs); // signature: number, BigNumber, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test4(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert1(arg0), arg1); // signature: number, Fraction
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, convert2(arg1), varArgs); // signature: number, Fraction, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, Array
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, arg1, varArgs); // signature: number, Array, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test2(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: number, Matrix
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]); ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.help"></a>[function <span class="apidocSignatureSpan">mathjs.</span>help (arg0)](#apidoc.element.mathjs.help)
- description and source-code
```javascript
function help(arg0) {
  "use strict";
  var name = 'help';
  // type: any
  if (arguments.length === 1) {
    return signature0(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.hypot"></a>[function <span class="apidocSignatureSpan">mathjs.</span>hypot (arg0)](#apidoc.element.mathjs.hypot)
- description and source-code
```javascript
function hypot(arg0) {
  "use strict";
  var name = 'hypot';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0) || test3(arg0) || test4(arg0) || test5(arg0) || test5(arg0) || test6(arg0) || test6(arg0) || test7(arg0) || test7
(arg0)) { // type: ...number|BigNumber|Fraction|string|boolean|null
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test2(arguments[i]) || test3(arguments[i])) {
        varArgs.push(arguments[i]);
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert0(arguments[i]));
      }
      else if (test5(arguments[i])) {
        varArgs.push(convert1(arguments[i]));
      }
      else if (test5(arguments[i])) {
        varArgs.push(convert2(arguments[i]));
      }
      else if (test6(arguments[i])) {
        varArgs.push(convert3(arguments[i]));
      }
      else if (test6(arguments[i])) {
        varArgs.push(convert4(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert5(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert6(arguments[i]));
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'number,BigNumber');
      }
    }
    return signature2(varArgs); // signature: ...number|BigNumber|Fraction|string|boolean|null
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,number,BigNumber');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.im"></a>[function <span class="apidocSignatureSpan">mathjs.</span>im (arg0)](#apidoc.element.mathjs.im)
- description and source-code
```javascript
function im(arg0) {
  "use strict";
  var name = 'im';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.import"></a>[function <span class="apidocSignatureSpan">mathjs.</span>import (object, options)](#apidoc.element.mathjs.import)
- description and source-code
```javascript
function math_import(object, options) {
  var num = arguments.length;
  if (num != 1 && num != 2) {
    throw new ArgumentsError('import', num, 1, 2);
  }

  if (!options) {
    options = {};
  }

  if (isFactory(object)) {
    _importFactory(object, options);
  }
  // TODO: allow a typed-function with name too
  else if (Array.isArray(object)) {
    object.forEach(function (entry) {
      math_import(entry, options);
    });
  }
  else if (typeof object === 'object') {
    // a map with functions
    for (var name in object) {
      if (object.hasOwnProperty(name)) {
        var value = object[name];
        if (isSupportedType(value)) {
          _import(name, value, options);
        }
        else if (isFactory(object)) {
          _importFactory(object, options);
        }
        else {
          math_import(value, options);
        }
      }
    }
  }
  else {
    if (!options.silent) {
      throw new TypeError('Factory, Object, or Array expected');
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.index"></a>[function <span class="apidocSignatureSpan">mathjs.</span>index (arg0)](#apidoc.element.mathjs.index)
- description and source-code
```javascript
function index(arg0) {
  "use strict";
  var name = 'index';
  if (test0(arg0) || test1(arg0) || test2(arg0) || test3(arg0) || test4(arg0) || test5(arg0) || test6(arg0) || test7(arg0) || test7
(arg0) || test7(arg0) || test8(arg0) || test8(arg0) || test8(arg0)) { // type: ...number|string|BigNumber|Range|Array|Matrix|Fraction
|boolean|null
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test0(arguments[i]) || test1(arguments[i]) || test2(arguments[i]) || test3(arguments[i]) || test4(arguments[i]) || test5
(arguments[i])) {
        varArgs.push(arguments[i]);
      }
      else if (test6(arguments[i])) {
        varArgs.push(convert0(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert1(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert2(arguments[i]));
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert3(arguments[i]));
      }
      else if (test8(arguments[i])) {
        varArgs.push(convert4(arguments[i]));
      }
      else if (test8(arguments[i])) {
        varArgs.push(convert5(arguments[i]));
      }
      else if (test8(arguments[i])) {
        varArgs.push(convert6(arguments[i]));
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'number,string,BigNumber,Range,Array,Matrix');
      }
    }
    return signature0(varArgs); // signature: ...number|string|BigNumber|Range|Array|Matrix|Fraction|boolean|null
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,string,BigNumber,Range,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.intersect"></a>[function <span class="apidocSignatureSpan">mathjs.</span>intersect (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.intersect)
- description and source-code
```javascript
function intersect(arg0, arg1, arg2, arg3) {
  "use strict";
  var name = 'intersect';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (test0(arg2)) { // type: Array
        if (arguments.length === 3) {
          return signature0(arg0, arg1, arg2); // signature: Array, Array, Array
        }
        if (test0(arg3)) { // type: Array
          if (arguments.length === 4) {
            return signature1(arg0, arg1, arg2, arg3); // signature: Array, Array, Array, Array
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test1(arg3)) { // type: Matrix (convert to Array)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, arg2, convert0(arg3)); // signature: Array, Array, Array, Matrix
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'Array');
      }
      if (test1(arg2)) { // type: Matrix (convert to Array)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert0(arg2)); // signature: Array, Array, Matrix
        }
        if (test0(arg3)) { // type: Array
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert0(arg2), arg3); // signature: Array, Array, Matrix, Array
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test1(arg3)) { // type: Matrix (convert to Array)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert0(arg2), convert0(arg3)); // signature: Array, Array, Matrix, Matrix
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'Array');
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array');
    }
    if (test1(arg1)) { // type: Matrix
      if (test0(arg2)) { // type: Array
        if (arguments.length === 3) {
          return signature0(arg0, convert0(arg1), arg2); // signature: Array, Matrix, Array
        }
        if (test0(arg3)) { // type: Array
          if (arguments.length === 4) {
            return signature1(arg0, convert0(arg1), arg2, arg3); // signature: Array, Matrix, Array, Array
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test1(arg3)) { // type: Matrix (convert to Array)
          if (arguments.length === 4) {
            return signature1(arg0, convert0(arg1), arg2, convert0(arg3)); // signature: Array, Matrix, Array, Matrix
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'Array');
      }
      if (test1(arg2)) { // type: Matrix
        if (arguments.length === 3) {
          return signature2(convert1(arg0), arg1, arg2); // signature: Array, Matrix, Matrix
        }
        if (test0(arg3)) { // type: Array
          if (arguments.length === 4) {
            return signature1(arg0, convert0(arg1), convert0(arg2), arg3); // signature: Array, Matrix, Matrix, Array
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test1(arg3)) { // type: Matrix
          if (arguments.length === 4) {
            return signature3(convert1(arg0), arg1, arg2, arg3); // signature: Array, Matrix, Matrix, Matrix
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'Array,Matrix ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.inv"></a>[function <span class="apidocSignatureSpan">mathjs.</span>inv (arg0)](#apidoc.element.mathjs.inv)
- description and source-code
```javascript
function inv(arg0) {
  "use strict";
  var name = 'inv';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
  }
  // type: any
  if (arguments.length === 1) {
    return signature1(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isInteger"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isInteger (arg0)](#apidoc.element.mathjs.isInteger)
- description and source-code
```javascript
function isInteger(arg0) {
  "use strict";
  var name = 'isInteger';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Fraction,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isNaN"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isNaN (arg0)](#apidoc.element.mathjs.isNaN)
- description and source-code
```javascript
function isNaN(arg0) {
  "use strict";
  var name = 'isNaN';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isNegative"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isNegative (arg0)](#apidoc.element.mathjs.isNegative)
- description and source-code
```javascript
function isNegative(arg0) {
  "use strict";
  var name = 'isNegative';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isNumeric"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isNumeric (arg0)](#apidoc.element.mathjs.isNumeric)
- description and source-code
```javascript
function isNumeric(arg0) {
  "use strict";
  var name = 'isNumeric';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature0(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature1(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean
    if (arguments.length === 1) {
      return signature0(arg0); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,string,Array,Matrix,boolean
');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isPositive"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isPositive (arg0)](#apidoc.element.mathjs.isPositive)
- description and source-code
```javascript
function isPositive(arg0) {
  "use strict";
  var name = 'isPositive';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isPrime"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isPrime (arg0)](#apidoc.element.mathjs.isPrime)
- description and source-code
```javascript
function isPrime(arg0) {
  "use strict";
  var name = 'isPrime';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature1(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature1(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.isZero"></a>[function <span class="apidocSignatureSpan">mathjs.</span>isZero (arg0)](#apidoc.element.mathjs.isZero)
- description and source-code
```javascript
function isZero(arg0) {
  "use strict";
  var name = 'isZero';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.kldivergence"></a>[function <span class="apidocSignatureSpan">mathjs.</span>kldivergence (arg0, arg1)](#apidoc.element.mathjs.kldivergence)
- description and source-code
```javascript
function kldivergence(arg0, arg1) {
  "use strict";
  var name = 'kldivergence';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.kron"></a>[function <span class="apidocSignatureSpan">mathjs.</span>kron (arg0, arg1)](#apidoc.element.mathjs.kron)
- description and source-code
```javascript
function kron(arg0, arg1) {
  "use strict";
  var name = 'kron';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.larger"></a>[function <span class="apidocSignatureSpan">mathjs.</span>larger (arg0, arg1)](#apidoc.element.mathjs.larger)
- description and source-code
```javascript
function larger(arg0, arg1) {
  "use strict";
  var name = 'larger';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, BigNu ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.largerEq"></a>[function <span class="apidocSignatureSpan">mathjs.</span>largerEq (arg0, arg1)](#apidoc.element.mathjs.largerEq)
- description and source-code
```javascript
function largerEq(arg0, arg1) {
  "use strict";
  var name = 'largerEq';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, B ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.lcm"></a>[function <span class="apidocSignatureSpan">mathjs.</span>lcm (arg0, arg1, arg2)](#apidoc.element.mathjs.lcm)
- description and source-code
```javascript
function lcm(arg0, arg1, arg2) {
  "use strict";
  var name = 'lcm';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, arg1, varArgs); // signature: number, number, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test3(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, arg1, varArgs); // signature: number, BigNumber, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test4(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert1(arg0), arg1); // signature: number, Fraction
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, convert2(arg1), varArgs); // signature: number, Fraction, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, Array
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]);
          } else {
            throw createError(name, arguments.length, i, arguments[i], 'Array,Matrix,number,BigNumber');
          }
        }
        return signature1(arg0, arg1, varArgs); // signature: number, Array, ...Array|Matrix|number|BigNumber
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Array,Matrix,number,BigNumber');
    }
    if (test2(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: number, Matrix
      }
      if (test1(arg2) || test2(arg2) || test0(arg2) || test3(arg2)) { // type: ...Array|Matrix|number|BigNumber
        var varArgs = [arg2];
        for (var i = 3; i < arguments.length; i++) {
          if (test1(arguments[i]) || test2(arguments[i]) || test0(arguments[i]) || test3(arguments[i])) {
            varArgs.push(arguments[i]); ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.leftShift"></a>[function <span class="apidocSignatureSpan">mathjs.</span>leftShift (arg0, arg1)](#apidoc.element.mathjs.leftShift)
- description and source-code
```javascript
function leftShift(arg0, arg1) {
  "use strict";
  var name = 'leftShift';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,Array,Matrix');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: BigNumber, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, conv ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.log"></a>[function <span class="apidocSignatureSpan">mathjs.</span>log (arg0, arg1)](#apidoc.element.mathjs.log)
- description and source-code
```javascript
function log(arg0, arg1) {
  "use strict";
  var name = 'log';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
  }
  // type: any
  // type: any
  if (arguments.length === 2) {
    return signature4(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'any');
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix,any');
}
```
- example usage
```shell
...
'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
...
```

#### <a name="apidoc.element.mathjs.log10"></a>[function <span class="apidocSignatureSpan">mathjs.</span>log10 (arg0)](#apidoc.element.mathjs.log10)
- description and source-code
```javascript
function log10(arg0) {
  "use strict";
  var name = 'log10';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.lsolve"></a>[function <span class="apidocSignatureSpan">mathjs.</span>lsolve (arg0, arg1)](#apidoc.element.mathjs.lsolve)
- description and source-code
```javascript
function lsolve(arg0, arg1) {
  "use strict";
  var name = 'lsolve';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test2(arg0)) { // type: DenseMatrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: DenseMatrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: DenseMatrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test3(arg0)) { // type: SparseMatrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: SparseMatrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: SparseMatrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Matrix (convert to Array)
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,DenseMatrix,SparseMatrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.lup"></a>[function <span class="apidocSignatureSpan">mathjs.</span>lup (arg0)](#apidoc.element.mathjs.lup)
- description and source-code
```javascript
function lup(arg0) {
  "use strict";
  var name = 'lup';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: DenseMatrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: DenseMatrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: SparseMatrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: SparseMatrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Matrix (convert to Array)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,DenseMatrix,SparseMatrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.lusolve"></a>[function <span class="apidocSignatureSpan">mathjs.</span>lusolve (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.lusolve)
- description and source-code
```javascript
function lusolve(arg0, arg1, arg2, arg3) {
  "use strict";
  var name = 'lusolve';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test2(arg0)) { // type: DenseMatrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: DenseMatrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: DenseMatrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test3(arg0)) { // type: SparseMatrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: SparseMatrix, Array
      }
      if (test4(arg2)) { // type: number
        if (test4(arg3)) { // type: number
          if (arguments.length === 4) {
            return signature3(arg0, arg1, arg2, arg3); // signature: SparseMatrix, Array, number, number
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test5(arg3)) { // type: string (convert to number)
          if (arguments.length === 4) {
            return signature3(arg0, arg1, arg2, convert0(arg3)); // signature: SparseMatrix, Array, number, string
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test6(arg3)) { // type: boolean (convert to number)
          if (arguments.length === 4) {
            return signature3(arg0, arg1, arg2, convert1(arg3)); // signature: SparseMatrix, Array, number, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test7(arg3)) { // type: null (convert to number)
          if (arguments.length === 4) {
            return signature3(arg0, arg1, arg2, convert2(arg3)); // signature: SparseMatrix, Array, number, null
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'number');
      }
      if (test5(arg2)) { // type: string (convert to number)
        if (test4(arg3)) { // type: number
          if (arguments.length === 4) {
            return signature3(arg0, arg1, convert0(arg2), arg3); // signature: SparseMatrix, Array, string, number
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test5(arg3)) { // type: string (convert to number)
          if (arguments.length === 4) {
            return signature3(arg0, arg1, convert0(arg2), convert0(arg3)); // signature: SparseMatrix, Array, string, string
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test6(arg3)) { // type: boolean (convert to number)
          if (arguments.length === 4) {
            return signature3(arg0, arg1, convert0(arg2), convert1(arg3)); ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.mad"></a>[function <span class="apidocSignatureSpan">mathjs.</span>mad (arg0)](#apidoc.element.mathjs.mad)
- description and source-code
```javascript
function mad(arg0) {
  "use strict";
  var name = 'mad';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature1(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.map"></a>[function <span class="apidocSignatureSpan">mathjs.</span>map (arg0, arg1)](#apidoc.element.mathjs.map)
- description and source-code
```javascript
function map(arg0, arg1) {
  "use strict";
  var name = 'map';
  if (test0(arg0)) { // type: Array
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  if (test2(arg0)) { // type: Matrix
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.matrix"></a>[function <span class="apidocSignatureSpan">mathjs.</span>matrix (arg0, arg1, arg2)](#apidoc.element.mathjs.matrix)
- description and source-code
```javascript
function matrix(arg0, arg1, arg2) {
  "use strict";
  var name = 'matrix';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature1(arg0); // signature: string
    }
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: string, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: string, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: string, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert2(arg1)); // signature: string, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Array, string
      }
      if (test0(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature4(arg0, arg1, arg2); // signature: Array, string, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, arg1, convert0(arg2)); // signature: Array, string, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, arg1, convert1(arg2)); // signature: Array, string, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, arg1, convert2(arg2)); // signature: Array, string, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string');
    }
    if (test1(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature4(arg0, convert0(arg1)); // signature: Array, number
      }
      if (test0(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature4(arg0, convert0(arg1), arg2); // signature: Array, number, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature4(arg0, convert0(arg1), convert0(arg2)); // signature: Array, number, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature4(a ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.max"></a>[function <span class="apidocSignatureSpan">mathjs.</span>max (arg0, arg1)](#apidoc.element.mathjs.max)
- description and source-code
```javascript
function max(arg0, arg1) {
  "use strict";
  var name = 'max';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Array, null
      }
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.mean"></a>[function <span class="apidocSignatureSpan">mathjs.</span>mean (arg0, arg1)](#apidoc.element.mathjs.mean)
- description and source-code
```javascript
function mean(arg0, arg1) {
  "use strict";
  var name = 'mean';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Array, null
      }
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.median"></a>[function <span class="apidocSignatureSpan">mathjs.</span>median (arg0, arg1)](#apidoc.element.mathjs.median)
- description and source-code
```javascript
function median(arg0, arg1) {
  "use strict";
  var name = 'median';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Array, null
      }
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.min"></a>[function <span class="apidocSignatureSpan">mathjs.</span>min (arg0, arg1)](#apidoc.element.mathjs.min)
- description and source-code
```javascript
function min(arg0, arg1) {
  "use strict";
  var name = 'min';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Array, null
      }
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.mod"></a>[function <span class="apidocSignatureSpan">mathjs.</span>mod (arg0, arg1)](#apidoc.element.mathjs.mod)
- description and source-code
```javascript
function mod(arg0, arg1) {
  "use strict";
  var name = 'mod';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: Fraction
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert5(arg0), arg1); // signature: Fraction, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature2(arg0, ar ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.mode"></a>[function <span class="apidocSignatureSpan">mathjs.</span>mode (arg0)](#apidoc.element.mathjs.mode)
- description and source-code
```javascript
function mode(arg0) {
  "use strict";
  var name = 'mode';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature1(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.multinomial"></a>[function <span class="apidocSignatureSpan">mathjs.</span>multinomial (arg0)](#apidoc.element.mathjs.multinomial)
- description and source-code
```javascript
function multinomial(arg0) {
  "use strict";
  var name = 'multinomial';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.multiply"></a>[function <span class="apidocSignatureSpan">mathjs.</span>multiply (arg0, arg1, arg2)](#apidoc.element.mathjs.multiply)
- description and source-code
```javascript
function multiply(arg0, arg1, arg2) {
  "use strict";
  var name = 'multiply';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
    }
    if (test4(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, Unit
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
    }
    if (test4(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Complex, Unit
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
    }
    if (test5(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, BigNumber
      }
    }
    if (test4(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: BigNumber, Unit
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: BigNumber, number
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert9(arg1)); // signature: BigNumber, Fraction
      }
    }
    if (test5(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert10(arg1)); // signature: BigNumber, string
      }
    }
    if (test6(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert11(arg1)); // signature: BigNumber, boolean
      }
    }
    if (test7(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert12(arg1)); // signature ...
```
- example usage
```shell
...
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.norm"></a>[function <span class="apidocSignatureSpan">mathjs.</span>norm (arg0, arg1)](#apidoc.element.mathjs.norm)
- description and source-code
```javascript
function norm(arg0, arg1) {
  "use strict";
  var name = 'norm';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,string');
  }
  if (test6(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Complex
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Complex, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Complex, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,string');
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature3(arg0); // signature: BigNumber
    }
    if (test0(arg1)) { // type: nu ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.not"></a>[function <span class="apidocSignatureSpan">mathjs.</span>not (arg0)](#apidoc.element.mathjs.not)
- description and source-code
```javascript
function not(arg0) {
  "use strict";
  var name = 'not';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.nthRoot"></a>[function <span class="apidocSignatureSpan">mathjs.</span>nthRoot (arg0, arg1)](#apidoc.element.mathjs.nthRoot)
- description and source-code
```javascript
function nthRoot(arg0, arg1) {
  "use strict";
  var name = 'nthRoot';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,Array,Matrix');
  }
  if (test8(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Complex
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature6(arg0, arg1); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature6(arg0, convert2(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature6(arg0, convert3(arg1)); // signature: Complex, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature6(arg0, convert4(arg1)); // signature: Complex, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number');
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature7(arg0); // signature: BigNumber
    }
    if ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.number"></a>[function <span class="apidocSignatureSpan">mathjs.</span>number (arg0, arg1)](#apidoc.element.mathjs.number)
- description and source-code
```javascript
function number(arg0, arg1) {
  "use strict";
  var name = 'number';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature1(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (test3(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: Unit, Unit
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: Unit, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature5(arg0, convert0(arg1)); // signature: Unit, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature5(arg0, convert1(arg1)); // signature: Unit, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature5(arg0, convert2(arg1)); // signature: Unit, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Unit,string');
  }
  if (test4(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature6(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature7(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature7(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature1(convert3(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature1(convert4(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,BigNumber,Fraction,Unit,string,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.off"></a>[function <span class="apidocSignatureSpan">mathjs.</span>off ()](#apidoc.element.mathjs.off)
- description and source-code
```javascript
off = function () { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.on"></a>[function <span class="apidocSignatureSpan">mathjs.</span>on ()](#apidoc.element.mathjs.on)
- description and source-code
```javascript
on = function () { [native code] }
```
- example usage
```shell
...

var object = require('./utils/object');
var bigConstants = require('./utils/bignumber/constants');

function factory (type, config, load, typed, math) {
// listen for changed in the configuration, automatically reload
// constants when needed
math.on('config', function (curr, prev) {
  if (curr.number !== prev.number) {
    factory(type, config, load, typed, math);
  }
});

math['true']     = true;
math['false']    = false;
...
```

#### <a name="apidoc.element.mathjs.once"></a>[function <span class="apidocSignatureSpan">mathjs.</span>once ()](#apidoc.element.mathjs.once)
- description and source-code
```javascript
once = function () { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.ones"></a>[function <span class="apidocSignatureSpan">mathjs.</span>ones (arg0, arg1)](#apidoc.element.mathjs.ones)
- description and source-code
```javascript
function ones(arg0, arg1) {
  "use strict";
  var name = 'ones';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: Array, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: Array, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert2(arg1)); // signature: Array, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: Matrix, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: Matrix, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert2(arg1)); // signature: Matrix, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test2(arg0) || test6(arg0) || test1(arg0) || test7(arg0) || test3(arg0) || test3(arg0) || test3(arg0) || test4(arg0) || test4
(arg0) || test4(arg0)) { // type: ...number|BigNumber|string|Fraction|boolean|null
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test2(arguments[i]) || test6(arguments[i]) || test1(arguments[i])) {
        varArgs.push(arguments[i]);
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert3(arguments[i]));
      }
      else if (test3(arguments[i])) {
        varArgs.push(convert4(arguments[i]));
      }
      else if (test3(arguments[i])) {
        varArgs.push(convert5(arguments[i]));
      }
      else if (test3(arguments[i])) {
        varArgs.push(convert1(arguments[i]));
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert6(arguments[i]));
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert2(arguments[i]));
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert7(arguments[i]));
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'number,BigNumber,string');
      }
    }
    return signature4(varArgs); // signature: ...number|BigNum ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.or"></a>[function <span class="apidocSignatureSpan">mathjs.</span>or (arg0, arg1)](#apidoc.element.mathjs.or)
- description and source-code
```javascript
function or(arg0, arg1) {
  "use strict";
  var name = 'or';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(convert1(arg0), convert2(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // si ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.parse"></a>[function <span class="apidocSignatureSpan">mathjs.</span>parse (arg0, arg1)](#apidoc.element.mathjs.parse)
- description and source-code
```javascript
function parse(arg0, arg1) {
  "use strict";
  var name = 'parse';
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature0(arg0); // signature: string
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Matrix, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test4(arg0)) { // type: number (convert to string)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: number
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: number, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test5(arg0)) { // type: boolean (convert to string)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(convert1(arg0), arg1); // signature: boolean, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test6(arg0)) { // type: null (convert to string)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(convert2(arg0), arg1); // signature: null, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.parser"></a>[function <span class="apidocSignatureSpan">mathjs.</span>parser ()](#apidoc.element.mathjs.parser)
- description and source-code
```javascript
function parser() {
  "use strict";
  var name = 'parser';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (arguments.length > 0) {
    throw createError(name, arguments.length, 0, arguments[0]);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.partitionSelect"></a>[function <span class="apidocSignatureSpan">mathjs.</span>partitionSelect (arg0, arg1, arg2)](#apidoc.element.mathjs.partitionSelect)
- description and source-code
```javascript
function partitionSelect(arg0, arg1, arg2) {
  "use strict";
  var name = 'partitionSelect';
  if (test0(arg0)) { // type: Array
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, number
      }
      if (test2(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature1(arg0, arg1, arg2); // signature: Array, number, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: function
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: Array, number, function
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, arg1, convert0(arg2)); // signature: Array, number, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, arg1, convert1(arg2)); // signature: Array, number, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test5(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, arg1, convert2(arg2)); // signature: Array, number, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string,function');
    }
    if (test2(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: Array, string
      }
      if (test2(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature1(arg0, convert3(arg1), arg2); // signature: Array, string, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: function
        if (arguments.length === 3) {
          return signature2(arg0, convert3(arg1), arg2); // signature: Array, string, function
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, convert3(arg1), convert0(arg2)); // signature: Array, string, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, convert3(arg1), convert1(arg2)); // signature: Array, string, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test5(arg2)) { // type: null (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, convert3(arg1), convert2(arg2)); // signature: Array, string, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'string,function');
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: Array, boolean
      }
      if (test2(arg2)) { // type: string
        if (arguments.length === 3) { ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.permutations"></a>[function <span class="apidocSignatureSpan">mathjs.</span>permutations (arg0, arg1)](#apidoc.element.mathjs.permutations)
- description and source-code
```javascript
function permutations(arg0, arg1) {
  "use strict";
  var name = 'permutations';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test1(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature0(arg0); // signature: BigNumber
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert5(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert6(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert7(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'BigNumber');
  }
  if (test2(arg0)) { // type: Fracti ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.pow"></a>[function <span class="apidocSignatureSpan">mathjs.</span>pow (arg0, arg1)](#apidoc.element.mathjs.pow)
- description and source-code
```javascript
function pow(arg0, arg1) {
  "use strict";
  var name = 'pow';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,Complex,BigNumber,Fraction');
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Complex');
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, argume ...
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.print"></a>[function <span class="apidocSignatureSpan">mathjs.</span>print (arg0, arg1, arg2)](#apidoc.element.mathjs.print)
- description and source-code
```javascript
function print(arg0, arg1, arg2) {
  "use strict";
  var name = 'print';
  if (test0(arg0)) { // type: string
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, Object
      }
      if (test2(arg2)) { // type: number
        if (arguments.length === 3) {
          return signature0(arg0, arg1, arg2); // signature: string, Object, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test0(arg2)) { // type: string (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert0(arg2)); // signature: string, Object, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert1(arg2)); // signature: string, Object, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: null (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert2(arg2)); // signature: string, Object, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature0(arg0, arg1, arg2); // signature: string, Object, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'number,Object');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test2(arg0)) { // type: number (convert to string)
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(convert3(arg0), arg1); // signature: number, Object
      }
      if (test2(arg2)) { // type: number
        if (arguments.length === 3) {
          return signature0(convert3(arg0), arg1, arg2); // signature: number, Object, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test0(arg2)) { // type: string (convert to number)
        if (arguments.length === 3) {
          return signature0(convert3(arg0), arg1, convert0(arg2)); // signature: number, Object, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to number)
        if (arguments.length === 3) {
          return signature0(convert3(arg0), arg1, convert1(arg2)); // signature: number, Object, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: null (convert to number)
        if (arguments.length === 3) {
          return signature0(convert3(arg0), arg1, convert2(arg2)); // signature: number, Object, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature0(convert3(arg0), arg1, arg2); // signature: number, Object, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'number,Object');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test3(arg0)) { // type: boolean (convert to string)
    if (test1(arg1)) { // typ ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.prod"></a>[function <span class="apidocSignatureSpan">mathjs.</span>prod (arg0, arg1)](#apidoc.element.mathjs.prod)
- description and source-code
```javascript
function prod(arg0, arg1) {
  "use strict";
  var name = 'prod';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Array, null
      }
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.quantileSeq"></a>[function <span class="apidocSignatureSpan">mathjs.</span>quantileSeq (data, probOrN, sorted)](#apidoc.element.mathjs.quantileSeq)
- description and source-code
```javascript
function quantileSeq(data, probOrN, sorted) {
  var probArr, dataArr, one;

  if (arguments.length < 2 || arguments.length > 3) {
    throw new SyntaxError('Function quantileSeq requires two or three parameters');
  }

  if (isCollection(data)) {
    sorted = sorted || false;
    if (typeof sorted === 'boolean') {
      dataArr = data.valueOf();
      if (isNumber(probOrN)) {
        if (probOrN < 0) {
          throw new Error('N/prob must be non-negative');
        }

        if (probOrN <= 1) {
          // quantileSeq([a, b, c, d, ...], prob[,sorted])
          return _quantileSeq(dataArr, probOrN, sorted);
        }

        if (probOrN > 1) {
          // quantileSeq([a, b, c, d, ...], N[,sorted])
          if (!isInteger(probOrN)) {
            throw new Error('N must be a positive integer');
          }

          var nPlusOne = probOrN + 1;
          probArr = new Array(probOrN);
          for (var i = 0; i < probOrN;) {
            probArr[i] = _quantileSeq(dataArr, (++i) / nPlusOne, sorted);
          }
          return probArr;
        }
      }

      if (probOrN && probOrN.isBigNumber) {
        if (probOrN.isNegative()) {
          throw new Error('N/prob must be non-negative');
        }

        one = new probOrN.constructor(1);

        if (probOrN.lte(one)) {
          // quantileSeq([a, b, c, d, ...], prob[,sorted])
          return _quantileSeq(dataArr, probOrN, sorted);
        }

        if (probOrN.gt(one)) {
          // quantileSeq([a, b, c, d, ...], N[,sorted])
          if (!probOrN.isInteger()) {
            throw new Error('N must be a positive integer');
          }

          // largest possible Array length is 2^32-1;
          // 2^32 < 10^15, thus safe conversion guaranteed
          var intN = probOrN.toNumber();
          if (intN > 4294967295) {
            throw new Error('N must be less than or equal to 2^32-1, as that is the maximum length of an Array');
          }

          var nPlusOne = new type.BigNumber(intN + 1);
          probArr = new Array(intN);
          for (var i = 0; i < intN;) {
            probArr[i] = _quantileSeq(dataArr, new type.BigNumber(++i).div(nPlusOne), sorted);
          }
          return probArr;
        }
      }

      if (Array.isArray(probOrN)) {
        // quantileSeq([a, b, c, d, ...], [prob1, prob2, ...][,sorted])
        probArr = new Array(probOrN.length);
        for (var i = 0; i < probArr.length; ++i) {
          var currProb = probOrN[i];
          if (isNumber(currProb)) {
            if (currProb < 0 || currProb > 1) {
              throw new Error('Probability must be between 0 and 1, inclusive');
            }
          } else if (currProb && currProb.isBigNumber) {
            one = new currProb.constructor(1);
            if (currProb.isNegative() || currProb.gt(one)) {
              throw new Error('Probability must be between 0 and 1, inclusive');
            }
          } else {
            throw new TypeError('Unexpected type of argument in function quantileSeq'); // FIXME: becomes redundant when converted
 to typed-function
          }

          probArr[i] = _quantileSeq(dataArr, currProb, sorted);
        }
        return probArr;
      }

      throw new TypeError('Unexpected type of argument in function quantileSeq'); // FIXME: becomes redundant when converted to
typed-function
    }

    throw new TypeError('Unexpected type of argument in function quantileSeq'); // FIXME: becomes redundant when converted to typed
-function
  }

  throw new TypeError('Unexpected type of argument in function quantileSeq'); // FIXME: becomes redundant when converted to typed
-function
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.range"></a>[function <span class="apidocSignatureSpan">mathjs.</span>range (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.range)
- description and source-code
```javascript
function range(arg0, arg1, arg2, arg3) {
  "use strict";
  var name = 'range';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, number
      }
      if (test0(arg2)) { // type: number
        if (arguments.length === 3) {
          return signature2(arg0, arg1, arg2); // signature: number, number, number
        }
        if (test1(arg3)) { // type: boolean
          if (arguments.length === 4) {
            return signature3(arg0, arg1, arg2, arg3); // signature: number, number, number, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'boolean');
      }
      if (test2(arg2)) { // type: BigNumber
        if (arguments.length === 3) {
          return signature4(convert1(arg0), convert1(arg1), arg2); // signature: number, number, BigNumber
        }
        if (test1(arg3)) { // type: boolean
          if (arguments.length === 4) {
            return signature5(convert1(arg0), convert1(arg1), arg2, arg3); // signature: number, number, BigNumber, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'boolean');
      }
      if (test1(arg2)) { // type: boolean
        if (arguments.length === 3) {
          return signature6(arg0, arg1, arg2); // signature: number, number, boolean
        }
        if (test1(arg3)) { // type: boolean
          if (arguments.length === 4) {
            return signature3(arg0, arg1, convert2(arg2), arg3); // signature: number, number, boolean, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'boolean');
      }
      if (test3(arg2)) { // type: Fraction (convert to BigNumber)
        if (arguments.length === 3) {
          return signature4(convert1(arg0), convert1(arg1), convert3(arg2)); // signature: number, number, Fraction
        }
        if (test1(arg3)) { // type: boolean
          if (arguments.length === 4) {
            return signature5(convert1(arg0), convert1(arg1), convert3(arg2), arg3); // signature: number, number, Fraction, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'boolean');
      }
      if (test4(arg2)) { // type: string (convert to number)
        if (arguments.length === 3) {
          return signature2(arg0, arg1, convert4(arg2)); // signature: number, number, string
        }
        if (test1(arg3)) { // type: boolean
          if (arguments.length === 4) {
            return signature3(arg0, arg1, convert4(arg2), arg3); // signature: number, number, string, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'boolean');
      }
      if (test5(arg2)) { // type: null (convert to number)
        if (arguments.length === 3) {
          return signature2(arg0, arg1, convert5(arg2)); // signature: number, number, null
        }
        if (test1(arg3)) { // type: boolean
          if (arguments.length === 4) {
            return signature3(arg0, arg1, convert5(arg2), arg3); // signature: number, number, null, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, ar ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.re"></a>[function <span class="apidocSignatureSpan">mathjs.</span>re (arg0)](#apidoc.element.mathjs.re)
- description and source-code
```javascript
function re(arg0) {
  "use strict";
  var name = 're';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.reshape"></a>[function <span class="apidocSignatureSpan">mathjs.</span>reshape (arg0, arg1)](#apidoc.element.mathjs.reshape)
- description and source-code
```javascript
function reshape(arg0, arg1) {
  "use strict";
  var name = 'reshape';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.resize"></a>[function <span class="apidocSignatureSpan">mathjs.</span>resize (x, size, defaultValue)](#apidoc.element.mathjs.resize)
- description and source-code
```javascript
function resize(x, size, defaultValue) {
  if (arguments.length != 2 && arguments.length != 3) {
    throw new ArgumentsError('resize', arguments.length, 2, 3);
  }

  if (size && size.isMatrix === true) {
    size = size.valueOf(); // get Array
  }

  if (size.length && size[0] && size[0].isBigNumber === true) {
    // convert bignumbers to numbers
    size = size.map(function (value) {
      return (value && value.isBigNumber === true) ? value.toNumber() : value;
    });
  }

  // check x is a Matrix
  if (x && x.isMatrix === true) {
    // use optimized matrix implementation, return copy
    return x.resize(size, defaultValue, true);
  }

  if (typeof x === 'string') {
    // resize string
    return _resizeString(x, size, defaultValue);
  }

  // check result should be a matrix
  var asMatrix = Array.isArray(x) ? false : (config.matrix !== 'Array');

  if (size.length == 0) {
    // output a scalar
    while (Array.isArray(x)) {
      x = x[0];
    }

    return clone(x);
  }
  else {
    // output an array/matrix
    if (!Array.isArray(x)) {
      x = [x];
    }
    x = clone(x);

    var res = array.resize(x, size, defaultValue);
    return asMatrix ? matrix(res) : res;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.rightArithShift"></a>[function <span class="apidocSignatureSpan">mathjs.</span>rightArithShift (arg0, arg1)](#apidoc.element.mathjs.rightArithShift)
- description and source-code
```javascript
function rightArithShift(arg0, arg1) {
  "use strict";
  var name = 'rightArithShift';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,Array,Matrix');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: BigNumber, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.rightLogShift"></a>[function <span class="apidocSignatureSpan">mathjs.</span>rightLogShift (arg0, arg1)](#apidoc.element.mathjs.rightLogShift)
- description and source-code
```javascript
function rightLogShift(arg0, arg1) {
  "use strict";
  var name = 'rightLogShift';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,Array,Matrix');
  }
  if (test6(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Array
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature3(arg0, convert3(arg1)); // signature: Array, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature3(arg0, c ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.round"></a>[function <span class="apidocSignatureSpan">mathjs.</span>round (arg0, arg1)](#apidoc.element.mathjs.round)
- description and source-code
```javascript
function round(arg0, arg1) {
  "use strict";
  var name = 'round';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test7(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber,Array,Matrix');
  }
  if (test8(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Complex
    }
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature6(arg0, arg1); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature7(arg0, arg1); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Complex, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Complex, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature7(arg0, convert0(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { ...
```
- example usage
```shell
...
[chained operations](http://mathjs.org/docs/chained_operations.html).

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
...
```

#### <a name="apidoc.element.mathjs.sec"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sec (arg0)](#apidoc.element.mathjs.sec)
- description and source-code
```javascript
function sec(arg0) {
  "use strict";
  var name = 'sec';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sech"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sech (arg0)](#apidoc.element.mathjs.sech)
- description and source-code
```javascript
function sech(arg0) {
  "use strict";
  var name = 'sech';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sign"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sign (arg0)](#apidoc.element.mathjs.sign)
- description and source-code
```javascript
function sign(arg0) {
  "use strict";
  var name = 'sign';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.simplify"></a>[function <span class="apidocSignatureSpan">mathjs.</span>simplify (arg0, arg1)](#apidoc.element.mathjs.simplify)
- description and source-code
```javascript
function simplify(arg0, arg1) {
  "use strict";
  var name = 'simplify';
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature0(arg0); // signature: string
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: string, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: string, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test3(arg0)) { // type: Node
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Node
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Node, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature3(arg0, convert0(arg1)); // signature: Node, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test4(arg0)) { // type: number (convert to string)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: number
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(convert1(arg0), arg1); // signature: number, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(convert1(arg0), convert0(arg1)); // signature: number, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test5(arg0)) { // type: boolean (convert to string)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(convert2(arg0), arg1); // signature: boolean, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(convert2(arg0), convert0(arg1)); // signature: boolean, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  if (test6(arg0)) { // type: null (convert to string)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(convert3(arg0), arg1); // signature: null, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature1(convert3(arg0), convert0(arg1)); // signature: null, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  throw createError(name, arguments.length, 0, arg ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sin"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sin (arg0)](#apidoc.element.mathjs.sin)
- description and source-code
```javascript
function sin(arg0) {
  "use strict";
  var name = 'sin';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sinh"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sinh (arg0)](#apidoc.element.mathjs.sinh)
- description and source-code
```javascript
function sinh(arg0) {
  "use strict";
  var name = 'sinh';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.size"></a>[function <span class="apidocSignatureSpan">mathjs.</span>size (arg0)](#apidoc.element.mathjs.size)
- description and source-code
```javascript
function size(arg0) {
  "use strict";
  var name = 'size';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature0(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature1(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: boolean
    if (arguments.length === 1) {
      return signature0(arg0); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: null
    if (arguments.length === 1) {
      return signature0(arg0); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,string,Array,Matrix,boolean,null');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.slu"></a>[function <span class="apidocSignatureSpan">mathjs.</span>slu (arg0, arg1, arg2)](#apidoc.element.mathjs.slu)
- description and source-code
```javascript
function slu(arg0, arg1, arg2) {
  "use strict";
  var name = 'slu';
  if (test0(arg0)) { // type: SparseMatrix
    if (test1(arg1)) { // type: number
      if (test1(arg2)) { // type: number
        if (arguments.length === 3) {
          return signature0(arg0, arg1, arg2); // signature: SparseMatrix, number, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: string (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert0(arg2)); // signature: SparseMatrix, number, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert1(arg2)); // signature: SparseMatrix, number, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: null (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, arg1, convert2(arg2)); // signature: SparseMatrix, number, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'number');
    }
    if (test2(arg1)) { // type: string (convert to number)
      if (test1(arg2)) { // type: number
        if (arguments.length === 3) {
          return signature0(arg0, convert0(arg1), arg2); // signature: SparseMatrix, string, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: string (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, convert0(arg1), convert0(arg2)); // signature: SparseMatrix, string, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, convert0(arg1), convert1(arg2)); // signature: SparseMatrix, string, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(arg2)) { // type: null (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, convert0(arg1), convert2(arg2)); // signature: SparseMatrix, string, null
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'number');
    }
    if (test3(arg1)) { // type: boolean (convert to number)
      if (test1(arg2)) { // type: number
        if (arguments.length === 3) {
          return signature0(arg0, convert1(arg1), arg2); // signature: SparseMatrix, boolean, number
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test2(arg2)) { // type: string (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, convert1(arg1), convert0(arg2)); // signature: SparseMatrix, boolean, string
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test3(arg2)) { // type: boolean (convert to number)
        if (arguments.length === 3) {
          return signature0(arg0, convert1(arg1), convert1(arg2)); // signature: SparseMatrix, boolean, boolean
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      if (test4(ar ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.smaller"></a>[function <span class="apidocSignatureSpan">mathjs.</span>smaller (arg0, arg1)](#apidoc.element.mathjs.smaller)
- description and source-code
```javascript
function smaller(arg0, arg1) {
  "use strict";
  var name = 'smaller';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, Big ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.smallerEq"></a>[function <span class="apidocSignatureSpan">mathjs.</span>smallerEq (arg0, arg1)](#apidoc.element.mathjs.smallerEq)
- description and source-code
```javascript
function smallerEq(arg0, arg1) {
  "use strict";
  var name = 'smallerEq';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sort"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sort (arg0, arg1)](#apidoc.element.mathjs.sort)
- description and source-code
```javascript
function sort(arg0, arg1) {
  "use strict";
  var name = 'sort';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string,function');
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: Matrix, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature4(arg0, convert0(arg1)); // signature: Matrix, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature4(arg0, convert1(arg1)); // signature: Matrix, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature4(arg0, convert2(arg1)); // signature: Matrix, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string,function');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sparse"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sparse (arg0, arg1)](#apidoc.element.mathjs.sparse)
- description and source-code
```javascript
function sparse(arg0, arg1) {
  "use strict";
  var name = 'sparse';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature1(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Array
    }
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Array, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature3(arg0, convert0(arg1)); // signature: Array, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature3(arg0, convert1(arg1)); // signature: Array, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature3(arg0, convert2(arg1)); // signature: Array, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Matrix
    }
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature3(arg0, convert0(arg1)); // signature: Matrix, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature3(arg0, convert1(arg1)); // signature: Matrix, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature3(arg0, convert2(arg1)); // signature: Matrix, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test2(arg0)) { // type: number (convert to string)
    if (arguments.length === 1) {
      return signature1(convert0(arg0)); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: boolean (convert to string)
    if (arguments.length === 1) {
      return signature1(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: null (convert to string)
    if (arguments.length === 1) {
      return signature1(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.splitUnit"></a>[function <span class="apidocSignatureSpan">mathjs.</span>splitUnit (arg0, arg1)](#apidoc.element.mathjs.splitUnit)
- description and source-code
```javascript
function splitUnit(arg0, arg1) {
  "use strict";
  var name = 'splitUnit';
  if (test0(arg0)) { // type: Unit
    if (test1(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Unit, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Matrix (convert to Array)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: Unit, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Unit');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sqrt"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sqrt (arg0)](#apidoc.element.mathjs.sqrt)
- description and source-code
```javascript
function sqrt(arg0) {
  "use strict";
  var name = 'sqrt';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
...
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
...
```

#### <a name="apidoc.element.mathjs.square"></a>[function <span class="apidocSignatureSpan">mathjs.</span>square (arg0)](#apidoc.element.mathjs.square)
- description and source-code
```javascript
function square(arg0) {
  "use strict";
  var name = 'square';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.squeeze"></a>[function <span class="apidocSignatureSpan">mathjs.</span>squeeze (arg0)](#apidoc.element.mathjs.squeeze)
- description and source-code
```javascript
function squeeze(arg0) {
  "use strict";
  var name = 'squeeze';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Matrix
    }
  }
  // type: any
  if (arguments.length === 1) {
    return signature2(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.std"></a>[function <span class="apidocSignatureSpan">mathjs.</span>std (arg0, arg1)](#apidoc.element.mathjs.std)
- description and source-code
```javascript
function std(arg0, arg1) {
  "use strict";
  var name = 'std';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, string
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: Array, number
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: Array, boolean
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: Array, null
      }
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Matrix, string
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: Matrix, number
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: Matrix, boolean
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature1(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.stirlingS2"></a>[function <span class="apidocSignatureSpan">mathjs.</span>stirlingS2 (arg0, arg1)](#apidoc.element.mathjs.stirlingS2)
- description and source-code
```javascript
function stirlingS2(arg0, arg1) {
  "use strict";
  var name = 'stirlingS2';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test2(arg0)) { // type: Fraction (convert to BigNumber)
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: Fraction, number
      }
      if (arguments.length > 2) {
        throw createErr ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.string"></a>[function <span class="apidocSignatureSpan">mathjs.</span>string (arg0)](#apidoc.element.mathjs.string)
- description and source-code
```javascript
function string(arg0) {
  "use strict";
  var name = 'string';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature1(arg0); // signature: number
    }
  }
  if (test1(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature2(arg0); // signature: string
    }
  }
  if (test2(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Array
    }
  }
  if (test3(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
  }
  if (test4(arg0)) { // type: boolean
    if (arguments.length === 1) {
      return signature4(arg0); // signature: boolean
    }
  }
  if (test5(arg0)) { // type: null
    if (arguments.length === 1) {
      return signature5(arg0); // signature: null
    }
  }
  // type: any
  if (arguments.length === 1) {
    return signature6(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,string,Array,Matrix,boolean,null,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.subset"></a>[function <span class="apidocSignatureSpan">mathjs.</span>subset (arg0, arg1, arg2, arg3)](#apidoc.element.mathjs.subset)
- description and source-code
```javascript
function subset(arg0, arg1, arg2, arg3) {
  "use strict";
  var name = 'subset';
  if (test0(arg0)) { // type: string
    if (test1(arg1)) { // type: Index
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, Index
      }
      if (test0(arg2)) { // type: string
        if (arguments.length === 3) {
          return signature1(arg0, arg1, arg2); // signature: string, Index, string
        }
        if (test0(arg3)) { // type: string
          if (arguments.length === 4) {
            return signature1(arg0, arg1, arg2, arg3); // signature: string, Index, string, string
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test2(arg3)) { // type: number (convert to string)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, arg2, convert0(arg3)); // signature: string, Index, string, number
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test3(arg3)) { // type: boolean (convert to string)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, arg2, convert1(arg3)); // signature: string, Index, string, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test4(arg3)) { // type: null (convert to string)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, arg2, convert2(arg3)); // signature: string, Index, string, null
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'string');
      }
      if (test2(arg2)) { // type: number (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, arg1, convert0(arg2)); // signature: string, Index, number
        }
        if (test0(arg3)) { // type: string
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert0(arg2), arg3); // signature: string, Index, number, string
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test2(arg3)) { // type: number (convert to string)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert0(arg2), convert0(arg3)); // signature: string, Index, number, number
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test3(arg3)) { // type: boolean (convert to string)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert0(arg2), convert1(arg3)); // signature: string, Index, number, boolean
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        if (test4(arg3)) { // type: null (convert to string)
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert0(arg2), convert2(arg3)); // signature: string, Index, number, null
          }
          if (arguments.length > 4) {
            throw createError(name, arguments.length, 4, arguments[4]);
          }
        }
        throw createError(name, arguments.length, 3, arguments[3], 'string');
      }
      if (test3(arg2)) { // type: boolean (convert to string)
        if (arguments.length === 3) {
          return signature1(arg0, arg1, convert1(arg2)); // signature: string, Index, boolean
        }
        if (test0(arg3)) { // type: string
          if (arguments.length === 4) {
            return signature1(arg0, arg1, convert1(arg2), arg3); // signature: string, Index, boolean, string
          }
          if (arguments.length > 4) { ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.subtract"></a>[function <span class="apidocSignatureSpan">mathjs.</span>subtract (arg0, arg1)](#apidoc.element.mathjs.subtract)
- description and source-code
```javascript
function subtract(arg0, arg1) {
  "use strict";
  var name = 'subtract';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction
      if (arguments.length === 2) {
        return signature3(convert2(arg0), arg1); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: BigNumber, B ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.sum"></a>[function <span class="apidocSignatureSpan">mathjs.</span>sum (arg0, arg1)](#apidoc.element.mathjs.sum)
- description and source-code
```javascript
function sum(arg0, arg1) {
  "use strict";
  var name = 'sum';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Array, null
      }
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, number
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, BigNumber
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, Fraction
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, string
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, boolean
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature1(arg0, convert3(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.tan"></a>[function <span class="apidocSignatureSpan">mathjs.</span>tan (arg0)](#apidoc.element.mathjs.tan)
- description and source-code
```javascript
function tan(arg0) {
  "use strict";
  var name = 'tan';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.tanh"></a>[function <span class="apidocSignatureSpan">mathjs.</span>tanh (arg0)](#apidoc.element.mathjs.tanh)
- description and source-code
```javascript
function tanh(arg0) {
  "use strict";
  var name = 'tanh';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Fraction (convert to BigNumber)
    if (arguments.length === 1) {
      return signature2(convert0(arg0)); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert3(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.to"></a>[function <span class="apidocSignatureSpan">mathjs.</span>to (arg0, arg1)](#apidoc.element.mathjs.to)
- description and source-code
```javascript
function to(arg0, arg1) {
  "use strict";
  var name = 'to';
  if (test0(arg0)) { // type: Unit
    if (test0(arg1)) { // type: Unit
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Unit, Unit
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Unit, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: Unit, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: Unit, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: Unit, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test5(arg0)) { // type: Array
    if (test5(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Array
      }
    }
    if (test6(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature3(arg0, arg1); // signature: Array, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (test5(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, Array
      }
    }
    if (test6(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature5(arg0, arg1); // signature: Matrix, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature6(arg0, arg1); // signature: Matrix, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  // type: any
  if (test5(arg1)) { // type: Array
    if (arguments.length === 2) {
      return signature7(arg0, arg1); // signature: any, Array
    }
  }
  if (test6(arg1)) { // type: Matrix
    if (arguments.length === 2) {
      return signature8(arg0, arg1); // signature: any, Matrix
    }
  }
  throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  throw createError(name, arguments.length, 0, arguments[0], 'Unit,Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.trace"></a>[function <span class="apidocSignatureSpan">mathjs.</span>trace (arg0)](#apidoc.element.mathjs.trace)
- description and source-code
```javascript
function trace(arg0) {
  "use strict";
  var name = 'trace';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Matrix
    }
  }
  // type: any
  if (arguments.length === 1) {
    return signature2(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.transpose"></a>[function <span class="apidocSignatureSpan">mathjs.</span>transpose (arg0)](#apidoc.element.mathjs.transpose)
- description and source-code
```javascript
function transpose(arg0) {
  "use strict";
  var name = 'transpose';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Matrix
    }
  }
  // type: any
  if (arguments.length === 1) {
    return signature2(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.BigNumber (v)](#apidoc.element.mathjs.type.BigNumber)
- description and source-code
```javascript
function Decimal(v) {
  var e, i, t,
    x = this;

  // Decimal called without new.
  if (!(x instanceof Decimal)) return new Decimal(v);

  // Retain a reference to this Decimal constructor, and shadow Decimal.prototype.constructor
  // which points to Object.
  x.constructor = Decimal;

  // Duplicate.
  if (v instanceof Decimal) {
    x.s = v.s;
    x.e = v.e;
    x.d = (v = v.d) ? v.slice() : v;
    return;
  }

  t = typeof v;

  if (t === 'number') {
    if (v === 0) {
      x.s = 1 / v < 0 ? -1 : 1;
      x.e = 0;
      x.d = [0];
      return;
    }

    if (v < 0) {
      v = -v;
      x.s = -1;
    } else {
      x.s = 1;
    }

    // Fast path for small integers.
    if (v === ~~v && v < 1e7) {
      for (e = 0, i = v; i >= 10; i /= 10) e++;
      x.e = e;
      x.d = [v];
      return;

    // Infinity, NaN.
    } else if (v * 0 !== 0) {
      if (!v) x.s = NaN;
      x.e = NaN;
      x.d = null;
      return;
    }

    return parseDecimal(x, v.toString());

  } else if (t !== 'string') {
    throw Error(invalidArgument + v);
  }

  // Minus sign?
  if (v.charCodeAt(0) === 45) {
    v = v.slice(1);
    x.s = -1;
  } else {
    x.s = 1;
  }

  return isDecimal.test(v) ? parseDecimal(x, v) : parseOther(x, v);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Chain (value)](#apidoc.element.mathjs.type.Chain)
- description and source-code
```javascript
function Chain(value) {
  if (!(this instanceof Chain)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (value && value.isChain) {
    this.value = value.value;
  }
  else {
    this.value = value;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Complex (a, b)](#apidoc.element.mathjs.type.Complex)
- description and source-code
```javascript
function Complex(a, b) {

  if (!(this instanceof Complex)) {
    return new Complex(a, b);
  }

  parse(a, b); // mutates P

  this["re"] = P["re"];
  this["im"] = P["im"];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.DenseMatrix (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix)
- description and source-code
```javascript
function DenseMatrix(data, datatype) {
  if (!(this instanceof DenseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if (data && data.isMatrix === true) {
    // check data is a DenseMatrix
    if (data.type === 'DenseMatrix') {
      // clone data & size
      this._data = object.clone(data._data);
      this._size = object.clone(data._size);
      this._datatype = datatype || data._datatype;
    }
    else {
      // build data from existing matrix
      this._data = data.toArray();
      this._size = data.size();
      this._datatype = datatype || data._datatype;
    }
  }
  else if (data && isArray(data.data) && isArray(data.size)) {
    // initialize fields from JSON representation
    this._data = data.data;
    this._size = data.size;
    this._datatype = datatype || data.datatype;
  }
  else if (isArray(data)) {
    // replace nested Matrices with Arrays
    this._data = preprocess(data);
    // get the dimensions of the array
    this._size = array.size(this._data);
    // verify the dimensions of the array, TODO: compute size while processing array
    array.validate(this._data, this._size);
    // data type unknown
    this._datatype = datatype;
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._data = [];
    this._size = [0];
    this._datatype = datatype;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.FibonacciHeap ()](#apidoc.element.mathjs.type.FibonacciHeap)
- description and source-code
```javascript
function FibonacciHeap() {
  if (!(this instanceof FibonacciHeap))
    throw new SyntaxError('Constructor must be called with the new operator');

  // initialize fields
  this._minimum = null;
  this._size = 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Fraction (a, b)](#apidoc.element.mathjs.type.Fraction)
- description and source-code
```javascript
function Fraction(a, b) {

  if (!(this instanceof Fraction)) {
    return new Fraction(a, b);
  }

  parse(a, b);

  if (Fraction['REDUCE']) {
    a = gcd(P["d"], P["n"]); // Abuse a
  } else {
    a = 1;
  }

  this["s"] = P["s"];
  this["n"] = P["n"] / a;
  this["d"] = P["d"] / a;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Help"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Help (doc)](#apidoc.element.mathjs.type.Help)
- description and source-code
```javascript
function Help(doc) {
  if (!(this instanceof Help)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (!doc)  throw new Error('Argument "doc" missing');

  this.doc = doc;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.ImmutableDenseMatrix (data, datatype)](#apidoc.element.mathjs.type.ImmutableDenseMatrix)
- description and source-code
```javascript
function ImmutableDenseMatrix(data, datatype) {
  if (!(this instanceof ImmutableDenseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if ((data && data.isMatrix === true) || isArray(data)) {
    // use DenseMatrix implementation
    var matrix = new DenseMatrix(data, datatype);
    // internal structures
    this._data = matrix._data;
    this._size = matrix._size;
    this._datatype = matrix._datatype;
    this._min = null;
    this._max = null;
  }
  else if (data && isArray(data.data) && isArray(data.size)) {
    // initialize fields from JSON representation
    this._data = data.data;
    this._size = data.size;
    this._datatype = data.datatype;
    this._min = typeof data.min !== 'undefined' ? data.min : null;
    this._max = typeof data.max !== 'undefined' ? data.max : null;
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._data = [];
    this._size = [0];
    this._datatype = datatype;
    this._min = null;
    this._max = null;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Index (ranges)](#apidoc.element.mathjs.type.Index)
- description and source-code
```javascript
function Index(ranges) {
  if (!(this instanceof Index)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this._dimensions = [];
  this._isScalar = true;

  for (var i = 0, ii = arguments.length; i < ii; i++) {
    var arg = arguments[i];

    if (arg && (arg.isRange === true)) {
      this._dimensions.push(arg);
      this._isScalar = false;
    }
    else if (arg && (Array.isArray(arg) || arg.isMatrix === true)) {
      // create matrix
      var m = _createImmutableMatrix(arg.valueOf());
      this._dimensions.push(m);
      // size
      var size = m.size();
      // scalar
      if (size.length !== 1 || size[0] !== 1) {
        this._isScalar = false;
      }
    }
    else if (typeof arg === 'number') {
      this._dimensions.push(_createImmutableMatrix([arg]));
    }
    else if (typeof arg === 'string') {
      // object property (arguments.count should be 1)
      this._dimensions.push(arg);
    }
    // TODO: implement support for wildcard '*'
    else {
      throw new TypeError('Dimension must be an Array, Matrix, number, string, or Range');
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Matrix ()](#apidoc.element.mathjs.type.Matrix)
- description and source-code
```javascript
function Matrix() {
  if (!(this instanceof Matrix)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Range (start, end, step)](#apidoc.element.mathjs.type.Range)
- description and source-code
```javascript
function Range(start, end, step) {
  if (!(this instanceof Range)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (start != null) {
    if (start.isBigNumber === true)
      start = start.toNumber();
    else if (typeof start !== 'number')
      throw new TypeError('Parameter start must be a number');
  }
  if (end != null) {
    if (end.isBigNumber === true)
      end = end.toNumber();
    else if (typeof end !== 'number')
      throw new TypeError('Parameter end must be a number');
  }
  if (step != null) {
    if (step.isBigNumber === true)
      step = step.toNumber();
    else if (typeof step !== 'number')
      throw new TypeError('Parameter step must be a number');
  }

  this.start = (start != null) ? parseFloat(start) : 0;
  this.end   = (end != null)   ? parseFloat(end)   : 0;
  this.step  = (step != null)  ? parseFloat(step)  : 1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ResultSet"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.ResultSet (entries)](#apidoc.element.mathjs.type.ResultSet)
- description and source-code
```javascript
function ResultSet(entries) {
  if (!(this instanceof ResultSet)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.entries = entries || [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Spa"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Spa ()](#apidoc.element.mathjs.type.Spa)
- description and source-code
```javascript
function Spa() {
  if (!(this instanceof Spa))
    throw new SyntaxError('Constructor must be called with the new operator');

  // allocate vector, TODO use typed arrays
  this._values = [];
  this._heap = new type.FibonacciHeap();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.SparseMatrix (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix)
- description and source-code
```javascript
function SparseMatrix(data, datatype) {
  if (!(this instanceof SparseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if (data && data.isMatrix === true) {
    // create from matrix
    _createFromMatrix(this, data, datatype);
  }
  else if (data && isArray(data.index) && isArray(data.ptr) && isArray(data.size)) {
    // initialize fields
    this._values = data.values;
    this._index = data.index;
    this._ptr = data.ptr;
    this._size = data.size;
    this._datatype = datatype || data.datatype;
  }
  else if (isArray(data)) {
    // create from array
    _createFromArray(this, data, datatype);
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._values = [];
    this._index = [];
    this._ptr = [0];
    this._size = [0, 0];
    this._datatype = datatype;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit"></a>[function <span class="apidocSignatureSpan">mathjs.</span>type.Unit (value, name)](#apidoc.element.mathjs.type.Unit)
- description and source-code
```javascript
function Unit(value, name) {
  if (!(this instanceof Unit)) {
    throw new Error('Constructor must be called with the new operator');
  }

  if (!(value === undefined || isNumeric(value) || value.isComplex)) {
    throw new TypeError('First parameter in Unit constructor must be number, BigNumber, Fraction, Complex, or undefined');
  }
  if (name != undefined && (typeof name !== 'string' || name == '')) {
    throw new TypeError('Second parameter in Unit constructor must be a string');
  }

  if (name != undefined) {
    var u = Unit.parse(name);
    this.units = u.units;
    this.dimensions = u.dimensions;
  }
  else {
    this.units = [
      {
        unit: UNIT_NONE,
        prefix: PREFIXES.NONE,  // link to a list with supported prefixes
        power: 0
      }
    ];
    this.dimensions = [];
    for(var i=0; i<BASE_DIMENSIONS.length; i++) {
      this.dimensions[i] = 0;
    }
  }

  this.value = (value != undefined) ? this._normalize(value) : null;

  this.fixPrefix = false; // if true, function format will not search for the
                          // best prefix but leave it as initially provided.
                          // fixPrefix is set true by the method Unit.to

  // The justification behind this is that if the constructor is explicitly called,
  // the caller wishes the units to be returned exactly as he supplied.
  this.isUnitListSimplified = true;

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.typed"></a>[function <span class="apidocSignatureSpan">mathjs.</span>typed (arg0, arg1)](#apidoc.element.mathjs.typed)
- description and source-code
```javascript
function typed(arg0, arg1) {
  "use strict";
  var name = 'typed';
  if (test0(arg0)) { // type: string
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test2(arg0)) { // type: ...Function
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test2(arguments[i])) {
        varArgs.push(arguments[i]);
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'Function');
      }
    }
    return signature1(varArgs); // signature: ...Function
  }
  if (test1(arg0)) { // type: Object
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Object
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Function,Object');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.typeof"></a>[function <span class="apidocSignatureSpan">mathjs.</span>typeof (arg0)](#apidoc.element.mathjs.typeof)
- description and source-code
```javascript
function _typeof(arg0) {
  "use strict";
  var name = '_typeof';
  // type: any
  if (arguments.length === 1) {
    return signature0(arg0); // signature: any
  }
  if (arguments.length > 1) {
    throw createError(name, arguments.length, 1, arguments[1]);
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.unaryMinus"></a>[function <span class="apidocSignatureSpan">mathjs.</span>unaryMinus (arg0)](#apidoc.element.mathjs.unaryMinus)
- description and source-code
```javascript
function unaryMinus(arg0) {
  "use strict";
  var name = 'unaryMinus';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature5(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: string (convert to number)
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean (convert to number)
    if (arguments.length === 1) {
      return signature0(convert1(arg0)); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null (convert to number)
    if (arguments.length === 1) {
      return signature0(convert2(arg0)); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.unaryPlus"></a>[function <span class="apidocSignatureSpan">mathjs.</span>unaryPlus (arg0)](#apidoc.element.mathjs.unaryPlus)
- description and source-code
```javascript
function unaryPlus(arg0) {
  "use strict";
  var name = 'unaryPlus';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(arg0); // signature: number
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test1(arg0)) { // type: Complex
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Complex
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (arguments.length === 1) {
      return signature2(arg0); // signature: BigNumber
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test3(arg0)) { // type: Fraction
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Fraction
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test4(arg0)) { // type: Unit
    if (arguments.length === 1) {
      return signature4(arg0); // signature: Unit
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test5(arg0)) { // type: string
    if (arguments.length === 1) {
      return signature5(arg0); // signature: string
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test6(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature6(arg0); // signature: Array
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test7(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature6(arg0); // signature: Matrix
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test8(arg0)) { // type: boolean
    if (arguments.length === 1) {
      return signature5(arg0); // signature: boolean
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  if (test9(arg0)) { // type: null
    if (arguments.length === 1) {
      return signature5(arg0); // signature: null
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'number,Complex,BigNumber,Fraction,Unit,string,Array,Matrix,boolean
,null');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.unequal"></a>[function <span class="apidocSignatureSpan">mathjs.</span>unequal (arg0, arg1)](#apidoc.element.mathjs.unequal)
- description and source-code
```javascript
function unequal(arg0, arg1) {
  "use strict";
  var name = 'unequal';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature2(arg0, arg1); // signature: Array, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  if (test1(arg0)) { // type: Matrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature3(arg0, arg1); // signature: Matrix, Array
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Matrix, Matrix
      }
    }
    // type: any
    if (arguments.length === 2) {
      return signature5(arg0, arg1); // signature: Matrix, any
    }
    if (arguments.length > 2) {
      throw createError(name, arguments.length, 2, arguments[2]);
    }
  }
  // type: any
  if (test0(arg1)) { // type: Array
    if (arguments.length === 2) {
      return signature6(arg0, arg1); // signature: any, Array
    }
  }
  if (test1(arg1)) { // type: Matrix
    if (arguments.length === 2) {
      return signature7(arg0, arg1); // signature: any, Matrix
    }
  }
  // type: any
  if (arguments.length === 2) {
    return signature8(arg0, arg1); // signature: any, any
  }
  if (arguments.length > 2) {
    throw createError(name, arguments.length, 2, arguments[2]);
  }
  throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix,any');
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.unit"></a>[function <span class="apidocSignatureSpan">mathjs.</span>unit (arg0, arg1)](#apidoc.element.mathjs.unit)
- description and source-code
```javascript
function unit(arg0, arg1) {
  "use strict";
  var name = 'unit';
  if (test0(arg0)) { // type: number
    if (arguments.length === 1) {
      return signature0(convert0(arg0)); // signature: number
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test4(arg0)) { // type: Complex
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Complex, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Complex, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test5(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test6(arg0)) { // type: Fraction
    if (test1(arg1)) { // type: string
      if (argument ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.usolve"></a>[function <span class="apidocSignatureSpan">mathjs.</span>usolve (arg0, arg1)](#apidoc.element.mathjs.usolve)
- description and source-code
```javascript
function usolve(arg0, arg1) {
  "use strict";
  var name = 'usolve';
  if (test0(arg0)) { // type: Array
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test2(arg0)) { // type: DenseMatrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: DenseMatrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: DenseMatrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test3(arg0)) { // type: SparseMatrix
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: SparseMatrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: SparseMatrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  if (test1(arg0)) { // type: Matrix (convert to Array)
    if (test0(arg1)) { // type: Array
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: Matrix, Array
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Matrix
      if (arguments.length === 2) {
        return signature0(convert0(arg0), arg1); // signature: Matrix, Matrix
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Array,Matrix');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,DenseMatrix,SparseMatrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.var"></a>[function <span class="apidocSignatureSpan">mathjs.</span>var (arg0, arg1)](#apidoc.element.mathjs.var)
- description and source-code
```javascript
function variance(arg0, arg1) {
  "use strict";
  var name = 'variance';
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Array, string
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Array, number
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Array, boolean
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Array, null
      }
    }
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature0(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, string
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Matrix, number
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: Matrix, boolean
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature1(arg0, convert2(arg1)); // signature: Matrix, null
      }
    }
  }
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature2(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix,any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.xgcd"></a>[function <span class="apidocSignatureSpan">mathjs.</span>xgcd (arg0, arg1)](#apidoc.element.mathjs.xgcd)
- description and source-code
```javascript
function xgcd(arg0, arg1) {
  "use strict";
  var name = 'xgcd';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(convert0(arg0), convert1(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'number,BigNumber');
  }
  if (test1(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: BigNumber, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: BigNumber, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert1(arg1)); // signature: BigNumber, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: string (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert5(arg1)); // signature: BigNumber, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: boolean (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: BigNumber, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: null (convert to BigNumber)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: BigNumber, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'BigNumber');
  }
  if (test2(arg0)) { // type: Fraction (convert to BigNumber)
    if (test1(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature1(convert1(arg0), arg1); // signature: Fraction, BigNumber
      }
      if ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.xor"></a>[function <span class="apidocSignatureSpan">mathjs.</span>xor (arg0, arg1)](#apidoc.element.mathjs.xor)
- description and source-code
```javascript
function xor(arg0, arg1) {
  "use strict";
  var name = 'xor';
  if (test0(arg0)) { // type: number
    if (test0(arg1)) { // type: number
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: number, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert0(arg0), arg1); // signature: number, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(convert1(arg0), arg1); // signature: number, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to BigNumber)
      if (arguments.length === 2) {
        return signature2(convert1(arg0), convert2(arg1)); // signature: number, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert3(arg1)); // signature: number, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test5(arg1)) { // type: boolean (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert4(arg1)); // signature: number, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test6(arg1)) { // type: null (convert to number)
      if (arguments.length === 2) {
        return signature0(arg0, convert5(arg1)); // signature: number, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test1(arg0)) { // type: Complex
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Complex, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test0(arg1)) { // type: number (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert0(arg1)); // signature: Complex, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert6(arg1)); // signature: Complex, BigNumber
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: Fraction (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert7(arg1)); // signature: Complex, Fraction
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: string (convert to Complex)
      if (arguments.length === 2) {
        return signature1(arg0, convert8(arg1)); // signature: Complex, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
  }
  if (test2(arg0)) { // type: BigNumber
    if (test1(arg1)) { // type: Complex
      if (arguments.length === 2) {
        return signature1(convert6(arg0), arg1); // signature: BigNumber, Complex
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: BigNumber
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.zeros"></a>[function <span class="apidocSignatureSpan">mathjs.</span>zeros (arg0, arg1)](#apidoc.element.mathjs.zeros)
- description and source-code
```javascript
function zeros(arg0, arg1) {
  "use strict";
  var name = 'zeros';
  if (arguments.length === 0) {
    return signature0(); // signature:
  }
  if (test0(arg0)) { // type: Array
    if (arguments.length === 1) {
      return signature1(arg0); // signature: Array
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Array, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: Array, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: Array, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert2(arg1)); // signature: Array, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test5(arg0)) { // type: Matrix
    if (arguments.length === 1) {
      return signature3(arg0); // signature: Matrix
    }
    if (test1(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: Matrix, string
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test2(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert0(arg1)); // signature: Matrix, number
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test3(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert1(arg1)); // signature: Matrix, boolean
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    if (test4(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature2(arg0, convert2(arg1)); // signature: Matrix, null
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string');
  }
  if (test2(arg0) || test6(arg0) || test1(arg0) || test7(arg0) || test3(arg0) || test3(arg0) || test3(arg0) || test4(arg0) || test4
(arg0) || test4(arg0)) { // type: ...number|BigNumber|string|Fraction|boolean|null
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test2(arguments[i]) || test6(arguments[i]) || test1(arguments[i])) {
        varArgs.push(arguments[i]);
      }
      else if (test7(arguments[i])) {
        varArgs.push(convert3(arguments[i]));
      }
      else if (test3(arguments[i])) {
        varArgs.push(convert4(arguments[i]));
      }
      else if (test3(arguments[i])) {
        varArgs.push(convert5(arguments[i]));
      }
      else if (test3(arguments[i])) {
        varArgs.push(convert1(arguments[i]));
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert6(arguments[i]));
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert2(arguments[i]));
      }
      else if (test4(arguments[i])) {
        varArgs.push(convert7(arguments[i]));
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'number,BigNumber,string');
      }
    }
    return signature4(varArgs); // signature: ...number|BigN ...
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.constants"></a>[module mathjs.constants](#apidoc.module.mathjs.constants)

#### <a name="apidoc.element.mathjs.constants.factory"></a>[function <span class="apidocSignatureSpan">mathjs.constants.</span>factory (type, config, load, typed, math)](#apidoc.element.mathjs.constants.factory)
- description and source-code
```javascript
function factory(type, config, load, typed, math) {
  // listen for changed in the configuration, automatically reload
  // constants when needed
  math.on('config', function (curr, prev) {
    if (curr.number !== prev.number) {
      factory(type, config, load, typed, math);
    }
  });

  math['true']     = true;
  math['false']    = false;
  math['null']     = null;
  math['uninitialized'] = require('./utils/array').UNINITIALIZED;

  if (config.number === 'BigNumber') {
    math['Infinity'] = new type.BigNumber(Infinity);
    math['NaN']      = new type.BigNumber(NaN);

    object.lazy(math, 'pi',  function () {return bigConstants.pi(type.BigNumber)});
    object.lazy(math, 'tau', function () {return bigConstants.tau(type.BigNumber)});
    object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
    object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

    // uppercase constants (for compatibility with built-in Math)
    object.lazy(math, 'E',       function () {return math.e;});
    object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
    object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
    object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
    object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
    object.lazy(math, 'PI',      function () {return math.pi});
    object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
    object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
  }
  else {
    math['Infinity'] = Infinity;
    math['NaN']      = NaN;

    math.pi  = Math.PI;
    math.tau = Math.PI * 2;
    math.e   = Math.E;
    math.phi = 1.61803398874989484820458683436563811772030917980576286213545; // golden ratio, (1+sqrt(5))/2

    // uppercase constants (for compatibility with built-in Math)
    math.E           = math.e;
    math.LN2         = Math.LN2;
    math.LN10        = Math.LN10;
    math.LOG2E       = Math.LOG2E;
    math.LOG10E      = Math.LOG10E;
    math.PI          = math.pi;
    math.SQRT1_2     = Math.SQRT1_2;
    math.SQRT2       = Math.SQRT2;
  }

  // complex i
  math.i = type.Complex.I;

  // meta information
  math.version = require('./version');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.core"></a>[module mathjs.core](#apidoc.module.mathjs.core)

#### <a name="apidoc.element.mathjs.core.create"></a>[function <span class="apidocSignatureSpan">mathjs.core.</span>create (options)](#apidoc.element.mathjs.core.create)
- description and source-code
```javascript
function create(options) {
  // simple test for ES5 support
  if (typeof Object.create !== 'function') {
    throw new Error('ES5 not supported by this JavaScript engine. ' +
    'Please load the es5-shim and es5-sham library for compatibility.');
  }

  // cached factories and instances
  var factories = [];
  var instances = [];

  // create a namespace for the mathjs instance, and attach emitter functions
  var math = emitter.mixin({});
  math.type = {};
  math.expression = {
    transform: Object.create(math)
  };
  math.algebra = {};

  // create a new typed instance
  math.typed = typedFactory.create(math.type);

  // create configuration options. These are private
  var _config = {
    // minimum relative difference between two compared values,
    // used by all comparison functions
    epsilon: 1e-12,

    // type of default matrix output. Choose 'matrix' (default) or 'array'
    matrix: 'Matrix',

    // type of default number output. Choose 'number' (default) 'BigNumber', or 'Fraction
    number: 'number',

    // number of significant digits in BigNumbers
    precision: 64,

    // predictable output type of functions. When true, output type depends only
    // on the input types. When false (default), output type can vary depending
    // on input values. For example 'math.sqrt(-4)' returns 'complex('2i')' when
    // predictable is false, and returns 'NaN' when true.
    predictable: false,

    // random seed for seeded pseudo random number generation
    // null = randomly seed
    randomSeed: null
  };

<span class="apidocCodeCommentSpan">  /**
   * Load a function or data type from a factory.
   * If the function or data type already exists, the existing instance is
   * returned.
   * @param {{type: string, name: string, factory: Function}} factory
   * @returns {*}
   */
</span>  function load (factory) {
    if (!isFactory(factory)) {
      throw new Error('Factory object with properties 'type', 'name', and 'factory' expected');
    }

    var index = factories.indexOf(factory);
    var instance;
    if (index === -1) {
      // doesn't yet exist
      if (factory.math === true) {
        // pass with math namespace
        instance = factory.factory(math.type, _config, load, math.typed, math);
      }
      else {
        instance = factory.factory(math.type, _config, load, math.typed);
      }

      // append to the cache
      factories.push(factory);
      instances.push(instance);
    }
    else {
      // already existing function, return the cached instance
      instance = instances[index];
    }

    return instance;
  }

  // load the import and config functions
  math['import'] = load(importFactory);
  math['config'] = load(configFactory);

  // apply options
  if (options) {
    math.config(options);
  }

  return math;
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```



# <a name="apidoc.module.mathjs.derivative"></a>[module mathjs.derivative](#apidoc.module.mathjs.derivative)

#### <a name="apidoc.element.mathjs.derivative.derivative"></a>[function <span class="apidocSignatureSpan">mathjs.</span>derivative (arg0, arg1, arg2)](#apidoc.element.mathjs.derivative.derivative)
- description and source-code
```javascript
function derivative(arg0, arg1, arg2) {
  "use strict";
  var name = 'derivative';
  if (test0(arg0)) { // type: string
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, string
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, arg1, arg2); // signature: string, string, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test2(arg1)) { // type: SymbolNode
      if (arguments.length === 2) {
        return signature2(arg0, arg1); // signature: string, SymbolNode
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature3(arg0, arg1, arg2); // signature: string, SymbolNode, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test3(arg1)) { // type: number (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert0(arg1)); // signature: string, number
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, convert0(arg1), arg2); // signature: string, number, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test4(arg1)) { // type: boolean (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert1(arg1)); // signature: string, boolean
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, convert1(arg1), arg2); // signature: string, boolean, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test5(arg1)) { // type: null (convert to string)
      if (arguments.length === 2) {
        return signature0(arg0, convert2(arg1)); // signature: string, null
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature1(arg0, convert2(arg1), arg2); // signature: string, null, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    throw createError(name, arguments.length, 1, arguments[1], 'string,SymbolNode');
  }
  if (test6(arg0)) { // type: Node
    if (test0(arg1)) { // type: string
      if (arguments.length === 2) {
        return signature4(arg0, arg1); // signature: Node, string
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature5(arg0, arg1, arg2); // signature: Node, string, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (test2(arg1)) { // type: SymbolNode
      if (arguments.length === 2) {
        return signature6(arg0, arg1); // signature: Node, SymbolNode
      }
      if (test1(arg2)) { // type: Object
        if (arguments.length === 3) {
          return signature7(arg0, arg1, arg2); // signature: Node, SymbolNode, Object
        }
        if (arguments.length > 3) {
          throw createError(name, arguments.length, 3, arguments[3]);
        }
      }
      throw createError(name, arguments.length, 2, arguments[2], 'Object');
    }
    if (t ...
```
- example usage
```shell
...

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7
...
```

#### <a name="apidoc.element.mathjs.derivative.toTex"></a>[function <span class="apidocSignatureSpan">mathjs.derivative.</span>toTex (deriv)](#apidoc.element.mathjs.derivative.toTex)
- description and source-code
```javascript
toTex = function (deriv) {
  return _derivTex.apply(null, deriv.args);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.derivative.signatures"></a>[module mathjs.derivative.signatures](#apidoc.module.mathjs.derivative.signatures)



# <a name="apidoc.module.mathjs.error"></a>[module mathjs.error](#apidoc.module.mathjs.error)

#### <a name="apidoc.element.mathjs.error.ArgumentsError"></a>[function <span class="apidocSignatureSpan">mathjs.error.</span>ArgumentsError (fn, count, min, max)](#apidoc.element.mathjs.error.ArgumentsError)
- description and source-code
```javascript
function ArgumentsError(fn, count, min, max) {
  if (!(this instanceof ArgumentsError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.fn = fn;
  this.count = count;
  this.min = min;
  this.max = max;

  this.message = 'Wrong number of arguments in function ' + fn +
      ' (' + count + ' provided, ' +
      min + ((max != undefined) ? ('-' + max) : '') + ' expected)';

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.DimensionError"></a>[function <span class="apidocSignatureSpan">mathjs.error.</span>DimensionError (actual, expected, relation)](#apidoc.element.mathjs.error.DimensionError)
- description and source-code
```javascript
function DimensionError(actual, expected, relation) {
  if (!(this instanceof DimensionError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.actual   = actual;
  this.expected = expected;
  this.relation = relation;

  this.message = 'Dimension mismatch (' +
      (Array.isArray(actual) ? ('[' + actual.join(', ') + ']') : actual) +
      ' ' + (this.relation || '!=') + ' ' +
      (Array.isArray(expected) ? ('[' + expected.join(', ') + ']') : expected) +
      ')';

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.IndexError"></a>[function <span class="apidocSignatureSpan">mathjs.error.</span>IndexError (index, min, max)](#apidoc.element.mathjs.error.IndexError)
- description and source-code
```javascript
function IndexError(index, min, max) {
  if (!(this instanceof IndexError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.index = index;
  if (arguments.length < 3) {
    this.min = 0;
    this.max = min;
  }
  else {
    this.min = min;
    this.max = max;
  }

  if (this.min !== undefined && this.index < this.min) {
    this.message = 'Index out of range (' + this.index + ' < ' + this.min + ')';
  }
  else if (this.max !== undefined && this.index >= this.max) {
    this.message = 'Index out of range (' + this.index + ' > ' + (this.max - 1) + ')';
  }
  else {
    this.message = 'Index out of range (' + this.index + ')';
  }

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.ArgumentsError"></a>[module mathjs.error.ArgumentsError](#apidoc.module.mathjs.error.ArgumentsError)

#### <a name="apidoc.element.mathjs.error.ArgumentsError.ArgumentsError"></a>[function <span class="apidocSignatureSpan">mathjs.error.</span>ArgumentsError (fn, count, min, max)](#apidoc.element.mathjs.error.ArgumentsError.ArgumentsError)
- description and source-code
```javascript
function ArgumentsError(fn, count, min, max) {
  if (!(this instanceof ArgumentsError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.fn = fn;
  this.count = count;
  this.min = min;
  this.max = max;

  this.message = 'Wrong number of arguments in function ' + fn +
      ' (' + count + ' provided, ' +
      min + ((max != undefined) ? ('-' + max) : '') + ' expected)';

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.ArgumentsError.prototype"></a>[module mathjs.error.ArgumentsError.prototype](#apidoc.module.mathjs.error.ArgumentsError.prototype)

#### <a name="apidoc.element.mathjs.error.ArgumentsError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.ArgumentsError.prototype.constructor"></a>[module mathjs.error.ArgumentsError.prototype.constructor](#apidoc.module.mathjs.error.ArgumentsError.prototype.constructor)

#### <a name="apidoc.element.mathjs.error.ArgumentsError.prototype.constructor.constructor"></a>[function <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor.constructor)
- description and source-code
```javascript
function Function() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.error.ArgumentsError.prototype.constructor.captureStackTrace"></a>[function <span class="apidocSignatureSpan">mathjs.error.ArgumentsError.prototype.constructor.</span>captureStackTrace ()](#apidoc.element.mathjs.error.ArgumentsError.prototype.constructor.captureStackTrace)
- description and source-code
```javascript
function captureStackTrace() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.DimensionError"></a>[module mathjs.error.DimensionError](#apidoc.module.mathjs.error.DimensionError)

#### <a name="apidoc.element.mathjs.error.DimensionError.DimensionError"></a>[function <span class="apidocSignatureSpan">mathjs.error.</span>DimensionError (actual, expected, relation)](#apidoc.element.mathjs.error.DimensionError.DimensionError)
- description and source-code
```javascript
function DimensionError(actual, expected, relation) {
  if (!(this instanceof DimensionError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.actual   = actual;
  this.expected = expected;
  this.relation = relation;

  this.message = 'Dimension mismatch (' +
      (Array.isArray(actual) ? ('[' + actual.join(', ') + ']') : actual) +
      ' ' + (this.relation || '!=') + ' ' +
      (Array.isArray(expected) ? ('[' + expected.join(', ') + ']') : expected) +
      ')';

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.DimensionError.prototype"></a>[module mathjs.error.DimensionError.prototype](#apidoc.module.mathjs.error.DimensionError.prototype)

#### <a name="apidoc.element.mathjs.error.DimensionError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">mathjs.error.DimensionError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.DimensionError.prototype.constructor)
- description and source-code
```javascript
function RangeError() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.IndexError"></a>[module mathjs.error.IndexError](#apidoc.module.mathjs.error.IndexError)

#### <a name="apidoc.element.mathjs.error.IndexError.IndexError"></a>[function <span class="apidocSignatureSpan">mathjs.error.</span>IndexError (index, min, max)](#apidoc.element.mathjs.error.IndexError.IndexError)
- description and source-code
```javascript
function IndexError(index, min, max) {
  if (!(this instanceof IndexError)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.index = index;
  if (arguments.length < 3) {
    this.min = 0;
    this.max = min;
  }
  else {
    this.min = min;
    this.max = max;
  }

  if (this.min !== undefined && this.index < this.min) {
    this.message = 'Index out of range (' + this.index + ' < ' + this.min + ')';
  }
  else if (this.max !== undefined && this.index >= this.max) {
    this.message = 'Index out of range (' + this.index + ' > ' + (this.max - 1) + ')';
  }
  else {
    this.message = 'Index out of range (' + this.index + ')';
  }

  this.stack = (new Error()).stack;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.error.IndexError.prototype"></a>[module mathjs.error.IndexError.prototype](#apidoc.module.mathjs.error.IndexError.prototype)

#### <a name="apidoc.element.mathjs.error.IndexError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">mathjs.error.IndexError.prototype.</span>constructor ()](#apidoc.element.mathjs.error.IndexError.prototype.constructor)
- description and source-code
```javascript
function RangeError() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression"></a>[module mathjs.expression](#apidoc.module.mathjs.expression)

#### <a name="apidoc.element.mathjs.expression.Parser"></a>[function <span class="apidocSignatureSpan">mathjs.expression.</span>Parser ()](#apidoc.element.mathjs.expression.Parser)
- description and source-code
```javascript
function Parser() {
  if (!(this instanceof Parser)) {
    throw new SyntaxError(
        'Constructor must be called with the new operator');
  }
  this.scope = {};
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse"></a>[function <span class="apidocSignatureSpan">mathjs.expression.</span>parse (expr, options)](#apidoc.element.mathjs.expression.parse)
- description and source-code
```javascript
function parse(expr, options) {
  if (arguments.length != 1 && arguments.length != 2) {
    throw new ArgumentsError('parse', arguments.length, 1, 2);
  }

  // pass extra nodes
  extra_nodes = (options && options.nodes) ? options.nodes : {};

  if (typeof expr === 'string') {
    // parse a single expression
    expression = expr;
    return parseStart();
  }
  else if (Array.isArray(expr) || expr instanceof type.Matrix) {
    // parse an array or matrix with expressions
    return deepMap(expr, function (elem) {
      if (typeof elem !== 'string') throw new TypeError('String expected');

      expression = elem;
      return parseStart();
    });
  }
  else {
    // oops
    throw new TypeError('String or matrix expected');
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.Parser"></a>[module mathjs.expression.Parser](#apidoc.module.mathjs.expression.Parser)

#### <a name="apidoc.element.mathjs.expression.Parser.Parser"></a>[function <span class="apidocSignatureSpan">mathjs.expression.</span>Parser ()](#apidoc.element.mathjs.expression.Parser.Parser)
- description and source-code
```javascript
function Parser() {
  if (!(this instanceof Parser)) {
    throw new SyntaxError(
        'Constructor must be called with the new operator');
  }
  this.scope = {};
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.Parser.prototype"></a>[module mathjs.expression.Parser.prototype](#apidoc.module.mathjs.expression.Parser.prototype)

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.clear"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>clear ()](#apidoc.element.mathjs.expression.Parser.prototype.clear)
- description and source-code
```javascript
clear = function () {
  for (var name in this.scope) {
    if (this.scope.hasOwnProperty(name)) {
      delete this.scope[name];
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>compile (expr)](#apidoc.element.mathjs.expression.Parser.prototype.compile)
- description and source-code
```javascript
compile = function (expr) {
  throw new Error('Parser.compile is deprecated. Use math.compile instead.');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.eval"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>eval (expr)](#apidoc.element.mathjs.expression.Parser.prototype.eval)
- description and source-code
```javascript
eval = function (expr) {
  // TODO: validate arguments
  return _parse(expr)
      .compile()
      .eval(this.scope);
}
```
- example usage
```shell
...
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
...
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.get"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>get (name)](#apidoc.element.mathjs.expression.Parser.prototype.get)
- description and source-code
```javascript
get = function (name) {
  // TODO: validate arguments
  return this.scope[name];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.getAll"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>getAll ()](#apidoc.element.mathjs.expression.Parser.prototype.getAll)
- description and source-code
```javascript
getAll = function () {
  return extend({}, this.scope);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.parse"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>parse (expr)](#apidoc.element.mathjs.expression.Parser.prototype.parse)
- description and source-code
```javascript
parse = function (expr) {
  throw new Error('Parser.parse is deprecated. Use math.parse instead.');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.remove"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>remove (name)](#apidoc.element.mathjs.expression.Parser.prototype.remove)
- description and source-code
```javascript
remove = function (name) {
  // TODO: validate arguments
  delete this.scope[name];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.Parser.prototype.set"></a>[function <span class="apidocSignatureSpan">mathjs.expression.Parser.prototype.</span>set (name, value)](#apidoc.element.mathjs.expression.Parser.prototype.set)
- description and source-code
```javascript
set = function (name, value) {
  // TODO: validate arguments
  return this.scope[name] = value;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node"></a>[module mathjs.expression.node](#apidoc.module.mathjs.expression.node)

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>AccessorNode (object, index)](#apidoc.element.mathjs.expression.node.AccessorNode)
- description and source-code
```javascript
function AccessorNode(object, index) {
  if (!(this instanceof AccessorNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (!(object && object.isNode)) {
    throw new TypeError('Node expected for parameter "object"');
  }
  if (!(index && index.isIndexNode)) {
    throw new TypeError('IndexNode expected for parameter "index"');
  }

  this.object = object || null;
  this.index = index;

  // readonly property name
  Object.defineProperty(this, 'name', {
    get: function () {
      if (this.index) {
        return (this.index.isObjectProperty())
            ? this.index.getObjectProperty()
            : '';
      }
      else {
        return this.object.name || '';
      }
    }.bind(this),
    set: function () {
      throw new Error('Cannot assign a new name, name is read-only');
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ArrayNode (items)](#apidoc.element.mathjs.expression.node.ArrayNode)
- description and source-code
```javascript
function ArrayNode(items) {
  if (!(this instanceof ArrayNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.items = items || [];

  // validate input
  if (!Array.isArray(this.items)
      || !this.items.every(function (item) {return item && item.isNode;})) {
    throw new TypeError('Array containing Nodes expected');
  }

  // TODO: deprecated since v3, remove some day
  var deprecated = function () {
    throw new Error('Property 'ArrayNode.nodes' is deprecated, use 'ArrayNode.items' instead');
  };
  Object.defineProperty(this, 'nodes', { get: deprecated, set: deprecated });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>AssignmentNode (object, index, value)](#apidoc.element.mathjs.expression.node.AssignmentNode)
- description and source-code
```javascript
function AssignmentNode(object, index, value) {
  if (!(this instanceof AssignmentNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.object = object;
  this.index = value ? index : null;
  this.value = value ? value : index;

  // validate input
  if (!object || !(object.isSymbolNode || object.isAccessorNode)) {
    throw new TypeError('SymbolNode or AccessorNode expected as "object"');
  }
  if (object && object.isSymbolNode && object.name === 'end') {
    throw new Error('Cannot assign to symbol "end"');
  }
  if (this.index && !this.index.isIndexNode) {
    throw new TypeError('IndexNode expected as "index"');
  }
  if (!this.value || !this.value.isNode) {
    throw new TypeError('Node expected as "value"');
  }

  // readonly property name
  Object.defineProperty(this, 'name', {
    get: function () {
      if (this.index) {
        return (this.index.isObjectProperty())
            ? this.index.getObjectProperty()
            : '';
      }
      else {
        return this.object.name || '';
      }
    }.bind(this),
    set: function () {
      throw new Error('Cannot assign a new name, name is read-only');
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.BlockNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>BlockNode (blocks)](#apidoc.element.mathjs.expression.node.BlockNode)
- description and source-code
```javascript
function BlockNode(blocks) {
  if (!(this instanceof BlockNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  // validate input, copy blocks
  if (!Array.isArray(blocks)) throw new Error('Array expected');
  this.blocks = blocks.map(function (block) {
    var node = block && block.node;
    var visible = block && block.visible !== undefined ? block.visible : true;

    if (!(node && node.isNode))      throw new TypeError('Property "node" must be a Node');
    if (typeof visible !== 'boolean') throw new TypeError('Property "visible" must be a boolean');

    return {
      node: node,
      visible: visible
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ConditionalNode (condition, trueExpr, falseExpr)](#apidoc.element.mathjs.expression.node.ConditionalNode)
- description and source-code
```javascript
function ConditionalNode(condition, trueExpr, falseExpr) {
  if (!(this instanceof ConditionalNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }
  if (!(condition && condition.isNode)) throw new TypeError('Parameter condition must be a Node');
  if (!(trueExpr && trueExpr.isNode))  throw new TypeError('Parameter trueExpr must be a Node');
  if (!(falseExpr && falseExpr.isNode)) throw new TypeError('Parameter falseExpr must be a Node');

  this.condition = condition;
  this.trueExpr = trueExpr;
  this.falseExpr = falseExpr;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ConstantNode (value, valueType)](#apidoc.element.mathjs.expression.node.ConstantNode)
- description and source-code
```javascript
function ConstantNode(value, valueType) {
  if (!(this instanceof ConstantNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (valueType) {
    if (typeof valueType !== 'string') {
      throw new TypeError('String expected for parameter "valueType"');
    }
    if (typeof value !== 'string') {
      throw new TypeError('String expected for parameter "value"');
    }

    this.value = value;
    this.valueType = valueType;
  }
  else {
    // stringify the value and determine the type
    this.value = value + '';
    this.valueType = getType(value);
  }

  if (!SUPPORTED_TYPES[this.valueType]) {
    throw new TypeError('Unsupported type of value "' + this.valueType + '"');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>FunctionAssignmentNode (name, params, expr)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode)
- description and source-code
```javascript
function FunctionAssignmentNode(name, params, expr) {
  if (!(this instanceof FunctionAssignmentNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  // validate input
  if (typeof name !== 'string') throw new TypeError('String expected for parameter "name"');
  if (!Array.isArray(params))  throw new TypeError('Array containing strings or objects expected for parameter "params"');
  if (!(expr && expr.isNode)) throw new TypeError('Node expected for parameter "expr"');
  if (name in keywords) throw new Error('Illegal function name, "' + name + '" is a reserved keyword');

  this.name = name;
  this.params = params.map(function (param) {
    return param && param.name || param;
  });
  this.types = params.map(function (param) {
    return param && param.type || 'any'
  });
  this.expr = expr;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>FunctionNode (fn, args)](#apidoc.element.mathjs.expression.node.FunctionNode)
- description and source-code
```javascript
function FunctionNode(fn, args) {
  if (!(this instanceof FunctionNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (typeof fn === 'string') {
    fn = new SymbolNode(fn);
  }

  // validate input
  if (!fn || !fn.isNode) throw new TypeError('Node expected as parameter "fn"');
  if (!Array.isArray(args)
      || !args.every(function (arg) {return arg && arg.isNode;})) {
    throw new TypeError('Array containing Nodes expected for parameter "args"');
  }

  this.fn = fn;
  this.args = args || [];

  // readonly property name
  Object.defineProperty(this, 'name', {
    get: function () {
      return this.fn.name || '';
    }.bind(this),
    set: function () {
      throw new Error('Cannot assign a new name, name is read-only');
    }
  });

  // TODO: deprecated since v3, remove some day
  var deprecated = function () {
    throw new Error('Property 'FunctionNode.object' is deprecated, use 'FunctionNode.fn' instead');
  };
  Object.defineProperty(this, 'object', { get: deprecated, set: deprecated });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>IndexNode (dimensions, dotNotation)](#apidoc.element.mathjs.expression.node.IndexNode)
- description and source-code
```javascript
function IndexNode(dimensions, dotNotation) {
  if (!(this instanceof IndexNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.dimensions = dimensions;
  this.dotNotation = dotNotation || false;

  // validate input
  if (!isArray(dimensions)
      || !dimensions.every(function (range) {return range && range.isNode;})) {
    throw new TypeError('Array containing Nodes expected for parameter "dimensions"');
  }
  if (this.dotNotation && !this.isObjectProperty()) {
    throw new Error('dotNotation only applicable for object properties');
  }

  // TODO: deprecated since v3, remove some day
  var deprecated = function () {
    throw new Error('Property 'IndexNode.object' is deprecated, use 'IndexNode.fn' instead');
  };
  Object.defineProperty(this, 'object', { get: deprecated, set: deprecated });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>Node ()](#apidoc.element.mathjs.expression.node.Node)
- description and source-code
```javascript
function Node() {
  if (!(this instanceof Node)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ObjectNode (properties)](#apidoc.element.mathjs.expression.node.ObjectNode)
- description and source-code
```javascript
function ObjectNode(properties) {
  if (!(this instanceof ObjectNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.properties = properties || {};

  // validate input
  if (properties) {
    if (!(typeof properties === 'object') || Object.keys(properties).some(function (key) {
          return !properties[key] || !properties[key].isNode;
        })) {
      throw new TypeError('Object containing Nodes expected');
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>OperatorNode (op, fn, args, implicit)](#apidoc.element.mathjs.expression.node.OperatorNode)
- description and source-code
```javascript
function OperatorNode(op, fn, args, implicit) {
  if (!(this instanceof OperatorNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  //validate input
  if (typeof op !== 'string') {
    throw new TypeError('string expected for parameter "op"');
  }
  if (typeof fn !== 'string') {
    throw new TypeError('string expected for parameter "fn"');
  }
  if (!Array.isArray(args)
      || !args.every(function (node) {return node && node.isNode;})) {
    throw new TypeError('Array containing Nodes expected for parameter "args"');
  }

  this.implicit = (implicit === true);
  this.op = op;
  this.fn = fn;
  this.args = args || [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>ParenthesisNode (content)](#apidoc.element.mathjs.expression.node.ParenthesisNode)
- description and source-code
```javascript
function ParenthesisNode(content) {
  if (!(this instanceof ParenthesisNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  // validate input
  if (!(content && content.isNode)) {
    throw new TypeError('Node expected for parameter "content"');
  }

  this.content = content;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>RangeNode (start, end, step)](#apidoc.element.mathjs.expression.node.RangeNode)
- description and source-code
```javascript
function RangeNode(start, end, step) {
  if (!(this instanceof RangeNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  // validate inputs
  if (!(start && start.isNode)) throw new TypeError('Node expected');
  if (!(end && end.isNode)) throw new TypeError('Node expected');
  if (step && !(step && step.isNode)) throw new TypeError('Node expected');
  if (arguments.length > 3) throw new Error('Too many arguments');

  this.start = start;         // included lower-bound
  this.end = end;           // included upper-bound
  this.step = step || null;  // optional step
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>SymbolNode (name)](#apidoc.element.mathjs.expression.node.SymbolNode)
- description and source-code
```javascript
function SymbolNode(name) {
  if (!(this instanceof SymbolNode)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  // validate input
  if (typeof name !== 'string')  throw new TypeError('String expected for parameter "name"');

  this.name = name;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.UpdateNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.</span>UpdateNode ()](#apidoc.element.mathjs.expression.node.UpdateNode)
- description and source-code
```javascript
function UpdateNode() {
  // TODO: deprecated since v3. Cleanup some day
  throw new Error('UpdateNode is deprecated. Use AssignmentNode instead.');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.AccessorNode.prototype"></a>[module mathjs.expression.node.AccessorNode.prototype](#apidoc.module.mathjs.expression.node.AccessorNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  defs.access = access;
  defs.getSafeValue = getSafeValue;

  var object = this.object._compile(defs, args);
  var index = this.index._compile(defs, args);

  if (this.index.isObjectProperty()) {
    var prop = this.index.getObjectProperty();
    return 'getSafeValue(' + object + '["' + prop + '"])';
  }
  else if (this.index.needsSize()) {
    // if some parameters use the 'end' parameter, we need to calculate the size
    return '(function () {' +
        '  var object = ' + object + ';' +
        '  var size = math.size(object).valueOf();' +
        '  return access(object, ' + index + ');' +
        '})()';
  }
  else {
    return 'access(' + object + ', ' + index + ')';
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var object = this.object.toString(options);
  if (needParenthesis(this.object)) {
    object = '(' + object + ')';
  }

  return object + this.index.toString(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var object = this.object.toTex(options);
  if (needParenthesis(this.object)) {
    object = '\\left(' + object + '\\right)';
  }

  return object + this.index.toTex(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.AccessorNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new AccessorNode(this.object, this.index);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  callback(this.object, 'object', this);
  callback(this.index, 'index', this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AccessorNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AccessorNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.AccessorNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  return new AccessorNode(
      this._ifNode(callback(this.object, 'object', this)),
      this._ifNode(callback(this.index, 'index', this))
  );
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.ArrayNode.prototype"></a>[module mathjs.expression.node.ArrayNode.prototype](#apidoc.module.mathjs.expression.node.ArrayNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  var asMatrix = (defs.math.config().matrix !== 'Array');

  var items = this.items.map(function (node) {
    return node._compile(defs, args);
  });

  return (asMatrix ? 'math.matrix([' : '[') +
      items.join(',') +
      (asMatrix ? '])' : ']');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var items = this.items.map(function (node) {
    return node.toString(options);
  });
  return '[' + items.join(', ') + ']';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var s = '\\begin{bmatrix}';

  this.items.forEach(function(node) {
    if (node.items) {
      s += node.items.map(function(childNode) {
        return childNode.toTex(options);
      }).join('&');
    }
    else {
      s += node.toTex(options);
    }

    // new line
    s += '\\\\';
  });
  s += '\\end{bmatrix}';
  return s;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ArrayNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new ArrayNode(this.items.slice(0));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var i = 0; i < this.items.length; i++) {
    var node = this.items[i];
    callback(node, 'items[' + i + ']', this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ArrayNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ArrayNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ArrayNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var items = [];
  for (var i = 0; i < this.items.length; i++) {
    items[i] = this._ifNode(callback(this.items[i], 'items[' + i + ']', this));
  }
  return new ArrayNode(items);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.AssignmentNode.prototype"></a>[module mathjs.expression.node.AssignmentNode.prototype](#apidoc.module.mathjs.expression.node.AssignmentNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  defs.assign = assign;
  defs.access = access;

  var size;
  var object = this.object._compile(defs, args);
  var index = this.index ? this.index._compile(defs, args) : null;
  var value = this.value._compile(defs, args);

  if (!this.index) {
    // apply a variable to the scope, for example 'a=2'
    if (!this.object.isSymbolNode) {
      throw new TypeError('SymbolNode expected as object');
    }

    return 'scope["' + this.object.name + '"] = ' + value;
  }
  else if (this.index.isObjectProperty()) {
    // apply an object property for example 'a.b=2'
    return object + '["' + this.index.getObjectProperty() + '"] = ' + value;
  }
  else if (this.object.isSymbolNode) {
    // update a matrix subset, for example 'a[2]=3'
    size = this.index.needsSize() ? 'var size = math.size(object).valueOf();' : '';

    // apply updated object to scope
    return '(function () {' +
        '  var object = ' + object + ';' +
        '  var value = ' + value + ';' +
        '  ' + size +
        '  scope["' + this.object.name + '"] = assign(object, ' + index + ', value);' +
        '  return value;' +
        '})()';
  }
  else { // this.object.isAccessorNode === true
    // update a matrix subset, for example 'a.b[2]=3'
    size = this.index.needsSize() ? 'var size = math.size(object).valueOf();' : '';

    // we will not use the _compile of the AccessorNode, but compile it
    // ourselves here as we need the parent object of the AccessorNode:
    // wee need to apply the updated object to parent object
    var parentObject = this.object.object._compile(defs, args);

    if (this.object.index.isObjectProperty()) {
      var parentProperty = '["' + this.object.index.getObjectProperty() + '"]';
      return '(function () {' +
          '  var parent = ' + parentObject + ';' +
          '  var object = parent' + parentProperty + ';' + // parentIndex is a property
          '  var value = ' + value + ';' +
          size +
          '  parent' + parentProperty + ' = assign(object, ' + index + ', value);' +
          '  return value;' +
          '})()';
    }
    else {
      // if some parameters use the 'end' parameter, we need to calculate the size
      var parentSize = this.object.index.needsSize() ? 'var size = math.size(parent).valueOf();' : '';
      var parentIndex = this.object.index._compile(defs, args);

      return '(function () {' +
          '  var parent = ' + parentObject + ';' +
          '  ' + parentSize +
          '  var parentIndex = ' + parentIndex + ';' +
          '  var object = access(parent, parentIndex);' +
          '  var value = ' + value + ';' +
          '  ' + size +
          '  assign(parent, parentIndex, assign(object, ' + index + ', value));' +
          '  return value;' +
          '})()';
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var object = this.object.toString(options);
  var index = this.index ? this.index.toString(options) : '';
  var value = this.value.toString(options);
  if (needParenthesis(this, options && options.parenthesis)) {
    value = '(' + value + ')';
  }

  return object + index + ' = ' + value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var object = this.object.toTex(options);
  var index = this.index ? this.index.toTex(options) : '';
  var value = this.value.toTex(options);
  if (needParenthesis(this, options && options.parenthesis)) {
    value = '\\left(' + value + '\\right)';
  }

  return object + index + ':=' + value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new AssignmentNode(this.object, this.index, this.value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  callback(this.object, 'object', this);
  if (this.index) {
    callback(this.index, 'index', this);
  }
  callback(this.value, 'value', this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.AssignmentNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.AssignmentNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.AssignmentNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var object = this._ifNode(callback(this.object, 'object', this));
  var index = this.index
      ? this._ifNode(callback(this.index, 'index', this))
      : null;
  var value = this._ifNode(callback(this.value, 'value', this));

  return new AssignmentNode(object, index, value);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.BlockNode.prototype"></a>[module mathjs.expression.node.BlockNode.prototype](#apidoc.module.mathjs.expression.node.BlockNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.BlockNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.BlockNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  defs.ResultSet = ResultSet;
  var blocks = this.blocks.map(function (param) {
    var js = param.node._compile(defs, args);
    if (param.visible) {
      return 'results.push(' + js + ');';
    }
    else {
      return js + ';';
    }
  });

  return '(function () {' +
      'var results = [];' +
      blocks.join('') +
      'return new ResultSet(results);' +
      '})()';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.BlockNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.BlockNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  return this.blocks.map(function (param) {
    return param.node.toString(options) + (param.visible ? '' : ';');
  }).join('\n');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.BlockNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.BlockNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  return this.blocks.map(function (param) {
    return param.node.toTex(options) + (param.visible ? '' : ';');
  }).join('\\;\\;\n');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.BlockNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.BlockNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var blocks = this.blocks.map(function (block) {
    return {
      node: block.node,
      visible: block.visible
    };
  });

  return new BlockNode(blocks);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.BlockNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.BlockNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var i = 0; i < this.blocks.length; i++) {
    callback(this.blocks[i].node, 'blocks[' + i + '].node', this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.BlockNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.BlockNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.BlockNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var blocks = [];
  for (var i = 0; i < this.blocks.length; i++) {
    var block = this.blocks[i];
    var node = this._ifNode(callback(block.node, 'blocks[' + i + '].node', this));
    blocks[i] = {
      node: node,
      visible: block.visible
    };
  }
  return new BlockNode(blocks);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.ConditionalNode.prototype"></a>[module mathjs.expression.node.ConditionalNode.prototype](#apidoc.module.mathjs.expression.node.ConditionalNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
<span class="apidocCodeCommentSpan">  /**
   * Test whether a condition is met
   * @param {*} condition
   * @returns {boolean} true if condition is true or non-zero, else false
   */
</span>  defs.testCondition = function (condition) {
    if (typeof condition === 'number'
        || typeof condition === 'boolean'
        || typeof condition === 'string') {
      return condition ? true : false;
    }

    if (condition) {
      if (condition.isBigNumber === true) {
        return condition.isZero() ? false : true;
      }

      if (condition.isComplex === true) {
        return (condition.re || condition.im) ? true : false;
      }

      if (condition.isUnit === true) {
        return condition.value ? true : false;
      }
    }

    if (condition === null || condition === undefined) {
      return false;
    }

    throw new TypeError('Unsupported type of condition "' + defs.math['typeof'](condition) + '"');
  };

  return (
    'testCondition(' + this.condition._compile(defs, args) + ') ? ' +
    '( ' + this.trueExpr._compile(defs, args) + ') : ' +
    '( ' + this.falseExpr._compile(defs, args) + ')'
  );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var precedence = operators.getPrecedence(this, parenthesis);

  //Enclose Arguments in parentheses if they are an OperatorNode
  //or have lower or equal precedence
  //NOTE: enclosing all OperatorNodes in parentheses is a decision
  //purely based on aesthetics and readability
  var condition = this.condition.toString(options);
  var conditionPrecedence = operators.getPrecedence(this.condition, parenthesis);
  if ((parenthesis === 'all')
      || (this.condition.type === 'OperatorNode')
      || ((conditionPrecedence !== null) && (conditionPrecedence <= precedence))) {
    condition = '(' + condition + ')';
  }

  var trueExpr = this.trueExpr.toString(options);
  var truePrecedence = operators.getPrecedence(this.trueExpr, parenthesis);
  if ((parenthesis === 'all')
      || (this.trueExpr.type === 'OperatorNode')
      || ((truePrecedence !== null) && (truePrecedence <= precedence))) {
    trueExpr = '(' + trueExpr + ')';
  }

  var falseExpr = this.falseExpr.toString(options);
  var falsePrecedence = operators.getPrecedence(this.falseExpr, parenthesis);
  if ((parenthesis === 'all')
      || (this.falseExpr.type === 'OperatorNode')
      || ((falsePrecedence !== null) && (falsePrecedence <= precedence))) {
    falseExpr = '(' + falseExpr + ')';
  }
  return condition + ' ? ' + trueExpr + ' : ' + falseExpr;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  return '\\begin{cases} {'
      + this.trueExpr.toTex(options) + '}, &\\quad{\\text{if }\\;'
      + this.condition.toTex(options)
      + '}\\\\{' + this.falseExpr.toTex(options)
      + '}, &\\quad{\\text{otherwise}}\\end{cases}';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new ConditionalNode(this.condition, this.trueExpr, this.falseExpr);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  callback(this.condition, 'condition', this);
  callback(this.trueExpr, 'trueExpr', this);
  callback(this.falseExpr, 'falseExpr', this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConditionalNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConditionalNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ConditionalNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  return new ConditionalNode(
      this._ifNode(callback(this.condition, 'condition', this)),
      this._ifNode(callback(this.trueExpr, 'trueExpr', this)),
      this._ifNode(callback(this.falseExpr, 'falseExpr', this))
  );
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.ConstantNode.prototype"></a>[module mathjs.expression.node.ConstantNode.prototype](#apidoc.module.mathjs.expression.node.ConstantNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  switch (this.valueType) {
    case 'number':
      // TODO: replace this with using config.number
      var numConfig = defs.math.config().number;
      if (numConfig === 'BigNumber') {
        return 'math.bignumber("' + this.value + '")';
      }
      else if (numConfig === 'Fraction') {
        return 'math.fraction("' + this.value + '")';
      }
      else {
        // remove leading zeros like '003.2' which are not allowed by JavaScript
        return this.value.replace(/^(0*)[0-9]/, function (match, zeros) {
          return match.substring(zeros.length);
        });
      }

    case 'string':
      return '"' + this.value + '"';

    case 'boolean':
      return this.value;

    case 'undefined':
      return this.value;

    case 'null':
      return this.value;

    default:
      // TODO: move this error to the constructor?
      throw new TypeError('Unsupported type of constant "' + this.valueType + '"');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  switch (this.valueType) {
    case 'string':
      return '"' + this.value + '"';

    default:
      return this.value;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var value = this.value,
      index;
  switch (this.valueType) {
    case 'string':
      return '\\mathtt{"' + value + '"}';

    case 'number':
      index = value.toLowerCase().indexOf('e');
      if (index !== -1) {
        return value.substring(0, index) + '\\cdot10^{' +
            value.substring(index + 1) + '}';
      }
      return value;

    default:
      return value;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ConstantNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new ConstantNode(this.value, this.valueType);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  // nothing to do, we don't have childs
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ConstantNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ConstantNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ConstantNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  return this.clone();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.FunctionAssignmentNode.prototype"></a>[module mathjs.expression.node.FunctionAssignmentNode.prototype](#apidoc.module.mathjs.expression.node.FunctionAssignmentNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  defs.typed = typed;

  // we extend the original args and add the args to the child object
  var childArgs = Object.create(args);
  this.params.forEach(function (variable) {
    childArgs[variable] = true;
  });

  // compile the function expression with the child args
  var jsExpr = this.expr._compile(defs, childArgs);

  return 'scope["' + this.name + '"] = ' +
      '  (function () {' +
      '    var fn = typed("' + this.name + '", {' +
      '      "' + this.types.join(',') + '": function (' + this.params.join(',') + ') {' +
      '        return ' + jsExpr + '' +
      '      }' +
      '    });' +
      '    fn.syntax = "' + this.name + '(' + this.params.join(', ') + ')";' +
      '    return fn;' +
      '  })()';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var expr = this.expr.toString(options);
  if (needParenthesis(this, parenthesis)) {
    expr = '(' + expr + ')';
  }
  return this.name + '(' + this.params.join(', ') + ') = ' + expr;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var expr = this.expr.toTex(options);
  if (needParenthesis(this, parenthesis)) {
    expr = '\\left(' + expr + '\\right)';
  }

  return '\\mathrm{' + this.name
      + '}\\left(' + this.params.map(latex.toSymbol).join(',') + '\\right):=' + expr;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new FunctionAssignmentNode(this.name, this.params.slice(0), this.expr);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  callback(this.expr, 'expr', this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionAssignmentNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.FunctionAssignmentNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var expr = this._ifNode(callback(this.expr, 'expr', this));

  return new FunctionAssignmentNode(this.name, this.params.slice(0), expr);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.FunctionNode.prototype"></a>[module mathjs.expression.node.FunctionNode.prototype](#apidoc.module.mathjs.expression.node.FunctionNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  // compile fn and arguments
  var jsFn = this.fn._compile(defs, args);
  var jsArgs = this.args.map(function (arg) {
    return arg._compile(defs, args);
  });
  var jsScope = this._compileScope(defs, args);
  var argsName;

  if (this.fn.isSymbolNode) {
    // we can statically determine whether the function has an rawArgs property
    var name = this.fn.name;
    var fn = defs.math[name];
    var isRaw = (typeof fn === 'function') && (fn.rawArgs == true);

    if (isRaw) {
      // pass unevaluated parameters (nodes) to the function
      argsName = this._getUniqueArgumentsName(defs);
      defs[argsName] = this.args;

      return jsFn + '(' + argsName + ', math, ' + jsScope + ')';
    }
    else {
      // "regular" evaluation
      return jsFn + '(' + jsArgs.join(', ') + ')';
    }
  }
  else if (this.fn.isAccessorNode && this.fn.index.isObjectProperty()) {
    // execute the function with the right context: the object of the AccessorNode
    argsName = this._getUniqueArgumentsName(defs);
    defs[argsName] = this.args;
    defs.getSafeValue = getSafeValue

    var jsObject = this.fn.object._compile(defs, args);
    var prop = this.fn.index.getObjectProperty();

    return '(function () {' +
        'var object = ' + jsObject + ';' +
        'var value = getSafeValue(object["' + prop + '"]);' +
        'return (value && value.rawArgs) ' +
        ' ? object["' + prop + '"](' + argsName + ', math, ' + jsScope + ')' +
        ' : object["' + prop + '"](' + jsArgs.join(', ') + ')' +
        '})()';
  }
  else { // this.fn.isAccessorNode && !this.fn.index.isObjectProperty()
    // we have to dynamically determine whether the function has a rawArgs property
    argsName = this._getUniqueArgumentsName(defs);
    defs[argsName] = this.args;

    return '(function () {' +
        'var fn = ' + jsFn + ';' +
        'return (fn && fn.rawArgs) ' +
        ' ? fn(' + argsName + ', math, ' + jsScope + ')' +
        ' : fn(' + jsArgs.join(', ') + ')' +
        '})()';
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype._compileScope"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_compileScope (defs, args)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._compileScope)
- description and source-code
```javascript
_compileScope = function (defs, args) {
  var names = Object.keys(args);
  if (names.length === 0) {
    return 'scope';
  }
  else {
    // merge arguments into scope
    defs.extend = extend;

    var jsArgs = names
        .map(function (arg) {
          return '"' + arg + '": ' + arg;
        })
        .join(', ');

    return 'extend(extend({}, scope), {' + jsArgs + '})';
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype._getUniqueArgumentsName"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_getUniqueArgumentsName (defs)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._getUniqueArgumentsName)
- description and source-code
```javascript
_getUniqueArgumentsName = function (defs) {
  var argsName;
  var i = 0;

  do {
    argsName = 'args' + i;
    i++;
  }
  while (argsName in defs);

  return argsName;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var args = this.args.map(function (arg) {
    return arg.toString(options);
  });

  // format the arguments like "add(2, 4.2)"
  return this.fn.toString(options) + '(' + args.join(', ') + ')';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var args = this.args.map(function (arg) { //get LaTeX of the arguments
    return arg.toTex(options);
  });

  var latexConverter;

  if (math[this.name] && ((typeof math[this.name].toTex === 'function') || (typeof math[this.name].toTex === 'object') || (typeof
 math[this.name].toTex === 'string'))) {
    //.toTex is a callback function
    latexConverter = math[this.name].toTex;
  }

  var customToTex;
  switch (typeof latexConverter) {
    case 'function': //a callback function
      customToTex = latexConverter(this, options);
      break;
    case 'string': //a template string
      customToTex = expandTemplate(latexConverter, this, options);
      break;
    case 'object': //an object with different "converters" for different numbers of arguments
      switch (typeof latexConverter[args.length]) {
        case 'function':
          customToTex = latexConverter[args.length](this, options);
          break;
        case 'string':
          customToTex = expandTemplate(latexConverter[args.length], this, options);
          break;
      }
  }

  if (typeof customToTex !== 'undefined') {
    return customToTex;
  }

  return expandTemplate(latex.defaultTemplate, this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new FunctionNode(this.fn, this.args.slice(0));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var i = 0; i < this.args.length; i++) {
    callback(this.args[i], 'args[' + i + ']', this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype.getIdentifier"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>getIdentifier ()](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.getIdentifier)
- description and source-code
```javascript
getIdentifier = function () {
  return this.type + ':' + this.name;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var fn = this.fn.map(callback);
  var args = [];
  for (var i = 0; i < this.args.length; i++) {
    args[i] = this._ifNode(callback(this.args[i], 'args[' + i + ']', this));
  }
  return new FunctionNode(fn, args);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>toString (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.toString)
- description and source-code
```javascript
toString = function (options) {
  var customString;
  var name = this.fn.toString(options);
  if (options && (typeof options.handler === 'object') && options.handler.hasOwnProperty(name)) {
    //callback is a map of callback functions
    customString = options.handler[name](this, options);
  }

  if (typeof customString !== 'undefined') {
    return customString;
  }

  //fall back to Node's toString
  return nodeToString.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.FunctionNode.prototype.toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.FunctionNode.prototype.</span>toTex (options)](#apidoc.element.mathjs.expression.node.FunctionNode.prototype.toTex)
- description and source-code
```javascript
toTex = function (options) {
  var customTex;
  if (options && (typeof options.handler === 'object') && options.handler.hasOwnProperty(this.name)) {
    //callback is a map of callback functions
    customTex = options.handler[this.name](this, options);
  }

  if (typeof customTex !== 'undefined') {
    return customTex;
  }

  //fall back to Node's toTex
  return nodeToTex.call(this, options);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.IndexNode.prototype"></a>[module mathjs.expression.node.IndexNode.prototype](#apidoc.module.mathjs.expression.node.IndexNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.IndexNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  // args can be mutated by IndexNode, when dimensions use 'end'
  var childArgs = Object.create(args);

  // helper function to create a Range from start, step and end
  defs.range = function (start, end, step) {
    return new Range(
        (start && start.isBigNumber === true) ? start.toNumber() : start,
        (end   && end.isBigNumber === true)   ? end.toNumber()   : end,
        (step  && step.isBigNumber === true)  ? step.toNumber()  : step
    );
  };

  // TODO: implement support for bignumber (currently bignumbers are silently
  //       reduced to numbers when changing the value to zero-based)

  // TODO: Optimization: when the range values are ConstantNodes,
  //       we can beforehand resolve the zero-based value

  // optimization for a simple object property
  var dimensions = this.dimensions.map(function (range, i) {
    if (range && range.isRangeNode) {
      if (range.needsEnd()) {
        childArgs.end = true;

        // resolve end and create range
        return '(function () {' +
            'var end = size[' + i + ']; ' +
            'return range(' +
            range.start._compile(defs, childArgs) + ', ' +
            range.end._compile(defs, childArgs) + ', ' +
            (range.step ? range.step._compile(defs, childArgs) : '1') +
            '); ' +
            '})()';
      }
      else {
        // create range
        return 'range(' +
            range.start._compile(defs, childArgs) + ', ' +
            range.end._compile(defs, childArgs) + ', ' +
            (range.step ? range.step._compile(defs, childArgs) : '1') +
            ')';
      }
    }
    if (range.isSymbolNode && range.name === 'end') {
      childArgs.end = true;

      // resolve the parameter 'end'
      return '(function () {' +
          'var end = size[' + i + ']; ' +
          'return ' + range._compile(defs, childArgs) + '; ' +
          '})()'
    }
    else { // ConstantNode
      return range._compile(defs, childArgs);
    }
  });

  return 'math.index(' + dimensions.join(', ') + ')';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.IndexNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  // format the parameters like "[1, 0:5]"
  return this.dotNotation
      ? ('.' + this.getObjectProperty())
      : ('[' + this.dimensions.join(', ') + ']');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.IndexNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var dimensions = this.dimensions.map(function (range) {
    return range.toTex(options);
  });

  return this.dotNotation
      ? ('.' + this.getObjectProperty() + '')
      : ('_{' + dimensions.join(',') + '}');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new IndexNode(this.dimensions.slice(0));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.IndexNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var i = 0; i < this.dimensions.length; i++) {
    callback(this.dimensions[i], 'dimensions[' + i + ']', this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype.getObjectProperty"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>getObjectProperty ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.getObjectProperty)
- description and source-code
```javascript
getObjectProperty = function () {
  return this.isObjectProperty() ? this.dimensions[0].value : null;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype.isObjectProperty"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>isObjectProperty ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.isObjectProperty)
- description and source-code
```javascript
isObjectProperty = function () {
  return this.dimensions.length === 1 &&
      this.dimensions[0].isConstantNode &&
      this.dimensions[0].valueType === 'string';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.IndexNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var dimensions = [];
  for (var i = 0; i < this.dimensions.length; i++) {
    dimensions[i] = this._ifNode(callback(this.dimensions[i], 'dimensions[' + i + ']', this));
  }

  return new IndexNode(dimensions);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.IndexNode.prototype.needsSize"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.IndexNode.prototype.</span>needsSize ()](#apidoc.element.mathjs.expression.node.IndexNode.prototype.needsSize)
- description and source-code
```javascript
needsSize = function () {
  return this.dimensions.some(function (range) {
    return (range.isRangeNode && range.needsEnd()) ||
        (range.isSymbolNode && range.name === 'end');
  });
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.Node.prototype"></a>[module mathjs.expression.node.Node.prototype](#apidoc.module.mathjs.expression.node.Node.prototype)

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.Node.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  // must be implemented by each of the Node implementations
  throw new Error('Cannot compile a Node interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype._ifNode"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_ifNode (node)](#apidoc.element.mathjs.expression.node.Node.prototype._ifNode)
- description and source-code
```javascript
_ifNode = function (node) {
  if (!(node && node.isNode)) {
    throw new TypeError('Callback function must return a Node');
  }

  return node;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_toString ()](#apidoc.element.mathjs.expression.node.Node.prototype._toString)
- description and source-code
```javascript
_toString = function () {
  //must be implemented by each of the Node implementations
  throw new Error('_toString not implemented for ' + this.type);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.Node.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  //must be implemented by each of the Node implementations
  throw new Error('_toTex not implemented for ' + this.type);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.Node.prototype.clone)
- description and source-code
```javascript
clone = function () {
  // must be implemented by each of the Node implementations
  throw new Error('Cannot clone a Node interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.cloneDeep"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>cloneDeep ()](#apidoc.element.mathjs.expression.node.Node.prototype.cloneDeep)
- description and source-code
```javascript
cloneDeep = function () {
  return this.map(function (node) {
    return node.cloneDeep();
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>compile ()](#apidoc.element.mathjs.expression.node.Node.prototype.compile)
- description and source-code
```javascript
compile = function () {
  // TODO: calling compile(math) is deprecated since version 2.0.0. Remove this warning some day
  if (arguments.length > 0) {
    throw new Error('Calling compile(math) is deprecated. Call the function as compile() instead.');
  }

  // definitions globally available inside the closure of the compiled expressions
  var defs = {
    math: math.expression.transform,
    args: {}, // can be filled with names of FunctionAssignment arguments
    _validateScope: _validateScope
  };

  // will be used to put local function arguments
  var args = {};

  var code = this._compile(defs, args);

  var defsCode = Object.keys(defs).map(function (name) {
    return '    var ' + name + ' = defs["' + name + '"];';
  });

  var factoryCode =
      defsCode.join(' ') +
      'return {' +
      '  "eval": function (scope) {' +
      '    if (scope) _validateScope(scope);' +
      '    scope = scope || {};' +
      '    return ' + code + ';' +
      '  }' +
      '};';

  var factory = new Function('defs', factoryCode);
  return factory(defs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.equals"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>equals (other)](#apidoc.element.mathjs.expression.node.Node.prototype.equals)
- description and source-code
```javascript
equals = function (other) {
  return other
      ? deepEqual(this, other)
      : false
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.eval"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>eval (scope)](#apidoc.element.mathjs.expression.node.Node.prototype.eval)
- description and source-code
```javascript
eval = function (scope) {
  return this.compile().eval(scope);
}
```
- example usage
```shell
...
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
...
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.filter"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>filter (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.filter)
- description and source-code
```javascript
filter = function (callback) {
  var nodes = [];

  this.traverse(function (node, path, parent) {
    if (callback(node, path, parent)) {
      nodes.push(node);
    }
  });

  return nodes;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.find"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>find ()](#apidoc.element.mathjs.expression.node.Node.prototype.find)
- description and source-code
```javascript
find = function () {
  throw new Error('Function Node.find is deprecated. Use Node.filter instead.');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  // must be implemented by each of the Node implementations
  throw new Error('Cannot run forEach on a Node interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.getContent"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>getContent ()](#apidoc.element.mathjs.expression.node.Node.prototype.getContent)
- description and source-code
```javascript
getContent = function () {
  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.getIdentifier"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>getIdentifier ()](#apidoc.element.mathjs.expression.node.Node.prototype.getIdentifier)
- description and source-code
```javascript
getIdentifier = function () {
  return this.type;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  // must be implemented by each of the Node implementations
  throw new Error('Cannot run map on a Node interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.match"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>match ()](#apidoc.element.mathjs.expression.node.Node.prototype.match)
- description and source-code
```javascript
match = function () {
  throw new Error('Function Node.match is deprecated. See functions Node.filter, Node.transform, Node.traverse.');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>toString (options)](#apidoc.element.mathjs.expression.node.Node.prototype.toString)
- description and source-code
```javascript
toString = function (options) {
  var customString;
  if (options && typeof options == "object") {
      switch (typeof options.handler) {
        case 'object':
        case 'undefined':
          break;
        case 'function':
          customString = options.handler(this, options);
          break;
        default:
          throw new TypeError('Object or function expected as callback');
      }
  }

  if (typeof customString !== 'undefined') {
    return customString;
  }

  return this._toString(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>toTex (options)](#apidoc.element.mathjs.expression.node.Node.prototype.toTex)
- description and source-code
```javascript
toTex = function (options) {
  var customTex;
  if (options && typeof options == 'object') {
    switch (typeof options.handler) {
      case 'object':
      case 'undefined':
        break;
      case 'function':
        customTex = options.handler(this, options);
        break;
      default:
        throw new TypeError('Object or function expected as callback');
    }
  }

  if (typeof customTex !== 'undefined') {
    return customTex;
  }

  return this._toTex(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.transform"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>transform (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.transform)
- description and source-code
```javascript
transform = function (callback) {
  // traverse over all childs
  function _transform (node, callback) {
    return node.map(function(child, path, parent) {
      var replacement = callback(child, path, parent);
      return _transform(replacement, callback);
    });
  }

  var replacement = callback(this, null, null);
  return _transform(replacement, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.Node.prototype.traverse"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.Node.prototype.</span>traverse (callback)](#apidoc.element.mathjs.expression.node.Node.prototype.traverse)
- description and source-code
```javascript
traverse = function (callback) {
  // execute callback for itself
  callback(this, null, null);

  // recursively traverse over all childs of a node
  function _traverse(node, callback) {
    node.forEach(function (child, path, parent) {
      callback(child, path, parent);
      _traverse(child, callback);
    });
  }

  _traverse(this, callback);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.ObjectNode.prototype"></a>[module mathjs.expression.node.ObjectNode.prototype](#apidoc.module.mathjs.expression.node.ObjectNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  var entries = [];
  for (var key in this.properties) {
    if (this.properties.hasOwnProperty(key)) {
      entries.push('"' + key + '": ' + this.properties[key]._compile(defs, args));
    }
  }
  return '{' + entries.join(', ') + '}';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var entries = [];
  for (var key in this.properties) {
    if (this.properties.hasOwnProperty(key)) {
      entries.push('"' + key + '": ' + this.properties[key].toString(options));
    }
  }
  return '{' + entries.join(', ') + '}';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var entries = [];
  for (var key in this.properties) {
    if (this.properties.hasOwnProperty(key)) {
      entries.push("\\mathbf{" + key + ':} & ' + this.properties[key].toTex(options) + "\\\\");
    }
  }
  return '\\left\\{\\begin{array}{ll}' + entries.join('\n') + '\\end{array}\\right\\}';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ObjectNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var properties = {};
  for (var key in this.properties) {
    if (this.properties.hasOwnProperty(key)) {
      properties[key] = this.properties[key];
    }
  }
  return new ObjectNode(properties);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var key in this.properties) {
    if (this.properties.hasOwnProperty(key)) {
      callback(this.properties[key], 'properties["' + key + '"]', this);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ObjectNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ObjectNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ObjectNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var properties = {};
  for (var key in this.properties) {
    if (this.properties.hasOwnProperty(key)) {
      properties[key] = this._ifNode(callback(this.properties[key], 'properties["' + key + '"]', this));
    }
  }
  return new ObjectNode(properties);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.OperatorNode.prototype"></a>[module mathjs.expression.node.OperatorNode.prototype](#apidoc.module.mathjs.expression.node.OperatorNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  if (!defs.math[this.fn]) {
    throw new Error('Function ' + this.fn + ' missing in provided namespace "math"');
  }

  var jsArgs = this.args.map(function (arg) {
    return arg._compile(defs, args);
  });

  return 'math.' + this.fn + '(' + jsArgs.join(', ') + ')';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var implicit = (options && options.implicit) ? options.implicit : 'hide';
  var args = this.args;
  var parens = calculateNecessaryParentheses(this, parenthesis, args, false);

  if (args.length === 1) { //unary operators
    var assoc = operators.getAssociativity(this, parenthesis);

    var operand = args[0].toString(options);
    if (parens[0]) {
      operand = '(' + operand + ')';
    }

    if (assoc === 'right') { //prefix operator
      return this.op + operand;
    }
    else if (assoc === 'left') { //postfix
      return operand + this.op;
    }

    //fall back to postfix
    return operand + this.op;
  } else if (args.length == 2) {
    var lhs = args[0].toString(options); //left hand side
    var rhs = args[1].toString(options); //right hand side
    if (parens[0]) { //left hand side in parenthesis?
      lhs = '(' + lhs + ')';
    }
    if (parens[1]) { //right hand side in parenthesis?
      rhs = '(' + rhs + ')';
    }

    if (this.implicit && (this.getIdentifier() === 'OperatorNode:multiply') && (implicit == 'hide')) {
      return lhs + ' ' + rhs;
    }

    return lhs + ' ' + this.op + ' ' + rhs;
  } else if ((args.length > 2) && ((this.getIdentifier() === 'OperatorNode:add') || (this.getIdentifier() === 'OperatorNode:multiply
'))) {
    var stringifiedArgs = args.map(function (arg, index) {
      arg = arg.toString(options);
      if (parens[index]) { //put in parenthesis?
        arg = '(' + arg + ')';
      }

      return arg;
    });

    if (this.implicit && (this.getIdentifier() === 'OperatorNode:multiply') && (implicit === 'hide')) {
      return stringifiedArgs.join(' ');
    }

    return stringifiedArgs.join(' ' + this.op + ' ');
  } else {
    //fallback to formatting as a function call
    return this.fn + '(' + this.args.join(', ') + ')';
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var implicit = (options && options.implicit) ? options.implicit : 'hide';
  var args = this.args;
  var parens = calculateNecessaryParentheses(this, parenthesis, args, true);
  var op = latex.operators[this.fn];
  op = typeof op === 'undefined' ? this.op : op; //fall back to using this.op

  if (args.length === 1) { //unary operators
    var assoc = operators.getAssociativity(this, parenthesis);

    var operand = args[0].toTex(options);
    if (parens[0]) {
      operand = '\\left(' + operand + '\\right)';
    }

    if (assoc === 'right') { //prefix operator
      return op + operand;
    }
    else if (assoc === 'left') { //postfix operator
      return operand + op;
    }

    //fall back to postfix
    return operand + op;
  } else if (args.length === 2) { //binary operators
    var lhs = args[0]; //left hand side
    var lhsTex = lhs.toTex(options);
    if (parens[0]) {
      lhsTex = '\\left(' + lhsTex + '\\right)';
    }

    var rhs = args[1]; //right hand side
    var rhsTex = rhs.toTex(options);
    if (parens[1]) {
      rhsTex = '\\left(' + rhsTex + '\\right)';
    }

    //handle some exceptions (due to the way LaTeX works)
    var lhsIdentifier;
    if (parenthesis === 'keep') {
      lhsIdentifier = lhs.getIdentifier();
    }
    else {
      //Ignore ParenthesisNodes if in 'keep' mode
      lhsIdentifier = lhs.getContent().getIdentifier();
    }
    switch (this.getIdentifier()) {
      case 'OperatorNode:divide':
        //op contains '\\frac' at this point
        return op + '{' + lhsTex + '}' + '{' + rhsTex + '}';
      case 'OperatorNode:pow':
        lhsTex = '{' + lhsTex + '}';
        rhsTex = '{' + rhsTex + '}';
        switch (lhsIdentifier) {
          case 'ConditionalNode': //
          case 'OperatorNode:divide':
            lhsTex = '\\left(' + lhsTex + '\\right)';
        }
      case 'OperatorNode:multiply':
        if (this.implicit && (implicit === 'hide')) {
          return lhsTex + '~' + rhsTex;
        }
    }
    return lhsTex + op + rhsTex;
  } else if ((args.length > 2) && ((this.getIdentifier() === 'OperatorNode:add') || (this.getIdentifier() === 'OperatorNode:multiply
'))) {
    var texifiedArgs = args.map(function (arg, index) {
      arg = arg.toTex(options);
      if (parens[index]) {
        arg = '\\left(' + arg + '\\right)';
      }
      return arg;
    });

    if ((this.getIdentifier() === 'OperatorNode:multiply') && this.implicit) {
      return texifiedArgs.join('~');
    }

    return texifiedArgs.join(op)
  } else {
    //fall back to formatting as a function call
    //as this is a fallback, it doesn't use
    //fancy function names
    return '\\mathrm{' + this.fn + '}\\left('
        + args.map(function (arg) {
          return arg.toTex(options);
        }).join(',') + '\\right)';
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new OperatorNode(this.op, this.fn, this.args.slice(0), this.implicit);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var i = 0; i < this.args.length; i++) {
    callback(this.args[i], 'args[' + i + ']', this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype.getIdentifier"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>getIdentifier ()](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.getIdentifier)
- description and source-code
```javascript
getIdentifier = function () {
  return this.type + ':' + this.fn;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.OperatorNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.OperatorNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.OperatorNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var args = [];
  for (var i = 0; i < this.args.length; i++) {
    args[i] = this._ifNode(callback(this.args[i], 'args[' + i + ']', this));
  }
  return new OperatorNode(this.op, this.fn, args);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.ParenthesisNode.prototype"></a>[module mathjs.expression.node.ParenthesisNode.prototype](#apidoc.module.mathjs.expression.node.ParenthesisNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  return this.content._compile(defs, args);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  if ((!options) || (options && !options.parenthesis) || (options && options.parenthesis === 'keep')) {
    return '(' + this.content.toString(options) + ')';
  }
  return this.content.toString(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  if ((!options) || (options && !options.parenthesis) || (options && options.parenthesis === 'keep')) {
    return '\\left(' + this.content.toTex(options) + '\\right)';
  }
  return this.content.toTex(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new ParenthesisNode(this.content);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  callback(this.content, 'content', this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.getContent"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>getContent ()](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.getContent)
- description and source-code
```javascript
getContent = function () {
  return this.content.getContent();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.ParenthesisNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.ParenthesisNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var content = callback(this.content, 'content', this);
  return new ParenthesisNode(content);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.RangeNode.prototype"></a>[module mathjs.expression.node.RangeNode.prototype](#apidoc.module.mathjs.expression.node.RangeNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.RangeNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  return 'math.range(' +
      this.start._compile(defs, args) + ', ' +
      this.end._compile(defs, args) +
      (this.step ? (', ' + this.step._compile(defs, args)) : '') +
      ')';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.RangeNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var parens = calculateNecessaryParentheses(this, parenthesis);

  //format string as start:step:stop
  var str;

  var start = this.start.toString(options);
  if (parens.start) {
    start = '(' + start + ')';
  }
  str = start;

  if (this.step) {
    var step = this.step.toString(options);
    if (parens.step) {
      step = '(' + step + ')';
    }
    str += ':' + step;
  }

  var end = this.end.toString(options);
  if (parens.end) {
    end = '(' + end + ')';
  }
  str += ':' + end;

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.RangeNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var parenthesis = (options && options.parenthesis) ? options.parenthesis : 'keep';
  var parens = calculateNecessaryParentheses(this, parenthesis);

  var str = this.start.toTex(options);
  if (parens.start) {
    str = '\\left(' + str + '\\right)';
  }

  if (this.step) {
    var step = this.step.toTex(options);
    if (parens.step) {
      step = '\\left(' + step + '\\right)';
    }
    str += ':' + step;
  }

  var end = this.end.toTex(options);
  if (parens.end) {
    end = '\\left(' + end + '\\right)';
  }
  str += ':' + end;

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.RangeNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new RangeNode(this.start, this.end, this.step && this.step);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.RangeNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  callback(this.start, 'start', this);
  callback(this.end, 'end', this);
  if (this.step) {
    callback(this.step, 'step', this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.RangeNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  return new RangeNode(
      this._ifNode(callback(this.start, 'start', this)),
      this._ifNode(callback(this.end, 'end', this)),
      this.step && this._ifNode(callback(this.step, 'step', this))
  );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.RangeNode.prototype.needsEnd"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.RangeNode.prototype.</span>needsEnd ()](#apidoc.element.mathjs.expression.node.RangeNode.prototype.needsEnd)
- description and source-code
```javascript
needsEnd = function () {
  // find all 'end' symbols in this RangeNode
  var endSymbols = this.filter(function (node) {
    return (node && node.isSymbolNode) && (node.name == 'end');
  });

  return endSymbols.length > 0;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.node.SymbolNode.prototype"></a>[module mathjs.expression.node.SymbolNode.prototype](#apidoc.module.mathjs.expression.node.SymbolNode.prototype)

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode.prototype._compile"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>_compile (defs, args)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype._compile)
- description and source-code
```javascript
_compile = function (defs, args) {
  // add a function to the definitions
  defs['undef'] = undef;
  defs['Unit'] = Unit;
  defs.getSafeValue = getSafeValue;

  if (args[this.name]) {
    // this is a FunctionAssignment argument
    // (like an x when inside the expression of a function assignment 'f(x) = ...')
    return this.name;
  }
  else if (this.name in defs.math) {
    return 'getSafeValue("' + this.name + '" in scope ? scope["' + this.name + '"] : math["' + this.name + '"])';
  }
  else {
    return 'getSafeValue(' +
        '"' + this.name + '" in scope ? scope["' + this.name + '"] : ' +
        (Unit.isValuelessUnit(this.name) ?
        'new Unit(null, "' + this.name + '")' :
        'undef("' + this.name + '")') +
        ')';
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode.prototype._toString"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>_toString (options)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype._toString)
- description and source-code
```javascript
_toString = function (options) {
  return this.name;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode.prototype._toTex"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>_toTex (options)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype._toTex)
- description and source-code
```javascript
_toTex = function (options) {
  var isUnit = false;
  if ((typeof math[this.name] === 'undefined') && Unit.isValuelessUnit(this.name)) {
    isUnit = true;
  }
  var symbol = latex.toSymbol(this.name, isUnit);
  if (symbol[0] === '\\') {
    //no space needed if the symbol starts with '\'
    return symbol;
  }
  //the space prevents symbols from breaking stuff like '\cdot' if it's written right before the symbol
  return ' ' + symbol;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>clone ()](#apidoc.element.mathjs.expression.node.SymbolNode.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new SymbolNode(this.name);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>forEach (callback)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  // nothing to do, we don't have childs
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.node.SymbolNode.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.node.SymbolNode.prototype.</span>map (callback)](#apidoc.element.mathjs.expression.node.SymbolNode.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  return this.clone();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.parse"></a>[module mathjs.expression.parse](#apidoc.module.mathjs.expression.parse)

#### <a name="apidoc.element.mathjs.expression.parse.parse"></a>[function <span class="apidocSignatureSpan">mathjs.expression.</span>parse (expr, options)](#apidoc.element.mathjs.expression.parse.parse)
- description and source-code
```javascript
function parse(expr, options) {
  if (arguments.length != 1 && arguments.length != 2) {
    throw new ArgumentsError('parse', arguments.length, 1, 2);
  }

  // pass extra nodes
  extra_nodes = (options && options.nodes) ? options.nodes : {};

  if (typeof expr === 'string') {
    // parse a single expression
    expression = expr;
    return parseStart();
  }
  else if (Array.isArray(expr) || expr instanceof type.Matrix) {
    // parse an array or matrix with expressions
    return deepMap(expr, function (elem) {
      if (typeof elem !== 'string') throw new TypeError('String expected');

      expression = elem;
      return parseStart();
    });
  }
  else {
    // oops
    throw new TypeError('String or matrix expected');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isAlpha"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isAlpha (c, cPrev, cNext)](#apidoc.element.mathjs.expression.parse.isAlpha)
- description and source-code
```javascript
function isAlpha(c, cPrev, cNext) {
  return parse.isValidLatinOrGreek(c)
      || parse.isValidMathSymbol(c, cNext)
      || parse.isValidMathSymbol(cPrev, c);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isDecimalMark"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isDecimalMark (c, cNext)](#apidoc.element.mathjs.expression.parse.isDecimalMark)
- description and source-code
```javascript
function isDecimalMark(c, cNext) {
  return c == '.' && cNext !== '/' && cNext !== '*' && cNext !== '^';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isDigit"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isDigit (c)](#apidoc.element.mathjs.expression.parse.isDigit)
- description and source-code
```javascript
function isDigit(c) {
  return (c >= '0' && c <= '9');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isDigitDot"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isDigitDot (c)](#apidoc.element.mathjs.expression.parse.isDigitDot)
- description and source-code
```javascript
function isDigitDot(c) {
  return ((c >= '0' && c <= '9') || c == '.');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isValidLatinOrGreek"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isValidLatinOrGreek (c)](#apidoc.element.mathjs.expression.parse.isValidLatinOrGreek)
- description and source-code
```javascript
function isValidLatinOrGreek(c) {
  return /^[a-zA-Z_\u00C0-\u02AF\u0370-\u03FF\u2100-\u214F]$/.test(c);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isValidMathSymbol"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isValidMathSymbol (high, low)](#apidoc.element.mathjs.expression.parse.isValidMathSymbol)
- description and source-code
```javascript
function isValidMathSymbol(high, low) {
  return /^[\uD835]$/.test(high) &&
      /^[\uDC00-\uDFFF]$/.test(low) &&
      /^[^\uDC55\uDC9D\uDCA0\uDCA1\uDCA3\uDCA4\uDCA7\uDCA8\uDCAD\uDCBA\uDCBC\uDCC4\uDD06\uDD0B\uDD0C\uDD15\uDD1D\uDD3A\uDD3F\uDD45\uDD47-\uDD49\uDD51\uDEA6\uDEA7\uDFCC\uDFCD]$/.test(low);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.parse.isWhitespace"></a>[function <span class="apidocSignatureSpan">mathjs.expression.parse.</span>isWhitespace (c, nestingLevel)](#apidoc.element.mathjs.expression.parse.isWhitespace)
- description and source-code
```javascript
function isWhitespace(c, nestingLevel) {
  // TODO: also take '\r' carriage return as newline? Or does that give problems on mac?
  return c == ' ' || c == '\t' || (c == '\n' && nestingLevel > 0);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.expression.transform"></a>[module mathjs.expression.transform](#apidoc.module.mathjs.expression.transform)

#### <a name="apidoc.element.mathjs.expression.transform.concat"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>concat (arg0)](#apidoc.element.mathjs.expression.transform.concat)
- description and source-code
```javascript
function concat(arg0) {
  "use strict";
  var name = 'concat';
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature0(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.filter"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>filter (args, math, scope)](#apidoc.element.mathjs.expression.transform.filter)
- description and source-code
```javascript
function filterTransform(args, math, scope) {
  var x, test;

  if (args[0]) {
    x = args[0].compile().eval(scope);
  }

  if (args[1]) {
    if (args[1] && args[1].isSymbolNode) {
      // a function pointer, like filter([3, -2, 5], myTestFunction);
      test = args[1].compile().eval(scope);
    }
    else {
      // an equation like filter([3, -2, 5], x > 0)

      // find an undefined symbol
      var _scope = scope || {};
      var symbol = args[1]
          .filter(function (node) {
            return (node && node.isSymbolNode) &&
                !(node.name in math) &&
                !(node.name in _scope);
          })[0];

      // create a test function for this equation
      var sub = Object.create(_scope);
      var eq = args[1].compile();
      if (symbol) {
        var name = symbol.name;
        test = function (x) {
          sub[name] = x;
          return eq.eval(sub);
        }
      }
      else {
        throw new Error('No undefined variable found in filter equation');
      }
    }
  }

  return filter(x, test);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>forEach (arg0, arg1)](#apidoc.element.mathjs.expression.transform.forEach)
- description and source-code
```javascript
function forEach(arg0, arg1) {
  "use strict";
  var name = 'forEach';
  if (test0(arg0)) { // type: Array
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  if (test2(arg0)) { // type: Matrix
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Matrix, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.index"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>index ()](#apidoc.element.mathjs.expression.transform.index)
- description and source-code
```javascript
function indexTransform() {
  var args = [];
  for (var i = 0, ii = arguments.length; i < ii; i++) {
    var arg = arguments[i];

    // change from one-based to zero based, and convert BigNumber to number
    if (arg && arg.isRange === true) {
      arg.start--;
      arg.end -= (arg.step > 0 ? 0 : 2);
    }
    else if (arg && arg.isSet === true) {
      arg = arg.map(function (v) { return v - 1; });
    }
    else if (arg && (arg.isArray === true || arg.isMatrix)) {
      arg = arg.map(function (v) { return v - 1; });
    }
    else if (typeof arg === 'number') {
      arg--;
    }
    else if (arg && arg.isBigNumber === true) {
      arg = arg.toNumber() - 1;
    }
    else if (typeof arg === 'string') {
      // leave as is
    }
    else {
      throw new TypeError('Dimension must be an Array, Matrix, number, string, or Range');
    }

    args[i] = arg;
  }

  var res = new type.Index();
  type.Index.apply(res, args);
  return res;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.map"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>map (arg0, arg1)](#apidoc.element.mathjs.expression.transform.map)
- description and source-code
```javascript
function max(arg0, arg1) {
  "use strict";
  var name = 'max';
  if (test0(arg0)) { // type: Array
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: Array, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  if (test2(arg0)) { // type: Matrix
    if (test1(arg1)) { // type: function
      if (arguments.length === 2) {
        return signature1(arg0, arg1); // signature: Matrix, function
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'function');
  }
  throw createError(name, arguments.length, 0, arguments[0], 'Array,Matrix');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.max"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>max (arg0)](#apidoc.element.mathjs.expression.transform.max)
- description and source-code
```javascript
function max(arg0) {
  "use strict";
  var name = 'max';
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature0(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.mean"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>mean (arg0)](#apidoc.element.mathjs.expression.transform.mean)
- description and source-code
```javascript
function mean(arg0) {
  "use strict";
  var name = 'mean';
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature0(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.min"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>min (arg0)](#apidoc.element.mathjs.expression.transform.min)
- description and source-code
```javascript
function min(arg0) {
  "use strict";
  var name = 'min';
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature0(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.range"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>range (arg0)](#apidoc.element.mathjs.expression.transform.range)
- description and source-code
```javascript
function range(arg0) {
  "use strict";
  var name = 'range';
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature0(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.expression.transform.subset"></a>[function <span class="apidocSignatureSpan">mathjs.expression.transform.</span>subset (arg0)](#apidoc.element.mathjs.expression.transform.subset)
- description and source-code
```javascript
function subset(arg0) {
  "use strict";
  var name = 'subset';
  if (arguments.length > 0) {
    var varArgs = [];
    for (var i = 0; i < arguments.length; i++) {
      varArgs.push(arguments[i]);
    }
    return signature0(varArgs); // signature: ...any
  }
  throw createError(name, arguments.length, 0, arguments[0], 'any');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.json"></a>[module mathjs.json](#apidoc.module.mathjs.json)

#### <a name="apidoc.element.mathjs.json.reviver"></a>[function <span class="apidocSignatureSpan">mathjs.json.</span>reviver (key, value)](#apidoc.element.mathjs.json.reviver)
- description and source-code
```javascript
function reviver(key, value) {
  var constructor = type[value && value.mathjs];
  if (constructor && typeof constructor.fromJSON === 'function') {
    return constructor.fromJSON(value);
  }

  return value;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type"></a>[module mathjs.type](#apidoc.module.mathjs.type)

#### <a name="apidoc.element.mathjs.type.BigNumber"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>BigNumber (v)](#apidoc.element.mathjs.type.BigNumber)
- description and source-code
```javascript
function Decimal(v) {
  var e, i, t,
    x = this;

  // Decimal called without new.
  if (!(x instanceof Decimal)) return new Decimal(v);

  // Retain a reference to this Decimal constructor, and shadow Decimal.prototype.constructor
  // which points to Object.
  x.constructor = Decimal;

  // Duplicate.
  if (v instanceof Decimal) {
    x.s = v.s;
    x.e = v.e;
    x.d = (v = v.d) ? v.slice() : v;
    return;
  }

  t = typeof v;

  if (t === 'number') {
    if (v === 0) {
      x.s = 1 / v < 0 ? -1 : 1;
      x.e = 0;
      x.d = [0];
      return;
    }

    if (v < 0) {
      v = -v;
      x.s = -1;
    } else {
      x.s = 1;
    }

    // Fast path for small integers.
    if (v === ~~v && v < 1e7) {
      for (e = 0, i = v; i >= 10; i /= 10) e++;
      x.e = e;
      x.d = [v];
      return;

    // Infinity, NaN.
    } else if (v * 0 !== 0) {
      if (!v) x.s = NaN;
      x.e = NaN;
      x.d = null;
      return;
    }

    return parseDecimal(x, v.toString());

  } else if (t !== 'string') {
    throw Error(invalidArgument + v);
  }

  // Minus sign?
  if (v.charCodeAt(0) === 45) {
    v = v.slice(1);
    x.s = -1;
  } else {
    x.s = 1;
  }

  return isDecimal.test(v) ? parseDecimal(x, v) : parseOther(x, v);
}
```
- example usage
```shell
...

  math['true']     = true;
  math['false']    = false;
  math['null']     = null;
  math['uninitialized'] = require('./utils/array').UNINITIALIZED;

  if (config.number === 'BigNumber') {
math['Infinity'] = new type.BigNumber(Infinity);
math['NaN']      = new type.BigNumber(NaN);

object.lazy(math, 'pi',  function () {return bigConstants.pi(type.BigNumber)});
object.lazy(math, 'tau', function () {return bigConstants.tau(type.BigNumber)});
object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2
...
```

#### <a name="apidoc.element.mathjs.type.Chain"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Chain (value)](#apidoc.element.mathjs.type.Chain)
- description and source-code
```javascript
function Chain(value) {
  if (!(this instanceof Chain)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (value && value.isChain) {
    this.value = value.value;
  }
  else {
    this.value = value;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Complex (a, b)](#apidoc.element.mathjs.type.Complex)
- description and source-code
```javascript
function Complex(a, b) {

  if (!(this instanceof Complex)) {
    return new Complex(a, b);
  }

  parse(a, b); // mutates P

  this["re"] = P["re"];
  this["im"] = P["im"];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>DenseMatrix (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix)
- description and source-code
```javascript
function DenseMatrix(data, datatype) {
  if (!(this instanceof DenseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if (data && data.isMatrix === true) {
    // check data is a DenseMatrix
    if (data.type === 'DenseMatrix') {
      // clone data & size
      this._data = object.clone(data._data);
      this._size = object.clone(data._size);
      this._datatype = datatype || data._datatype;
    }
    else {
      // build data from existing matrix
      this._data = data.toArray();
      this._size = data.size();
      this._datatype = datatype || data._datatype;
    }
  }
  else if (data && isArray(data.data) && isArray(data.size)) {
    // initialize fields from JSON representation
    this._data = data.data;
    this._size = data.size;
    this._datatype = datatype || data.datatype;
  }
  else if (isArray(data)) {
    // replace nested Matrices with Arrays
    this._data = preprocess(data);
    // get the dimensions of the array
    this._size = array.size(this._data);
    // verify the dimensions of the array, TODO: compute size while processing array
    array.validate(this._data, this._size);
    // data type unknown
    this._datatype = datatype;
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._data = [];
    this._size = [0];
    this._datatype = datatype;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>FibonacciHeap ()](#apidoc.element.mathjs.type.FibonacciHeap)
- description and source-code
```javascript
function FibonacciHeap() {
  if (!(this instanceof FibonacciHeap))
    throw new SyntaxError('Constructor must be called with the new operator');

  // initialize fields
  this._minimum = null;
  this._size = 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Fraction (a, b)](#apidoc.element.mathjs.type.Fraction)
- description and source-code
```javascript
function Fraction(a, b) {

  if (!(this instanceof Fraction)) {
    return new Fraction(a, b);
  }

  parse(a, b);

  if (Fraction['REDUCE']) {
    a = gcd(P["d"], P["n"]); // Abuse a
  } else {
    a = 1;
  }

  this["s"] = P["s"];
  this["n"] = P["n"] / a;
  this["d"] = P["d"] / a;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Help"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Help (doc)](#apidoc.element.mathjs.type.Help)
- description and source-code
```javascript
function Help(doc) {
  if (!(this instanceof Help)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (!doc)  throw new Error('Argument "doc" missing');

  this.doc = doc;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>ImmutableDenseMatrix (data, datatype)](#apidoc.element.mathjs.type.ImmutableDenseMatrix)
- description and source-code
```javascript
function ImmutableDenseMatrix(data, datatype) {
  if (!(this instanceof ImmutableDenseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if ((data && data.isMatrix === true) || isArray(data)) {
    // use DenseMatrix implementation
    var matrix = new DenseMatrix(data, datatype);
    // internal structures
    this._data = matrix._data;
    this._size = matrix._size;
    this._datatype = matrix._datatype;
    this._min = null;
    this._max = null;
  }
  else if (data && isArray(data.data) && isArray(data.size)) {
    // initialize fields from JSON representation
    this._data = data.data;
    this._size = data.size;
    this._datatype = data.datatype;
    this._min = typeof data.min !== 'undefined' ? data.min : null;
    this._max = typeof data.max !== 'undefined' ? data.max : null;
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._data = [];
    this._size = [0];
    this._datatype = datatype;
    this._min = null;
    this._max = null;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Index (ranges)](#apidoc.element.mathjs.type.Index)
- description and source-code
```javascript
function Index(ranges) {
  if (!(this instanceof Index)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this._dimensions = [];
  this._isScalar = true;

  for (var i = 0, ii = arguments.length; i < ii; i++) {
    var arg = arguments[i];

    if (arg && (arg.isRange === true)) {
      this._dimensions.push(arg);
      this._isScalar = false;
    }
    else if (arg && (Array.isArray(arg) || arg.isMatrix === true)) {
      // create matrix
      var m = _createImmutableMatrix(arg.valueOf());
      this._dimensions.push(m);
      // size
      var size = m.size();
      // scalar
      if (size.length !== 1 || size[0] !== 1) {
        this._isScalar = false;
      }
    }
    else if (typeof arg === 'number') {
      this._dimensions.push(_createImmutableMatrix([arg]));
    }
    else if (typeof arg === 'string') {
      // object property (arguments.count should be 1)
      this._dimensions.push(arg);
    }
    // TODO: implement support for wildcard '*'
    else {
      throw new TypeError('Dimension must be an Array, Matrix, number, string, or Range');
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Matrix ()](#apidoc.element.mathjs.type.Matrix)
- description and source-code
```javascript
function Matrix() {
  if (!(this instanceof Matrix)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Range (start, end, step)](#apidoc.element.mathjs.type.Range)
- description and source-code
```javascript
function Range(start, end, step) {
  if (!(this instanceof Range)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (start != null) {
    if (start.isBigNumber === true)
      start = start.toNumber();
    else if (typeof start !== 'number')
      throw new TypeError('Parameter start must be a number');
  }
  if (end != null) {
    if (end.isBigNumber === true)
      end = end.toNumber();
    else if (typeof end !== 'number')
      throw new TypeError('Parameter end must be a number');
  }
  if (step != null) {
    if (step.isBigNumber === true)
      step = step.toNumber();
    else if (typeof step !== 'number')
      throw new TypeError('Parameter step must be a number');
  }

  this.start = (start != null) ? parseFloat(start) : 0;
  this.end   = (end != null)   ? parseFloat(end)   : 0;
  this.step  = (step != null)  ? parseFloat(step)  : 1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ResultSet"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>ResultSet (entries)](#apidoc.element.mathjs.type.ResultSet)
- description and source-code
```javascript
function ResultSet(entries) {
  if (!(this instanceof ResultSet)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.entries = entries || [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Spa"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Spa ()](#apidoc.element.mathjs.type.Spa)
- description and source-code
```javascript
function Spa() {
  if (!(this instanceof Spa))
    throw new SyntaxError('Constructor must be called with the new operator');

  // allocate vector, TODO use typed arrays
  this._values = [];
  this._heap = new type.FibonacciHeap();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>SparseMatrix (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix)
- description and source-code
```javascript
function SparseMatrix(data, datatype) {
  if (!(this instanceof SparseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if (data && data.isMatrix === true) {
    // create from matrix
    _createFromMatrix(this, data, datatype);
  }
  else if (data && isArray(data.index) && isArray(data.ptr) && isArray(data.size)) {
    // initialize fields
    this._values = data.values;
    this._index = data.index;
    this._ptr = data.ptr;
    this._size = data.size;
    this._datatype = datatype || data.datatype;
  }
  else if (isArray(data)) {
    // create from array
    _createFromArray(this, data, datatype);
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._values = [];
    this._index = [];
    this._ptr = [0];
    this._size = [0, 0];
    this._datatype = datatype;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Unit (value, name)](#apidoc.element.mathjs.type.Unit)
- description and source-code
```javascript
function Unit(value, name) {
  if (!(this instanceof Unit)) {
    throw new Error('Constructor must be called with the new operator');
  }

  if (!(value === undefined || isNumeric(value) || value.isComplex)) {
    throw new TypeError('First parameter in Unit constructor must be number, BigNumber, Fraction, Complex, or undefined');
  }
  if (name != undefined && (typeof name !== 'string' || name == '')) {
    throw new TypeError('Second parameter in Unit constructor must be a string');
  }

  if (name != undefined) {
    var u = Unit.parse(name);
    this.units = u.units;
    this.dimensions = u.dimensions;
  }
  else {
    this.units = [
      {
        unit: UNIT_NONE,
        prefix: PREFIXES.NONE,  // link to a list with supported prefixes
        power: 0
      }
    ];
    this.dimensions = [];
    for(var i=0; i<BASE_DIMENSIONS.length; i++) {
      this.dimensions[i] = 0;
    }
  }

  this.value = (value != undefined) ? this._normalize(value) : null;

  this.fixPrefix = false; // if true, function format will not search for the
                          // best prefix but leave it as initially provided.
                          // fixPrefix is set true by the method Unit.to

  // The justification behind this is that if the constructor is explicitly called,
  // the caller wishes the units to be returned exactly as he supplied.
  this.isUnitListSimplified = true;

}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.BigNumber"></a>[module mathjs.type.BigNumber](#apidoc.module.mathjs.type.BigNumber)

#### <a name="apidoc.element.mathjs.type.BigNumber.BigNumber"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>BigNumber (v)](#apidoc.element.mathjs.type.BigNumber.BigNumber)
- description and source-code
```javascript
function Decimal(v) {
  var e, i, t,
    x = this;

  // Decimal called without new.
  if (!(x instanceof Decimal)) return new Decimal(v);

  // Retain a reference to this Decimal constructor, and shadow Decimal.prototype.constructor
  // which points to Object.
  x.constructor = Decimal;

  // Duplicate.
  if (v instanceof Decimal) {
    x.s = v.s;
    x.e = v.e;
    x.d = (v = v.d) ? v.slice() : v;
    return;
  }

  t = typeof v;

  if (t === 'number') {
    if (v === 0) {
      x.s = 1 / v < 0 ? -1 : 1;
      x.e = 0;
      x.d = [0];
      return;
    }

    if (v < 0) {
      v = -v;
      x.s = -1;
    } else {
      x.s = 1;
    }

    // Fast path for small integers.
    if (v === ~~v && v < 1e7) {
      for (e = 0, i = v; i >= 10; i /= 10) e++;
      x.e = e;
      x.d = [v];
      return;

    // Infinity, NaN.
    } else if (v * 0 !== 0) {
      if (!v) x.s = NaN;
      x.e = NaN;
      x.d = null;
      return;
    }

    return parseDecimal(x, v.toString());

  } else if (t !== 'string') {
    throw Error(invalidArgument + v);
  }

  // Minus sign?
  if (v.charCodeAt(0) === 45) {
    v = v.slice(1);
    x.s = -1;
  } else {
    x.s = 1;
  }

  return isDecimal.test(v) ? parseDecimal(x, v) : parseOther(x, v);
}
```
- example usage
```shell
...

  math['true']     = true;
  math['false']    = false;
  math['null']     = null;
  math['uninitialized'] = require('./utils/array').UNINITIALIZED;

  if (config.number === 'BigNumber') {
math['Infinity'] = new type.BigNumber(Infinity);
math['NaN']      = new type.BigNumber(NaN);

object.lazy(math, 'pi',  function () {return bigConstants.pi(type.BigNumber)});
object.lazy(math, 'tau', function () {return bigConstants.tau(type.BigNumber)});
object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.abs"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>abs (x)](#apidoc.element.mathjs.type.BigNumber.abs)
- description and source-code
```javascript
function abs(x) {
  return new this(x).abs();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.acos"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>acos (x)](#apidoc.element.mathjs.type.BigNumber.acos)
- description and source-code
```javascript
function acos(x) {
  return new this(x).acos();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.acosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>acosh (x)](#apidoc.element.mathjs.type.BigNumber.acosh)
- description and source-code
```javascript
function acosh(x) {
  return new this(x).acosh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.add"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>add (x, y)](#apidoc.element.mathjs.type.BigNumber.add)
- description and source-code
```javascript
function add(x, y) {
  return new this(x).plus(y);
}
```
- example usage
```shell
...
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.asin"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>asin (x)](#apidoc.element.mathjs.type.BigNumber.asin)
- description and source-code
```javascript
function asin(x) {
  return new this(x).asin();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.asinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>asinh (x)](#apidoc.element.mathjs.type.BigNumber.asinh)
- description and source-code
```javascript
function asinh(x) {
  return new this(x).asinh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.atan"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>atan (x)](#apidoc.element.mathjs.type.BigNumber.atan)
- description and source-code
```javascript
function atan(x) {
  return new this(x).atan();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.atan2"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>atan2 (y, x)](#apidoc.element.mathjs.type.BigNumber.atan2)
- description and source-code
```javascript
function atan2(y, x) {
  y = new this(y);
  x = new this(x);
  var r,
    pr = this.precision,
    rm = this.rounding,
    wpr = pr + 4;

  // Either NaN
  if (!y.s || !x.s) {
    r = new this(NaN);

  // Both ±Infinity
  } else if (!y.d && !x.d) {
    r = getPi(this, wpr, 1).times(x.s > 0 ? 0.25 : 0.75);
    r.s = y.s;

  // x is ±Infinity or y is ±0
  } else if (!x.d || y.isZero()) {
    r = x.s < 0 ? getPi(this, pr, rm) : new this(0);
    r.s = y.s;

  // y is ±Infinity or x is ±0
  } else if (!y.d || x.isZero()) {
    r = getPi(this, wpr, 1).times(0.5);
    r.s = y.s;

  // Both non-zero and finite
  } else if (x.s < 0) {
    this.precision = wpr;
    this.rounding = 1;
    r = this.atan(divide(y, x, wpr, 1));
    x = getPi(this, wpr, 1);
    this.precision = pr;
    this.rounding = rm;
    r = y.s < 0 ? r.minus(x) : r.plus(x);
  } else {
    r = this.atan(divide(y, x, wpr, 1));
  }

  return r;
}
```
- example usage
```shell
...

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.atanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>atanh (x)](#apidoc.element.mathjs.type.BigNumber.atanh)
- description and source-code
```javascript
function atanh(x) {
  return new this(x).atanh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.cbrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>cbrt (x)](#apidoc.element.mathjs.type.BigNumber.cbrt)
- description and source-code
```javascript
function cbrt(x) {
  return new this(x).cbrt();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.ceil"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ceil (x)](#apidoc.element.mathjs.type.BigNumber.ceil)
- description and source-code
```javascript
function ceil(x) {
  return finalise(x = new this(x), x.e + 1, 2);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>clone (obj)](#apidoc.element.mathjs.type.BigNumber.clone)
- description and source-code
```javascript
function clone(obj) {
  var i, p, ps;

  /*
   * The Decimal constructor and exported function.
   * Return a new Decimal instance.
   *
   * v {number|string|Decimal} A numeric value.
   *
   */
  function Decimal(v) {
    var e, i, t,
      x = this;

    // Decimal called without new.
    if (!(x instanceof Decimal)) return new Decimal(v);

    // Retain a reference to this Decimal constructor, and shadow Decimal.prototype.constructor
    // which points to Object.
    x.constructor = Decimal;

    // Duplicate.
    if (v instanceof Decimal) {
      x.s = v.s;
      x.e = v.e;
      x.d = (v = v.d) ? v.slice() : v;
      return;
    }

    t = typeof v;

    if (t === 'number') {
      if (v === 0) {
        x.s = 1 / v < 0 ? -1 : 1;
        x.e = 0;
        x.d = [0];
        return;
      }

      if (v < 0) {
        v = -v;
        x.s = -1;
      } else {
        x.s = 1;
      }

      // Fast path for small integers.
      if (v === ~~v && v < 1e7) {
        for (e = 0, i = v; i >= 10; i /= 10) e++;
        x.e = e;
        x.d = [v];
        return;

      // Infinity, NaN.
      } else if (v * 0 !== 0) {
        if (!v) x.s = NaN;
        x.e = NaN;
        x.d = null;
        return;
      }

      return parseDecimal(x, v.toString());

    } else if (t !== 'string') {
      throw Error(invalidArgument + v);
    }

    // Minus sign?
    if (v.charCodeAt(0) === 45) {
      v = v.slice(1);
      x.s = -1;
    } else {
      x.s = 1;
    }

    return isDecimal.test(v) ? parseDecimal(x, v) : parseOther(x, v);
  }

  Decimal.prototype = P;

  Decimal.ROUND_UP = 0;
  Decimal.ROUND_DOWN = 1;
  Decimal.ROUND_CEIL = 2;
  Decimal.ROUND_FLOOR = 3;
  Decimal.ROUND_HALF_UP = 4;
  Decimal.ROUND_HALF_DOWN = 5;
  Decimal.ROUND_HALF_EVEN = 6;
  Decimal.ROUND_HALF_CEIL = 7;
  Decimal.ROUND_HALF_FLOOR = 8;
  Decimal.EUCLID = 9;

  Decimal.config = Decimal.set = config;
  Decimal.clone = clone;

  Decimal.abs = abs;
  Decimal.acos = acos;
  Decimal.acosh = acosh;        // ES6
  Decimal.add = add;
  Decimal.asin = asin;
  Decimal.asinh = asinh;        // ES6
  Decimal.atan = atan;
  Decimal.atanh = atanh;        // ES6
  Decimal.atan2 = atan2;
  Decimal.cbrt = cbrt;          // ES6
  Decimal.ceil = ceil;
  Decimal.cos = cos;
  Decimal.cosh = cosh;          // ES6
  Decimal.div = div;
  Decimal.exp = exp;
  Decimal.floor = floor;
  Decimal.hypot = hypot;        // ES6
  Decimal.ln = ln;
  Decimal.log = log;
  Decimal.log10 = log10;        // ES6
  Decimal.log2 = log2;          // ES6
  Decimal.max = max;
  Decimal.min = min;
  Decimal.mod = mod;
  Decimal.mul = mul;
  Decimal.pow = pow;
  Decimal.random = random;
  Decimal.round = round;
  Decimal.sign = sign;          // ES6
  Decimal.sin = sin;
  Decimal.sinh = sinh;          // ES6
  Decimal.sqrt = sqrt;
  Decimal.sub = sub;
  Decimal.tan = tan;
  Decimal.tanh = tanh;          // ES6
  Decimal.trunc = trunc;        // ES6

  if (obj === void 0) obj = {};
  if (obj) {
    ps = ['precision', 'rounding', 'toExpNeg', 'toExpPos', 'maxE', 'minE', 'modulo', 'crypto'];
    for (i = 0; i < ps.length;) if (!obj.hasOwnProperty(p = ps[i++])) obj[p] = this[p];
  }

  Decimal.config(obj);

  return Decimal;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.config"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>config (obj)](#apidoc.element.mathjs.type.BigNumber.config)
- description and source-code
```javascript
function config(obj) {
  if (!obj || typeof obj !== 'object') throw Error(decimalError + 'Object expected');
  var i, p, v,
    ps = [
      'precision', 1, MAX_DIGITS,
      'rounding', 0, 8,
      'toExpNeg', -EXP_LIMIT, 0,
      'toExpPos', 0, EXP_LIMIT,
      'maxE', 0, EXP_LIMIT,
      'minE', -EXP_LIMIT, 0,
      'modulo', 0, 9
    ];

  for (i = 0; i < ps.length; i += 3) {
    if ((v = obj[p = ps[i]]) !== void 0) {
      if (mathfloor(v) === v && v >= ps[i + 1] && v <= ps[i + 2]) this[p] = v;
      else throw Error(invalidArgument + p + ': ' + v);
    }
  }

  if ((v = obj[p = 'crypto']) !== void 0) {
    if (v === true || v === false || v === 0 || v === 1) {
      if (v) {
        if (typeof crypto != 'undefined' && crypto &&
          (crypto.getRandomValues || crypto.randomBytes)) {
          this[p] = true;
        } else {
          throw Error(cryptoUnavailable);
        }
      } else {
        this[p] = false;
      }
    } else {
      throw Error(invalidArgument + p + ': ' + v);
    }
  }

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.cos"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>cos (x)](#apidoc.element.mathjs.type.BigNumber.cos)
- description and source-code
```javascript
function cos(x) {
  return new this(x).cos();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.cosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>cosh (x)](#apidoc.element.mathjs.type.BigNumber.cosh)
- description and source-code
```javascript
function cosh(x) {
  return new this(x).cosh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.div"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>div (x, y)](#apidoc.element.mathjs.type.BigNumber.div)
- description and source-code
```javascript
function div(x, y) {
  return new this(x).div(y);
}
```
- example usage
```shell
...
  object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
  object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

  // uppercase constants (for compatibility with built-in Math)
  object.lazy(math, 'E',       function () {return math.e;});
  object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
  object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
  object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
  object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
  object.lazy(math, 'PI',      function () {return math.pi});
  object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
  object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
}
else {
  math['Infinity'] = Infinity;
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.exp"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>exp (x)](#apidoc.element.mathjs.type.BigNumber.exp)
- description and source-code
```javascript
function exp(x) {
  return new this(x).exp();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.floor"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>floor (x)](#apidoc.element.mathjs.type.BigNumber.floor)
- description and source-code
```javascript
function floor(x) {
  return finalise(x = new this(x), x.e + 1, 3);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>fromJSON (json)](#apidoc.element.mathjs.type.BigNumber.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new BigNumber(json.value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.hypot"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>hypot ()](#apidoc.element.mathjs.type.BigNumber.hypot)
- description and source-code
```javascript
function hypot() {
  var i, n,
    t = new this(0);

  external = false;

  for (i = 0; i < arguments.length;) {
    n = new this(arguments[i++]);
    if (!n.d) {
      if (n.s) {
        external = true;
        return new this(1 / 0);
      }
      t = n;
    } else if (t.d) {
      t = t.plus(n.times(n));
    }
  }

  external = true;

  return t.sqrt();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.ln"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>ln (x)](#apidoc.element.mathjs.type.BigNumber.ln)
- description and source-code
```javascript
function ln(x) {
  return new this(x).ln();
}
```
- example usage
```shell
...
  object.lazy(math, 'pi',  function () {return bigConstants.pi(type.BigNumber)});
  object.lazy(math, 'tau', function () {return bigConstants.tau(type.BigNumber)});
  object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
  object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

  // uppercase constants (for compatibility with built-in Math)
  object.lazy(math, 'E',       function () {return math.e;});
  object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
  object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
  object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
  object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
  object.lazy(math, 'PI',      function () {return math.pi});
  object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
  object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
}
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.log"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>log (x, y)](#apidoc.element.mathjs.type.BigNumber.log)
- description and source-code
```javascript
function log(x, y) {
  return new this(x).log(y);
}
```
- example usage
```shell
...
'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.log10"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>log10 (x)](#apidoc.element.mathjs.type.BigNumber.log10)
- description and source-code
```javascript
function log10(x) {
  return new this(x).log(10);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.log2"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>log2 (x)](#apidoc.element.mathjs.type.BigNumber.log2)
- description and source-code
```javascript
function log2(x) {
  return new this(x).log(2);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.max"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>max ()](#apidoc.element.mathjs.type.BigNumber.max)
- description and source-code
```javascript
function max() {
  return maxOrMin(this, arguments, 'lt');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.min"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>min ()](#apidoc.element.mathjs.type.BigNumber.min)
- description and source-code
```javascript
function min() {
  return maxOrMin(this, arguments, 'gt');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.mod"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>mod (x, y)](#apidoc.element.mathjs.type.BigNumber.mod)
- description and source-code
```javascript
function mod(x, y) {
  return new this(x).mod(y);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.mul"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>mul (x, y)](#apidoc.element.mathjs.type.BigNumber.mul)
- description and source-code
```javascript
function mul(x, y) {
  return new this(x).mul(y);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.pow"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>pow (x, y)](#apidoc.element.mathjs.type.BigNumber.pow)
- description and source-code
```javascript
function pow(x, y) {
  return new this(x).pow(y);
}
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.random"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>random (sd)](#apidoc.element.mathjs.type.BigNumber.random)
- description and source-code
```javascript
function random(sd) {
  var d, e, k, n,
    i = 0,
    r = new this(1),
    rd = [];

  if (sd === void 0) sd = this.precision;
  else checkInt32(sd, 1, MAX_DIGITS);

  k = Math.ceil(sd / LOG_BASE);

  if (!this.crypto) {
    for (; i < k;) rd[i++] = Math.random() * 1e7 | 0;

  // Browsers supporting crypto.getRandomValues.
  } else if (crypto.getRandomValues) {
    d = crypto.getRandomValues(new Uint32Array(k));

    for (; i < k;) {
      n = d[i];

      // 0 <= n < 4294967296
      // Probability n >= 4.29e9, is 4967296 / 4294967296 = 0.00116 (1 in 865).
      if (n >= 4.29e9) {
        d[i] = crypto.getRandomValues(new Uint32Array(1))[0];
      } else {

        // 0 <= n <= 4289999999
        // 0 <= (n % 1e7) <= 9999999
        rd[i++] = n % 1e7;
      }
    }

  // Node.js supporting crypto.randomBytes.
  } else if (crypto.randomBytes) {

    // buffer
    d = crypto.randomBytes(k *= 4);

    for (; i < k;) {

      // 0 <= n < 2147483648
      n = d[i] + (d[i + 1] << 8) + (d[i + 2] << 16) + ((d[i + 3] & 0x7f) << 24);

      // Probability n >= 2.14e9, is 7483648 / 2147483648 = 0.0035 (1 in 286).
      if (n >= 2.14e9) {
        crypto.randomBytes(4).copy(d, i);
      } else {

        // 0 <= n <= 2139999999
        // 0 <= (n % 1e7) <= 9999999
        rd.push(n % 1e7);
        i += 4;
      }
    }

    i = k / 4;
  } else {
    throw Error(cryptoUnavailable);
  }

  k = rd[--i];
  sd %= LOG_BASE;

  // Convert trailing digits to zeros according to sd.
  if (k && sd) {
    n = mathpow(10, LOG_BASE - sd);
    rd[i] = (k / n | 0) * n;
  }

  // Remove trailing words which are zero.
  for (; rd[i] === 0; i--) rd.pop();

  // Zero?
  if (i < 0) {
    e = 0;
    rd = [0];
  } else {
    e = -1;

    // Remove leading words which are zero and adjust exponent accordingly.
    for (; rd[0] === 0; e -= LOG_BASE) rd.shift();

    // Count the digits of the first word of rd to determine leading zeros.
    for (k = 1, n = rd[0]; n >= 10; n /= 10) k++;

    // Adjust the exponent for leading zeros of the first word of rd.
    if (k < LOG_BASE) e -= LOG_BASE - k;
  }

  r.e = e;
  r.d = rd;

  return r;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.round"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>round (x)](#apidoc.element.mathjs.type.BigNumber.round)
- description and source-code
```javascript
function round(x) {
  return finalise(x = new this(x), x.e + 1, this.rounding);
}
```
- example usage
```shell
...
[chained operations](http://mathjs.org/docs/chained_operations.html).

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.set"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>set (obj)](#apidoc.element.mathjs.type.BigNumber.set)
- description and source-code
```javascript
function config(obj) {
  if (!obj || typeof obj !== 'object') throw Error(decimalError + 'Object expected');
  var i, p, v,
    ps = [
      'precision', 1, MAX_DIGITS,
      'rounding', 0, 8,
      'toExpNeg', -EXP_LIMIT, 0,
      'toExpPos', 0, EXP_LIMIT,
      'maxE', 0, EXP_LIMIT,
      'minE', -EXP_LIMIT, 0,
      'modulo', 0, 9
    ];

  for (i = 0; i < ps.length; i += 3) {
    if ((v = obj[p = ps[i]]) !== void 0) {
      if (mathfloor(v) === v && v >= ps[i + 1] && v <= ps[i + 2]) this[p] = v;
      else throw Error(invalidArgument + p + ': ' + v);
    }
  }

  if ((v = obj[p = 'crypto']) !== void 0) {
    if (v === true || v === false || v === 0 || v === 1) {
      if (v) {
        if (typeof crypto != 'undefined' && crypto &&
          (crypto.getRandomValues || crypto.randomBytes)) {
          this[p] = true;
        } else {
          throw Error(cryptoUnavailable);
        }
      } else {
        this[p] = false;
      }
    } else {
      throw Error(invalidArgument + p + ': ' + v);
    }
  }

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.sign"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sign (x)](#apidoc.element.mathjs.type.BigNumber.sign)
- description and source-code
```javascript
function sign(x) {
  x = new this(x);
  return x.d ? (x.d[0] ? x.s : 0 * x.s) : x.s || NaN;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.sin"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sin (x)](#apidoc.element.mathjs.type.BigNumber.sin)
- description and source-code
```javascript
function sin(x) {
  return new this(x).sin();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.sinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sinh (x)](#apidoc.element.mathjs.type.BigNumber.sinh)
- description and source-code
```javascript
function sinh(x) {
  return new this(x).sinh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.sqrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sqrt (x)](#apidoc.element.mathjs.type.BigNumber.sqrt)
- description and source-code
```javascript
function sqrt(x) {
  return new this(x).sqrt();
}
```
- example usage
```shell
...
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.sub"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>sub (x, y)](#apidoc.element.mathjs.type.BigNumber.sub)
- description and source-code
```javascript
function sub(x, y) {
  return new this(x).sub(y);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.tan"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>tan (x)](#apidoc.element.mathjs.type.BigNumber.tan)
- description and source-code
```javascript
function tan(x) {
  return new this(x).tan();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.tanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>tanh (x)](#apidoc.element.mathjs.type.BigNumber.tanh)
- description and source-code
```javascript
function tanh(x) {
  return new this(x).tanh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.trunc"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.</span>trunc (x)](#apidoc.element.mathjs.type.BigNumber.trunc)
- description and source-code
```javascript
function trunc(x) {
  return finalise(x = new this(x), x.e + 1, 1);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.BigNumber.prototype"></a>[module mathjs.type.BigNumber.prototype](#apidoc.module.mathjs.type.BigNumber.prototype)

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.abs"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>abs ()](#apidoc.element.mathjs.type.BigNumber.prototype.abs)
- description and source-code
```javascript
abs = function () {
  var x = new this.constructor(this);
  if (x.s < 0) x.s = 1;
  return finalise(x);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.absoluteValue"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>absoluteValue ()](#apidoc.element.mathjs.type.BigNumber.prototype.absoluteValue)
- description and source-code
```javascript
absoluteValue = function () {
  var x = new this.constructor(this);
  if (x.s < 0) x.s = 1;
  return finalise(x);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.acos"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>acos ()](#apidoc.element.mathjs.type.BigNumber.prototype.acos)
- description and source-code
```javascript
acos = function () {
  var halfPi,
    x = this,
    Ctor = x.constructor,
    k = x.abs().cmp(1),
    pr = Ctor.precision,
    rm = Ctor.rounding;

  if (k !== -1) {
    return k === 0
      // |x| is 1
      ? x.isNeg() ? getPi(Ctor, pr, rm) : new Ctor(0)
      // |x| > 1 or x is NaN
      : new Ctor(NaN);
  }

  if (x.isZero()) return getPi(Ctor, pr + 4, rm).times(0.5);

  // TODO? Special case acos(0.5) = pi/3 and acos(-0.5) = 2*pi/3

  Ctor.precision = pr + 6;
  Ctor.rounding = 1;

  x = x.asin();
  halfPi = getPi(Ctor, pr + 4, rm).times(0.5);

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return halfPi.minus(x);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.acosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>acosh ()](#apidoc.element.mathjs.type.BigNumber.prototype.acosh)
- description and source-code
```javascript
acosh = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (x.lte(1)) return new Ctor(x.eq(1) ? 0 : NaN);
  if (!x.isFinite()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(Math.abs(x.e), x.sd()) + 4;
  Ctor.rounding = 1;
  external = false;

  x = x.times(x).minus(1).sqrt().plus(x);

  external = true;
  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.ln();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.add"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>add (y)](#apidoc.element.mathjs.type.BigNumber.prototype.add)
- description and source-code
```javascript
add = function (y) {
  var carry, d, e, i, k, len, pr, rm, xd, yd,
    x = this,
    Ctor = x.constructor;

  y = new Ctor(y);

  // If either is not finite...
  if (!x.d || !y.d) {

    // Return NaN if either is NaN.
    if (!x.s || !y.s) y = new Ctor(NaN);

    // Return x if y is finite and x is ±Infinity.
    // Return x if both are ±Infinity with the same sign.
    // Return NaN if both are ±Infinity with different signs.
    // Return y if x is finite and y is ±Infinity.
    else if (!x.d) y = new Ctor(y.d || x.s === y.s ? x : NaN);

    return y;
  }

   // If signs differ...
  if (x.s != y.s) {
    y.s = -y.s;
    return x.minus(y);
  }

  xd = x.d;
  yd = y.d;
  pr = Ctor.precision;
  rm = Ctor.rounding;

  // If either is zero...
  if (!xd[0] || !yd[0]) {

    // Return x if y is zero.
    // Return y if y is non-zero.
    if (!yd[0]) y = new Ctor(x);

    return external ? finalise(y, pr, rm) : y;
  }

  // x and y are finite, non-zero numbers with the same sign.

  // Calculate base 1e7 exponents.
  k = mathfloor(x.e / LOG_BASE);
  e = mathfloor(y.e / LOG_BASE);

  xd = xd.slice();
  i = k - e;

  // If base 1e7 exponents differ...
  if (i) {

    if (i < 0) {
      d = xd;
      i = -i;
      len = yd.length;
    } else {
      d = yd;
      e = k;
      len = xd.length;
    }

    // Limit number of zeros prepended to max(ceil(pr / LOG_BASE), len) + 1.
    k = Math.ceil(pr / LOG_BASE);
    len = k > len ? k + 1 : len + 1;

    if (i > len) {
      i = len;
      d.length = 1;
    }

    // Prepend zeros to equalise exponents. Note: Faster to use reverse then do unshifts.
    d.reverse();
    for (; i--;) d.push(0);
    d.reverse();
  }

  len = xd.length;
  i = yd.length;

  // If yd is longer than xd, swap xd and yd so xd points to the longer array.
  if (len - i < 0) {
    i = len;
    d = yd;
    yd = xd;
    xd = d;
  }

  // Only start adding at yd.length - 1 as the further digits of xd can be left as they are.
  for (carry = 0; i;) {
    carry = (xd[--i] = xd[i] + yd[i] + carry) / BASE | 0;
    xd[i] %= BASE;
  }

  if (carry) {
    xd.unshift(carry);
    ++e;
  }

  // Remove trailing zeros.
  // No need to check for zero, as +x + +y != 0 && -x + -y != 0
  for (len = xd.length; xd[--len] == 0;) xd.pop();

  y.d = xd;
  y.e = getBase10Exponent(xd, e);

  return external ? finalise(y, pr, rm) : y;
}
```
- example usage
```shell
...
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.asin"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>asin ()](#apidoc.element.mathjs.type.BigNumber.prototype.asin)
- description and source-code
```javascript
asin = function () {
  var halfPi, k,
    pr, rm,
    x = this,
    Ctor = x.constructor;

  if (x.isZero()) return new Ctor(x);

  k = x.abs().cmp(1);
  pr = Ctor.precision;
  rm = Ctor.rounding;

  if (k !== -1) {

    // |x| is 1
    if (k === 0) {
      halfPi = getPi(Ctor, pr + 4, rm).times(0.5);
      halfPi.s = x.s;
      return halfPi;
    }

    // |x| > 1 or x is NaN
    return new Ctor(NaN);
  }

  // TODO? Special case asin(1/2) = pi/6 and asin(-1/2) = -pi/6

  Ctor.precision = pr + 6;
  Ctor.rounding = 1;

  x = x.div(new Ctor(1).minus(x.times(x)).sqrt().plus(1)).atan();

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.times(2);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.asinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>asinh ()](#apidoc.element.mathjs.type.BigNumber.prototype.asinh)
- description and source-code
```javascript
asinh = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite() || x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + 2 * Math.max(Math.abs(x.e), x.sd()) + 6;
  Ctor.rounding = 1;
  external = false;

  x = x.times(x).plus(1).sqrt().plus(x);

  external = true;
  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.ln();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.atan"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>atan ()](#apidoc.element.mathjs.type.BigNumber.prototype.atan)
- description and source-code
```javascript
atan = function () {
  var i, j, k, n, px, t, r, wpr, x2,
    x = this,
    Ctor = x.constructor,
    pr = Ctor.precision,
    rm = Ctor.rounding;

  if (!x.isFinite()) {
    if (!x.s) return new Ctor(NaN);
    if (pr + 4 <= PI_PRECISION) {
      r = getPi(Ctor, pr + 4, rm).times(0.5);
      r.s = x.s;
      return r;
    }
  } else if (x.isZero()) {
    return new Ctor(x);
  } else if (x.abs().eq(1) && pr + 4 <= PI_PRECISION) {
    r = getPi(Ctor, pr + 4, rm).times(0.25);
    r.s = x.s;
    return r;
  }

  Ctor.precision = wpr = pr + 10;
  Ctor.rounding = 1;

  // TODO? if (x >= 1 && pr <= PI_PRECISION) atan(x) = halfPi * x.s - atan(1 / x);

  // Argument reduction
  // Ensure |x| < 0.42
  // atan(x) = 2 * atan(x / (1 + sqrt(1 + x^2)))

  k = Math.min(28, wpr / LOG_BASE + 2 | 0);

  for (i = k; i; --i) x = x.div(x.times(x).plus(1).sqrt().plus(1));

  external = false;

  j = Math.ceil(wpr / LOG_BASE);
  n = 1;
  x2 = x.times(x);
  r = new Ctor(x);
  px = x;

  // atan(x) = x - x^3/3 + x^5/5 - x^7/7 + ...
  for (; i !== -1;) {
    px = px.times(x2);
    t = r.minus(px.div(n += 2));

    px = px.times(x2);
    r = t.plus(px.div(n += 2));

    if (r.d[j] !== void 0) for (i = j; r.d[i] === t.d[i] && i--;);
  }

  if (k) r = r.times(2 << (k - 1));

  external = true;

  return finalise(r, Ctor.precision = pr, Ctor.rounding = rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.atanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>atanh ()](#apidoc.element.mathjs.type.BigNumber.prototype.atanh)
- description and source-code
```javascript
atanh = function () {
  var pr, rm, wpr, xsd,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(NaN);
  if (x.e >= 0) return new Ctor(x.abs().eq(1) ? x.s / 0 : x.isZero() ? x : NaN);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  xsd = x.sd();

  if (Math.max(xsd, pr) < 2 * -x.e - 1) return finalise(new Ctor(x), pr, rm, true);

  Ctor.precision = wpr = xsd - x.e;

  x = divide(x.plus(1), new Ctor(1).minus(x), wpr + pr, 1);

  Ctor.precision = pr + 4;
  Ctor.rounding = 1;

  x = x.ln();

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.times(0.5);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.cbrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cbrt ()](#apidoc.element.mathjs.type.BigNumber.prototype.cbrt)
- description and source-code
```javascript
cbrt = function () {
  var e, m, n, r, rep, s, sd, t, t3, t3plusx,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite() || x.isZero()) return new Ctor(x);
  external = false;

  // Initial estimate.
  s = x.s * Math.pow(x.s * x, 1 / 3);

   // Math.cbrt underflow/overflow?
   // Pass x to Math.pow as integer, then adjust the exponent of the result.
  if (!s || Math.abs(s) == 1 / 0) {
    n = digitsToString(x.d);
    e = x.e;

    // Adjust n exponent so it is a multiple of 3 away from x exponent.
    if (s = (e - n.length + 1) % 3) n += (s == 1 || s == -2 ? '0' : '00');
    s = Math.pow(n, 1 / 3);

    // Rarely, e may be one less than the result exponent value.
    e = mathfloor((e + 1) / 3) - (e % 3 == (e < 0 ? -1 : 2));

    if (s == 1 / 0) {
      n = '5e' + e;
    } else {
      n = s.toExponential();
      n = n.slice(0, n.indexOf('e') + 1) + e;
    }

    r = new Ctor(n);
    r.s = x.s;
  } else {
    r = new Ctor(s.toString());
  }

  sd = (e = Ctor.precision) + 3;

  // Halley's method.
  // TODO? Compare Newton's method.
  for (;;) {
    t = r;
    t3 = t.times(t).times(t);
    t3plusx = t3.plus(x);
    r = divide(t3plusx.plus(x).times(t), t3plusx.plus(t3), sd + 2, 1);

    // TODO? Replace with for-loop and checkRoundingDigits.
    if (digitsToString(t.d).slice(0, sd) === (n = digitsToString(r.d)).slice(0, sd)) {
      n = n.slice(sd - 3, sd + 1);

      // The 4th rounding digit may be in error by -1 so if the 4 rounding digits are 9999 or 4999
      // , i.e. approaching a rounding boundary, continue the iteration.
      if (n == '9999' || !rep && n == '4999') {

        // On the first iteration only, check to see if rounding up gives the exact result as the
        // nines may infinitely repeat.
        if (!rep) {
          finalise(t, e + 1, 0);

          if (t.times(t).times(t).eq(x)) {
            r = t;
            break;
          }
        }

        sd += 4;
        rep = 1;
      } else {

        // If the rounding digits are null, 0{0,4} or 50{0,3}, check for an exact result.
        // If not, then there are further digits and m will be truthy.
        if (!+n || !+n.slice(1) && n.charAt(0) == '5') {

          // Truncate to the first rounding digit.
          finalise(r, e + 1, 1);
          m = !r.times(r).times(r).eq(x);
        }

        break;
      }
    }
  }

  external = true;

  return finalise(r, e, Ctor.rounding, m);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.ceil"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>ceil ()](#apidoc.element.mathjs.type.BigNumber.prototype.ceil)
- description and source-code
```javascript
ceil = function () {
  return finalise(new this.constructor(this), this.e + 1, 2);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.cmp"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cmp (y)](#apidoc.element.mathjs.type.BigNumber.prototype.cmp)
- description and source-code
```javascript
cmp = function (y) {
  var i, j, xdL, ydL,
    x = this,
    xd = x.d,
    yd = (y = new x.constructor(y)).d,
    xs = x.s,
    ys = y.s;

  // Either NaN or ±Infinity?
  if (!xd || !yd) {
    return !xs || !ys ? NaN : xs !== ys ? xs : xd === yd ? 0 : !xd ^ xs < 0 ? 1 : -1;
  }

  // Either zero?
  if (!xd[0] || !yd[0]) return xd[0] ? xs : yd[0] ? -ys : 0;

  // Signs differ?
  if (xs !== ys) return xs;

  // Compare exponents.
  if (x.e !== y.e) return x.e > y.e ^ xs < 0 ? 1 : -1;

  xdL = xd.length;
  ydL = yd.length;

  // Compare digit by digit.
  for (i = 0, j = xdL < ydL ? xdL : ydL; i < j; ++i) {
    if (xd[i] !== yd[i]) return xd[i] > yd[i] ^ xs < 0 ? 1 : -1;
  }

  // Compare lengths.
  return xdL === ydL ? 0 : xdL > ydL ^ xs < 0 ? 1 : -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.comparedTo"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>comparedTo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.comparedTo)
- description and source-code
```javascript
comparedTo = function (y) {
  var i, j, xdL, ydL,
    x = this,
    xd = x.d,
    yd = (y = new x.constructor(y)).d,
    xs = x.s,
    ys = y.s;

  // Either NaN or ±Infinity?
  if (!xd || !yd) {
    return !xs || !ys ? NaN : xs !== ys ? xs : xd === yd ? 0 : !xd ^ xs < 0 ? 1 : -1;
  }

  // Either zero?
  if (!xd[0] || !yd[0]) return xd[0] ? xs : yd[0] ? -ys : 0;

  // Signs differ?
  if (xs !== ys) return xs;

  // Compare exponents.
  if (x.e !== y.e) return x.e > y.e ^ xs < 0 ? 1 : -1;

  xdL = xd.length;
  ydL = yd.length;

  // Compare digit by digit.
  for (i = 0, j = xdL < ydL ? xdL : ydL; i < j; ++i) {
    if (xd[i] !== yd[i]) return xd[i] > yd[i] ^ xs < 0 ? 1 : -1;
  }

  // Compare lengths.
  return xdL === ydL ? 0 : xdL > ydL ^ xs < 0 ? 1 : -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.cos"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cos ()](#apidoc.element.mathjs.type.BigNumber.prototype.cos)
- description and source-code
```javascript
cos = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.d) return new Ctor(NaN);

  // cos(0) = cos(-0) = 1
  if (!x.d[0]) return new Ctor(1);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + LOG_BASE;
  Ctor.rounding = 1;

  x = cosine(Ctor, toLessThanHalfPi(Ctor, x));

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(quadrant == 2 || quadrant == 3 ? x.neg() : x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.cosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cosh ()](#apidoc.element.mathjs.type.BigNumber.prototype.cosh)
- description and source-code
```javascript
cosh = function () {
  var k, n, pr, rm, len,
    x = this,
    Ctor = x.constructor,
    one = new Ctor(1);

  if (!x.isFinite()) return new Ctor(x.s ? 1 / 0 : NaN);
  if (x.isZero()) return one;

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + 4;
  Ctor.rounding = 1;
  len = x.d.length;

  // Argument reduction: cos(4x) = 1 - 8cos^2(x) + 8cos^4(x) + 1
  // i.e. cos(x) = 1 - cos^2(x/4)(8 - 8cos^2(x/4))

  // Estimate the optimum number of times to use the argument reduction.
  // TODO? Estimation reused from cosine() and may not be optimal here.
  if (len < 32) {
    k = Math.ceil(len / 3);
    n = Math.pow(4, -k).toString();
  } else {
    k = 16;
    n = '2.3283064365386962890625e-10';
  }

  x = taylorSeries(Ctor, 1, x.times(n), new Ctor(1), true);

  // Reverse argument reduction
  var cosh2_x,
    i = k,
    d8 = new Ctor(8);
  for (; i--;) {
    cosh2_x = x.times(x);
    x = one.minus(cosh2_x.times(d8.minus(cosh2_x.times(d8))));
  }

  return finalise(x, Ctor.precision = pr, Ctor.rounding = rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.cosine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.cosine)
- description and source-code
```javascript
cosine = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.d) return new Ctor(NaN);

  // cos(0) = cos(-0) = 1
  if (!x.d[0]) return new Ctor(1);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + LOG_BASE;
  Ctor.rounding = 1;

  x = cosine(Ctor, toLessThanHalfPi(Ctor, x));

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(quadrant == 2 || quadrant == 3 ? x.neg() : x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.cubeRoot"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>cubeRoot ()](#apidoc.element.mathjs.type.BigNumber.prototype.cubeRoot)
- description and source-code
```javascript
cubeRoot = function () {
  var e, m, n, r, rep, s, sd, t, t3, t3plusx,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite() || x.isZero()) return new Ctor(x);
  external = false;

  // Initial estimate.
  s = x.s * Math.pow(x.s * x, 1 / 3);

   // Math.cbrt underflow/overflow?
   // Pass x to Math.pow as integer, then adjust the exponent of the result.
  if (!s || Math.abs(s) == 1 / 0) {
    n = digitsToString(x.d);
    e = x.e;

    // Adjust n exponent so it is a multiple of 3 away from x exponent.
    if (s = (e - n.length + 1) % 3) n += (s == 1 || s == -2 ? '0' : '00');
    s = Math.pow(n, 1 / 3);

    // Rarely, e may be one less than the result exponent value.
    e = mathfloor((e + 1) / 3) - (e % 3 == (e < 0 ? -1 : 2));

    if (s == 1 / 0) {
      n = '5e' + e;
    } else {
      n = s.toExponential();
      n = n.slice(0, n.indexOf('e') + 1) + e;
    }

    r = new Ctor(n);
    r.s = x.s;
  } else {
    r = new Ctor(s.toString());
  }

  sd = (e = Ctor.precision) + 3;

  // Halley's method.
  // TODO? Compare Newton's method.
  for (;;) {
    t = r;
    t3 = t.times(t).times(t);
    t3plusx = t3.plus(x);
    r = divide(t3plusx.plus(x).times(t), t3plusx.plus(t3), sd + 2, 1);

    // TODO? Replace with for-loop and checkRoundingDigits.
    if (digitsToString(t.d).slice(0, sd) === (n = digitsToString(r.d)).slice(0, sd)) {
      n = n.slice(sd - 3, sd + 1);

      // The 4th rounding digit may be in error by -1 so if the 4 rounding digits are 9999 or 4999
      // , i.e. approaching a rounding boundary, continue the iteration.
      if (n == '9999' || !rep && n == '4999') {

        // On the first iteration only, check to see if rounding up gives the exact result as the
        // nines may infinitely repeat.
        if (!rep) {
          finalise(t, e + 1, 0);

          if (t.times(t).times(t).eq(x)) {
            r = t;
            break;
          }
        }

        sd += 4;
        rep = 1;
      } else {

        // If the rounding digits are null, 0{0,4} or 50{0,3}, check for an exact result.
        // If not, then there are further digits and m will be truthy.
        if (!+n || !+n.slice(1) && n.charAt(0) == '5') {

          // Truncate to the first rounding digit.
          finalise(r, e + 1, 1);
          m = !r.times(r).times(r).eq(x);
        }

        break;
      }
    }
  }

  external = true;

  return finalise(r, e, Ctor.rounding, m);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.decimalPlaces"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>decimalPlaces ()](#apidoc.element.mathjs.type.BigNumber.prototype.decimalPlaces)
- description and source-code
```javascript
decimalPlaces = function () {
  var w,
    d = this.d,
    n = NaN;

  if (d) {
    w = d.length - 1;
    n = (w - mathfloor(this.e / LOG_BASE)) * LOG_BASE;

    // Subtract the number of trailing zeros of the last word.
    w = d[w];
    if (w) for (; w % 10 == 0; w /= 10) n--;
    if (n < 0) n = 0;
  }

  return n;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.div"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>div (y)](#apidoc.element.mathjs.type.BigNumber.prototype.div)
- description and source-code
```javascript
div = function (y) {
  return divide(this, new this.constructor(y));
}
```
- example usage
```shell
...
  object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
  object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

  // uppercase constants (for compatibility with built-in Math)
  object.lazy(math, 'E',       function () {return math.e;});
  object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
  object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
  object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
  object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
  object.lazy(math, 'PI',      function () {return math.pi});
  object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
  object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
}
else {
  math['Infinity'] = Infinity;
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.divToInt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>divToInt (y)](#apidoc.element.mathjs.type.BigNumber.prototype.divToInt)
- description and source-code
```javascript
divToInt = function (y) {
  var x = this,
    Ctor = x.constructor;
  return finalise(divide(x, new Ctor(y), 0, 1, 1), Ctor.precision, Ctor.rounding);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.dividedBy"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>dividedBy (y)](#apidoc.element.mathjs.type.BigNumber.prototype.dividedBy)
- description and source-code
```javascript
dividedBy = function (y) {
  return divide(this, new this.constructor(y));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.dividedToIntegerBy"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>dividedToIntegerBy (y)](#apidoc.element.mathjs.type.BigNumber.prototype.dividedToIntegerBy)
- description and source-code
```javascript
dividedToIntegerBy = function (y) {
  var x = this,
    Ctor = x.constructor;
  return finalise(divide(x, new Ctor(y), 0, 1, 1), Ctor.precision, Ctor.rounding);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.dp"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>dp ()](#apidoc.element.mathjs.type.BigNumber.prototype.dp)
- description and source-code
```javascript
dp = function () {
  var w,
    d = this.d,
    n = NaN;

  if (d) {
    w = d.length - 1;
    n = (w - mathfloor(this.e / LOG_BASE)) * LOG_BASE;

    // Subtract the number of trailing zeros of the last word.
    w = d[w];
    if (w) for (; w % 10 == 0; w /= 10) n--;
    if (n < 0) n = 0;
  }

  return n;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.eq"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>eq (y)](#apidoc.element.mathjs.type.BigNumber.prototype.eq)
- description and source-code
```javascript
eq = function (y) {
  return this.cmp(y) === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.equals"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>equals (y)](#apidoc.element.mathjs.type.BigNumber.prototype.equals)
- description and source-code
```javascript
equals = function (y) {
  return this.cmp(y) === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.exp"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>exp ()](#apidoc.element.mathjs.type.BigNumber.prototype.exp)
- description and source-code
```javascript
exp = function () {
  return naturalExponential(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.floor"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>floor ()](#apidoc.element.mathjs.type.BigNumber.prototype.floor)
- description and source-code
```javascript
floor = function () {
  return finalise(new this.constructor(this), this.e + 1, 3);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.greaterThan"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>greaterThan (y)](#apidoc.element.mathjs.type.BigNumber.prototype.greaterThan)
- description and source-code
```javascript
greaterThan = function (y) {
  return this.cmp(y) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.greaterThanOrEqualTo"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>greaterThanOrEqualTo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.greaterThanOrEqualTo)
- description and source-code
```javascript
greaterThanOrEqualTo = function (y) {
  var k = this.cmp(y);
  return k == 1 || k === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.gt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>gt (y)](#apidoc.element.mathjs.type.BigNumber.prototype.gt)
- description and source-code
```javascript
gt = function (y) {
  return this.cmp(y) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.gte"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>gte (y)](#apidoc.element.mathjs.type.BigNumber.prototype.gte)
- description and source-code
```javascript
gte = function (y) {
  var k = this.cmp(y);
  return k == 1 || k === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicCosine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>hyperbolicCosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicCosine)
- description and source-code
```javascript
hyperbolicCosine = function () {
  var k, n, pr, rm, len,
    x = this,
    Ctor = x.constructor,
    one = new Ctor(1);

  if (!x.isFinite()) return new Ctor(x.s ? 1 / 0 : NaN);
  if (x.isZero()) return one;

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + 4;
  Ctor.rounding = 1;
  len = x.d.length;

  // Argument reduction: cos(4x) = 1 - 8cos^2(x) + 8cos^4(x) + 1
  // i.e. cos(x) = 1 - cos^2(x/4)(8 - 8cos^2(x/4))

  // Estimate the optimum number of times to use the argument reduction.
  // TODO? Estimation reused from cosine() and may not be optimal here.
  if (len < 32) {
    k = Math.ceil(len / 3);
    n = Math.pow(4, -k).toString();
  } else {
    k = 16;
    n = '2.3283064365386962890625e-10';
  }

  x = taylorSeries(Ctor, 1, x.times(n), new Ctor(1), true);

  // Reverse argument reduction
  var cosh2_x,
    i = k,
    d8 = new Ctor(8);
  for (; i--;) {
    cosh2_x = x.times(x);
    x = one.minus(cosh2_x.times(d8.minus(cosh2_x.times(d8))));
  }

  return finalise(x, Ctor.precision = pr, Ctor.rounding = rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicSine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>hyperbolicSine ()](#apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicSine)
- description and source-code
```javascript
hyperbolicSine = function () {
  var k, pr, rm, len,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite() || x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + 4;
  Ctor.rounding = 1;
  len = x.d.length;

  if (len < 3) {
    x = taylorSeries(Ctor, 2, x, x, true);
  } else {

    // Alternative argument reduction: sinh(3x) = sinh(x)(3 + 4sinh^2(x))
    // i.e. sinh(x) = sinh(x/3)(3 + 4sinh^2(x/3))
    // 3 multiplications and 1 addition

    // Argument reduction: sinh(5x) = sinh(x)(5 + sinh^2(x)(20 + 16sinh^2(x)))
    // i.e. sinh(x) = sinh(x/5)(5 + sinh^2(x/5)(20 + 16sinh^2(x/5)))
    // 4 multiplications and 2 additions

    // Estimate the optimum number of times to use the argument reduction.
    k = 1.4 * Math.sqrt(len);
    k = k > 16 ? 16 : k | 0;

    x = x.times(Math.pow(5, -k));

    x = taylorSeries(Ctor, 2, x, x, true);

    // Reverse argument reduction
    var sinh2_x,
      d5 = new Ctor(5),
      d16 = new Ctor(16),
      d20 = new Ctor(20);
    for (; k--;) {
      sinh2_x = x.times(x);
      x = x.times(d5.plus(sinh2_x.times(d16.times(sinh2_x).plus(d20))));
    }
  }

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicTangent"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>hyperbolicTangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.hyperbolicTangent)
- description and source-code
```javascript
hyperbolicTangent = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(x.s);
  if (x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + 7;
  Ctor.rounding = 1;

  return divide(x.sinh(), x.cosh(), Ctor.precision = pr, Ctor.rounding = rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.inverseCosine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseCosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseCosine)
- description and source-code
```javascript
inverseCosine = function () {
  var halfPi,
    x = this,
    Ctor = x.constructor,
    k = x.abs().cmp(1),
    pr = Ctor.precision,
    rm = Ctor.rounding;

  if (k !== -1) {
    return k === 0
      // |x| is 1
      ? x.isNeg() ? getPi(Ctor, pr, rm) : new Ctor(0)
      // |x| > 1 or x is NaN
      : new Ctor(NaN);
  }

  if (x.isZero()) return getPi(Ctor, pr + 4, rm).times(0.5);

  // TODO? Special case acos(0.5) = pi/3 and acos(-0.5) = 2*pi/3

  Ctor.precision = pr + 6;
  Ctor.rounding = 1;

  x = x.asin();
  halfPi = getPi(Ctor, pr + 4, rm).times(0.5);

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return halfPi.minus(x);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicCosine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseHyperbolicCosine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicCosine)
- description and source-code
```javascript
inverseHyperbolicCosine = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (x.lte(1)) return new Ctor(x.eq(1) ? 0 : NaN);
  if (!x.isFinite()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(Math.abs(x.e), x.sd()) + 4;
  Ctor.rounding = 1;
  external = false;

  x = x.times(x).minus(1).sqrt().plus(x);

  external = true;
  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.ln();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicSine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseHyperbolicSine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicSine)
- description and source-code
```javascript
inverseHyperbolicSine = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite() || x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + 2 * Math.max(Math.abs(x.e), x.sd()) + 6;
  Ctor.rounding = 1;
  external = false;

  x = x.times(x).plus(1).sqrt().plus(x);

  external = true;
  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.ln();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicTangent"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseHyperbolicTangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseHyperbolicTangent)
- description and source-code
```javascript
inverseHyperbolicTangent = function () {
  var pr, rm, wpr, xsd,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(NaN);
  if (x.e >= 0) return new Ctor(x.abs().eq(1) ? x.s / 0 : x.isZero() ? x : NaN);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  xsd = x.sd();

  if (Math.max(xsd, pr) < 2 * -x.e - 1) return finalise(new Ctor(x), pr, rm, true);

  Ctor.precision = wpr = xsd - x.e;

  x = divide(x.plus(1), new Ctor(1).minus(x), wpr + pr, 1);

  Ctor.precision = pr + 4;
  Ctor.rounding = 1;

  x = x.ln();

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.times(0.5);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.inverseSine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseSine ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseSine)
- description and source-code
```javascript
inverseSine = function () {
  var halfPi, k,
    pr, rm,
    x = this,
    Ctor = x.constructor;

  if (x.isZero()) return new Ctor(x);

  k = x.abs().cmp(1);
  pr = Ctor.precision;
  rm = Ctor.rounding;

  if (k !== -1) {

    // |x| is 1
    if (k === 0) {
      halfPi = getPi(Ctor, pr + 4, rm).times(0.5);
      halfPi.s = x.s;
      return halfPi;
    }

    // |x| > 1 or x is NaN
    return new Ctor(NaN);
  }

  // TODO? Special case asin(1/2) = pi/6 and asin(-1/2) = -pi/6

  Ctor.precision = pr + 6;
  Ctor.rounding = 1;

  x = x.div(new Ctor(1).minus(x.times(x)).sqrt().plus(1)).atan();

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return x.times(2);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.inverseTangent"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>inverseTangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.inverseTangent)
- description and source-code
```javascript
inverseTangent = function () {
  var i, j, k, n, px, t, r, wpr, x2,
    x = this,
    Ctor = x.constructor,
    pr = Ctor.precision,
    rm = Ctor.rounding;

  if (!x.isFinite()) {
    if (!x.s) return new Ctor(NaN);
    if (pr + 4 <= PI_PRECISION) {
      r = getPi(Ctor, pr + 4, rm).times(0.5);
      r.s = x.s;
      return r;
    }
  } else if (x.isZero()) {
    return new Ctor(x);
  } else if (x.abs().eq(1) && pr + 4 <= PI_PRECISION) {
    r = getPi(Ctor, pr + 4, rm).times(0.25);
    r.s = x.s;
    return r;
  }

  Ctor.precision = wpr = pr + 10;
  Ctor.rounding = 1;

  // TODO? if (x >= 1 && pr <= PI_PRECISION) atan(x) = halfPi * x.s - atan(1 / x);

  // Argument reduction
  // Ensure |x| < 0.42
  // atan(x) = 2 * atan(x / (1 + sqrt(1 + x^2)))

  k = Math.min(28, wpr / LOG_BASE + 2 | 0);

  for (i = k; i; --i) x = x.div(x.times(x).plus(1).sqrt().plus(1));

  external = false;

  j = Math.ceil(wpr / LOG_BASE);
  n = 1;
  x2 = x.times(x);
  r = new Ctor(x);
  px = x;

  // atan(x) = x - x^3/3 + x^5/5 - x^7/7 + ...
  for (; i !== -1;) {
    px = px.times(x2);
    t = r.minus(px.div(n += 2));

    px = px.times(x2);
    r = t.plus(px.div(n += 2));

    if (r.d[j] !== void 0) for (i = j; r.d[i] === t.d[i] && i--;);
  }

  if (k) r = r.times(2 << (k - 1));

  external = true;

  return finalise(r, Ctor.precision = pr, Ctor.rounding = rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isFinite"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isFinite ()](#apidoc.element.mathjs.type.BigNumber.prototype.isFinite)
- description and source-code
```javascript
isFinite = function () {
  return !!this.d;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isInt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isInt ()](#apidoc.element.mathjs.type.BigNumber.prototype.isInt)
- description and source-code
```javascript
isInt = function () {
  return !!this.d && mathfloor(this.e / LOG_BASE) > this.d.length - 2;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isInteger"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isInteger ()](#apidoc.element.mathjs.type.BigNumber.prototype.isInteger)
- description and source-code
```javascript
isInteger = function () {
  return !!this.d && mathfloor(this.e / LOG_BASE) > this.d.length - 2;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isNaN"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isNaN ()](#apidoc.element.mathjs.type.BigNumber.prototype.isNaN)
- description and source-code
```javascript
isNaN = function () {
  return !this.s;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isNeg"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isNeg ()](#apidoc.element.mathjs.type.BigNumber.prototype.isNeg)
- description and source-code
```javascript
isNeg = function () {
  return this.s < 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isNegative"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isNegative ()](#apidoc.element.mathjs.type.BigNumber.prototype.isNegative)
- description and source-code
```javascript
isNegative = function () {
  return this.s < 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isPos"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isPos ()](#apidoc.element.mathjs.type.BigNumber.prototype.isPos)
- description and source-code
```javascript
isPos = function () {
  return this.s > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isPositive"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isPositive ()](#apidoc.element.mathjs.type.BigNumber.prototype.isPositive)
- description and source-code
```javascript
isPositive = function () {
  return this.s > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.isZero"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>isZero ()](#apidoc.element.mathjs.type.BigNumber.prototype.isZero)
- description and source-code
```javascript
isZero = function () {
  return !!this.d && this.d[0] === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.lessThan"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lessThan (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lessThan)
- description and source-code
```javascript
lessThan = function (y) {
  return this.cmp(y) < 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.lessThanOrEqualTo"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lessThanOrEqualTo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lessThanOrEqualTo)
- description and source-code
```javascript
lessThanOrEqualTo = function (y) {
  return this.cmp(y) < 1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.ln"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>ln ()](#apidoc.element.mathjs.type.BigNumber.prototype.ln)
- description and source-code
```javascript
ln = function () {
  return naturalLogarithm(this);
}
```
- example usage
```shell
...
  object.lazy(math, 'pi',  function () {return bigConstants.pi(type.BigNumber)});
  object.lazy(math, 'tau', function () {return bigConstants.tau(type.BigNumber)});
  object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
  object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

  // uppercase constants (for compatibility with built-in Math)
  object.lazy(math, 'E',       function () {return math.e;});
  object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
  object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
  object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
  object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
  object.lazy(math, 'PI',      function () {return math.pi});
  object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
  object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
}
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.log"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>log (base)](#apidoc.element.mathjs.type.BigNumber.prototype.log)
- description and source-code
```javascript
log = function (base) {
  var isBase10, d, denominator, k, inf, num, sd, r,
    arg = this,
    Ctor = arg.constructor,
    pr = Ctor.precision,
    rm = Ctor.rounding,
    guard = 5;

  // Default base is 10.
  if (base == null) {
    base = new Ctor(10);
    isBase10 = true;
  } else {
    base = new Ctor(base);
    d = base.d;

    // Return NaN if base is negative, or non-finite, or is 0 or 1.
    if (base.s < 0 || !d || !d[0] || base.eq(1)) return new Ctor(NaN);

    isBase10 = base.eq(10);
  }

  d = arg.d;

  // Is arg negative, non-finite, 0 or 1?
  if (arg.s < 0 || !d || !d[0] || arg.eq(1)) {
    return new Ctor(d && !d[0] ? -1 / 0 : arg.s != 1 ? NaN : d ? 0 : 1 / 0);
  }

  // The result will have a non-terminating decimal expansion if base is 10 and arg is not an
  // integer power of 10.
  if (isBase10) {
    if (d.length > 1) {
      inf = true;
    } else {
      for (k = d[0]; k % 10 === 0;) k /= 10;
      inf = k !== 1;
    }
  }

  external = false;
  sd = pr + guard;
  num = naturalLogarithm(arg, sd);
  denominator = isBase10 ? getLn10(Ctor, sd + 10) : naturalLogarithm(base, sd);

  // The result will have 5 rounding digits.
  r = divide(num, denominator, sd, 1);

  // If at a rounding boundary, i.e. the result's rounding digits are [49]9999 or [50]0000,
  // calculate 10 further digits.
  //
  // If the result is known to have an infinite decimal expansion, repeat this until it is clear
  // that the result is above or below the boundary. Otherwise, if after calculating the 10
  // further digits, the last 14 are nines, round up and assume the result is exact.
  // Also assume the result is exact if the last 14 are zero.
  //
  // Example of a result that will be incorrectly rounded:
  // log[1048576](4503599627370502) = 2.60000000000000009610279511444746...
  // The above result correctly rounded using ROUND_CEIL to 1 decimal place should be 2.7, but it
  // will be given as 2.6 as there are 15 zeros immediately after the requested decimal place, so
  // the exact result would be assumed to be 2.6, which rounded using ROUND_CEIL to 1 decimal
  // place is still 2.6.
  if (checkRoundingDigits(r.d, k = pr, rm)) {

    do {
      sd += 10;
      num = naturalLogarithm(arg, sd);
      denominator = isBase10 ? getLn10(Ctor, sd + 10) : naturalLogarithm(base, sd);
      r = divide(num, denominator, sd, 1);

      if (!inf) {

        // Check for 14 nines from the 2nd rounding digit, as the first may be 4.
        if (+digitsToString(r.d).slice(k + 1, k + 15) + 1 == 1e14) {
          r = finalise(r, pr + 1, 0);
        }

        break;
      }
    } while (checkRoundingDigits(r.d, k += 10, rm));
  }

  external = true;

  return finalise(r, pr, rm);
}
```
- example usage
```shell
...
'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.logarithm"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>logarithm (base)](#apidoc.element.mathjs.type.BigNumber.prototype.logarithm)
- description and source-code
```javascript
logarithm = function (base) {
  var isBase10, d, denominator, k, inf, num, sd, r,
    arg = this,
    Ctor = arg.constructor,
    pr = Ctor.precision,
    rm = Ctor.rounding,
    guard = 5;

  // Default base is 10.
  if (base == null) {
    base = new Ctor(10);
    isBase10 = true;
  } else {
    base = new Ctor(base);
    d = base.d;

    // Return NaN if base is negative, or non-finite, or is 0 or 1.
    if (base.s < 0 || !d || !d[0] || base.eq(1)) return new Ctor(NaN);

    isBase10 = base.eq(10);
  }

  d = arg.d;

  // Is arg negative, non-finite, 0 or 1?
  if (arg.s < 0 || !d || !d[0] || arg.eq(1)) {
    return new Ctor(d && !d[0] ? -1 / 0 : arg.s != 1 ? NaN : d ? 0 : 1 / 0);
  }

  // The result will have a non-terminating decimal expansion if base is 10 and arg is not an
  // integer power of 10.
  if (isBase10) {
    if (d.length > 1) {
      inf = true;
    } else {
      for (k = d[0]; k % 10 === 0;) k /= 10;
      inf = k !== 1;
    }
  }

  external = false;
  sd = pr + guard;
  num = naturalLogarithm(arg, sd);
  denominator = isBase10 ? getLn10(Ctor, sd + 10) : naturalLogarithm(base, sd);

  // The result will have 5 rounding digits.
  r = divide(num, denominator, sd, 1);

  // If at a rounding boundary, i.e. the result's rounding digits are [49]9999 or [50]0000,
  // calculate 10 further digits.
  //
  // If the result is known to have an infinite decimal expansion, repeat this until it is clear
  // that the result is above or below the boundary. Otherwise, if after calculating the 10
  // further digits, the last 14 are nines, round up and assume the result is exact.
  // Also assume the result is exact if the last 14 are zero.
  //
  // Example of a result that will be incorrectly rounded:
  // log[1048576](4503599627370502) = 2.60000000000000009610279511444746...
  // The above result correctly rounded using ROUND_CEIL to 1 decimal place should be 2.7, but it
  // will be given as 2.6 as there are 15 zeros immediately after the requested decimal place, so
  // the exact result would be assumed to be 2.6, which rounded using ROUND_CEIL to 1 decimal
  // place is still 2.6.
  if (checkRoundingDigits(r.d, k = pr, rm)) {

    do {
      sd += 10;
      num = naturalLogarithm(arg, sd);
      denominator = isBase10 ? getLn10(Ctor, sd + 10) : naturalLogarithm(base, sd);
      r = divide(num, denominator, sd, 1);

      if (!inf) {

        // Check for 14 nines from the 2nd rounding digit, as the first may be 4.
        if (+digitsToString(r.d).slice(k + 1, k + 15) + 1 == 1e14) {
          r = finalise(r, pr + 1, 0);
        }

        break;
      }
    } while (checkRoundingDigits(r.d, k += 10, rm));
  }

  external = true;

  return finalise(r, pr, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.lt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lt (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lt)
- description and source-code
```javascript
lt = function (y) {
  return this.cmp(y) < 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.lte"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>lte (y)](#apidoc.element.mathjs.type.BigNumber.prototype.lte)
- description and source-code
```javascript
lte = function (y) {
  return this.cmp(y) < 1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.minus"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>minus (y)](#apidoc.element.mathjs.type.BigNumber.prototype.minus)
- description and source-code
```javascript
minus = function (y) {
  var d, e, i, j, k, len, pr, rm, xd, xe, xLTy, yd,
    x = this,
    Ctor = x.constructor;

  y = new Ctor(y);

  // If either is not finite...
  if (!x.d || !y.d) {

    // Return NaN if either is NaN.
    if (!x.s || !y.s) y = new Ctor(NaN);

    // Return y negated if x is finite and y is ±Infinity.
    else if (x.d) y.s = -y.s;

    // Return x if y is finite and x is ±Infinity.
    // Return x if both are ±Infinity with different signs.
    // Return NaN if both are ±Infinity with the same sign.
    else y = new Ctor(y.d || x.s !== y.s ? x : NaN);

    return y;
  }

  // If signs differ...
  if (x.s != y.s) {
    y.s = -y.s;
    return x.plus(y);
  }

  xd = x.d;
  yd = y.d;
  pr = Ctor.precision;
  rm = Ctor.rounding;

  // If either is zero...
  if (!xd[0] || !yd[0]) {

    // Return y negated if x is zero and y is non-zero.
    if (yd[0]) y.s = -y.s;

    // Return x if y is zero and x is non-zero.
    else if (xd[0]) y = new Ctor(x);

    // Return zero if both are zero.
    // From IEEE 754 (2008) 6.3: 0 - 0 = -0 - -0 = -0 when rounding to -Infinity.
    else return new Ctor(rm === 3 ? -0 : 0);

    return external ? finalise(y, pr, rm) : y;
  }

  // x and y are finite, non-zero numbers with the same sign.

  // Calculate base 1e7 exponents.
  e = mathfloor(y.e / LOG_BASE);
  xe = mathfloor(x.e / LOG_BASE);

  xd = xd.slice();
  k = xe - e;

  // If base 1e7 exponents differ...
  if (k) {
    xLTy = k < 0;

    if (xLTy) {
      d = xd;
      k = -k;
      len = yd.length;
    } else {
      d = yd;
      e = xe;
      len = xd.length;
    }

    // Numbers with massively different exponents would result in a very high number of
    // zeros needing to be prepended, but this can be avoided while still ensuring correct
    // rounding by limiting the number of zeros to 'Math.ceil(pr / LOG_BASE) + 2'.
    i = Math.max(Math.ceil(pr / LOG_BASE), len) + 2;

    if (k > i) {
      k = i;
      d.length = 1;
    }

    // Prepend zeros to equalise exponents.
    d.reverse();
    for (i = k; i--;) d.push(0);
    d.reverse();

  // Base 1e7 exponents equal.
  } else {

    // Check digits to determine which is the bigger number.

    i = xd.length;
    len = yd.length;
    xLTy = i < len;
    if (xLTy) len = i;

    for (i = 0; i < len; i++) {
      if (xd[i] != yd[i]) {
        xLTy = xd[i] < yd[i];
        break;
      }
    }

    k = 0;
  }

  if (xLTy) {
    d = xd;
    xd = yd;
    yd = d;
    y.s = -y.s;
  }

  len = xd.length;

  // Append zeros to 'xd' if shorter.
  // Don't add zeros to 'yd' if shorter as subtraction only needs to start at 'yd' length.
  for (i = yd.length - len; i > 0; --i) xd[len++] = 0;

  // Subtract yd from xd.
  for (i = yd.length; i > k;) {

    if (xd[--i] < yd[i]) {
      for (j = i; j && xd[--j] === 0;) xd[j] = BASE - 1;
      --xd[j];
      xd[i] += BASE;
    }

    xd[i] -= yd[i];
  }

  // Remove trailing zeros.
  for (; xd[--len] === 0;) xd.pop();

  // Remove leading zeros and adjust exponent accordingly.
  for (; xd[0] === 0; xd.shift()) --e;

  // Zero?
  if (!xd[0]) return new Ctor(rm === 3 ? -0 : 0);

  y.d = xd;
  y.e = getBase10Exponent(xd, e);

  return external ? finalise(y, pr, rm) : y;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.mod"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>mod (y)](#apidoc.element.mathjs.type.BigNumber.prototype.mod)
- description and source-code
```javascript
mod = function (y) {
  var q,
    x = this,
    Ctor = x.constructor;

  y = new Ctor(y);

  // Return NaN if x is ±Infinity or NaN, or y is NaN or ±0.
  if (!x.d || !y.s || y.d && !y.d[0]) return new Ctor(NaN);

  // Return x if y is ±Infinity or x is ±0.
  if (!y.d || x.d && !x.d[0]) {
    return finalise(new Ctor(x), Ctor.precision, Ctor.rounding);
  }

  // Prevent rounding of intermediate calculations.
  external = false;

  if (Ctor.modulo == 9) {

    // Euclidian division: q = sign(y) * floor(x / abs(y))
    // result = x - q * y    where  0 <= result < abs(y)
    q = divide(x, y.abs(), 0, 3, 1);
    q.s *= y.s;
  } else {
    q = divide(x, y, 0, Ctor.modulo, 1);
  }

  q = q.times(y);

  external = true;

  return x.minus(q);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.modulo"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>modulo (y)](#apidoc.element.mathjs.type.BigNumber.prototype.modulo)
- description and source-code
```javascript
modulo = function (y) {
  var q,
    x = this,
    Ctor = x.constructor;

  y = new Ctor(y);

  // Return NaN if x is ±Infinity or NaN, or y is NaN or ±0.
  if (!x.d || !y.s || y.d && !y.d[0]) return new Ctor(NaN);

  // Return x if y is ±Infinity or x is ±0.
  if (!y.d || x.d && !x.d[0]) {
    return finalise(new Ctor(x), Ctor.precision, Ctor.rounding);
  }

  // Prevent rounding of intermediate calculations.
  external = false;

  if (Ctor.modulo == 9) {

    // Euclidian division: q = sign(y) * floor(x / abs(y))
    // result = x - q * y    where  0 <= result < abs(y)
    q = divide(x, y.abs(), 0, 3, 1);
    q.s *= y.s;
  } else {
    q = divide(x, y, 0, Ctor.modulo, 1);
  }

  q = q.times(y);

  external = true;

  return x.minus(q);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.mul"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>mul (y)](#apidoc.element.mathjs.type.BigNumber.prototype.mul)
- description and source-code
```javascript
mul = function (y) {
  var carry, e, i, k, r, rL, t, xdL, ydL,
    x = this,
    Ctor = x.constructor,
    xd = x.d,
    yd = (y = new Ctor(y)).d;

  y.s *= x.s;

   // If either is NaN, ±Infinity or ±0...
  if (!xd || !xd[0] || !yd || !yd[0]) {

    return new Ctor(!y.s || xd && !xd[0] && !yd || yd && !yd[0] && !xd

      // Return NaN if either is NaN.
      // Return NaN if x is ±0 and y is ±Infinity, or y is ±0 and x is ±Infinity.
      ? NaN

      // Return ±Infinity if either is ±Infinity.
      // Return ±0 if either is ±0.
      : !xd || !yd ? y.s / 0 : y.s * 0);
  }

  e = mathfloor(x.e / LOG_BASE) + mathfloor(y.e / LOG_BASE);
  xdL = xd.length;
  ydL = yd.length;

  // Ensure xd points to the longer array.
  if (xdL < ydL) {
    r = xd;
    xd = yd;
    yd = r;
    rL = xdL;
    xdL = ydL;
    ydL = rL;
  }

  // Initialise the result array with zeros.
  r = [];
  rL = xdL + ydL;
  for (i = rL; i--;) r.push(0);

  // Multiply!
  for (i = ydL; --i >= 0;) {
    carry = 0;
    for (k = xdL + i; k > i;) {
      t = r[k] + yd[i] * xd[k - i - 1] + carry;
      r[k--] = t % BASE | 0;
      carry = t / BASE | 0;
    }

    r[k] = (r[k] + carry) % BASE | 0;
  }

  // Remove trailing zeros.
  for (; !r[--rL];) r.pop();

  if (carry) ++e;
  else r.shift();

  y.d = r;
  y.e = getBase10Exponent(r, e);

  return external ? finalise(y, Ctor.precision, Ctor.rounding) : y;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.naturalExponential"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>naturalExponential ()](#apidoc.element.mathjs.type.BigNumber.prototype.naturalExponential)
- description and source-code
```javascript
naturalExponential = function () {
  return naturalExponential(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.naturalLogarithm"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>naturalLogarithm ()](#apidoc.element.mathjs.type.BigNumber.prototype.naturalLogarithm)
- description and source-code
```javascript
naturalLogarithm = function () {
  return naturalLogarithm(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.neg"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>neg ()](#apidoc.element.mathjs.type.BigNumber.prototype.neg)
- description and source-code
```javascript
neg = function () {
  var x = new this.constructor(this);
  x.s = -x.s;
  return finalise(x);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.negated"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>negated ()](#apidoc.element.mathjs.type.BigNumber.prototype.negated)
- description and source-code
```javascript
negated = function () {
  var x = new this.constructor(this);
  x.s = -x.s;
  return finalise(x);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.plus"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>plus (y)](#apidoc.element.mathjs.type.BigNumber.prototype.plus)
- description and source-code
```javascript
plus = function (y) {
  var carry, d, e, i, k, len, pr, rm, xd, yd,
    x = this,
    Ctor = x.constructor;

  y = new Ctor(y);

  // If either is not finite...
  if (!x.d || !y.d) {

    // Return NaN if either is NaN.
    if (!x.s || !y.s) y = new Ctor(NaN);

    // Return x if y is finite and x is ±Infinity.
    // Return x if both are ±Infinity with the same sign.
    // Return NaN if both are ±Infinity with different signs.
    // Return y if x is finite and y is ±Infinity.
    else if (!x.d) y = new Ctor(y.d || x.s === y.s ? x : NaN);

    return y;
  }

   // If signs differ...
  if (x.s != y.s) {
    y.s = -y.s;
    return x.minus(y);
  }

  xd = x.d;
  yd = y.d;
  pr = Ctor.precision;
  rm = Ctor.rounding;

  // If either is zero...
  if (!xd[0] || !yd[0]) {

    // Return x if y is zero.
    // Return y if y is non-zero.
    if (!yd[0]) y = new Ctor(x);

    return external ? finalise(y, pr, rm) : y;
  }

  // x and y are finite, non-zero numbers with the same sign.

  // Calculate base 1e7 exponents.
  k = mathfloor(x.e / LOG_BASE);
  e = mathfloor(y.e / LOG_BASE);

  xd = xd.slice();
  i = k - e;

  // If base 1e7 exponents differ...
  if (i) {

    if (i < 0) {
      d = xd;
      i = -i;
      len = yd.length;
    } else {
      d = yd;
      e = k;
      len = xd.length;
    }

    // Limit number of zeros prepended to max(ceil(pr / LOG_BASE), len) + 1.
    k = Math.ceil(pr / LOG_BASE);
    len = k > len ? k + 1 : len + 1;

    if (i > len) {
      i = len;
      d.length = 1;
    }

    // Prepend zeros to equalise exponents. Note: Faster to use reverse then do unshifts.
    d.reverse();
    for (; i--;) d.push(0);
    d.reverse();
  }

  len = xd.length;
  i = yd.length;

  // If yd is longer than xd, swap xd and yd so xd points to the longer array.
  if (len - i < 0) {
    i = len;
    d = yd;
    yd = xd;
    xd = d;
  }

  // Only start adding at yd.length - 1 as the further digits of xd can be left as they are.
  for (carry = 0; i;) {
    carry = (xd[--i] = xd[i] + yd[i] + carry) / BASE | 0;
    xd[i] %= BASE;
  }

  if (carry) {
    xd.unshift(carry);
    ++e;
  }

  // Remove trailing zeros.
  // No need to check for zero, as +x + +y != 0 && -x + -y != 0
  for (len = xd.length; xd[--len] == 0;) xd.pop();

  y.d = xd;
  y.e = getBase10Exponent(xd, e);

  return external ? finalise(y, pr, rm) : y;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.pow"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>pow (y)](#apidoc.element.mathjs.type.BigNumber.prototype.pow)
- description and source-code
```javascript
pow = function (y) {
  var e, k, pr, r, rm, sign, yIsInt,
    x = this,
    Ctor = x.constructor,
    yn = +(y = new Ctor(y));

  // Either ±Infinity, NaN or ±0?
  if (!x.d || !y.d || !x.d[0] || !y.d[0]) return  new Ctor(mathpow(+x, yn));

  x = new Ctor(x);

  if (x.eq(1)) return x;

  pr = Ctor.precision;
  rm = Ctor.rounding;

  if (y.eq(1)) return finalise(x, pr, rm);

  e = mathfloor(y.e / LOG_BASE);
  k = y.d.length - 1;
  yIsInt = e >= k;
  sign = x.s;

  if (!yIsInt) {
    if (sign < 0) return new Ctor(NaN);

  // If y is a small integer use the 'exponentiation by squaring' algorithm.
  } else if ((k = yn < 0 ? -yn : yn) <= MAX_SAFE_INTEGER) {
    r = intPow(Ctor, x, k, pr);
    return y.s < 0 ? new Ctor(1).div(r) : finalise(r, pr, rm);
  }

  // Result is negative if x is negative and the last digit of integer y is odd.
  sign = sign < 0 && y.d[Math.max(e, k)] & 1 ? -1 : 1;

  // Estimate result exponent.
  // x^y = 10^e,  where e = y * log10(x)
  // log10(x) = log10(x_significand) + x_exponent
  // log10(x_significand) = ln(x_significand) / ln(10)
  k = mathpow(+x, yn);
  e = k == 0 || !isFinite(k)
    ? mathfloor(yn * (Math.log('0.' + digitsToString(x.d)) / Math.LN10 + x.e + 1))
    : new Ctor(k + '').e;

  // Estimate may be incorrect e.g. x: 0.999999999999999999, y: 2.29, e: 0, r.e: -1.

  // Overflow/underflow?
  if (e > Ctor.maxE + 1 || e < Ctor.minE - 1) return new Ctor(e > 0 ? sign / 0 : 0);

  external = false;
  Ctor.rounding = x.s = 1;

  // Estimate the extra guard digits needed to ensure five correct rounding digits from
  // naturalLogarithm(x). Example of failure without these extra digits (precision: 10):
  // new Decimal(2.32456).pow('2087987436534566.46411')
  // should be 1.162377823e+764914905173815, but is 1.162355823e+764914905173815
  k = Math.min(12, (e + '').length);

  // r = x^y = exp(y*ln(x))
  r = naturalExponential(y.times(naturalLogarithm(x, pr + k)), pr);

  // Truncate to the required precision plus five rounding digits.
  r = finalise(r, pr + 5, 1);

  // If the rounding digits are [49]9999 or [50]0000 increase the precision by 10 and recalculate
  // the result.
  if (checkRoundingDigits(r.d, pr, rm)) {
    e = pr + 10;

    // Truncate to the increased precision plus five rounding digits.
    r = finalise(naturalExponential(y.times(naturalLogarithm(x, e + k)), e), e + 5, 1);

    // Check for 14 nines from the 2nd rounding digit (the first rounding digit may be 4 or 9).
    if (+digitsToString(r.d).slice(pr + 1, pr + 15) + 1 == 1e14) {
      r = finalise(r, pr + 1, 0);
    }
  }

  r.s = sign;
  external = true;
  Ctor.rounding = rm;

  return finalise(r, pr, rm);
}
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.precision"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>precision (z)](#apidoc.element.mathjs.type.BigNumber.prototype.precision)
- description and source-code
```javascript
precision = function (z) {
  var k,
    x = this;

  if (z !== void 0 && z !== !!z && z !== 1 && z !== 0) throw Error(invalidArgument + z);

  if (x.d) {
    k = getPrecision(x.d);
    if (z && x.e + 1 > k) k = x.e + 1;
  } else {
    k = NaN;
  }

  return k;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.round"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>round ()](#apidoc.element.mathjs.type.BigNumber.prototype.round)
- description and source-code
```javascript
round = function () {
  var x = this,
    Ctor = x.constructor;

  return finalise(new Ctor(x), x.e + 1, Ctor.rounding);
}
```
- example usage
```shell
...
[chained operations](http://mathjs.org/docs/chained_operations.html).

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.sd"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sd (z)](#apidoc.element.mathjs.type.BigNumber.prototype.sd)
- description and source-code
```javascript
sd = function (z) {
  var k,
    x = this;

  if (z !== void 0 && z !== !!z && z !== 1 && z !== 0) throw Error(invalidArgument + z);

  if (x.d) {
    k = getPrecision(x.d);
    if (z && x.e + 1 > k) k = x.e + 1;
  } else {
    k = NaN;
  }

  return k;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.sin"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sin ()](#apidoc.element.mathjs.type.BigNumber.prototype.sin)
- description and source-code
```javascript
sin = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(NaN);
  if (x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + LOG_BASE;
  Ctor.rounding = 1;

  x = sine(Ctor, toLessThanHalfPi(Ctor, x));

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(quadrant > 2 ? x.neg() : x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.sine"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sine ()](#apidoc.element.mathjs.type.BigNumber.prototype.sine)
- description and source-code
```javascript
sine = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(NaN);
  if (x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + LOG_BASE;
  Ctor.rounding = 1;

  x = sine(Ctor, toLessThanHalfPi(Ctor, x));

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(quadrant > 2 ? x.neg() : x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.sinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sinh ()](#apidoc.element.mathjs.type.BigNumber.prototype.sinh)
- description and source-code
```javascript
sinh = function () {
  var k, pr, rm, len,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite() || x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + Math.max(x.e, x.sd()) + 4;
  Ctor.rounding = 1;
  len = x.d.length;

  if (len < 3) {
    x = taylorSeries(Ctor, 2, x, x, true);
  } else {

    // Alternative argument reduction: sinh(3x) = sinh(x)(3 + 4sinh^2(x))
    // i.e. sinh(x) = sinh(x/3)(3 + 4sinh^2(x/3))
    // 3 multiplications and 1 addition

    // Argument reduction: sinh(5x) = sinh(x)(5 + sinh^2(x)(20 + 16sinh^2(x)))
    // i.e. sinh(x) = sinh(x/5)(5 + sinh^2(x/5)(20 + 16sinh^2(x/5)))
    // 4 multiplications and 2 additions

    // Estimate the optimum number of times to use the argument reduction.
    k = 1.4 * Math.sqrt(len);
    k = k > 16 ? 16 : k | 0;

    x = x.times(Math.pow(5, -k));

    x = taylorSeries(Ctor, 2, x, x, true);

    // Reverse argument reduction
    var sinh2_x,
      d5 = new Ctor(5),
      d16 = new Ctor(16),
      d20 = new Ctor(20);
    for (; k--;) {
      sinh2_x = x.times(x);
      x = x.times(d5.plus(sinh2_x.times(d16.times(sinh2_x).plus(d20))));
    }
  }

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.sqrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sqrt ()](#apidoc.element.mathjs.type.BigNumber.prototype.sqrt)
- description and source-code
```javascript
sqrt = function () {
  var m, n, sd, r, rep, t,
    x = this,
    d = x.d,
    e = x.e,
    s = x.s,
    Ctor = x.constructor;

  // Negative/NaN/Infinity/zero?
  if (s !== 1 || !d || !d[0]) {
    return new Ctor(!s || s < 0 && (!d || d[0]) ? NaN : d ? x : 1 / 0);
  }

  external = false;

  // Initial estimate.
  s = Math.sqrt(+x);

  // Math.sqrt underflow/overflow?
  // Pass x to Math.sqrt as integer, then adjust the exponent of the result.
  if (s == 0 || s == 1 / 0) {
    n = digitsToString(d);

    if ((n.length + e) % 2 == 0) n += '0';
    s = Math.sqrt(n);
    e = mathfloor((e + 1) / 2) - (e < 0 || e % 2);

    if (s == 1 / 0) {
      n = '1e' + e;
    } else {
      n = s.toExponential();
      n = n.slice(0, n.indexOf('e') + 1) + e;
    }

    r = new Ctor(n);
  } else {
    r = new Ctor(s.toString());
  }

  sd = (e = Ctor.precision) + 3;

  // Newton-Raphson iteration.
  for (;;) {
    t = r;
    r = t.plus(divide(x, t, sd + 2, 1)).times(0.5);

    // TODO? Replace with for-loop and checkRoundingDigits.
    if (digitsToString(t.d).slice(0, sd) === (n = digitsToString(r.d)).slice(0, sd)) {
      n = n.slice(sd - 3, sd + 1);

      // The 4th rounding digit may be in error by -1 so if the 4 rounding digits are 9999 or
      // 4999, i.e. approaching a rounding boundary, continue the iteration.
      if (n == '9999' || !rep && n == '4999') {

        // On the first iteration only, check to see if rounding up gives the exact result as the
        // nines may infinitely repeat.
        if (!rep) {
          finalise(t, e + 1, 0);

          if (t.times(t).eq(x)) {
            r = t;
            break;
          }
        }

        sd += 4;
        rep = 1;
      } else {

        // If the rounding digits are null, 0{0,4} or 50{0,3}, check for an exact result.
        // If not, then there are further digits and m will be truthy.
        if (!+n || !+n.slice(1) && n.charAt(0) == '5') {

          // Truncate to the first rounding digit.
          finalise(r, e + 1, 1);
          m = !r.times(r).eq(x);
        }

        break;
      }
    }
  }

  external = true;

  return finalise(r, e, Ctor.rounding, m);
}
```
- example usage
```shell
...
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
...
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.squareRoot"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>squareRoot ()](#apidoc.element.mathjs.type.BigNumber.prototype.squareRoot)
- description and source-code
```javascript
squareRoot = function () {
  var m, n, sd, r, rep, t,
    x = this,
    d = x.d,
    e = x.e,
    s = x.s,
    Ctor = x.constructor;

  // Negative/NaN/Infinity/zero?
  if (s !== 1 || !d || !d[0]) {
    return new Ctor(!s || s < 0 && (!d || d[0]) ? NaN : d ? x : 1 / 0);
  }

  external = false;

  // Initial estimate.
  s = Math.sqrt(+x);

  // Math.sqrt underflow/overflow?
  // Pass x to Math.sqrt as integer, then adjust the exponent of the result.
  if (s == 0 || s == 1 / 0) {
    n = digitsToString(d);

    if ((n.length + e) % 2 == 0) n += '0';
    s = Math.sqrt(n);
    e = mathfloor((e + 1) / 2) - (e < 0 || e % 2);

    if (s == 1 / 0) {
      n = '1e' + e;
    } else {
      n = s.toExponential();
      n = n.slice(0, n.indexOf('e') + 1) + e;
    }

    r = new Ctor(n);
  } else {
    r = new Ctor(s.toString());
  }

  sd = (e = Ctor.precision) + 3;

  // Newton-Raphson iteration.
  for (;;) {
    t = r;
    r = t.plus(divide(x, t, sd + 2, 1)).times(0.5);

    // TODO? Replace with for-loop and checkRoundingDigits.
    if (digitsToString(t.d).slice(0, sd) === (n = digitsToString(r.d)).slice(0, sd)) {
      n = n.slice(sd - 3, sd + 1);

      // The 4th rounding digit may be in error by -1 so if the 4 rounding digits are 9999 or
      // 4999, i.e. approaching a rounding boundary, continue the iteration.
      if (n == '9999' || !rep && n == '4999') {

        // On the first iteration only, check to see if rounding up gives the exact result as the
        // nines may infinitely repeat.
        if (!rep) {
          finalise(t, e + 1, 0);

          if (t.times(t).eq(x)) {
            r = t;
            break;
          }
        }

        sd += 4;
        rep = 1;
      } else {

        // If the rounding digits are null, 0{0,4} or 50{0,3}, check for an exact result.
        // If not, then there are further digits and m will be truthy.
        if (!+n || !+n.slice(1) && n.charAt(0) == '5') {

          // Truncate to the first rounding digit.
          finalise(r, e + 1, 1);
          m = !r.times(r).eq(x);
        }

        break;
      }
    }
  }

  external = true;

  return finalise(r, e, Ctor.rounding, m);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.sub"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>sub (y)](#apidoc.element.mathjs.type.BigNumber.prototype.sub)
- description and source-code
```javascript
sub = function (y) {
  var d, e, i, j, k, len, pr, rm, xd, xe, xLTy, yd,
    x = this,
    Ctor = x.constructor;

  y = new Ctor(y);

  // If either is not finite...
  if (!x.d || !y.d) {

    // Return NaN if either is NaN.
    if (!x.s || !y.s) y = new Ctor(NaN);

    // Return y negated if x is finite and y is ±Infinity.
    else if (x.d) y.s = -y.s;

    // Return x if y is finite and x is ±Infinity.
    // Return x if both are ±Infinity with different signs.
    // Return NaN if both are ±Infinity with the same sign.
    else y = new Ctor(y.d || x.s !== y.s ? x : NaN);

    return y;
  }

  // If signs differ...
  if (x.s != y.s) {
    y.s = -y.s;
    return x.plus(y);
  }

  xd = x.d;
  yd = y.d;
  pr = Ctor.precision;
  rm = Ctor.rounding;

  // If either is zero...
  if (!xd[0] || !yd[0]) {

    // Return y negated if x is zero and y is non-zero.
    if (yd[0]) y.s = -y.s;

    // Return x if y is zero and x is non-zero.
    else if (xd[0]) y = new Ctor(x);

    // Return zero if both are zero.
    // From IEEE 754 (2008) 6.3: 0 - 0 = -0 - -0 = -0 when rounding to -Infinity.
    else return new Ctor(rm === 3 ? -0 : 0);

    return external ? finalise(y, pr, rm) : y;
  }

  // x and y are finite, non-zero numbers with the same sign.

  // Calculate base 1e7 exponents.
  e = mathfloor(y.e / LOG_BASE);
  xe = mathfloor(x.e / LOG_BASE);

  xd = xd.slice();
  k = xe - e;

  // If base 1e7 exponents differ...
  if (k) {
    xLTy = k < 0;

    if (xLTy) {
      d = xd;
      k = -k;
      len = yd.length;
    } else {
      d = yd;
      e = xe;
      len = xd.length;
    }

    // Numbers with massively different exponents would result in a very high number of
    // zeros needing to be prepended, but this can be avoided while still ensuring correct
    // rounding by limiting the number of zeros to 'Math.ceil(pr / LOG_BASE) + 2'.
    i = Math.max(Math.ceil(pr / LOG_BASE), len) + 2;

    if (k > i) {
      k = i;
      d.length = 1;
    }

    // Prepend zeros to equalise exponents.
    d.reverse();
    for (i = k; i--;) d.push(0);
    d.reverse();

  // Base 1e7 exponents equal.
  } else {

    // Check digits to determine which is the bigger number.

    i = xd.length;
    len = yd.length;
    xLTy = i < len;
    if (xLTy) len = i;

    for (i = 0; i < len; i++) {
      if (xd[i] != yd[i]) {
        xLTy = xd[i] < yd[i];
        break;
      }
    }

    k = 0;
  }

  if (xLTy) {
    d = xd;
    xd = yd;
    yd = d;
    y.s = -y.s;
  }

  len = xd.length;

  // Append zeros to 'xd' if shorter.
  // Don't add zeros to 'yd' if shorter as subtraction only needs to start at 'yd' length.
  for (i = yd.length - len; i > 0; --i) xd[len++] = 0;

  // Subtract yd from xd.
  for (i = yd.length; i > k;) {

    if (xd[--i] < yd[i]) {
      for (j = i; j && xd[--j] === 0;) xd[j] = BASE - 1;
      --xd[j];
      xd[i] += BASE;
    }

    xd[i] -= yd[i];
  }

  // Remove trailing zeros.
  for (; xd[--len] === 0;) xd.pop();

  // Remove leading zeros and adjust exponent accordingly.
  for (; xd[0] === 0; xd.shift()) --e;

  // Zero?
  if (!xd[0]) return new Ctor(rm === 3 ? -0 : 0);

  y.d = xd;
  y.e = getBase10Exponent(xd, e);

  return external ? finalise(y, pr, rm) : y;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.tan"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>tan ()](#apidoc.element.mathjs.type.BigNumber.prototype.tan)
- description and source-code
```javascript
tan = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(NaN);
  if (x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + 10;
  Ctor.rounding = 1;

  x = x.sin();
  x.s = 1;
  x = divide(x, new Ctor(1).minus(x.times(x)).sqrt(), pr + 10, 0);

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(quadrant == 2 || quadrant == 4 ? x.neg() : x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.tangent"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>tangent ()](#apidoc.element.mathjs.type.BigNumber.prototype.tangent)
- description and source-code
```javascript
tangent = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(NaN);
  if (x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + 10;
  Ctor.rounding = 1;

  x = x.sin();
  x.s = 1;
  x = divide(x, new Ctor(1).minus(x.times(x)).sqrt(), pr + 10, 0);

  Ctor.precision = pr;
  Ctor.rounding = rm;

  return finalise(quadrant == 2 || quadrant == 4 ? x.neg() : x, pr, rm, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.tanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>tanh ()](#apidoc.element.mathjs.type.BigNumber.prototype.tanh)
- description and source-code
```javascript
tanh = function () {
  var pr, rm,
    x = this,
    Ctor = x.constructor;

  if (!x.isFinite()) return new Ctor(x.s);
  if (x.isZero()) return new Ctor(x);

  pr = Ctor.precision;
  rm = Ctor.rounding;
  Ctor.precision = pr + 7;
  Ctor.rounding = 1;

  return divide(x.sinh(), x.cosh(), Ctor.precision = pr, Ctor.rounding = rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.times"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>times (y)](#apidoc.element.mathjs.type.BigNumber.prototype.times)
- description and source-code
```javascript
times = function (y) {
  var carry, e, i, k, r, rL, t, xdL, ydL,
    x = this,
    Ctor = x.constructor,
    xd = x.d,
    yd = (y = new Ctor(y)).d;

  y.s *= x.s;

   // If either is NaN, ±Infinity or ±0...
  if (!xd || !xd[0] || !yd || !yd[0]) {

    return new Ctor(!y.s || xd && !xd[0] && !yd || yd && !yd[0] && !xd

      // Return NaN if either is NaN.
      // Return NaN if x is ±0 and y is ±Infinity, or y is ±0 and x is ±Infinity.
      ? NaN

      // Return ±Infinity if either is ±Infinity.
      // Return ±0 if either is ±0.
      : !xd || !yd ? y.s / 0 : y.s * 0);
  }

  e = mathfloor(x.e / LOG_BASE) + mathfloor(y.e / LOG_BASE);
  xdL = xd.length;
  ydL = yd.length;

  // Ensure xd points to the longer array.
  if (xdL < ydL) {
    r = xd;
    xd = yd;
    yd = r;
    rL = xdL;
    xdL = ydL;
    ydL = rL;
  }

  // Initialise the result array with zeros.
  r = [];
  rL = xdL + ydL;
  for (i = rL; i--;) r.push(0);

  // Multiply!
  for (i = ydL; --i >= 0;) {
    carry = 0;
    for (k = xdL + i; k > i;) {
      t = r[k] + yd[i] * xd[k - i - 1] + carry;
      r[k--] = t % BASE | 0;
      carry = t / BASE | 0;
    }

    r[k] = (r[k] + carry) % BASE | 0;
  }

  // Remove trailing zeros.
  for (; !r[--rL];) r.pop();

  if (carry) ++e;
  else r.shift();

  y.d = r;
  y.e = getBase10Exponent(r, e);

  return external ? finalise(y, Ctor.precision, Ctor.rounding) : y;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toBinary"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toBinary (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toBinary)
- description and source-code
```javascript
toBinary = function (sd, rm) {
  return toStringBinary(this, 2, sd, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toDP"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toDP (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toDP)
- description and source-code
```javascript
toDP = function (dp, rm) {
  var x = this,
    Ctor = x.constructor;

  x = new Ctor(x);
  if (dp === void 0) return x;

  checkInt32(dp, 0, MAX_DIGITS);

  if (rm === void 0) rm = Ctor.rounding;
  else checkInt32(rm, 0, 8);

  return finalise(x, dp + x.e + 1, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toDecimalPlaces"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toDecimalPlaces (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toDecimalPlaces)
- description and source-code
```javascript
toDecimalPlaces = function (dp, rm) {
  var x = this,
    Ctor = x.constructor;

  x = new Ctor(x);
  if (dp === void 0) return x;

  checkInt32(dp, 0, MAX_DIGITS);

  if (rm === void 0) rm = Ctor.rounding;
  else checkInt32(rm, 0, 8);

  return finalise(x, dp + x.e + 1, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toExponential"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toExponential (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toExponential)
- description and source-code
```javascript
toExponential = function (dp, rm) {
  var str,
    x = this,
    Ctor = x.constructor;

  if (dp === void 0) {
    str = finiteToString(x, true);
  } else {
    checkInt32(dp, 0, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);

    x = finalise(new Ctor(x), dp + 1, rm);
    str = finiteToString(x, true, dp + 1);
  }

  return x.isNeg() && !x.isZero() ? '-' + str : str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toFixed"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toFixed (dp, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toFixed)
- description and source-code
```javascript
toFixed = function (dp, rm) {
  var str, y,
    x = this,
    Ctor = x.constructor;

  if (dp === void 0) {
    str = finiteToString(x);
  } else {
    checkInt32(dp, 0, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);

    y = finalise(new Ctor(x), dp + x.e + 1, rm);
    str = finiteToString(y, false, dp + y.e + 1);
  }

  // To determine whether to add the minus sign look at the value before it was rounded,
  // i.e. look at 'x' rather than 'y'.
  return x.isNeg() && !x.isZero() ? '-' + str : str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toFraction"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toFraction (maxD)](#apidoc.element.mathjs.type.BigNumber.prototype.toFraction)
- description and source-code
```javascript
toFraction = function (maxD) {
  var d, d0, d1, d2, e, k, n, n0, n1, pr, q, r,
    x = this,
    xd = x.d,
    Ctor = x.constructor;

  if (!xd) return new Ctor(x);

  n1 = d0 = new Ctor(1);
  d1 = n0 = new Ctor(0);

  d = new Ctor(d1);
  e = d.e = getPrecision(xd) - x.e - 1;
  k = e % LOG_BASE;
  d.d[0] = mathpow(10, k < 0 ? LOG_BASE + k : k);

  if (maxD == null) {

    // d is 10**e, the minimum max-denominator needed.
    maxD = e > 0 ? d : n1;
  } else {
    n = new Ctor(maxD);
    if (!n.isInt() || n.lt(n1)) throw Error(invalidArgument + n);
    maxD = n.gt(d) ? (e > 0 ? d : n1) : n;
  }

  external = false;
  n = new Ctor(digitsToString(xd));
  pr = Ctor.precision;
  Ctor.precision = e = xd.length * LOG_BASE * 2;

  for (;;)  {
    q = divide(n, d, 0, 1, 1);
    d2 = d0.plus(q.times(d1));
    if (d2.cmp(maxD) == 1) break;
    d0 = d1;
    d1 = d2;
    d2 = n1;
    n1 = n0.plus(q.times(d2));
    n0 = d2;
    d2 = d;
    d = n.minus(q.times(d2));
    n = d2;
  }

  d2 = divide(maxD.minus(d0), d1, 0, 1, 1);
  n0 = n0.plus(d2.times(n1));
  d0 = d0.plus(d2.times(d1));
  n0.s = n1.s = x.s;

  // Determine which fraction is closer to x, n0/d0 or n1/d1?
  r = divide(n1, d1, e, 1).minus(x).abs().cmp(divide(n0, d0, e, 1).minus(x).abs()) < 1
      ? [n1, d1] : [n0, d0];

  Ctor.precision = pr;
  external = true;

  return r;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toHex"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toHex (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toHex)
- description and source-code
```javascript
toHex = function (sd, rm) {
  return toStringBinary(this, 16, sd, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toHexadecimal"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toHexadecimal (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toHexadecimal)
- description and source-code
```javascript
toHexadecimal = function (sd, rm) {
  return toStringBinary(this, 16, sd, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.BigNumber.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'BigNumber',
    value: this.toString()
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toNearest"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toNearest (y, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toNearest)
- description and source-code
```javascript
toNearest = function (y, rm) {
  var x = this,
    Ctor = x.constructor;

  x = new Ctor(x);

  if (y == null) {

    // If x is not finite, return x.
    if (!x.d) return x;

    y = new Ctor(1);
    rm = Ctor.rounding;
  } else {
    y = new Ctor(y);
    if (rm !== void 0) checkInt32(rm, 0, 8);

    // If x is not finite, return x if y is not NaN, else NaN.
    if (!x.d) return y.s ? x : y;

    // If y is not finite, return Infinity with the sign of x if y is Infinity, else NaN.
    if (!y.d) {
      if (y.s) y.s = x.s;
      return y;
    }
  }

  // If y is not zero, calculate the nearest multiple of y to x.
  if (y.d[0]) {
    external = false;
    if (rm < 4) rm = [4, 5, 7, 8][rm];
    x = divide(x, y, 0, rm, 1).times(y);
    external = true;
    finalise(x);

  // If y is zero, return zero with the sign of x.
  } else {
    y.s = x.s;
    x = y;
  }

  return x;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toNumber"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toNumber ()](#apidoc.element.mathjs.type.BigNumber.prototype.toNumber)
- description and source-code
```javascript
toNumber = function () {
  return +this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toOctal"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toOctal (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toOctal)
- description and source-code
```javascript
toOctal = function (sd, rm) {
  return toStringBinary(this, 8, sd, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toPower"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toPower (y)](#apidoc.element.mathjs.type.BigNumber.prototype.toPower)
- description and source-code
```javascript
toPower = function (y) {
  var e, k, pr, r, rm, sign, yIsInt,
    x = this,
    Ctor = x.constructor,
    yn = +(y = new Ctor(y));

  // Either ±Infinity, NaN or ±0?
  if (!x.d || !y.d || !x.d[0] || !y.d[0]) return  new Ctor(mathpow(+x, yn));

  x = new Ctor(x);

  if (x.eq(1)) return x;

  pr = Ctor.precision;
  rm = Ctor.rounding;

  if (y.eq(1)) return finalise(x, pr, rm);

  e = mathfloor(y.e / LOG_BASE);
  k = y.d.length - 1;
  yIsInt = e >= k;
  sign = x.s;

  if (!yIsInt) {
    if (sign < 0) return new Ctor(NaN);

  // If y is a small integer use the 'exponentiation by squaring' algorithm.
  } else if ((k = yn < 0 ? -yn : yn) <= MAX_SAFE_INTEGER) {
    r = intPow(Ctor, x, k, pr);
    return y.s < 0 ? new Ctor(1).div(r) : finalise(r, pr, rm);
  }

  // Result is negative if x is negative and the last digit of integer y is odd.
  sign = sign < 0 && y.d[Math.max(e, k)] & 1 ? -1 : 1;

  // Estimate result exponent.
  // x^y = 10^e,  where e = y * log10(x)
  // log10(x) = log10(x_significand) + x_exponent
  // log10(x_significand) = ln(x_significand) / ln(10)
  k = mathpow(+x, yn);
  e = k == 0 || !isFinite(k)
    ? mathfloor(yn * (Math.log('0.' + digitsToString(x.d)) / Math.LN10 + x.e + 1))
    : new Ctor(k + '').e;

  // Estimate may be incorrect e.g. x: 0.999999999999999999, y: 2.29, e: 0, r.e: -1.

  // Overflow/underflow?
  if (e > Ctor.maxE + 1 || e < Ctor.minE - 1) return new Ctor(e > 0 ? sign / 0 : 0);

  external = false;
  Ctor.rounding = x.s = 1;

  // Estimate the extra guard digits needed to ensure five correct rounding digits from
  // naturalLogarithm(x). Example of failure without these extra digits (precision: 10):
  // new Decimal(2.32456).pow('2087987436534566.46411')
  // should be 1.162377823e+764914905173815, but is 1.162355823e+764914905173815
  k = Math.min(12, (e + '').length);

  // r = x^y = exp(y*ln(x))
  r = naturalExponential(y.times(naturalLogarithm(x, pr + k)), pr);

  // Truncate to the required precision plus five rounding digits.
  r = finalise(r, pr + 5, 1);

  // If the rounding digits are [49]9999 or [50]0000 increase the precision by 10 and recalculate
  // the result.
  if (checkRoundingDigits(r.d, pr, rm)) {
    e = pr + 10;

    // Truncate to the increased precision plus five rounding digits.
    r = finalise(naturalExponential(y.times(naturalLogarithm(x, e + k)), e), e + 5, 1);

    // Check for 14 nines from the 2nd rounding digit (the first rounding digit may be 4 or 9).
    if (+digitsToString(r.d).slice(pr + 1, pr + 15) + 1 == 1e14) {
      r = finalise(r, pr + 1, 0);
    }
  }

  r.s = sign;
  external = true;
  Ctor.rounding = rm;

  return finalise(r, pr, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toPrecision"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toPrecision (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toPrecision)
- description and source-code
```javascript
toPrecision = function (sd, rm) {
  var str,
    x = this,
    Ctor = x.constructor;

  if (sd === void 0) {
    str = finiteToString(x, x.e <= Ctor.toExpNeg || x.e >= Ctor.toExpPos);
  } else {
    checkInt32(sd, 1, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);

    x = finalise(new Ctor(x), sd, rm);
    str = finiteToString(x, sd <= x.e || x.e <= Ctor.toExpNeg, sd);
  }

  return x.isNeg() && !x.isZero() ? '-' + str : str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toSD"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toSD (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toSD)
- description and source-code
```javascript
toSD = function (sd, rm) {
  var x = this,
    Ctor = x.constructor;

  if (sd === void 0) {
    sd = Ctor.precision;
    rm = Ctor.rounding;
  } else {
    checkInt32(sd, 1, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);
  }

  return finalise(new Ctor(x), sd, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toSignificantDigits"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toSignificantDigits (sd, rm)](#apidoc.element.mathjs.type.BigNumber.prototype.toSignificantDigits)
- description and source-code
```javascript
toSignificantDigits = function (sd, rm) {
  var x = this,
    Ctor = x.constructor;

  if (sd === void 0) {
    sd = Ctor.precision;
    rm = Ctor.rounding;
  } else {
    checkInt32(sd, 1, MAX_DIGITS);

    if (rm === void 0) rm = Ctor.rounding;
    else checkInt32(rm, 0, 8);
  }

  return finalise(new Ctor(x), sd, rm);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>toString ()](#apidoc.element.mathjs.type.BigNumber.prototype.toString)
- description and source-code
```javascript
toString = function () {
  var x = this,
    Ctor = x.constructor,
    str = finiteToString(x, x.e <= Ctor.toExpNeg || x.e >= Ctor.toExpPos);

  return x.isNeg() && !x.isZero() ? '-' + str : str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.trunc"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>trunc ()](#apidoc.element.mathjs.type.BigNumber.prototype.trunc)
- description and source-code
```javascript
trunc = function () {
  return finalise(new this.constructor(this), this.e + 1, 1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.truncated"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>truncated ()](#apidoc.element.mathjs.type.BigNumber.prototype.truncated)
- description and source-code
```javascript
truncated = function () {
  return finalise(new this.constructor(this), this.e + 1, 1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.BigNumber.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.BigNumber.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.BigNumber.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  var x = this,
    Ctor = x.constructor,
    str = finiteToString(x, x.e <= Ctor.toExpNeg || x.e >= Ctor.toExpPos);

  return x.isNeg() ? '-' + str : str;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Chain"></a>[module mathjs.type.Chain](#apidoc.module.mathjs.type.Chain)

#### <a name="apidoc.element.mathjs.type.Chain.Chain"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Chain (value)](#apidoc.element.mathjs.type.Chain.Chain)
- description and source-code
```javascript
function Chain(value) {
  if (!(this instanceof Chain)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (value && value.isChain) {
    this.value = value.value;
  }
  else {
    this.value = value;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.createProxy"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.</span>createProxy (arg0, arg1)](#apidoc.element.mathjs.type.Chain.createProxy)
- description and source-code
```javascript
createProxy = function (arg0, arg1) {
  if (typeof arg0 === 'string') {
    // createProxy(name, value)
    createProxy(arg0, arg1);
  }
  else {
    // createProxy(values)
    for (var prop in arg0) {
      if (arg0.hasOwnProperty(prop)) {
        createProxy(prop, arg0[prop]);
      }
    }
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Chain.prototype"></a>[module mathjs.type.Chain.prototype](#apidoc.module.mathjs.type.Chain.prototype)

#### <a name="apidoc.element.mathjs.type.Chain.prototype.abs"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>abs ()](#apidoc.element.mathjs.type.Chain.prototype.abs)
- description and source-code
```javascript
abs = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.acos"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acos ()](#apidoc.element.mathjs.type.Chain.prototype.acos)
- description and source-code
```javascript
acos = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.acosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acosh ()](#apidoc.element.mathjs.type.Chain.prototype.acosh)
- description and source-code
```javascript
acosh = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.acot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acot ()](#apidoc.element.mathjs.type.Chain.prototype.acot)
- description and source-code
```javascript
acot = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.acoth"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acoth ()](#apidoc.element.mathjs.type.Chain.prototype.acoth)
- description and source-code
```javascript
acoth = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.acsc"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acsc ()](#apidoc.element.mathjs.type.Chain.prototype.acsc)
- description and source-code
```javascript
acsc = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.acsch"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>acsch ()](#apidoc.element.mathjs.type.Chain.prototype.acsch)
- description and source-code
```javascript
acsch = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.add"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>add ()](#apidoc.element.mathjs.type.Chain.prototype.add)
- description and source-code
```javascript
add = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.and"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>and ()](#apidoc.element.mathjs.type.Chain.prototype.and)
- description and source-code
```javascript
and = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.arg"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>arg ()](#apidoc.element.mathjs.type.Chain.prototype.arg)
- description and source-code
```javascript
arg = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.asec"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asec ()](#apidoc.element.mathjs.type.Chain.prototype.asec)
- description and source-code
```javascript
asec = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.asech"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asech ()](#apidoc.element.mathjs.type.Chain.prototype.asech)
- description and source-code
```javascript
asech = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.asin"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asin ()](#apidoc.element.mathjs.type.Chain.prototype.asin)
- description and source-code
```javascript
asin = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.asinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>asinh ()](#apidoc.element.mathjs.type.Chain.prototype.asinh)
- description and source-code
```javascript
asinh = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.atan"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>atan ()](#apidoc.element.mathjs.type.Chain.prototype.atan)
- description and source-code
```javascript
atan = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.atan2"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>atan2 ()](#apidoc.element.mathjs.type.Chain.prototype.atan2)
- description and source-code
```javascript
atan2 = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.atanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>atanh ()](#apidoc.element.mathjs.type.Chain.prototype.atanh)
- description and source-code
```javascript
atanh = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.bellNumbers"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bellNumbers ()](#apidoc.element.mathjs.type.Chain.prototype.bellNumbers)
- description and source-code
```javascript
bellNumbers = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.bignumber"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bignumber ()](#apidoc.element.mathjs.type.Chain.prototype.bignumber)
- description and source-code
```javascript
bignumber = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.bitAnd"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitAnd ()](#apidoc.element.mathjs.type.Chain.prototype.bitAnd)
- description and source-code
```javascript
bitAnd = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.bitNot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitNot ()](#apidoc.element.mathjs.type.Chain.prototype.bitNot)
- description and source-code
```javascript
bitNot = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.bitOr"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitOr ()](#apidoc.element.mathjs.type.Chain.prototype.bitOr)
- description and source-code
```javascript
bitOr = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.bitXor"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>bitXor ()](#apidoc.element.mathjs.type.Chain.prototype.bitXor)
- description and source-code
```javascript
bitXor = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.boolean"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>boolean ()](#apidoc.element.mathjs.type.Chain.prototype.boolean)
- description and source-code
```javascript
boolean = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.catalan"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>catalan ()](#apidoc.element.mathjs.type.Chain.prototype.catalan)
- description and source-code
```javascript
catalan = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.cbrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cbrt ()](#apidoc.element.mathjs.type.Chain.prototype.cbrt)
- description and source-code
```javascript
cbrt = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.ceil"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>ceil ()](#apidoc.element.mathjs.type.Chain.prototype.ceil)
- description and source-code
```javascript
ceil = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.chain"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>chain ()](#apidoc.element.mathjs.type.Chain.prototype.chain)
- description and source-code
```javascript
chain = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>clone ()](#apidoc.element.mathjs.type.Chain.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.combinations"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>combinations ()](#apidoc.element.mathjs.type.Chain.prototype.combinations)
- description and source-code
```javascript
combinations = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.compare"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>compare ()](#apidoc.element.mathjs.type.Chain.prototype.compare)
- description and source-code
```javascript
compare = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.compile"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>compile ()](#apidoc.element.mathjs.type.Chain.prototype.compile)
- description and source-code
```javascript
compile = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.complex"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>complex ()](#apidoc.element.mathjs.type.Chain.prototype.complex)
- description and source-code
```javascript
complex = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.composition"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>composition ()](#apidoc.element.mathjs.type.Chain.prototype.composition)
- description and source-code
```javascript
composition = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.concat"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>concat ()](#apidoc.element.mathjs.type.Chain.prototype.concat)
- description and source-code
```javascript
concat = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.config"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>config ()](#apidoc.element.mathjs.type.Chain.prototype.config)
- description and source-code
```javascript
config = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.conj"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>conj ()](#apidoc.element.mathjs.type.Chain.prototype.conj)
- description and source-code
```javascript
conj = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.cos"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cos ()](#apidoc.element.mathjs.type.Chain.prototype.cos)
- description and source-code
```javascript
cos = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.cosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cosh ()](#apidoc.element.mathjs.type.Chain.prototype.cosh)
- description and source-code
```javascript
cosh = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.cot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cot ()](#apidoc.element.mathjs.type.Chain.prototype.cot)
- description and source-code
```javascript
cot = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.coth"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>coth ()](#apidoc.element.mathjs.type.Chain.prototype.coth)
- description and source-code
```javascript
coth = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.create"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>create ()](#apidoc.element.mathjs.type.Chain.prototype.create)
- description and source-code
```javascript
create = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.createUnit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>createUnit ()](#apidoc.element.mathjs.type.Chain.prototype.createUnit)
- description and source-code
```javascript
createUnit = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.cross"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cross ()](#apidoc.element.mathjs.type.Chain.prototype.cross)
- description and source-code
```javascript
cross = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.csc"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>csc ()](#apidoc.element.mathjs.type.Chain.prototype.csc)
- description and source-code
```javascript
csc = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.csch"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>csch ()](#apidoc.element.mathjs.type.Chain.prototype.csch)
- description and source-code
```javascript
csch = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.cube"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>cube ()](#apidoc.element.mathjs.type.Chain.prototype.cube)
- description and source-code
```javascript
cube = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.deepEqual"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>deepEqual ()](#apidoc.element.mathjs.type.Chain.prototype.deepEqual)
- description and source-code
```javascript
deepEqual = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.derivative"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>derivative ()](#apidoc.element.mathjs.type.Chain.prototype.derivative)
- description and source-code
```javascript
derivative = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.det"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>det ()](#apidoc.element.mathjs.type.Chain.prototype.det)
- description and source-code
```javascript
det = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.diag"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>diag ()](#apidoc.element.mathjs.type.Chain.prototype.diag)
- description and source-code
```javascript
diag = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.distance"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>distance ()](#apidoc.element.mathjs.type.Chain.prototype.distance)
- description and source-code
```javascript
distance = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.divide"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>divide ()](#apidoc.element.mathjs.type.Chain.prototype.divide)
- description and source-code
```javascript
divide = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.done"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>done ()](#apidoc.element.mathjs.type.Chain.prototype.done)
- description and source-code
```javascript
done = function () {
  return this.value;
}
```
- example usage
```shell
...
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.dot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dot ()](#apidoc.element.mathjs.type.Chain.prototype.dot)
- description and source-code
```javascript
dot = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.dotDivide"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dotDivide ()](#apidoc.element.mathjs.type.Chain.prototype.dotDivide)
- description and source-code
```javascript
dotDivide = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.dotMultiply"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dotMultiply ()](#apidoc.element.mathjs.type.Chain.prototype.dotMultiply)
- description and source-code
```javascript
dotMultiply = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.dotPow"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>dotPow ()](#apidoc.element.mathjs.type.Chain.prototype.dotPow)
- description and source-code
```javascript
dotPow = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.emit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>emit ()](#apidoc.element.mathjs.type.Chain.prototype.emit)
- description and source-code
```javascript
emit = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.equal"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>equal ()](#apidoc.element.mathjs.type.Chain.prototype.equal)
- description and source-code
```javascript
equal = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.erf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>erf ()](#apidoc.element.mathjs.type.Chain.prototype.erf)
- description and source-code
```javascript
erf = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.eval"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>eval ()](#apidoc.element.mathjs.type.Chain.prototype.eval)
- description and source-code
```javascript
eval = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.exp"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>exp ()](#apidoc.element.mathjs.type.Chain.prototype.exp)
- description and source-code
```javascript
exp = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.eye"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>eye ()](#apidoc.element.mathjs.type.Chain.prototype.eye)
- description and source-code
```javascript
eye = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.factorial"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>factorial ()](#apidoc.element.mathjs.type.Chain.prototype.factorial)
- description and source-code
```javascript
factorial = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.filter"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>filter ()](#apidoc.element.mathjs.type.Chain.prototype.filter)
- description and source-code
```javascript
filter = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.fix"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>fix ()](#apidoc.element.mathjs.type.Chain.prototype.fix)
- description and source-code
```javascript
fix = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.flatten"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>flatten ()](#apidoc.element.mathjs.type.Chain.prototype.flatten)
- description and source-code
```javascript
flatten = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.floor"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>floor ()](#apidoc.element.mathjs.type.Chain.prototype.floor)
- description and source-code
```javascript
floor = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>forEach ()](#apidoc.element.mathjs.type.Chain.prototype.forEach)
- description and source-code
```javascript
forEach = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>format ()](#apidoc.element.mathjs.type.Chain.prototype.format)
- description and source-code
```javascript
format = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.fraction"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>fraction ()](#apidoc.element.mathjs.type.Chain.prototype.fraction)
- description and source-code
```javascript
fraction = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.gamma"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>gamma ()](#apidoc.element.mathjs.type.Chain.prototype.gamma)
- description and source-code
```javascript
gamma = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.gcd"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>gcd ()](#apidoc.element.mathjs.type.Chain.prototype.gcd)
- description and source-code
```javascript
gcd = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.help"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>help ()](#apidoc.element.mathjs.type.Chain.prototype.help)
- description and source-code
```javascript
help = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.hypot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>hypot ()](#apidoc.element.mathjs.type.Chain.prototype.hypot)
- description and source-code
```javascript
hypot = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.im"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>im ()](#apidoc.element.mathjs.type.Chain.prototype.im)
- description and source-code
```javascript
im = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.import"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>import ()](#apidoc.element.mathjs.type.Chain.prototype.import)
- description and source-code
```javascript
import = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.index"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>index ()](#apidoc.element.mathjs.type.Chain.prototype.index)
- description and source-code
```javascript
index = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.intersect"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>intersect ()](#apidoc.element.mathjs.type.Chain.prototype.intersect)
- description and source-code
```javascript
intersect = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.inv"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>inv ()](#apidoc.element.mathjs.type.Chain.prototype.inv)
- description and source-code
```javascript
inv = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isInteger"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isInteger ()](#apidoc.element.mathjs.type.Chain.prototype.isInteger)
- description and source-code
```javascript
isInteger = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isNaN"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isNaN ()](#apidoc.element.mathjs.type.Chain.prototype.isNaN)
- description and source-code
```javascript
isNaN = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isNegative"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isNegative ()](#apidoc.element.mathjs.type.Chain.prototype.isNegative)
- description and source-code
```javascript
isNegative = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isNumeric"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isNumeric ()](#apidoc.element.mathjs.type.Chain.prototype.isNumeric)
- description and source-code
```javascript
isNumeric = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isPositive"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isPositive ()](#apidoc.element.mathjs.type.Chain.prototype.isPositive)
- description and source-code
```javascript
isPositive = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isPrime"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isPrime ()](#apidoc.element.mathjs.type.Chain.prototype.isPrime)
- description and source-code
```javascript
isPrime = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.isZero"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>isZero ()](#apidoc.element.mathjs.type.Chain.prototype.isZero)
- description and source-code
```javascript
isZero = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.kldivergence"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>kldivergence ()](#apidoc.element.mathjs.type.Chain.prototype.kldivergence)
- description and source-code
```javascript
kldivergence = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.kron"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>kron ()](#apidoc.element.mathjs.type.Chain.prototype.kron)
- description and source-code
```javascript
kron = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.larger"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>larger ()](#apidoc.element.mathjs.type.Chain.prototype.larger)
- description and source-code
```javascript
larger = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.largerEq"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>largerEq ()](#apidoc.element.mathjs.type.Chain.prototype.largerEq)
- description and source-code
```javascript
largerEq = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.lcm"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lcm ()](#apidoc.element.mathjs.type.Chain.prototype.lcm)
- description and source-code
```javascript
lcm = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.leftShift"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>leftShift ()](#apidoc.element.mathjs.type.Chain.prototype.leftShift)
- description and source-code
```javascript
leftShift = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.log"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>log ()](#apidoc.element.mathjs.type.Chain.prototype.log)
- description and source-code
```javascript
log = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.log10"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>log10 ()](#apidoc.element.mathjs.type.Chain.prototype.log10)
- description and source-code
```javascript
log10 = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.lsolve"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lsolve ()](#apidoc.element.mathjs.type.Chain.prototype.lsolve)
- description and source-code
```javascript
lsolve = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.lup"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lup ()](#apidoc.element.mathjs.type.Chain.prototype.lup)
- description and source-code
```javascript
lup = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.lusolve"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>lusolve ()](#apidoc.element.mathjs.type.Chain.prototype.lusolve)
- description and source-code
```javascript
lusolve = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.mad"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mad ()](#apidoc.element.mathjs.type.Chain.prototype.mad)
- description and source-code
```javascript
mad = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>map ()](#apidoc.element.mathjs.type.Chain.prototype.map)
- description and source-code
```javascript
map = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.matrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>matrix ()](#apidoc.element.mathjs.type.Chain.prototype.matrix)
- description and source-code
```javascript
matrix = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.max"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>max ()](#apidoc.element.mathjs.type.Chain.prototype.max)
- description and source-code
```javascript
max = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.mean"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mean ()](#apidoc.element.mathjs.type.Chain.prototype.mean)
- description and source-code
```javascript
mean = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.median"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>median ()](#apidoc.element.mathjs.type.Chain.prototype.median)
- description and source-code
```javascript
median = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.min"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>min ()](#apidoc.element.mathjs.type.Chain.prototype.min)
- description and source-code
```javascript
min = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.mod"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mod ()](#apidoc.element.mathjs.type.Chain.prototype.mod)
- description and source-code
```javascript
mod = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.mode"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>mode ()](#apidoc.element.mathjs.type.Chain.prototype.mode)
- description and source-code
```javascript
mode = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.multinomial"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>multinomial ()](#apidoc.element.mathjs.type.Chain.prototype.multinomial)
- description and source-code
```javascript
multinomial = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.multiply"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>multiply ()](#apidoc.element.mathjs.type.Chain.prototype.multiply)
- description and source-code
```javascript
multiply = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.norm"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>norm ()](#apidoc.element.mathjs.type.Chain.prototype.norm)
- description and source-code
```javascript
norm = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.not"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>not ()](#apidoc.element.mathjs.type.Chain.prototype.not)
- description and source-code
```javascript
not = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.nthRoot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>nthRoot ()](#apidoc.element.mathjs.type.Chain.prototype.nthRoot)
- description and source-code
```javascript
nthRoot = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.number"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>number ()](#apidoc.element.mathjs.type.Chain.prototype.number)
- description and source-code
```javascript
number = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.off"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>off ()](#apidoc.element.mathjs.type.Chain.prototype.off)
- description and source-code
```javascript
off = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.on"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>on ()](#apidoc.element.mathjs.type.Chain.prototype.on)
- description and source-code
```javascript
on = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...

var object = require('./utils/object');
var bigConstants = require('./utils/bignumber/constants');

function factory (type, config, load, typed, math) {
// listen for changed in the configuration, automatically reload
// constants when needed
math.on('config', function (curr, prev) {
  if (curr.number !== prev.number) {
    factory(type, config, load, typed, math);
  }
});

math['true']     = true;
math['false']    = false;
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.once"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>once ()](#apidoc.element.mathjs.type.Chain.prototype.once)
- description and source-code
```javascript
once = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.ones"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>ones ()](#apidoc.element.mathjs.type.Chain.prototype.ones)
- description and source-code
```javascript
ones = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.or"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>or ()](#apidoc.element.mathjs.type.Chain.prototype.or)
- description and source-code
```javascript
or = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.parse"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>parse ()](#apidoc.element.mathjs.type.Chain.prototype.parse)
- description and source-code
```javascript
parse = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.parser"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>parser ()](#apidoc.element.mathjs.type.Chain.prototype.parser)
- description and source-code
```javascript
parser = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.partitionSelect"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>partitionSelect ()](#apidoc.element.mathjs.type.Chain.prototype.partitionSelect)
- description and source-code
```javascript
partitionSelect = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.permutations"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>permutations ()](#apidoc.element.mathjs.type.Chain.prototype.permutations)
- description and source-code
```javascript
permutations = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.pow"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>pow ()](#apidoc.element.mathjs.type.Chain.prototype.pow)
- description and source-code
```javascript
pow = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.print"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>print ()](#apidoc.element.mathjs.type.Chain.prototype.print)
- description and source-code
```javascript
print = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.prod"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>prod ()](#apidoc.element.mathjs.type.Chain.prototype.prod)
- description and source-code
```javascript
prod = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.quantileSeq"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>quantileSeq ()](#apidoc.element.mathjs.type.Chain.prototype.quantileSeq)
- description and source-code
```javascript
quantileSeq = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.range"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>range ()](#apidoc.element.mathjs.type.Chain.prototype.range)
- description and source-code
```javascript
range = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.re"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>re ()](#apidoc.element.mathjs.type.Chain.prototype.re)
- description and source-code
```javascript
re = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.reshape"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>reshape ()](#apidoc.element.mathjs.type.Chain.prototype.reshape)
- description and source-code
```javascript
reshape = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.resize"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>resize ()](#apidoc.element.mathjs.type.Chain.prototype.resize)
- description and source-code
```javascript
resize = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.rightArithShift"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>rightArithShift ()](#apidoc.element.mathjs.type.Chain.prototype.rightArithShift)
- description and source-code
```javascript
rightArithShift = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.rightLogShift"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>rightLogShift ()](#apidoc.element.mathjs.type.Chain.prototype.rightLogShift)
- description and source-code
```javascript
rightLogShift = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.round"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>round ()](#apidoc.element.mathjs.type.Chain.prototype.round)
- description and source-code
```javascript
round = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
[chained operations](http://mathjs.org/docs/chained_operations.html).

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sec"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sec ()](#apidoc.element.mathjs.type.Chain.prototype.sec)
- description and source-code
```javascript
sec = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sech"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sech ()](#apidoc.element.mathjs.type.Chain.prototype.sech)
- description and source-code
```javascript
sech = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sign"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sign ()](#apidoc.element.mathjs.type.Chain.prototype.sign)
- description and source-code
```javascript
sign = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.simplify"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>simplify ()](#apidoc.element.mathjs.type.Chain.prototype.simplify)
- description and source-code
```javascript
simplify = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sin"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sin ()](#apidoc.element.mathjs.type.Chain.prototype.sin)
- description and source-code
```javascript
sin = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sinh ()](#apidoc.element.mathjs.type.Chain.prototype.sinh)
- description and source-code
```javascript
sinh = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>size ()](#apidoc.element.mathjs.type.Chain.prototype.size)
- description and source-code
```javascript
size = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.slu"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>slu ()](#apidoc.element.mathjs.type.Chain.prototype.slu)
- description and source-code
```javascript
slu = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.smaller"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>smaller ()](#apidoc.element.mathjs.type.Chain.prototype.smaller)
- description and source-code
```javascript
smaller = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.smallerEq"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>smallerEq ()](#apidoc.element.mathjs.type.Chain.prototype.smallerEq)
- description and source-code
```javascript
smallerEq = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sort"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sort ()](#apidoc.element.mathjs.type.Chain.prototype.sort)
- description and source-code
```javascript
sort = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sparse"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sparse ()](#apidoc.element.mathjs.type.Chain.prototype.sparse)
- description and source-code
```javascript
sparse = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.splitUnit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>splitUnit ()](#apidoc.element.mathjs.type.Chain.prototype.splitUnit)
- description and source-code
```javascript
splitUnit = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sqrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sqrt ()](#apidoc.element.mathjs.type.Chain.prototype.sqrt)
- description and source-code
```javascript
sqrt = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
...
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
...
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.square"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>square ()](#apidoc.element.mathjs.type.Chain.prototype.square)
- description and source-code
```javascript
square = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.squeeze"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>squeeze ()](#apidoc.element.mathjs.type.Chain.prototype.squeeze)
- description and source-code
```javascript
squeeze = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.std"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>std ()](#apidoc.element.mathjs.type.Chain.prototype.std)
- description and source-code
```javascript
std = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.stirlingS2"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>stirlingS2 ()](#apidoc.element.mathjs.type.Chain.prototype.stirlingS2)
- description and source-code
```javascript
stirlingS2 = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.string"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>string ()](#apidoc.element.mathjs.type.Chain.prototype.string)
- description and source-code
```javascript
string = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.subset"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>subset ()](#apidoc.element.mathjs.type.Chain.prototype.subset)
- description and source-code
```javascript
subset = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.subtract"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>subtract ()](#apidoc.element.mathjs.type.Chain.prototype.subtract)
- description and source-code
```javascript
subtract = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.sum"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>sum ()](#apidoc.element.mathjs.type.Chain.prototype.sum)
- description and source-code
```javascript
sum = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.tan"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>tan ()](#apidoc.element.mathjs.type.Chain.prototype.tan)
- description and source-code
```javascript
tan = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.tanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>tanh ()](#apidoc.element.mathjs.type.Chain.prototype.tanh)
- description and source-code
```javascript
tanh = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.to"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>to ()](#apidoc.element.mathjs.type.Chain.prototype.to)
- description and source-code
```javascript
to = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>toString ()](#apidoc.element.mathjs.type.Chain.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return format(this.value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.trace"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>trace ()](#apidoc.element.mathjs.type.Chain.prototype.trace)
- description and source-code
```javascript
trace = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.transpose"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>transpose ()](#apidoc.element.mathjs.type.Chain.prototype.transpose)
- description and source-code
```javascript
transpose = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.typed"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>typed ()](#apidoc.element.mathjs.type.Chain.prototype.typed)
- description and source-code
```javascript
typed = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.typeof"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>typeof ()](#apidoc.element.mathjs.type.Chain.prototype.typeof)
- description and source-code
```javascript
typeof = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.unaryMinus"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unaryMinus ()](#apidoc.element.mathjs.type.Chain.prototype.unaryMinus)
- description and source-code
```javascript
unaryMinus = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.unaryPlus"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unaryPlus ()](#apidoc.element.mathjs.type.Chain.prototype.unaryPlus)
- description and source-code
```javascript
unaryPlus = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.unequal"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unequal ()](#apidoc.element.mathjs.type.Chain.prototype.unequal)
- description and source-code
```javascript
unequal = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.unit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>unit ()](#apidoc.element.mathjs.type.Chain.prototype.unit)
- description and source-code
```javascript
unit = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.usolve"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>usolve ()](#apidoc.element.mathjs.type.Chain.prototype.usolve)
- description and source-code
```javascript
usolve = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Chain.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  return this.value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.var"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>var ()](#apidoc.element.mathjs.type.Chain.prototype.var)
- description and source-code
```javascript
var = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.xgcd"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>xgcd ()](#apidoc.element.mathjs.type.Chain.prototype.xgcd)
- description and source-code
```javascript
xgcd = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.xor"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>xor ()](#apidoc.element.mathjs.type.Chain.prototype.xor)
- description and source-code
```javascript
xor = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Chain.prototype.zeros"></a>[function <span class="apidocSignatureSpan">mathjs.type.Chain.prototype.</span>zeros ()](#apidoc.element.mathjs.type.Chain.prototype.zeros)
- description and source-code
```javascript
zeros = function () {
  var args = [this.value];  // 'this' will be the context of a Chain instance
  for (var i = 0; i < arguments.length; i++) {
    args[i + 1] = arguments[i];
  }

  return new Chain(fn.apply(fn, args));
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Complex"></a>[module mathjs.type.Complex](#apidoc.module.mathjs.type.Complex)

#### <a name="apidoc.element.mathjs.type.Complex.Complex"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Complex (a, b)](#apidoc.element.mathjs.type.Complex.Complex)
- description and source-code
```javascript
function Complex(a, b) {

  if (!(this instanceof Complex)) {
    return new Complex(a, b);
  }

  parse(a, b); // mutates P

  this["re"] = P["re"];
  this["im"] = P["im"];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.</span>fromJSON (json)](#apidoc.element.mathjs.type.Complex.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new Complex(json);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.fromPolar"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.</span>fromPolar (args)](#apidoc.element.mathjs.type.Complex.fromPolar)
- description and source-code
```javascript
fromPolar = function (args) {
  switch (arguments.length) {
    case 1:
      var arg = arguments[0];
      if (typeof arg === 'object') {
        return Complex(arg);
      }
      throw new TypeError('Input has to be an object with r and phi keys.');

    case 2:
      var r = arguments[0],
          phi = arguments[1];
      if (isNumber(r)) {
        if (phi && phi.isUnit && phi.hasBase('ANGLE')) {
          // convert unit to a number in radians
          phi = phi.toNumber('rad');
        }

        if (isNumber(phi)) {
          return new Complex({r: r, phi: phi});
        }

        throw new TypeError('Phi is not a number nor an angle unit.');
      } else {
        throw new TypeError('Radius r is not a number.');
      }

    default:
      throw new SyntaxError('Wrong number of arguments in function fromPolar');
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Complex.prototype"></a>[module mathjs.type.Complex.prototype](#apidoc.module.mathjs.type.Complex.prototype)

#### <a name="apidoc.element.mathjs.type.Complex.prototype.abs"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>abs ()](#apidoc.element.mathjs.type.Complex.prototype.abs)
- description and source-code
```javascript
abs = function () {

  var a = Math.abs(this["re"]);
  var b = Math.abs(this["im"]);

  if (a < 3000 && b < 3000) {
    return Math.sqrt(a * a + b * b);
  }

  if (a < b) {
    a = b;
    b = this["re"] / this["im"];
  } else {
    b = this["im"] / this["re"];
  }
  return a * Math.sqrt(1 + b * b);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.acos"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acos ()](#apidoc.element.mathjs.type.Complex.prototype.acos)
- description and source-code
```javascript
acos = function () {

  var a = this["re"];
  var b = this["im"];

  var t1 = new Complex(
           b * b - a * a + 1,
          -2 * a * b)['sqrt']();

  var t2 = new Complex(
          t1["re"] - b,
          t1["im"] + a)['log']();

  return new Complex(Math.PI / 2 - t2["im"], t2["re"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.acosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acosh ()](#apidoc.element.mathjs.type.Complex.prototype.acosh)
- description and source-code
```javascript
acosh = function () {

  var tmp;
  var res = this["acos"]();
  if (res["im"] <= 0) {
    tmp = res["re"];
    res["re"] = -res["im"];
    res["im"] = tmp;
  } else {
    tmp = res["im"];
    res["im"] = -res["re"];
    res["re"] = tmp;
  }
  return res;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.acot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acot ()](#apidoc.element.mathjs.type.Complex.prototype.acot)
- description and source-code
```javascript
acot = function () {

  var a = this["re"];
  var b = this["im"];

  if (b === 0) {
    return new Complex(Math.atan2(1, a), 0);
  }

  var d = a * a + b * b;
  return (d !== 0)
          ? new Complex(
                  a / d,
                 -b / d).atan()
          : new Complex(
                  (a !== 0) ? a / 0 : 0,
                  (b !== 0) ?-b / 0 : 0).atan();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.acoth"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acoth ()](#apidoc.element.mathjs.type.Complex.prototype.acoth)
- description and source-code
```javascript
acoth = function () {

  var a = this["re"];
  var b = this["im"];

  if (a === 0 && b === 0) {

    return new Complex(0, Math.PI / 2);
  }

  var d = a * a + b * b;
  return (d !== 0)
          ? new Complex(
                  a / d,
                 -b / d).atanh()
          : new Complex(
                  (a !== 0) ? a / 0 : 0,
                  (b !== 0) ?-b / 0 : 0).atanh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.acsc"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acsc ()](#apidoc.element.mathjs.type.Complex.prototype.acsc)
- description and source-code
```javascript
acsc = function () {

  var a = this["re"];
  var b = this["im"];

  if (a === 0 && b === 0) {
    return new Complex(Math.PI / 2, Infinity);
  }

  var d = a * a + b * b;
  return (d !== 0)
          ? new Complex(
                  a / d,
                 -b / d).asin()
          : new Complex(
                  (a !== 0) ? a / 0 : 0,
                  (b !== 0) ?-b / 0 : 0).asin();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.acsch"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>acsch ()](#apidoc.element.mathjs.type.Complex.prototype.acsch)
- description and source-code
```javascript
acsch = function () {

  var a = this["re"];
  var b = this["im"];

  if (b === 0) {

    return new Complex(
            (a !== 0)
            ? Math.log(a + Math.sqrt(a * a + 1))
            : Infinity, 0);
  }

  var d = a * a + b * b;
  return (d !== 0)
          ? new Complex(
                  a / d,
                  -b / d).asinh()
          : new Complex(
                  (a !== 0) ? a / 0 : 0,
                  (b !== 0) ?-b / 0 : 0).asinh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.add"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>add (a, b)](#apidoc.element.mathjs.type.Complex.prototype.add)
- description and source-code
```javascript
add = function (a, b) {

  parse(a, b); // mutates P

  return new Complex(
          this["re"] + P["re"],
          this["im"] + P["im"]);
}
```
- example usage
```shell
...
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.arg"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>arg ()](#apidoc.element.mathjs.type.Complex.prototype.arg)
- description and source-code
```javascript
arg = function () {

  return Math.atan2(this["im"], this["re"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.asec"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asec ()](#apidoc.element.mathjs.type.Complex.prototype.asec)
- description and source-code
```javascript
asec = function () {

  var a = this["re"];
  var b = this["im"];

  if (a === 0 && b === 0) {
    return new Complex(0, Infinity);
  }

  var d = a * a + b * b;
  return (d !== 0)
          ? new Complex(
                  a / d,
                  -b / d).acos()
          : new Complex(
                  (a !== 0) ? a / 0 : 0,
                  (b !== 0) ?-b / 0 : 0).acos();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.asech"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asech ()](#apidoc.element.mathjs.type.Complex.prototype.asech)
- description and source-code
```javascript
asech = function () {

  var a = this["re"];
  var b = this["im"];

  if (a === 0 && b === 0) {
    return new Complex(Infinity, 0);
  }

  var d = a * a + b * b;
  return (d !== 0)
          ? new Complex(
                  a / d,
                 -b / d).acosh()
          : new Complex(
                  (a !== 0) ? a / 0 : 0,
                  (b !== 0) ?-b / 0 : 0).acosh();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.asin"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asin ()](#apidoc.element.mathjs.type.Complex.prototype.asin)
- description and source-code
```javascript
asin = function () {

  var a = this["re"];
  var b = this["im"];

  var t1 = new Complex(
           b * b - a * a + 1,
          -2 * a * b)['sqrt']();

  var t2 = new Complex(
          t1['re'] - b,
          t1['im'] + a)['log']();

  return new Complex(t2['im'], -t2['re']);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.asinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>asinh ()](#apidoc.element.mathjs.type.Complex.prototype.asinh)
- description and source-code
```javascript
asinh = function () {

  var tmp = this["im"];
  this["im"] = -this["re"];
  this["re"] = tmp;
  var res = this["asin"]();

  this["re"] = -this["im"];
  this["im"] = tmp;
  tmp = res["re"];

  res["re"] = -res["im"];
  res["im"] = tmp;
  return res;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.atan"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>atan ()](#apidoc.element.mathjs.type.Complex.prototype.atan)
- description and source-code
```javascript
atan = function () {

  var a = this["re"];
  var b = this["im"];

  if (a === 0) {

    if (b === 1) {
      return new Complex(0, Infinity);
    }

    if (b === -1) {
      return new Complex(0, -Infinity);
    }
  }

  var d = a * a + (1.0 - b) * (1.0 - b);

  var t1 = new Complex(
          (1 - b * b - a * a) / d,
          -2 * a / d).log();

  return new Complex(-0.5 * t1["im"], 0.5 * t1["re"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.atanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>atanh ()](#apidoc.element.mathjs.type.Complex.prototype.atanh)
- description and source-code
```javascript
atanh = function () {

  var a = this["re"];
  var b = this["im"];

  var noIM = a > 1 && b === 0;
  var oneMinus = 1 - a;
  var onePlus = 1 + a;
  var d = oneMinus * oneMinus + b * b;

  var x = (d !== 0)
          ? new Complex(
                  (onePlus * oneMinus - b * b) / d,
                  (b * oneMinus + onePlus * b) / d)
          : new Complex(
                  (a !== -1) ? (a / 0) : 0,
                  (b !== 0) ? (b / 0) : 0);

  var temp = x["re"];
  x["re"] = logHypot(x["re"], x["im"]) / 2;
  x["im"] = Math.atan2(x["im"], temp) / 2;
  if (noIM) {
    x["im"] = -x["im"];
  }
  return x;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.ceil"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>ceil (places)](#apidoc.element.mathjs.type.Complex.prototype.ceil)
- description and source-code
```javascript
ceil = function (places) {

  places = Math.pow(10, places || 0);

  return new Complex(
          Math.ceil(this["re"] * places) / places,
          Math.ceil(this["im"] * places) / places);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>clone ()](#apidoc.element.mathjs.type.Complex.prototype.clone)
- description and source-code
```javascript
clone = function () {

  return new Complex(this["re"], this["im"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.conjugate"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>conjugate ()](#apidoc.element.mathjs.type.Complex.prototype.conjugate)
- description and source-code
```javascript
conjugate = function () {

  return new Complex(this["re"], -this["im"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.cos"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>cos ()](#apidoc.element.mathjs.type.Complex.prototype.cos)
- description and source-code
```javascript
cos = function () {

  var a = this["re"];
  var b = this["im"];

  return new Complex(
          Math.cos(a) * Math.cosh(b),
         -Math.sin(a) * Math.sinh(b));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.cosh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>cosh ()](#apidoc.element.mathjs.type.Complex.prototype.cosh)
- description and source-code
```javascript
cosh = function () {

  var a = this["re"];
  var b = this["im"];

  return new Complex(
          Math.cosh(a) * Math.cos(b),
          Math.sinh(a) * Math.sin(b));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.cot"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>cot ()](#apidoc.element.mathjs.type.Complex.prototype.cot)
- description and source-code
```javascript
cot = function () {

  var a = 2 * this["re"];
  var b = 2 * this["im"];
  var d = Math.cos(a) - Math.cosh(b);

  return new Complex(
         -Math.sin(a) / d,
          Math.sinh(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.coth"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>coth ()](#apidoc.element.mathjs.type.Complex.prototype.coth)
- description and source-code
```javascript
coth = function () {

  var a = 2 * this["re"];
  var b = 2 * this["im"];
  var d = Math.cosh(a) - Math.cos(b);

  return new Complex(
          Math.sinh(a) / d,
         -Math.sin(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.csc"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>csc ()](#apidoc.element.mathjs.type.Complex.prototype.csc)
- description and source-code
```javascript
csc = function () {

  var a = this["re"];
  var b = this["im"];
  var d = 0.5 * Math.cosh(2 * b) - 0.5 * Math.cos(2 * a);

  return new Complex(
          Math.sin(a) * Math.cosh(b) / d,
         -Math.cos(a) * Math.sinh(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.csch"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>csch ()](#apidoc.element.mathjs.type.Complex.prototype.csch)
- description and source-code
```javascript
csch = function () {

  var a = this["re"];
  var b = this["im"];
  var d = Math.cos(2 * b) - Math.cosh(2 * a);

  return new Complex(
       -2 * Math.sinh(a) * Math.cos(b) / d,
        2 * Math.cosh(a) * Math.sin(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.div"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>div (a, b)](#apidoc.element.mathjs.type.Complex.prototype.div)
- description and source-code
```javascript
div = function (a, b) {

  parse(a, b); // mutates P

  a = this["re"];
  b = this["im"];

  var c = P["re"];
  var d = P["im"];
  var t, x;

  // Divisor is zero
  if (0 === c && 0 === d) {
    return new Complex(
            (a !== 0) ? (a / 0) : 0,
            (b !== 0) ? (b / 0) : 0);
  }

  // Divisor is rational
  if (0 === d) {
    return new Complex(a / c, b / c);
  }

  if (Math.abs(c) < Math.abs(d)) {

    x = c / d;
    t = c * x + d;

    return new Complex(
            (a * x + b) / t,
            (b * x - a) / t);

  } else {

    x = d / c;
    t = d * x + c;

    return new Complex(
            (a + b * x) / t,
            (b - a * x) / t);
  }
}
```
- example usage
```shell
...
  object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
  object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

  // uppercase constants (for compatibility with built-in Math)
  object.lazy(math, 'E',       function () {return math.e;});
  object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
  object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
  object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
  object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
  object.lazy(math, 'PI',      function () {return math.pi});
  object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
  object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
}
else {
  math['Infinity'] = Infinity;
...
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.equals"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>equals (a, b)](#apidoc.element.mathjs.type.Complex.prototype.equals)
- description and source-code
```javascript
equals = function (a, b) {

  parse(a, b); // mutates P

  return Math.abs(P["re"] - this["re"]) <= Complex["EPSILON"] &&
         Math.abs(P["im"] - this["im"]) <= Complex["EPSILON"];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.exp"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>exp ()](#apidoc.element.mathjs.type.Complex.prototype.exp)
- description and source-code
```javascript
exp = function () {

  var tmp = Math.exp(this["re"]);

  if (this["im"] === 0) {
    //return new Complex(tmp, 0);
  }
  return new Complex(
          tmp * Math.cos(this["im"]),
          tmp * Math.sin(this["im"]));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.floor"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>floor (places)](#apidoc.element.mathjs.type.Complex.prototype.floor)
- description and source-code
```javascript
floor = function (places) {

  places = Math.pow(10, places || 0);

  return new Complex(
          Math.floor(this["re"] * places) / places,
          Math.floor(this["im"] * places) / places);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>format (options)](#apidoc.element.mathjs.type.Complex.prototype.format)
- description and source-code
```javascript
format = function (options) {
  var str = '';
  var im = this.im;
  var re = this.re;
  var strRe = format(this.re, options);
  var strIm = format(this.im, options);

  // round either re or im when smaller than the configured precision
  var precision = isNumber(options) ? options : options ? options.precision : null;
  if (precision !== null) {
    var epsilon = Math.pow(10, -precision);
    if (Math.abs(re / im) < epsilon) {
      re = 0;
    }
    if (Math.abs(im / re) < epsilon) {
      im = 0;
    }
  }

  if (im == 0) {
    // real value
    str = strRe;
  } else if (re == 0) {
    // purely complex value
    if (im == 1) {
      str = 'i';
    } else if (im == -1) {
      str = '-i';
    } else {
      str = strIm + 'i';
    }
  } else {
    // complex value
    if (im > 0) {
      if (im == 1) {
        str = strRe + ' + i';
      } else {
        str = strRe + ' + ' + strIm + 'i';
      }
    } else {
      if (im == -1) {
        str = strRe + ' - i';
      } else {
        str = strRe + ' - ' + strIm.substring(1) + 'i';
      }
    }
  }
  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.inverse"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>inverse ()](#apidoc.element.mathjs.type.Complex.prototype.inverse)
- description and source-code
```javascript
inverse = function () {

  var a = this["re"];
  var b = this["im"];

  var d = a * a + b * b;

  return new Complex(
          a !== 0 ? a / d : 0,
          b !== 0 ?-b / d : 0);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.isNaN"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>isNaN ()](#apidoc.element.mathjs.type.Complex.prototype.isNaN)
- description and source-code
```javascript
isNaN = function () {
  return isNaN(this['re']) || isNaN(this['im']);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.log"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>log ()](#apidoc.element.mathjs.type.Complex.prototype.log)
- description and source-code
```javascript
log = function () {

  var a = this["re"];
  var b = this["im"];

  if (b === 0 && a > 0) {
    //return new Complex(Math.log(a), 0);
  }

  return new Complex(
          logHypot(a, b),
          Math.atan2(b, a));
}
```
- example usage
```shell
...
'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
...
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.mul"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>mul (a, b)](#apidoc.element.mathjs.type.Complex.prototype.mul)
- description and source-code
```javascript
mul = function (a, b) {

  parse(a, b); // mutates P

  // Besides the addition/subtraction, this helps having a solution for rational Infinity
  if (P['im'] === 0 && this['im'] === 0) {
    return new Complex(this['re'] * P['re'], 0);
  }

  return new Complex(
          this["re"] * P["re"] - this["im"] * P["im"],
          this["re"] * P["im"] + this["im"] * P["re"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.neg"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>neg ()](#apidoc.element.mathjs.type.Complex.prototype.neg)
- description and source-code
```javascript
neg = function () {

  return new Complex(-this["re"], -this["im"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.pow"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>pow (a, b)](#apidoc.element.mathjs.type.Complex.prototype.pow)
- description and source-code
```javascript
pow = function (a, b) {

  parse(a, b); // mutates P

  a = this["re"];
  b = this["im"];

  if (a === 0 && b === 0) {
    return new Complex(0, 0);
  }

  var arg = Math.atan2(b, a);
  var loh = logHypot(a, b);

  if (P["im"] === 0) {

    if (b === 0 && a >= 0) {

      return new Complex(Math.pow(a, P["re"]), 0);

    } else if (a === 0) {

      switch (P["re"] % 4) {
        case 0:
          return new Complex(Math.pow(b, P["re"]), 0);
        case 1:
          return new Complex(0, Math.pow(b, P["re"]));
        case 2:
          return new Complex(-Math.pow(b, P["re"]), 0);
        case 3:
          return new Complex(0, -Math.pow(b, P["re"]));
      }
    }
  }

<span class="apidocCodeCommentSpan">  /* I couldn"t find a good formula, so here is a derivation and optimization
   *
   * z_1^z_2 = (a + bi)^(c + di)
   *         = exp((c + di) * log(a + bi)
   *         = pow(a^2 + b^2, (c + di) / 2) * exp(i(c + di)atan2(b, a))
   * =>...
   * Re = (pow(a^2 + b^2, c / 2) * exp(-d * atan2(b, a))) * cos(d * log(a^2 + b^2) / 2 + c * atan2(b, a))
   * Im = (pow(a^2 + b^2, c / 2) * exp(-d * atan2(b, a))) * sin(d * log(a^2 + b^2) / 2 + c * atan2(b, a))
   *
   * =>...
   * Re = exp(c * log(sqrt(a^2 + b^2)) - d * atan2(b, a)) * cos(d * log(sqrt(a^2 + b^2)) + c * atan2(b, a))
   * Im = exp(c * log(sqrt(a^2 + b^2)) - d * atan2(b, a)) * sin(d * log(sqrt(a^2 + b^2)) + c * atan2(b, a))
   *
   * =>
   * Re = exp(c * logsq2 - d * arg(z_1)) * cos(d * logsq2 + c * arg(z_1))
   * Im = exp(c * logsq2 - d * arg(z_1)) * sin(d * logsq2 + c * arg(z_1))
   *
   */
</span>
  a = Math.exp(P["re"] * loh - P["im"] * arg);
  b = P["im"] * loh + P["re"] * arg;
  return new Complex(
          a * Math.cos(b),
          a * Math.sin(b));
}
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.round"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>round (places)](#apidoc.element.mathjs.type.Complex.prototype.round)
- description and source-code
```javascript
round = function (places) {

  places = Math.pow(10, places || 0);

  return new Complex(
          Math.round(this["re"] * places) / places,
          Math.round(this["im"] * places) / places);
}
```
- example usage
```shell
...
[chained operations](http://mathjs.org/docs/chained_operations.html).

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
...
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sec"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sec ()](#apidoc.element.mathjs.type.Complex.prototype.sec)
- description and source-code
```javascript
sec = function () {

  var a = this["re"];
  var b = this["im"];
  var d = 0.5 * Math.cosh(2 * b) + 0.5 * Math.cos(2 * a);

  return new Complex(
          Math.cos(a) * Math.cosh(b) / d,
          Math.sin(a) * Math.sinh(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sech"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sech ()](#apidoc.element.mathjs.type.Complex.prototype.sech)
- description and source-code
```javascript
sech = function () {

  var a = this["re"];
  var b = this["im"];
  var d = Math.cos(2 * b) + Math.cosh(2 * a);

  return new Complex(
          2 * Math.cosh(a) * Math.cos(b) / d,
         -2 * Math.sinh(a) * Math.sin(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sign"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sign ()](#apidoc.element.mathjs.type.Complex.prototype.sign)
- description and source-code
```javascript
sign = function () {

  var abs = this["abs"]();

  return new Complex(
          this["re"] / abs,
          this["im"] / abs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sin"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sin ()](#apidoc.element.mathjs.type.Complex.prototype.sin)
- description and source-code
```javascript
sin = function () {

  var a = this["re"];
  var b = this["im"];

  return new Complex(
          Math.sin(a) * Math.cosh(b),
          Math.cos(a) * Math.sinh(b));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sinh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sinh ()](#apidoc.element.mathjs.type.Complex.prototype.sinh)
- description and source-code
```javascript
sinh = function () {

  var a = this["re"];
  var b = this["im"];

  return new Complex(
          Math.sinh(a) * Math.cos(b),
          Math.cosh(a) * Math.sin(b));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sqrt"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sqrt ()](#apidoc.element.mathjs.type.Complex.prototype.sqrt)
- description and source-code
```javascript
sqrt = function () {

  var a = this["re"];
  var b = this["im"];
  var r = this["abs"]();

  var re, im;

  if (a >= 0 && b === 0) {
    return new Complex(Math.sqrt(a), 0);
  }

  if (a >= 0) {
    re = 0.5 * Math.sqrt(2.0 * (r + a));
  } else {
    re = Math.abs(b) / Math.sqrt(2 * (r - a));
  }

  if (a <= 0) {
    im = 0.5 * Math.sqrt(2.0 * (r - a));
  } else {
    im = Math.abs(b) / Math.sqrt(2 * (r + a));
  }

  return new Complex(re, b >= 0 ? im : -im);
}
```
- example usage
```shell
...
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
...
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.sub"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>sub (a, b)](#apidoc.element.mathjs.type.Complex.prototype.sub)
- description and source-code
```javascript
sub = function (a, b) {

  parse(a, b); // mutates P

  return new Complex(
          this["re"] - P["re"],
          this["im"] - P["im"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.tan"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>tan ()](#apidoc.element.mathjs.type.Complex.prototype.tan)
- description and source-code
```javascript
tan = function () {

  var a = 2 * this["re"];
  var b = 2 * this["im"];
  var d = Math.cos(a) + Math.cosh(b);

  return new Complex(
          Math.sin(a) / d,
          Math.sinh(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.tanh"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>tanh ()](#apidoc.element.mathjs.type.Complex.prototype.tanh)
- description and source-code
```javascript
tanh = function () {

  var a = 2 * this["re"];
  var b = 2 * this["im"];
  var d = Math.cosh(a) + Math.cos(b);

  return new Complex(
          Math.sinh(a) / d,
          Math.sin(b) / d);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Complex.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'Complex',
    re: this.re,
    im: this.im
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.toPolar"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toPolar ()](#apidoc.element.mathjs.type.Complex.prototype.toPolar)
- description and source-code
```javascript
toPolar = function () {
  return {
    r: this.abs(),
    phi: this.arg()
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toString ()](#apidoc.element.mathjs.type.Complex.prototype.toString)
- description and source-code
```javascript
toString = function () {

  var a = this["re"];
  var b = this["im"];
  var ret = "";

  if (isNaN(a) || isNaN(b)) {
    return "NaN";
  }

  if (a !== 0) {
    ret+= a;
  }

  if (b !== 0) {

    if (a !== 0) {
      ret+= b < 0 ? " - " : " + ";
    } else if (b < 0) {
      ret+= "-";
    }

    b = Math.abs(b);

    if (1 !== b) {
      ret+= b;
    }
    ret+= "i";
  }

  if (!ret)
    return "0";

  return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.toVector"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>toVector ()](#apidoc.element.mathjs.type.Complex.prototype.toVector)
- description and source-code
```javascript
toVector = function () {

  return [this["re"], this["im"]];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Complex.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Complex.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Complex.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {

  var a = this["re"];
  var b = this["im"];
  var ret = "";

  if (isNaN(a) || isNaN(b)) {
    return "NaN";
  }

  if (a !== 0) {
    ret+= a;
  }

  if (b !== 0) {

    if (a !== 0) {
      ret+= b < 0 ? " - " : " + ";
    } else if (b < 0) {
      ret+= "-";
    }

    b = Math.abs(b);

    if (1 !== b) {
      ret+= b;
    }
    ret+= "i";
  }

  if (!ret)
    return "0";

  return ret;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.DenseMatrix"></a>[module mathjs.type.DenseMatrix](#apidoc.module.mathjs.type.DenseMatrix)

#### <a name="apidoc.element.mathjs.type.DenseMatrix.DenseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>DenseMatrix (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix.DenseMatrix)
- description and source-code
```javascript
function DenseMatrix(data, datatype) {
  if (!(this instanceof DenseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if (data && data.isMatrix === true) {
    // check data is a DenseMatrix
    if (data.type === 'DenseMatrix') {
      // clone data & size
      this._data = object.clone(data._data);
      this._size = object.clone(data._size);
      this._datatype = datatype || data._datatype;
    }
    else {
      // build data from existing matrix
      this._data = data.toArray();
      this._size = data.size();
      this._datatype = datatype || data._datatype;
    }
  }
  else if (data && isArray(data.data) && isArray(data.size)) {
    // initialize fields from JSON representation
    this._data = data.data;
    this._size = data.size;
    this._datatype = datatype || data.datatype;
  }
  else if (isArray(data)) {
    // replace nested Matrices with Arrays
    this._data = preprocess(data);
    // get the dimensions of the array
    this._size = array.size(this._data);
    // verify the dimensions of the array, TODO: compute size while processing array
    array.validate(this._data, this._size);
    // data type unknown
    this._datatype = datatype;
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._data = [];
    this._size = [0];
    this._datatype = datatype;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix._swapRows"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.</span>_swapRows (i, j, data)](#apidoc.element.mathjs.type.DenseMatrix._swapRows)
- description and source-code
```javascript
_swapRows = function (i, j, data) {
  // swap values i <-> j
  var vi = data[i];
  data[i] = data[j];
  data[j] = vi;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.diagonal"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.</span>diagonal (size, value, k, defaultValue, datatype)](#apidoc.element.mathjs.type.DenseMatrix.diagonal)
- description and source-code
```javascript
diagonal = function (size, value, k, defaultValue, datatype) {
  if (!isArray(size))
    throw new TypeError('Array expected, size parameter');
  if (size.length !== 2)
    throw new Error('Only two dimensions matrix are supported');

  // map size & validate
  size = size.map(function (s) {
    // check it is a big number
    if (s && s.isBigNumber === true) {
      // convert it
      s = s.toNumber();
    }
    // validate arguments
    if (!isNumber(s) || !isInteger(s) || s < 1) {
      throw new Error('Size values must be positive integers');
    }
    return s;
  });

  // validate k if any
  if (k) {
    // convert BigNumber to a number
    if (k && k.isBigNumber === true)
      k = k.toNumber();
    // is must be an integer
    if (!isNumber(k) || !isInteger(k)) {
      throw new TypeError ('The parameter k must be an integer number');
    }
  }
  else {
    // default value
    k = 0;
  }

  if (defaultValue && isString(datatype)) {
    // convert defaultValue to the same datatype
    defaultValue = typed.convert(defaultValue, datatype);
  }

  var kSuper = k > 0 ? k : 0;
  var kSub = k < 0 ? -k : 0;

  // rows and columns
  var rows = size[0];
  var columns = size[1];

  // number of non-zero items
  var n = Math.min(rows - kSub, columns -  kSuper);

  // value extraction function
  var _value;

  // check value
  if (isArray(value)) {
    // validate array
    if (value.length !== n) {
      // number of values in array must be n
      throw new Error('Invalid value array length');
    }
    // define function
    _value = function (i) {
      // return value @ i
      return value[i];
    };
  }
  else if (value && value.isMatrix === true) {
    // matrix size
    var ms = value.size();
    // validate matrix
    if (ms.length !== 1 || ms[0] !== n) {
      // number of values in array must be n
      throw new Error('Invalid matrix length');
    }
    // define function
    _value = function (i) {
      // return value @ i
      return value.get([i]);
    };
  }
  else {
    // define function
    _value = function () {
      // return value
      return value;
    };
  }

  // discover default value if needed
  if (!defaultValue) {
    // check first value in array
    defaultValue = (_value(0) && _value(0).isBigNumber === true) ? new type.BigNumber(0) : 0;
  }

  // empty array
  var data = [];

  // check we need to resize array
  if (size.length > 0) {
    // resize array
    data = array.resize(data, size, defaultValue);
    // fill diagonal
    for (var d = 0; d < n; d++) {
      data[d + kSub][d + kSuper] = _value(d);
    }
  }

  // create DenseMatrix
  return new DenseMatrix({
    data: data,
    size: [rows, columns]
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.</span>fromJSON (json)](#apidoc.element.mathjs.type.DenseMatrix.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new DenseMatrix(json);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.DenseMatrix.prototype"></a>[module mathjs.type.DenseMatrix.prototype](#apidoc.module.mathjs.type.DenseMatrix.prototype)

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var m = new DenseMatrix({
    data: object.clone(this._data),
    size: object.clone(this._size),
    datatype: this._datatype
  });
  return m;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.create"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>create (data, datatype)](#apidoc.element.mathjs.type.DenseMatrix.prototype.create)
- description and source-code
```javascript
create = function (data, datatype) {
  return new DenseMatrix(data, datatype);
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.datatype"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>datatype ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.datatype)
- description and source-code
```javascript
datatype = function () {
  return this._datatype;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.diagonal"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>diagonal (k)](#apidoc.element.mathjs.type.DenseMatrix.prototype.diagonal)
- description and source-code
```javascript
diagonal = function (k) {
  // validate k if any
  if (k) {
    // convert BigNumber to a number
    if (k.isBigNumber === true)
      k = k.toNumber();
    // is must be an integer
    if (!isNumber(k) || !isInteger(k)) {
      throw new TypeError ('The parameter k must be an integer number');
    }
  }
  else {
    // default value
    k = 0;
  }

  var kSuper = k > 0 ? k : 0;
  var kSub = k < 0 ? -k : 0;

  // rows & columns
  var rows = this._size[0];
  var columns = this._size[1];

  // number diagonal values
  var n = Math.min(rows - kSub, columns -  kSuper);

  // x is a matrix get diagonal from matrix
  var data = [];

  // loop rows
  for (var i = 0; i < n; i++) {
    data[i] = this._data[i + kSub][i + kSuper];
  }

  // create DenseMatrix
  return new DenseMatrix({
    data: data,
    size: [n],
    datatype: this._datatype
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.DenseMatrix.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  // matrix instance
  var me = this;
  var recurse = function (value, index) {
    if (isArray(value)) {
      value.forEach(function (child, i) {
        recurse(child, index.concat(i));
      });
    }
    else {
      callback(value, index, me);
    }
  };
  recurse(this._data, []);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>format (options)](#apidoc.element.mathjs.type.DenseMatrix.prototype.format)
- description and source-code
```javascript
format = function (options) {
  return string.format(this._data, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.get"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>get (index)](#apidoc.element.mathjs.type.DenseMatrix.prototype.get)
- description and source-code
```javascript
get = function (index) {
  if (!isArray(index))
    throw new TypeError('Array expected');
  if (index.length != this._size.length)
    throw new DimensionError(index.length, this._size.length);

  // check index
  for (var x = 0; x < index.length; x++)
    validateIndex(index[x], this._size[x]);

  var data = this._data;
  for (var i = 0, ii = index.length; i < ii; i++) {
    var index_i = index[i];
    validateIndex(index_i, data.length);
    data = data[index_i];
  }

  return data;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>map (callback)](#apidoc.element.mathjs.type.DenseMatrix.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  // matrix instance
  var me = this;
  var recurse = function (value, index) {
    if (isArray(value)) {
      return value.map(function (child, i) {
        return recurse(child, index.concat(i));
      });
    }
    else {
      return callback(value, index, me);
    }
  };
  // return dense format
  return new DenseMatrix({
    data: recurse(this._data, []),
    size: object.clone(this._size),
    datatype: this._datatype
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.reshape"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>reshape (size, copy)](#apidoc.element.mathjs.type.DenseMatrix.prototype.reshape)
- description and source-code
```javascript
reshape = function (size, copy) {
  var m = copy ? this.clone() : this;

  m._data = array.reshape(m._data, size);
  m._size = size.slice(0);
  return m;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.resize"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>resize (size, defaultValue, copy)](#apidoc.element.mathjs.type.DenseMatrix.prototype.resize)
- description and source-code
```javascript
resize = function (size, defaultValue, copy) {
  // validate arguments
  if (!isArray(size))
    throw new TypeError('Array expected');

  // matrix to resize
  var m = copy ? this.clone() : this;
  // resize matrix
  return _resize(m, size, defaultValue);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.set"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>set (index, value, defaultValue)](#apidoc.element.mathjs.type.DenseMatrix.prototype.set)
- description and source-code
```javascript
set = function (index, value, defaultValue) {
  if (!isArray(index))
    throw new TypeError('Array expected');
  if (index.length < this._size.length)
    throw new DimensionError(index.length, this._size.length, '<');

  var i, ii, index_i;

  // enlarge matrix when needed
  var size = index.map(function (i) {
    return i + 1;
  });
  _fit(this, size, defaultValue);

  // traverse over the dimensions
  var data = this._data;
  for (i = 0, ii = index.length - 1; i < ii; i++) {
    index_i = index[i];
    validateIndex(index_i, data.length);
    data = data[index_i];
  }

  // set new value
  index_i = index[index.length - 1];
  validateIndex(index_i, data.length);
  data[index_i] = value;

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>size ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.size)
- description and source-code
```javascript
size = function () {
  return this._size.slice(0); // return a clone of _size
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.storage"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>storage ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.storage)
- description and source-code
```javascript
storage = function () {
  return 'dense';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.subset"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>subset (index, replacement, defaultValue)](#apidoc.element.mathjs.type.DenseMatrix.prototype.subset)
- description and source-code
```javascript
subset = function (index, replacement, defaultValue) {
  switch (arguments.length) {
    case 1:
      return _get(this, index);

      // intentional fall through
    case 2:
    case 3:
      return _set(this, index, replacement, defaultValue);

    default:
      throw new SyntaxError('Wrong number of arguments');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.swapRows"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>swapRows (i, j)](#apidoc.element.mathjs.type.DenseMatrix.prototype.swapRows)
- description and source-code
```javascript
swapRows = function (i, j) {
  // check index
  if (!isNumber(i) || !isInteger(i) || !isNumber(j) || !isInteger(j)) {
    throw new Error('Row index must be positive integers');
  }
  // check dimensions
  if (this._size.length !== 2) {
    throw new Error('Only two dimensional matrix is supported');
  }
  // validate index
  validateIndex(i, this._size[0]);
  validateIndex(j, this._size[0]);

  // swap rows
  DenseMatrix._swapRows(i, j, this._data);
  // return current instance
  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.toArray"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>toArray ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.toArray)
- description and source-code
```javascript
toArray = function () {
  return object.clone(this._data);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'DenseMatrix',
    data: this._data,
    size: this._size,
    datatype: this._datatype
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>toString ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return string.format(this._data);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.DenseMatrix.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.DenseMatrix.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.DenseMatrix.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  return this._data;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.FibonacciHeap"></a>[module mathjs.type.FibonacciHeap](#apidoc.module.mathjs.type.FibonacciHeap)

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.FibonacciHeap"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>FibonacciHeap ()](#apidoc.element.mathjs.type.FibonacciHeap.FibonacciHeap)
- description and source-code
```javascript
function FibonacciHeap() {
  if (!(this instanceof FibonacciHeap))
    throw new SyntaxError('Constructor must be called with the new operator');

  // initialize fields
  this._minimum = null;
  this._size = 0;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.FibonacciHeap.prototype"></a>[module mathjs.type.FibonacciHeap.prototype](#apidoc.module.mathjs.type.FibonacciHeap.prototype)

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.prototype.clear"></a>[function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>clear ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.clear)
- description and source-code
```javascript
clear = function () {
  this._minimum = null;
  this._size = 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.prototype.extractMinimum"></a>[function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>extractMinimum ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.extractMinimum)
- description and source-code
```javascript
extractMinimum = function () {
  // node to remove
  var node = this._minimum;
  // check we have a minimum
  if (node === null)
    return node;
  // current minimum
  var minimum = this._minimum;
  // get number of children
  var numberOfChildren = node.degree;
  // pointer to the first child
  var x = node.child;
  // for each child of node do...
  while (numberOfChildren > 0) {
    // store node in right side
    var tempRight = x.right;
    // remove x from child list
    x.left.right = x.right;
    x.right.left = x.left;
    // add x to root list of heap
    x.left = minimum;
    x.right = minimum.right;
    minimum.right = x;
    x.right.left = x;
    // set Parent[x] to null
    x.parent = null;
    x = tempRight;
    numberOfChildren--;
  }
  // remove node from root list of heap
  node.left.right = node.right;
  node.right.left = node.left;
  // update minimum
  if (node == node.right) {
    // empty
    minimum = null;
  }
  else {
    // update minimum
    minimum = node.right;
    // we need to update the pointer to the root with minimum key
    minimum = _findMinimumNode(minimum, this._size);
  }
  // decrement size of heap
  this._size--;
  // update minimum
  this._minimum = minimum;
  // return node
  return node;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.prototype.insert"></a>[function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>insert (key, value)](#apidoc.element.mathjs.type.FibonacciHeap.prototype.insert)
- description and source-code
```javascript
insert = function (key, value) {
  // create node
  var node = {
    key: key,
    value: value,
    degree: 0
  };
  // check we have a node in the minimum
  if (this._minimum) {
    // minimum node
    var minimum = this._minimum;
    // update left & right of node
    node.left = minimum;
    node.right = minimum.right;
    minimum.right = node;
    node.right.left = node;
    // update minimum node in heap if needed
    if (smaller(key, minimum.key)) {
      // node has a smaller key, use it as minimum
      this._minimum = node;
    }
  }
  else {
    // set left & right
    node.left = node;
    node.right = node;
    // this is the first node
    this._minimum = node;
  }
  // increment number of nodes in heap
  this._size++;
  // return node
  return node;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.prototype.isEmpty"></a>[function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>isEmpty ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.isEmpty)
- description and source-code
```javascript
isEmpty = function () {
  return !!this._minimum;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.prototype.remove"></a>[function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>remove (node)](#apidoc.element.mathjs.type.FibonacciHeap.prototype.remove)
- description and source-code
```javascript
remove = function (node) {
  // decrease key value
  this._minimum = _decreaseKey(this._minimum, node, -1);
  // remove the smallest
  this.extractMinimum();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.FibonacciHeap.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.FibonacciHeap.prototype.</span>size ()](#apidoc.element.mathjs.type.FibonacciHeap.prototype.size)
- description and source-code
```javascript
size = function () {
  return this._size;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Fraction"></a>[module mathjs.type.Fraction](#apidoc.module.mathjs.type.Fraction)

#### <a name="apidoc.element.mathjs.type.Fraction.Fraction"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Fraction (a, b)](#apidoc.element.mathjs.type.Fraction.Fraction)
- description and source-code
```javascript
function Fraction(a, b) {

  if (!(this instanceof Fraction)) {
    return new Fraction(a, b);
  }

  parse(a, b);

  if (Fraction['REDUCE']) {
    a = gcd(P["d"], P["n"]); // Abuse a
  } else {
    a = 1;
  }

  this["s"] = P["s"];
  this["n"] = P["n"] / a;
  this["d"] = P["d"] / a;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.DivisionByZero"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>DivisionByZero ()](#apidoc.element.mathjs.type.Fraction.DivisionByZero)
- description and source-code
```javascript
DivisionByZero = function () {
  var temp = Error.apply(this, arguments);
  temp.name = this.name = name;
  this.stack = temp.stack;
  this.message = temp.message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.InvalidParameter"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>InvalidParameter ()](#apidoc.element.mathjs.type.Fraction.InvalidParameter)
- description and source-code
```javascript
InvalidParameter = function () {
  var temp = Error.apply(this, arguments);
  temp.name = this.name = name;
  this.stack = temp.stack;
  this.message = temp.message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.</span>fromJSON (json)](#apidoc.element.mathjs.type.Fraction.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new Fraction(json);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Fraction.prototype"></a>[module mathjs.type.Fraction.prototype](#apidoc.module.mathjs.type.Fraction.prototype)

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.abs"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>abs ()](#apidoc.element.mathjs.type.Fraction.prototype.abs)
- description and source-code
```javascript
abs = function () {

  return new Fraction(this["n"], this["d"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.add"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>add (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.add)
- description and source-code
```javascript
add = function (a, b) {

  parse(a, b);
  return new Fraction(
          this["s"] * this["n"] * P["d"] + P["s"] * this["d"] * P["n"],
          this["d"] * P["d"]
          );
}
```
- example usage
```shell
...
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.ceil"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>ceil (places)](#apidoc.element.mathjs.type.Fraction.prototype.ceil)
- description and source-code
```javascript
ceil = function (places) {

  places = Math.pow(10, places || 0);

  if (isNaN(this["n"]) || isNaN(this["d"])) {
    return new Fraction(NaN);
  }
  return new Fraction(Math.ceil(places * this["s"] * this["n"] / this["d"]), places);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>clone ()](#apidoc.element.mathjs.type.Fraction.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new Fraction(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.compare"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>compare (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.compare)
- description and source-code
```javascript
compare = function (a, b) {

  parse(a, b);
  var t = (this["s"] * this["n"] * P["d"] - P["s"] * P["n"] * this["d"]);
  return (0 < t) - (t < 0);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.div"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>div (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.div)
- description and source-code
```javascript
div = function (a, b) {

  parse(a, b);
  return new Fraction(
          this["s"] * P["s"] * this["n"] * P["d"],
          this["d"] * P["n"]
          );
}
```
- example usage
```shell
...
  object.lazy(math, 'e',   function () {return bigConstants.e(type.BigNumber)});
  object.lazy(math, 'phi', function () {return bigConstants.phi(type.BigNumber)}); // golden ratio, (1+sqrt(5))/2

  // uppercase constants (for compatibility with built-in Math)
  object.lazy(math, 'E',       function () {return math.e;});
  object.lazy(math, 'LN2',     function () {return new type.BigNumber(2).ln();});
  object.lazy(math, 'LN10',    function () {return new type.BigNumber(10).ln()});
  object.lazy(math, 'LOG2E',   function () {return new type.BigNumber(1).div(new type.BigNumber(2).ln());});
  object.lazy(math, 'LOG10E',  function () {return new type.BigNumber(1).div(new type.BigNumber(10).ln())});
  object.lazy(math, 'PI',      function () {return math.pi});
  object.lazy(math, 'SQRT1_2', function () {return new type.BigNumber('0.5').sqrt()});
  object.lazy(math, 'SQRT2',   function () {return new type.BigNumber(2).sqrt()});
}
else {
  math['Infinity'] = Infinity;
...
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.divisible"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>divisible (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.divisible)
- description and source-code
```javascript
divisible = function (a, b) {

  parse(a, b);
  return !(!(P["n"] * this["d"]) || ((this["n"] * P["d"]) % (P["n"] * this["d"])));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.equals"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>equals (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.equals)
- description and source-code
```javascript
equals = function (a, b) {

  parse(a, b);
  return this["s"] * this["n"] * P["d"] === P["s"] * P["n"] * this["d"]; // Same as compare() === 0
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.floor"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>floor (places)](#apidoc.element.mathjs.type.Fraction.prototype.floor)
- description and source-code
```javascript
floor = function (places) {

  places = Math.pow(10, places || 0);

  if (isNaN(this["n"]) || isNaN(this["d"])) {
    return new Fraction(NaN);
  }
  return new Fraction(Math.floor(places * this["s"] * this["n"] / this["d"]), places);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.gcd"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>gcd (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.gcd)
- description and source-code
```javascript
gcd = function (a, b) {

  parse(a, b);

  // gcd(a / b, c / d) = gcd(a, c) / lcm(b, d)

  return new Fraction(gcd(P["n"], this["n"]), P["d"] * this["d"] / gcd(P["d"], this["d"]));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.inverse"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>inverse ()](#apidoc.element.mathjs.type.Fraction.prototype.inverse)
- description and source-code
```javascript
inverse = function () {

  return new Fraction(this["s"] * this["d"], this["n"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.lcm"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>lcm (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.lcm)
- description and source-code
```javascript
lcm = function (a, b) {

  parse(a, b);

  // lcm(a / b, c / d) = lcm(a, c) / gcd(b, d)

  if (P["n"] === 0 && this["n"] === 0) {
    return new Fraction;
  }
  return new Fraction(P["n"] * this["n"] / gcd(P["n"], this["n"]), gcd(P["d"], this["d"]));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.mod"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>mod (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.mod)
- description and source-code
```javascript
mod = function (a, b) {

  if (isNaN(this['n']) || isNaN(this['d'])) {
    return new Fraction(NaN);
  }

  if (a === undefined) {
    return new Fraction(this["s"] * this["n"] % this["d"], 1);
  }

  parse(a, b);
  if (0 === P["n"] && 0 === this["d"]) {
    Fraction(0, 0); // Throw DivisionByZero
  }

<span class="apidocCodeCommentSpan">  /*
   * First silly attempt, kinda slow
   *
   return that["sub"]({
   "n": num["n"] * Math.floor((this.n / this.d) / (num.n / num.d)),
   "d": num["d"],
   "s": this["s"]
   });*/
</span>
  /*
   * New attempt: a1 / b1 = a2 / b2 * q + r
   * => b2 * a1 = a2 * b1 * q + b1 * b2 * r
   * => (b2 * a1 % a2 * b1) / (b1 * b2)
   */
  return new Fraction(
          (this["s"] * P["d"] * this["n"]) % (P["n"] * this["d"]),
          P["d"] * this["d"]
          );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.mul"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>mul (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.mul)
- description and source-code
```javascript
mul = function (a, b) {

  parse(a, b);
  return new Fraction(
          this["s"] * P["s"] * this["n"] * P["n"],
          this["d"] * P["d"]
          );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.neg"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>neg ()](#apidoc.element.mathjs.type.Fraction.prototype.neg)
- description and source-code
```javascript
neg = function () {

  return new Fraction(-this["s"] * this["n"], this["d"]);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.pow"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>pow (m)](#apidoc.element.mathjs.type.Fraction.prototype.pow)
- description and source-code
```javascript
pow = function (m) {

  if (m < 0) {
    return new Fraction(Math.pow(this['s'] * this["d"],-m), Math.pow(this["n"],-m));
  } else {
    return new Fraction(Math.pow(this['s'] * this["n"], m), Math.pow(this["d"], m));
  }
}
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.round"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>round (places)](#apidoc.element.mathjs.type.Fraction.prototype.round)
- description and source-code
```javascript
round = function (places) {

  places = Math.pow(10, places || 0);

  if (isNaN(this["n"]) || isNaN(this["d"])) {
    return new Fraction(NaN);
  }
  return new Fraction(Math.round(places * this["s"] * this["n"] / this["d"]), places);
}
```
- example usage
```shell
...
[chained operations](http://mathjs.org/docs/chained_operations.html).

'''js
// load math.js
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
...
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.sub"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>sub (a, b)](#apidoc.element.mathjs.type.Fraction.prototype.sub)
- description and source-code
```javascript
sub = function (a, b) {

  parse(a, b);
  return new Fraction(
          this["s"] * this["n"] * P["d"] - P["s"] * this["d"] * P["n"],
          this["d"] * P["d"]
          );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.toContinued"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toContinued ()](#apidoc.element.mathjs.type.Fraction.prototype.toContinued)
- description and source-code
```javascript
toContinued = function () {

  var t;
  var a = this['n'];
  var b = this['d'];
  var res = [];

  do {
    res.push(Math.floor(a / b));
    t = a % b;
    a = b;
    b = t;
  } while (a !== 1);

  return res;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.toFraction"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toFraction (excludeWhole)](#apidoc.element.mathjs.type.Fraction.prototype.toFraction)
- description and source-code
```javascript
toFraction = function (excludeWhole) {

  var whole, str = "";
  var n = this["n"];
  var d = this["d"];
  if (this["s"] < 0) {
    str+= '-';
  }

  if (d === 1) {
    str+= n;
  } else {

    if (excludeWhole && (whole = Math.floor(n / d)) > 0) {
      str+= whole;
      str+= " ";
      n%= d;
    }

    str+= n;
    str+= '/';
    str+= d;
  }
  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Fraction.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'Fraction',
    n: this.s * this.n,
    d: this.d
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.toLatex"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toLatex (excludeWhole)](#apidoc.element.mathjs.type.Fraction.prototype.toLatex)
- description and source-code
```javascript
toLatex = function (excludeWhole) {

  var whole, str = "";
  var n = this["n"];
  var d = this["d"];
  if (this["s"] < 0) {
    str+= '-';
  }

  if (d === 1) {
    str+= n;
  } else {

    if (excludeWhole && (whole = Math.floor(n / d)) > 0) {
      str+= whole;
      n%= d;
    }

    str+= "\\frac{";
    str+= n;
    str+= '}{';
    str+= d;
    str+= '}';
  }
  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>toString ()](#apidoc.element.mathjs.type.Fraction.prototype.toString)
- description and source-code
```javascript
toString = function () {

  var g;
  var N = this["n"];
  var D = this["d"];

  if (isNaN(N) || isNaN(D)) {
    return "NaN";
  }

  if (!Fraction['REDUCE']) {
    g = gcd(N, D);
    N/= g;
    D/= g;
  }

  var p = String(N).split(""); // Numerator chars
  var t = 0; // Tmp var

  var ret = [~this["s"] ? "" : "-", "", ""]; // Return array, [0] is zero sign, [1] before comma, [2] after
  var zeros = ""; // Collection variable for zeros

  var cycLen = cycleLen(N, D); // Cycle length
  var cycOff = cycleStart(N, D, cycLen); // Cycle start

  var j = -1;
  var n = 1; // str index

  // rough estimate to fill zeros
  var length = 15 + cycLen + cycOff + p.length; // 15 = decimal places when no repitation

  for (var i = 0; i < length; i++, t*= 10) {

    if (i < p.length) {
      t+= Number(p[i]);
    } else {
      n = 2;
      j++; // Start now => after comma
    }

    if (cycLen > 0) { // If we have a repeating part
      if (j === cycOff) {
        ret[n]+= zeros + "(";
        zeros = "";
      } else if (j === cycLen + cycOff) {
        ret[n]+= zeros + ")";
        break;
      }
    }

    if (t >= D) {
      ret[n]+= zeros + ((t / D) | 0); // Flush zeros, Add current digit
      zeros = "";
      t = t % D;
    } else if (n > 1) { // Add zeros to the zero buffer
      zeros+= "0";
    } else if (ret[n]) { // If before comma, add zero only if already something was added
      ret[n]+= "0";
    }
  }

  // If it's empty, it's a leading zero only
  ret[0]+= ret[1] || "0";

  // If there is something after the comma, add the comma sign
  if (ret[2]) {
    return ret[0] + "." + ret[2];
  }
  return ret[0];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Fraction.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Fraction.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Fraction.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {

  return this["s"] * this["n"] / this["d"];
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Help"></a>[module mathjs.type.Help](#apidoc.module.mathjs.type.Help)

#### <a name="apidoc.element.mathjs.type.Help.Help"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Help (doc)](#apidoc.element.mathjs.type.Help.Help)
- description and source-code
```javascript
function Help(doc) {
  if (!(this instanceof Help)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (!doc)  throw new Error('Argument "doc" missing');

  this.doc = doc;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Help.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Help.</span>fromJSON (json)](#apidoc.element.mathjs.type.Help.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  var doc = {};
  for (var prop in json) {
    if (prop !== 'mathjs') { // ignore mathjs field
      doc[prop] = json[prop];
    }
  }
  return new Help(doc);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Help.prototype"></a>[module mathjs.type.Help.prototype](#apidoc.module.mathjs.type.Help.prototype)

#### <a name="apidoc.element.mathjs.type.Help.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Help.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  var obj = object.clone(this.doc);
  obj.mathjs = 'Help';
  return obj;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Help.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>toString ()](#apidoc.element.mathjs.type.Help.prototype.toString)
- description and source-code
```javascript
toString = function () {
  var doc = this.doc || {};
  var desc = '\n';

  if (doc.name) {
    desc += 'Name: ' + doc.name + '\n\n';
  }
  if (doc.category) {
    desc += 'Category: ' + doc.category + '\n\n';
  }
  if (doc.description) {
    desc += 'Description:\n    ' + doc.description + '\n\n';
  }
  if (doc.syntax) {
    desc += 'Syntax:\n    ' + doc.syntax.join('\n    ') + '\n\n';
  }
  if (doc.examples) {
    desc += 'Examples:\n';
    for (var i = 0; i < doc.examples.length; i++) {
      var expr = doc.examples[i];
      desc += '    ' + expr + '\n';

      var res;
      try {
        res = parser.eval(expr);
      }
      catch (e) {
        res = e;
      }
      if (res && !res.isHelp) {
        desc += '        ' + string.format(res, {precision: 14}) + '\n';
      }
    }
    desc += '\n';
  }
  if (doc.seealso) {
    desc += 'See also: ' + doc.seealso.join(', ') + '\n';
  }

  return desc;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Help.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Help.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Help.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  var doc = this.doc || {};
  var desc = '\n';

  if (doc.name) {
    desc += 'Name: ' + doc.name + '\n\n';
  }
  if (doc.category) {
    desc += 'Category: ' + doc.category + '\n\n';
  }
  if (doc.description) {
    desc += 'Description:\n    ' + doc.description + '\n\n';
  }
  if (doc.syntax) {
    desc += 'Syntax:\n    ' + doc.syntax.join('\n    ') + '\n\n';
  }
  if (doc.examples) {
    desc += 'Examples:\n';
    for (var i = 0; i < doc.examples.length; i++) {
      var expr = doc.examples[i];
      desc += '    ' + expr + '\n';

      var res;
      try {
        res = parser.eval(expr);
      }
      catch (e) {
        res = e;
      }
      if (res && !res.isHelp) {
        desc += '        ' + string.format(res, {precision: 14}) + '\n';
      }
    }
    desc += '\n';
  }
  if (doc.seealso) {
    desc += 'See also: ' + doc.seealso.join(', ') + '\n';
  }

  return desc;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.ImmutableDenseMatrix"></a>[module mathjs.type.ImmutableDenseMatrix](#apidoc.module.mathjs.type.ImmutableDenseMatrix)

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.ImmutableDenseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>ImmutableDenseMatrix (data, datatype)](#apidoc.element.mathjs.type.ImmutableDenseMatrix.ImmutableDenseMatrix)
- description and source-code
```javascript
function ImmutableDenseMatrix(data, datatype) {
  if (!(this instanceof ImmutableDenseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if ((data && data.isMatrix === true) || isArray(data)) {
    // use DenseMatrix implementation
    var matrix = new DenseMatrix(data, datatype);
    // internal structures
    this._data = matrix._data;
    this._size = matrix._size;
    this._datatype = matrix._datatype;
    this._min = null;
    this._max = null;
  }
  else if (data && isArray(data.data) && isArray(data.size)) {
    // initialize fields from JSON representation
    this._data = data.data;
    this._size = data.size;
    this._datatype = data.datatype;
    this._min = typeof data.min !== 'undefined' ? data.min : null;
    this._max = typeof data.max !== 'undefined' ? data.max : null;
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._data = [];
    this._size = [0];
    this._datatype = datatype;
    this._min = null;
    this._max = null;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.</span>fromJSON (json)](#apidoc.element.mathjs.type.ImmutableDenseMatrix.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new ImmutableDenseMatrix(json);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.ImmutableDenseMatrix.prototype"></a>[module mathjs.type.ImmutableDenseMatrix.prototype](#apidoc.module.mathjs.type.ImmutableDenseMatrix.prototype)

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var m = new ImmutableDenseMatrix({
    data: object.clone(this._data),
    size: object.clone(this._size),
    datatype: this._datatype
  });
  return m;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.max"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>max ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.max)
- description and source-code
```javascript
max = function () {
  // check max has been calculated before
  if (this._max === null) {
    // maximum
    var m = null;
    // compute max
    this.forEach(function (v) {
      if (m === null || smaller(m, v))
        m = v;
    });
    this._max = m !== null ? m : undefined;
  }
  return this._max;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.min"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>min ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.min)
- description and source-code
```javascript
min = function () {
  // check min has been calculated before
  if (this._min === null) {
    // minimum
    var m = null;
    // compute min
    this.forEach(function (v) {
      if (m === null || smaller(v, m))
        m = v;
    });
    this._min = m !== null ? m : undefined;
  }
  return this._min;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.reshape"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>reshape ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.reshape)
- description and source-code
```javascript
reshape = function () {
  throw new Error('Cannot invoke reshape on an Immutable Matrix instance');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.resize"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>resize ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.resize)
- description and source-code
```javascript
resize = function () {
  throw new Error('Cannot invoke resize on an Immutable Matrix instance');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.set"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>set ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.set)
- description and source-code
```javascript
set = function () {
  throw new Error('Cannot invoke set on an Immutable Matrix instance');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.subset"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>subset (index)](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.subset)
- description and source-code
```javascript
subset = function (index) {
  switch (arguments.length) {
    case 1:
      // use base implementation
      var m = DenseMatrix.prototype.subset.call(this, index);
      // check result is a matrix
      if (m.isMatrix) {
        // return immutable matrix
        return new ImmutableDenseMatrix({
          data: m._data,
          size: m._size,
          datatype: m._datatype
        });
      }
      return m;

      // intentional fall through
    case 2:
    case 3:
      throw new Error('Cannot invoke set subset on an Immutable Matrix instance');

    default:
      throw new SyntaxError('Wrong number of arguments');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.swapRows"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>swapRows ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.swapRows)
- description and source-code
```javascript
swapRows = function () {
  throw new Error('Cannot invoke swapRows on an Immutable Matrix instance');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.ImmutableDenseMatrix.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.ImmutableDenseMatrix.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'ImmutableDenseMatrix',
    data: this._data,
    size: this._size,
    datatype: this._datatype
  };
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Index"></a>[module mathjs.type.Index](#apidoc.module.mathjs.type.Index)

#### <a name="apidoc.element.mathjs.type.Index.Index"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Index (ranges)](#apidoc.element.mathjs.type.Index.Index)
- description and source-code
```javascript
function Index(ranges) {
  if (!(this instanceof Index)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this._dimensions = [];
  this._isScalar = true;

  for (var i = 0, ii = arguments.length; i < ii; i++) {
    var arg = arguments[i];

    if (arg && (arg.isRange === true)) {
      this._dimensions.push(arg);
      this._isScalar = false;
    }
    else if (arg && (Array.isArray(arg) || arg.isMatrix === true)) {
      // create matrix
      var m = _createImmutableMatrix(arg.valueOf());
      this._dimensions.push(m);
      // size
      var size = m.size();
      // scalar
      if (size.length !== 1 || size[0] !== 1) {
        this._isScalar = false;
      }
    }
    else if (typeof arg === 'number') {
      this._dimensions.push(_createImmutableMatrix([arg]));
    }
    else if (typeof arg === 'string') {
      // object property (arguments.count should be 1)
      this._dimensions.push(arg);
    }
    // TODO: implement support for wildcard '*'
    else {
      throw new TypeError('Dimension must be an Array, Matrix, number, string, or Range');
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.create"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.</span>create (ranges)](#apidoc.element.mathjs.type.Index.create)
- description and source-code
```javascript
create = function (ranges) {
  var index = new Index();
  Index.apply(index, ranges);
  return index;
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.type.Index.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.</span>fromJSON (json)](#apidoc.element.mathjs.type.Index.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return Index.create(json.dimensions);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Index.prototype"></a>[module mathjs.type.Index.prototype](#apidoc.module.mathjs.type.Index.prototype)

#### <a name="apidoc.element.mathjs.type.Index.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>clone ()](#apidoc.element.mathjs.type.Index.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var index = new Index();
  index._dimensions = clone(this._dimensions);
  index._isScalar = this._isScalar;
  return index;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.dimension"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>dimension (dim)](#apidoc.element.mathjs.type.Index.prototype.dimension)
- description and source-code
```javascript
dimension = function (dim) {
  return this._dimensions[dim] || null;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.Index.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    callback(this._dimensions[i], i, this);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.getObjectProperty"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>getObjectProperty ()](#apidoc.element.mathjs.type.Index.prototype.getObjectProperty)
- description and source-code
```javascript
getObjectProperty = function () {
  return this.isObjectProperty() ? this._dimensions[0] : null;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.isObjectProperty"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>isObjectProperty ()](#apidoc.element.mathjs.type.Index.prototype.isObjectProperty)
- description and source-code
```javascript
isObjectProperty = function () {
  return this._dimensions.length === 1 && typeof this._dimensions[0] === 'string';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.isScalar"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>isScalar ()](#apidoc.element.mathjs.type.Index.prototype.isScalar)
- description and source-code
```javascript
isScalar = function () {
  return this._isScalar;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.max"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>max ()](#apidoc.element.mathjs.type.Index.prototype.max)
- description and source-code
```javascript
max = function () {
  var values = [];

  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    var range = this._dimensions[i];
    values[i] = (typeof range === 'string') ? range : range.max();
  }

  return values;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.min"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>min ()](#apidoc.element.mathjs.type.Index.prototype.min)
- description and source-code
```javascript
min = function () {
  var values = [];

  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    var range = this._dimensions[i];
    values[i] = (typeof range === 'string') ? range : range.min();
  }

  return values;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>size ()](#apidoc.element.mathjs.type.Index.prototype.size)
- description and source-code
```javascript
size = function () {
  var size = [];

  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    var d = this._dimensions[i];
    size[i] = (typeof d === 'string') ? 1 : d.size()[0];
  }

  return size;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.toArray"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>toArray ()](#apidoc.element.mathjs.type.Index.prototype.toArray)
- description and source-code
```javascript
toArray = function () {
  var array = [];
  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    var dimension = this._dimensions[i];
    array.push((typeof dimension === 'string') ? dimension : dimension.toArray());
  }
  return array;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Index.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'Index',
    dimensions: this._dimensions
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>toString ()](#apidoc.element.mathjs.type.Index.prototype.toString)
- description and source-code
```javascript
toString = function () {
  var strings = [];

  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    var dimension = this._dimensions[i];
    if (typeof dimension === 'string') {
      strings.push(JSON.stringify(dimension));
    }
    else {
      strings.push(dimension.toString());
    }
  }

  return '[' + strings.join(', ') + ']';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Index.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Index.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Index.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  var array = [];
  for (var i = 0, ii = this._dimensions.length; i < ii; i++) {
    var dimension = this._dimensions[i];
    array.push((typeof dimension === 'string') ? dimension : dimension.toArray());
  }
  return array;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Matrix"></a>[module mathjs.type.Matrix](#apidoc.module.mathjs.type.Matrix)

#### <a name="apidoc.element.mathjs.type.Matrix.Matrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Matrix ()](#apidoc.element.mathjs.type.Matrix.Matrix)
- description and source-code
```javascript
function Matrix() {
  if (!(this instanceof Matrix)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.storage"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.</span>storage (format)](#apidoc.element.mathjs.type.Matrix.storage)
- description and source-code
```javascript
storage = function (format) {
  // check storage format is a string
  if (!isString(format)) {
    throw new TypeError('format must be a string value');
  }

  // get storage format constructor
  var constructor = Matrix._storage[format];
  if (!constructor) {
    throw new SyntaxError('Unsupported matrix storage format: ' + format);
  }

  // return storage constructor
  return constructor;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Matrix.prototype"></a>[module mathjs.type.Matrix.prototype](#apidoc.module.mathjs.type.Matrix.prototype)

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.Matrix.prototype.clone)
- description and source-code
```javascript
clone = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke clone on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.create"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>create (data, datatype)](#apidoc.element.mathjs.type.Matrix.prototype.create)
- description and source-code
```javascript
create = function (data, datatype) {
  throw new Error('Cannot invoke create on a Matrix interface');
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.datatype"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>datatype ()](#apidoc.element.mathjs.type.Matrix.prototype.datatype)
- description and source-code
```javascript
datatype = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke datatype on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.Matrix.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke forEach on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>format (options)](#apidoc.element.mathjs.type.Matrix.prototype.format)
- description and source-code
```javascript
format = function (options) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke format on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.get"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>get (index)](#apidoc.element.mathjs.type.Matrix.prototype.get)
- description and source-code
```javascript
get = function (index) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke get on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>map (callback, skipZeros)](#apidoc.element.mathjs.type.Matrix.prototype.map)
- description and source-code
```javascript
map = function (callback, skipZeros) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke map on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.reshape"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>reshape (size, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.reshape)
- description and source-code
```javascript
reshape = function (size, defaultValue) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke reshape on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.resize"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>resize (size, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.resize)
- description and source-code
```javascript
resize = function (size, defaultValue) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke resize on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.set"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>set (index, value, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.set)
- description and source-code
```javascript
set = function (index, value, defaultValue) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke set on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>size ()](#apidoc.element.mathjs.type.Matrix.prototype.size)
- description and source-code
```javascript
size = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke size on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.storage"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>storage ()](#apidoc.element.mathjs.type.Matrix.prototype.storage)
- description and source-code
```javascript
storage = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke storage on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.subset"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>subset (index, replacement, defaultValue)](#apidoc.element.mathjs.type.Matrix.prototype.subset)
- description and source-code
```javascript
subset = function (index, replacement, defaultValue) {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke subset on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.toArray"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>toArray ()](#apidoc.element.mathjs.type.Matrix.prototype.toArray)
- description and source-code
```javascript
toArray = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke toArray on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>toString ()](#apidoc.element.mathjs.type.Matrix.prototype.toString)
- description and source-code
```javascript
toString = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke toString on a Matrix interface');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Matrix.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Matrix.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Matrix.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  // must be implemented by each of the Matrix implementations
  throw new Error('Cannot invoke valueOf on a Matrix interface');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Range"></a>[module mathjs.type.Range](#apidoc.module.mathjs.type.Range)

#### <a name="apidoc.element.mathjs.type.Range.Range"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Range (start, end, step)](#apidoc.element.mathjs.type.Range.Range)
- description and source-code
```javascript
function Range(start, end, step) {
  if (!(this instanceof Range)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  if (start != null) {
    if (start.isBigNumber === true)
      start = start.toNumber();
    else if (typeof start !== 'number')
      throw new TypeError('Parameter start must be a number');
  }
  if (end != null) {
    if (end.isBigNumber === true)
      end = end.toNumber();
    else if (typeof end !== 'number')
      throw new TypeError('Parameter end must be a number');
  }
  if (step != null) {
    if (step.isBigNumber === true)
      step = step.toNumber();
    else if (typeof step !== 'number')
      throw new TypeError('Parameter step must be a number');
  }

  this.start = (start != null) ? parseFloat(start) : 0;
  this.end   = (end != null)   ? parseFloat(end)   : 0;
  this.step  = (step != null)  ? parseFloat(step)  : 1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.</span>fromJSON (json)](#apidoc.element.mathjs.type.Range.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new Range(json.start, json.end, json.step);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.parse"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.</span>parse (str)](#apidoc.element.mathjs.type.Range.parse)
- description and source-code
```javascript
parse = function (str) {
  if (typeof str !== 'string') {
    return null;
  }

  var args = str.split(':');
  var nums = args.map(function (arg) {
    return parseFloat(arg);
  });

  var invalid = nums.some(function (num) {
    return isNaN(num);
  });
  if (invalid) {
    return null;
  }

  switch (nums.length) {
    case 2:
      return new Range(nums[0], nums[1]);
    case 3:
      return new Range(nums[0], nums[2], nums[1]);
    default:
      return null;
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Range.prototype"></a>[module mathjs.type.Range.prototype](#apidoc.module.mathjs.type.Range.prototype)

#### <a name="apidoc.element.mathjs.type.Range.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>clone ()](#apidoc.element.mathjs.type.Range.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new Range(this.start, this.end, this.step);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>forEach (callback)](#apidoc.element.mathjs.type.Range.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback) {
  var x = this.start;
  var step = this.step;
  var end = this.end;
  var i = 0;

  if (step > 0) {
    while (x < end) {
      callback(x, [i], this);
      x += step;
      i++;
    }
  }
  else if (step < 0) {
    while (x > end) {
      callback(x, [i], this);
      x += step;
      i++;
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>format (options)](#apidoc.element.mathjs.type.Range.prototype.format)
- description and source-code
```javascript
format = function (options) {
  var str = number.format(this.start, options);

  if (this.step != 1) {
    str += ':' + number.format(this.step, options);
  }
  str += ':' + number.format(this.end, options);
  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>map (callback)](#apidoc.element.mathjs.type.Range.prototype.map)
- description and source-code
```javascript
map = function (callback) {
  var array = [];
  this.forEach(function (value, index, obj) {
    array[index[0]] = callback(value, index, obj);
  });
  return array;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.max"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>max ()](#apidoc.element.mathjs.type.Range.prototype.max)
- description and source-code
```javascript
max = function () {
  var size = this.size()[0];

  if (size > 0) {
    if (this.step > 0) {
      // positive step
      return this.start + (size - 1) * this.step;
    }
    else {
      // negative step
      return this.start;
    }
  }
  else {
    return undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.min"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>min ()](#apidoc.element.mathjs.type.Range.prototype.min)
- description and source-code
```javascript
min = function () {
  var size = this.size()[0];

  if (size > 0) {
    if (this.step > 0) {
      // positive step
      return this.start;
    }
    else {
      // negative step
      return this.start + (size - 1) * this.step;
    }
  }
  else {
    return undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>size ()](#apidoc.element.mathjs.type.Range.prototype.size)
- description and source-code
```javascript
size = function () {
  var len = 0,
      start = this.start,
      step = this.step,
      end = this.end,
      diff = end - start;

  if (number.sign(step) == number.sign(diff)) {
    len = Math.ceil((diff) / step);
  }
  else if (diff == 0) {
    len = 0;
  }

  if (isNaN(len)) {
    len = 0;
  }
  return [len];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.toArray"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>toArray ()](#apidoc.element.mathjs.type.Range.prototype.toArray)
- description and source-code
```javascript
toArray = function () {
  var array = [];
  this.forEach(function (value, index) {
    array[index[0]] = value;
  });
  return array;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Range.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'Range',
    start: this.start,
    end: this.end,
    step: this.step
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>toString ()](#apidoc.element.mathjs.type.Range.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return this.format();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Range.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Range.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Range.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  // TODO: implement a caching mechanism for range.valueOf()
  return this.toArray();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.ResultSet"></a>[module mathjs.type.ResultSet](#apidoc.module.mathjs.type.ResultSet)

#### <a name="apidoc.element.mathjs.type.ResultSet.ResultSet"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>ResultSet (entries)](#apidoc.element.mathjs.type.ResultSet.ResultSet)
- description and source-code
```javascript
function ResultSet(entries) {
  if (!(this instanceof ResultSet)) {
    throw new SyntaxError('Constructor must be called with the new operator');
  }

  this.entries = entries || [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ResultSet.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.ResultSet.</span>fromJSON (json)](#apidoc.element.mathjs.type.ResultSet.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new ResultSet(json.entries);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.ResultSet.prototype"></a>[module mathjs.type.ResultSet.prototype](#apidoc.module.mathjs.type.ResultSet.prototype)

#### <a name="apidoc.element.mathjs.type.ResultSet.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.ResultSet.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'ResultSet',
    entries: this.entries
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ResultSet.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>toString ()](#apidoc.element.mathjs.type.ResultSet.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return '[' + this.entries.join(', ') + ']';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.ResultSet.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.ResultSet.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.ResultSet.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  return this.entries;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Spa"></a>[module mathjs.type.Spa](#apidoc.module.mathjs.type.Spa)

#### <a name="apidoc.element.mathjs.type.Spa.Spa"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Spa ()](#apidoc.element.mathjs.type.Spa.Spa)
- description and source-code
```javascript
function Spa() {
  if (!(this instanceof Spa))
    throw new SyntaxError('Constructor must be called with the new operator');

  // allocate vector, TODO use typed arrays
  this._values = [];
  this._heap = new type.FibonacciHeap();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Spa.prototype"></a>[module mathjs.type.Spa.prototype](#apidoc.module.mathjs.type.Spa.prototype)

#### <a name="apidoc.element.mathjs.type.Spa.prototype.accumulate"></a>[function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>accumulate (i, v)](#apidoc.element.mathjs.type.Spa.prototype.accumulate)
- description and source-code
```javascript
accumulate = function (i, v) {
  // node @ i
  var node = this._values[i];
  if (!node) {
    // insert in heap
    node = this._heap.insert(i, v);
    // initialize value
    this._values[i] = node;
  }
  else {
    // accumulate value
    node.value = add(node.value, v);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Spa.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>forEach (from, to, callback)](#apidoc.element.mathjs.type.Spa.prototype.forEach)
- description and source-code
```javascript
forEach = function (from, to, callback) {
  // references
  var heap = this._heap;
  var values = this._values;
  // nodes
  var nodes = [];
  // node with minimum key, save it
  var node = heap.extractMinimum();
  if (node)
    nodes.push(node);
  // extract nodes from heap (ordered)
  while (node && node.key <= to) {
    // check it is in range
    if (node.key >= from) {
      // check value is not zero
      if (!equalScalar(node.value, 0)) {
        // invoke callback
        callback(node.key, node.value, this);
      }
    }
    // extract next node, save it
    node = heap.extractMinimum();
    if (node)
      nodes.push(node);
  }
  // reinsert all nodes in heap
  for (var i = 0; i < nodes.length; i++) {
    // current node
    var n = nodes[i];
    // insert node in heap
    node = heap.insert(n.key, n.value);
    // update values
    values[node.key] = node;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Spa.prototype.get"></a>[function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>get (i)](#apidoc.element.mathjs.type.Spa.prototype.get)
- description and source-code
```javascript
get = function (i) {
  var node = this._values[i];
  if (node)
    return node.value;
  return 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Spa.prototype.set"></a>[function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>set (i, v)](#apidoc.element.mathjs.type.Spa.prototype.set)
- description and source-code
```javascript
set = function (i, v) {
  // check we have a value @ i
  if (!this._values[i]) {
    // insert in heap
    var node = this._heap.insert(i, v);
    // set the value @ i
    this._values[i] = node;
  }
  else {
    // update the value @ i
    this._values[i].value = v;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Spa.prototype.swap"></a>[function <span class="apidocSignatureSpan">mathjs.type.Spa.prototype.</span>swap (i, j)](#apidoc.element.mathjs.type.Spa.prototype.swap)
- description and source-code
```javascript
swap = function (i, j) {
  // node @ i and j
  var nodei = this._values[i];
  var nodej = this._values[j];
  // check we need to insert indeces
  if (!nodei && nodej) {
    // insert in heap
    nodei = this._heap.insert(i, nodej.value);
    // remove from heap
    this._heap.remove(nodej);
    // set values
    this._values[i] = nodei;
    this._values[j] = undefined;
  }
  else if (nodei && !nodej) {
    // insert in heap
    nodej = this._heap.insert(j, nodei.value);
    // remove from heap
    this._heap.remove(nodei);
    // set values
    this._values[j] = nodej;
    this._values[i] = undefined;
  }
  else if (nodei && nodej) {
    // swap values
    var v = nodei.value;
    nodei.value = nodej.value;
    nodej.value = v;
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.SparseMatrix"></a>[module mathjs.type.SparseMatrix](#apidoc.module.mathjs.type.SparseMatrix)

#### <a name="apidoc.element.mathjs.type.SparseMatrix.SparseMatrix"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>SparseMatrix (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix.SparseMatrix)
- description and source-code
```javascript
function SparseMatrix(data, datatype) {
  if (!(this instanceof SparseMatrix))
    throw new SyntaxError('Constructor must be called with the new operator');
  if (datatype && !isString(datatype))
    throw new Error('Invalid datatype: ' + datatype);

  if (data && data.isMatrix === true) {
    // create from matrix
    _createFromMatrix(this, data, datatype);
  }
  else if (data && isArray(data.index) && isArray(data.ptr) && isArray(data.size)) {
    // initialize fields
    this._values = data.values;
    this._index = data.index;
    this._ptr = data.ptr;
    this._size = data.size;
    this._datatype = datatype || data.datatype;
  }
  else if (isArray(data)) {
    // create from array
    _createFromArray(this, data, datatype);
  }
  else if (data) {
    // unsupported type
    throw new TypeError('Unsupported type of data (' + util.types.type(data) + ')');
  }
  else {
    // nothing provided
    this._values = [];
    this._index = [];
    this._ptr = [0];
    this._size = [0, 0];
    this._datatype = datatype;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix._forEachRow"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>_forEachRow (j, values, index, ptr, callback)](#apidoc.element.mathjs.type.SparseMatrix._forEachRow)
- description and source-code
```javascript
_forEachRow = function (j, values, index, ptr, callback) {
  // indeces for column j
  var k0 = ptr[j];
  var k1 = ptr[j + 1];
  // loop
  for (var k = k0; k < k1; k++) {
    // invoke callback
    callback(index[k], values[k]);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix._swapRows"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>_swapRows (x, y, columns, values, index, ptr)](#apidoc.element.mathjs.type.SparseMatrix._swapRows)
- description and source-code
```javascript
_swapRows = function (x, y, columns, values, index, ptr) {
  // loop columns
  for (var j = 0; j < columns; j++) {
    // k0 <= k < k1 where k0 = _ptr[j] && k1 = _ptr[j+1]
    var k0 = ptr[j];
    var k1 = ptr[j + 1];
    // find value index @ x
    var kx = _getValueIndex(x, k0, k1, index);
    // find value index @ x
    var ky = _getValueIndex(y, k0, k1, index);
    // check both rows exist in matrix
    if (kx < k1 && ky < k1 && index[kx] === x && index[ky] === y) {
      // swap values (check for pattern matrix)
      if (values) {
        var v = values[kx];
        values[kx] = values[ky];
        values[ky] = v;
      }
      // next column
      continue;
    }
    // check x row exist & no y row
    if (kx < k1 && index[kx] === x && (ky >= k1 || index[ky] !== y)) {
      // value @ x (check for pattern matrix)
      var vx = values ? values[kx] : undefined;
      // insert value @ y
      index.splice(ky, 0, y);
      if (values)
        values.splice(ky, 0, vx);
      // remove value @ x (adjust array index if needed)
      index.splice(ky <= kx ? kx + 1 : kx, 1);
      if (values)
        values.splice(ky <= kx ? kx + 1 : kx, 1);
      // next column
      continue;
    }
    // check y row exist & no x row
    if (ky < k1 && index[ky] === y && (kx >= k1 || index[kx] !== x)) {
      // value @ y (check for pattern matrix)
      var vy = values ? values[ky] : undefined;
      // insert value @ x
      index.splice(kx, 0, x);
      if (values)
        values.splice(kx, 0, vy);
      // remove value @ y (adjust array index if needed)
      index.splice(kx <= ky ? ky + 1 : ky, 1);
      if (values)
        values.splice(kx <= ky ? ky + 1 : ky, 1);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.diagonal"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>diagonal (size, value, k, defaultValue, datatype)](#apidoc.element.mathjs.type.SparseMatrix.diagonal)
- description and source-code
```javascript
diagonal = function (size, value, k, defaultValue, datatype) {
  if (!isArray(size))
    throw new TypeError('Array expected, size parameter');
  if (size.length !== 2)
    throw new Error('Only two dimensions matrix are supported');

  // map size & validate
  size = size.map(function (s) {
    // check it is a big number
    if (s && s.isBigNumber === true) {
      // convert it
      s = s.toNumber();
    }
    // validate arguments
    if (!isNumber(s) || !isInteger(s) || s < 1) {
      throw new Error('Size values must be positive integers');
    }
    return s;
  });

  // validate k if any
  if (k) {
    // convert BigNumber to a number
    if (k.isBigNumber === true)
      k = k.toNumber();
    // is must be an integer
    if (!isNumber(k) || !isInteger(k)) {
      throw new TypeError ('The parameter k must be an integer number');
    }
  }
  else {
    // default value
    k = 0;
  }

  // equal signature to use
  var eq = equalScalar;
  // zero value
  var zero = 0;

  if (isString(datatype)) {
    // find signature that matches (datatype, datatype)
    eq = typed.find(equalScalar, [datatype, datatype]) || equalScalar;
    // convert 0 to the same datatype
    zero = typed.convert(0, datatype);
  }

  var kSuper = k > 0 ? k : 0;
  var kSub = k < 0 ? -k : 0;

  // rows and columns
  var rows = size[0];
  var columns = size[1];

  // number of non-zero items
  var n = Math.min(rows - kSub, columns -  kSuper);

  // value extraction function
  var _value;

  // check value
  if (isArray(value)) {
    // validate array
    if (value.length !== n) {
      // number of values in array must be n
      throw new Error('Invalid value array length');
    }
    // define function
    _value = function (i) {
      // return value @ i
      return value[i];
    };
  }
  else if (value && value.isMatrix === true) {
    // matrix size
    var ms = value.size();
    // validate matrix
    if (ms.length !== 1 || ms[0] !== n) {
      // number of values in array must be n
      throw new Error('Invalid matrix length');
    }
    // define function
    _value = function (i) {
      // return value @ i
      return value.get([i]);
    };
  }
  else {
    // define function
    _value = function () {
      // return value
      return value;
    };
  }

  // create arrays
  var values = [];
  var index = [];
  var ptr = [];

  // loop items
  for (var j = 0; j < columns; j++) {
    // number of rows with value
    ptr.push(values.length);
    // diagonal index
    var i = j - kSuper;
    // check we need to set diagonal value
    if (i >= 0 && i < n) {
      // get value @ i
      var v = _value(i);
      // check for zero
      if (!eq(v, zero)) {
        // column
        index.push(i + kSub);
        // add value
        values.push(v);
      }
    }
  }
  // last value should be number of values
  ptr.push(values.length);
  // create SparseMatrix
  return new SparseMatrix({
    values: values,
    index: index,
    ptr: ptr,
    size: [rows, columns]
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.</span>fromJSON (json)](#apidoc.element.mathjs.type.SparseMatrix.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  return new SparseMatrix(json);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.SparseMatrix.prototype"></a>[module mathjs.type.SparseMatrix.prototype](#apidoc.module.mathjs.type.SparseMatrix.prototype)

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>clone ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var m = new SparseMatrix({
    values: this._values ? object.clone(this._values) : undefined,
    index: object.clone(this._index),
    ptr: object.clone(this._ptr),
    size: object.clone(this._size),
    datatype: this._datatype
  });
  return m;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.create"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>create (data, datatype)](#apidoc.element.mathjs.type.SparseMatrix.prototype.create)
- description and source-code
```javascript
create = function (data, datatype) {
  return new SparseMatrix(data, datatype);
}
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.datatype"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>datatype ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.datatype)
- description and source-code
```javascript
datatype = function () {
  return this._datatype;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.density"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>density ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.density)
- description and source-code
```javascript
density = function () {
  // rows & columns
  var rows = this._size[0];
  var columns = this._size[1];
  // calculate density
  return rows !== 0 && columns !== 0 ? (this._index.length / (rows * columns)) : 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.diagonal"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>diagonal (k)](#apidoc.element.mathjs.type.SparseMatrix.prototype.diagonal)
- description and source-code
```javascript
diagonal = function (k) {
  // validate k if any
  if (k) {
    // convert BigNumber to a number
    if (k.isBigNumber === true)
      k = k.toNumber();
    // is must be an integer
    if (!isNumber(k) || !isInteger(k)) {
      throw new TypeError ('The parameter k must be an integer number');
    }
  }
  else {
    // default value
    k = 0;
  }

  var kSuper = k > 0 ? k : 0;
  var kSub = k < 0 ? -k : 0;

  // rows & columns
  var rows = this._size[0];
  var columns = this._size[1];

  // number diagonal values
  var n = Math.min(rows - kSub, columns -  kSuper);

  // diagonal arrays
  var values = [];
  var index = [];
  var ptr = [];
  // initial ptr value
  ptr[0] = 0;
  // loop columns
  for (var j = kSuper; j < columns && values.length < n; j++) {
    // k0 <= k < k1 where k0 = _ptr[j] && k1 = _ptr[j+1]
    var k0 = this._ptr[j];
    var k1 = this._ptr[j + 1];
    // loop x within [k0, k1[
    for (var x = k0; x < k1; x++) {
      // row index
      var i = this._index[x];
      // check row
      if (i === j - kSuper + kSub) {
        // value on this column
        values.push(this._values[x]);
        // store row
        index[values.length - 1] = i - kSub;
        // exit loop
        break;
      }
    }
  }
  // close ptr
  ptr.push(values.length);
  // return matrix
  return new SparseMatrix({
    values: values,
    index: index,
    ptr: ptr,
    size: [n, 1]
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.forEach"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>forEach (callback, skipZeros)](#apidoc.element.mathjs.type.SparseMatrix.prototype.forEach)
- description and source-code
```javascript
forEach = function (callback, skipZeros) {
  // check it is a pattern matrix
  if (!this._values)
    throw new Error('Cannot invoke forEach on a Pattern only matrix');
  // matrix instance
  var me = this;
  // rows and columns
  var rows = this._size[0];
  var columns = this._size[1];
  // loop columns
  for (var j = 0; j < columns; j++) {
    // k0 <= k < k1 where k0 = _ptr[j] && k1 = _ptr[j+1]
    var k0 = this._ptr[j];
    var k1 = this._ptr[j + 1];
    // column pointer
    var p = 0;
    // loop k within [k0, k1[
    for (var k = k0; k < k1; k++) {
      // row index
      var i = this._index[k];
      // check we need to process zeros
      if (!skipZeros) {
        // zero values
        for (var x = p; x < i; x++)
          callback(0, [x, j], me);
      }
      // value @ k
      callback(this._values[k], [i, j], me);
      // update pointer
      p = i + 1;
    }
    // check we need to process zeros
    if (!skipZeros) {
      // zero values
      for (var y = p; y < rows; y++)
        callback(0, [y, j], me);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>format (options)](#apidoc.element.mathjs.type.SparseMatrix.prototype.format)
- description and source-code
```javascript
format = function (options) {
  // rows and columns
  var rows = this._size[0];
  var columns = this._size[1];
  // density
  var density = this.density();
  // rows & columns
  var str = 'Sparse Matrix [' + string.format(rows, options) + ' x ' + string.format(columns, options) + '] density: ' + string.
format(density, options) + '\n';
  // loop columns
  for (var j = 0; j < columns; j++) {
    // k0 <= k < k1 where k0 = _ptr[j] && k1 = _ptr[j+1]
    var k0 = this._ptr[j];
    var k1 = this._ptr[j + 1];
    // loop k within [k0, k1[
    for (var k = k0; k < k1; k++) {
      // row index
      var i = this._index[k];
      // append value
      str += '\n    (' + string.format(i, options) + ', ' + string.format(j, options) + ') ==> ' + (this._values ? string.format
(this._values[k], options) : 'X');
    }
  }
  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.get"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>get (index)](#apidoc.element.mathjs.type.SparseMatrix.prototype.get)
- description and source-code
```javascript
get = function (index) {
  if (!isArray(index))
    throw new TypeError('Array expected');
  if (index.length != this._size.length)
    throw new DimensionError(index.length, this._size.length);

  // check it is a pattern matrix
  if (!this._values)
    throw new Error('Cannot invoke get on a Pattern only matrix');

  // row and column
  var i = index[0];
  var j = index[1];

  // check i, j are valid
  validateIndex(i, this._size[0]);
  validateIndex(j, this._size[1]);

  // find value index
  var k = _getValueIndex(i, this._ptr[j], this._ptr[j + 1], this._index);
  // check k is prior to next column k and it is in the correct row
  if (k < this._ptr[j + 1] && this._index[k] === i)
    return this._values[k];

  return 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.map"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>map (callback, skipZeros)](#apidoc.element.mathjs.type.SparseMatrix.prototype.map)
- description and source-code
```javascript
map = function (callback, skipZeros) {
  // check it is a pattern matrix
  if (!this._values)
    throw new Error('Cannot invoke map on a Pattern only matrix');
  // matrix instance
  var me = this;
  // rows and columns
  var rows = this._size[0];
  var columns = this._size[1];
  // invoke callback
  var invoke = function (v, i, j) {
    // invoke callback
    return callback(v, [i, j], me);
  };
  // invoke _map
  return _map(this, 0, rows - 1, 0, columns - 1, invoke, skipZeros);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.reshape"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>reshape (size, copy)](#apidoc.element.mathjs.type.SparseMatrix.prototype.reshape)
- description and source-code
```javascript
reshape = function (size, copy) {

  // validate arguments
  if (!isArray(size))
    throw new TypeError('Array expected');
  if (size.length !== 2)
    throw new Error('Sparse matrices can only be reshaped in two dimensions');

  // check sizes
  size.forEach(function (value) {
    if (!number.isNumber(value) || !number.isInteger(value) || value < 0) {
      throw new TypeError('Invalid size, must contain positive integers ' +
                          '(size: ' + string.format(size) + ')');
    }
  });

  // m * n must not change
  if(this._size[0] * this._size[1] !== size[0] * size[1]) {
    throw new Error('Reshaping sparse matrix will result in the wrong number of elements');
  }

  // matrix to reshape
  var m = copy ? this.clone() : this;

  // return unchanged if the same shape
  if(this._size[0] === size[0] && this._size[1] === size[1]) {
    return m;
  }

  // Convert to COO format (generate a column index)
  var colIndex = [];
  for(var i=0; i<m._ptr.length; i++) {
    for(var j=0; j<m._ptr[i+1]-m._ptr[i]; j++) {
      colIndex.push(i);
    }
  }

  // Clone the values array
  var values = m._values.slice();

  // Clone the row index array
  var rowIndex = m._index.slice();

  // Transform the (row, column) indices
  for(var i=0; i<m._index.length; i++) {
    var r1 = rowIndex[i];
    var c1 = colIndex[i];
    var flat = r1 * m._size[1] + c1;
    colIndex[i] = flat % size[1];
    rowIndex[i] = Math.floor(flat / size[1]);
  }

  // Now reshaping is supposed to preserve the row-major order, BUT these sparse matrices are stored
  // in column-major order, so we have to reorder the value array now. One option is to use a multisort,
  // sorting several arrays based on some other array.

  // OR, we could easily just:

  // 1. Remove all values from the matrix
  m._values.length = 0;
  m._index.length = 0;
  m._ptr.length = size[1] + 1;
  m._size = size.slice();
  for(var i=0; i<m._ptr.length; i++) {
    m._ptr[i] = 0;
  }

  // 2. Re-insert all elements in the proper order (simplified code from SparseMatrix.prototype.set)
  // This step is probably the most time-consuming
  for(var h=0; h<values.length; h++) {
    var i = rowIndex[h];
    var j = colIndex[h];
    var v = values[h];
    var k = _getValueIndex(i, m._ptr[j], m._ptr[j + 1], m._index);
    _insert(k, i, j, v, m._values, m._index, m._ptr);
  }

  // The value indices are inserted out of order, but apparently that's... still OK?

  return m;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.resize"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>resize (size, defaultValue, copy)](#apidoc.element.mathjs.type.SparseMatrix.prototype.resize)
- description and source-code
```javascript
resize = function (size, defaultValue, copy) {
  // validate arguments
  if (!isArray(size))
    throw new TypeError('Array expected');
  if (size.length !== 2)
    throw new Error('Only two dimensions matrix are supported');

  // check sizes
  size.forEach(function (value) {
    if (!number.isNumber(value) || !number.isInteger(value) || value < 0) {
      throw new TypeError('Invalid size, must contain positive integers ' +
                          '(size: ' + string.format(size) + ')');
    }
  });

  // matrix to resize
  var m = copy ? this.clone() : this;
  // resize matrix
  return _resize(m, size[0], size[1], defaultValue);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.set"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>set (index, v, defaultValue)](#apidoc.element.mathjs.type.SparseMatrix.prototype.set)
- description and source-code
```javascript
set = function (index, v, defaultValue) {
  if (!isArray(index))
    throw new TypeError('Array expected');
  if (index.length != this._size.length)
    throw new DimensionError(index.length, this._size.length);

  // check it is a pattern matrix
  if (!this._values)
    throw new Error('Cannot invoke set on a Pattern only matrix');

  // row and column
  var i = index[0];
  var j = index[1];

  // rows & columns
  var rows = this._size[0];
  var columns = this._size[1];

  // equal signature to use
  var eq = equalScalar;
  // zero value
  var zero = 0;

  if (isString(this._datatype)) {
    // find signature that matches (datatype, datatype)
    eq = typed.find(equalScalar, [this._datatype, this._datatype]) || equalScalar;
    // convert 0 to the same datatype
    zero = typed.convert(0, this._datatype);
  }

  // check we need to resize matrix
  if (i > rows - 1 || j > columns - 1) {
    // resize matrix
    _resize(this, Math.max(i + 1, rows), Math.max(j + 1, columns), defaultValue);
    // update rows & columns
    rows = this._size[0];
    columns = this._size[1];
  }

  // check i, j are valid
  validateIndex(i, rows);
  validateIndex(j, columns);

  // find value index
  var k = _getValueIndex(i, this._ptr[j], this._ptr[j + 1], this._index);
  // check k is prior to next column k and it is in the correct row
  if (k < this._ptr[j + 1] && this._index[k] === i) {
    // check value != 0
    if (!eq(v, zero)) {
      // update value
      this._values[k] = v;
    }
    else {
      // remove value from matrix
      _remove(k, j, this._values, this._index, this._ptr);
    }
  }
  else {
    // insert value @ (i, j)
    _insert(k, i, j, v, this._values, this._index, this._ptr);
  }

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.size"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>size ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.size)
- description and source-code
```javascript
size = function () {
  return this._size.slice(0); // copy the Array
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.storage"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>storage ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.storage)
- description and source-code
```javascript
storage = function () {
  return 'sparse';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.subset"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>subset (index, replacement, defaultValue)](#apidoc.element.mathjs.type.SparseMatrix.prototype.subset)
- description and source-code
```javascript
subset = function (index, replacement, defaultValue) { // check it is a pattern matrix
  if (!this._values)
    throw new Error('Cannot invoke subset on a Pattern only matrix');

  // check arguments
  switch (arguments.length) {
    case 1:
      return _getsubset(this, index);

      // intentional fall through
    case 2:
    case 3:
      return _setsubset(this, index, replacement, defaultValue);

    default:
      throw new SyntaxError('Wrong number of arguments');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.swapRows"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>swapRows (i, j)](#apidoc.element.mathjs.type.SparseMatrix.prototype.swapRows)
- description and source-code
```javascript
swapRows = function (i, j) {
  // check index
  if (!isNumber(i) || !isInteger(i) || !isNumber(j) || !isInteger(j)) {
    throw new Error('Row index must be positive integers');
  }
  // check dimensions
  if (this._size.length !== 2) {
    throw new Error('Only two dimensional matrix is supported');
  }
  // validate index
  validateIndex(i, this._size[0]);
  validateIndex(j, this._size[0]);

  // swap rows
  SparseMatrix._swapRows(i, j, this._size[1], this._values, this._index, this._ptr);
  // return current instance
  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.toArray"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>toArray ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.toArray)
- description and source-code
```javascript
toArray = function () {
  return _toArray(this._values, this._index, this._ptr, this._size, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'SparseMatrix',
    values: this._values,
    index: this._index,
    ptr: this._ptr,
    size: this._size,
    datatype: this._datatype
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>toString ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return string.format(this.toArray());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.SparseMatrix.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.SparseMatrix.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.SparseMatrix.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  return _toArray(this._values, this._index, this._ptr, this._size, false);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Unit"></a>[module mathjs.type.Unit](#apidoc.module.mathjs.type.Unit)

#### <a name="apidoc.element.mathjs.type.Unit.Unit"></a>[function <span class="apidocSignatureSpan">mathjs.type.</span>Unit (value, name)](#apidoc.element.mathjs.type.Unit.Unit)
- description and source-code
```javascript
function Unit(value, name) {
  if (!(this instanceof Unit)) {
    throw new Error('Constructor must be called with the new operator');
  }

  if (!(value === undefined || isNumeric(value) || value.isComplex)) {
    throw new TypeError('First parameter in Unit constructor must be number, BigNumber, Fraction, Complex, or undefined');
  }
  if (name != undefined && (typeof name !== 'string' || name == '')) {
    throw new TypeError('Second parameter in Unit constructor must be a string');
  }

  if (name != undefined) {
    var u = Unit.parse(name);
    this.units = u.units;
    this.dimensions = u.dimensions;
  }
  else {
    this.units = [
      {
        unit: UNIT_NONE,
        prefix: PREFIXES.NONE,  // link to a list with supported prefixes
        power: 0
      }
    ];
    this.dimensions = [];
    for(var i=0; i<BASE_DIMENSIONS.length; i++) {
      this.dimensions[i] = 0;
    }
  }

  this.value = (value != undefined) ? this._normalize(value) : null;

  this.fixPrefix = false; // if true, function format will not search for the
                          // best prefix but leave it as initially provided.
                          // fixPrefix is set true by the method Unit.to

  // The justification behind this is that if the constructor is explicitly called,
  // the caller wishes the units to be returned exactly as he supplied.
  this.isUnitListSimplified = true;

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit._getNumberConverter"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>_getNumberConverter (type)](#apidoc.element.mathjs.type.Unit._getNumberConverter)
- description and source-code
```javascript
_getNumberConverter = function (type) {
  if (!Unit.typeConverters[type]) {
    throw new TypeError('Unsupported type "' + type + '"');
  }

  return Unit.typeConverters[type];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.createUnit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>createUnit (obj, options)](#apidoc.element.mathjs.type.Unit.createUnit)
- description and source-code
```javascript
createUnit = function (obj, options) {

  if(typeof(obj) !== 'object') {
    throw new TypeError("createUnit expects first parameter to be of type 'Object'");
  }

  // Remove all units and aliases we are overriding
  if(options && options.override) {
    for(var key in obj) {
      if(obj.hasOwnProperty(key)) {
        Unit.deleteUnit(key);
      }
      if(obj[key].aliases) {
        for(var i=0; i<obj[key].aliases.length; i++) {
          Unit.deleteUnit(obj[key].aliases[i]);
        }
      }
    }
  }

  // TODO: traverse multiple times until all units have been added
  var lastUnit;
  for(var key in obj) {
    if(obj.hasOwnProperty(key)) {
      lastUnit = Unit.createUnitSingle(key, obj[key]);
    }
  }
  return lastUnit;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.createUnitSingle"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>createUnitSingle (name, obj, options)](#apidoc.element.mathjs.type.Unit.createUnitSingle)
- description and source-code
```javascript
createUnitSingle = function (name, obj, options) {

  if(typeof(obj) === 'undefined' || obj === null) {
    obj = {};
  }

  if(typeof(name) !== 'string') {
    throw new TypeError("createUnitSingle expects first parameter to be of type 'string'");
  }

  // Check collisions with existing units
  if(UNITS.hasOwnProperty(name)) {
    throw new Error('Cannot create unit "' + name + '": a unit with that name already exists');
  }

  // TODO: Validate name for collisions with other built-in functions (like abs or cos, for example), and for acceptable variable
 names. For example, '42' is probably not a valid unit. Nor is '%', since it is also an operator.

  assertUnitNameIsValid(name);

  var defUnit = null;   // The Unit from which the new unit will be created.
  var aliases = [];
  var offset = 0;
  var definition;
  var prefixes;
  if(obj && obj.type === 'Unit') {
    defUnit = obj.clone();
  }
  else if(typeof(obj) === 'string') {
    if(obj !== '') {
      definition = obj;
    }
  }
  else if(typeof(obj) === 'object') {
    definition = obj.definition;
    prefixes = obj.prefixes;
    offset = obj.offset;
    aliases = obj.aliases;
  }
  else {
    throw new TypeError('Cannot create unit "' + name + '" from "' + obj.toString() + '": expecting "string" or "Unit" or "Object
"');
  }

  if(aliases) {
    for (var i=0; i<aliases.length; i++) {
      if(UNITS.hasOwnProperty(aliases[i])) {
        throw new Error('Cannot create alias "' + aliases[i] + '": a unit with that name already exists');
      }
    }
  }

  if(definition && typeof(definition) === 'string' && !defUnit) {
    try {
      defUnit = Unit.parse(definition, {allowNoUnits: true});
    }
    catch (ex) {
      ex.message = 'Could not create unit "' + name + '" from "' + definition + '": ' + ex.message;
      throw(ex);
    }
  }
  else if(definition && definition.type === 'Unit') {
    defUnit = definition.clone();
  }

  aliases = aliases || [];
  offset = offset || 0;
  if(prefixes && prefixes.toUpperCase)
    prefixes = PREFIXES[prefixes.toUpperCase()] || PREFIXES.NONE;
  else
    prefixes = PREFIXES.NONE;


  // If defUnit is null, it is because the user did not
  // specify a defintion. So create a new base dimension.
  var newUnit = {};
  if(!defUnit) {
    // Add a new base dimension
    var baseName = name + "_STUFF";   // foo --> foo_STUFF, or the essence of foo
    if(BASE_DIMENSIONS.indexOf(baseName) >= 0) {
      throw new Error('Cannot create new base unit "' + name + '": a base unit with that name already exists (and cannot be overridden
)');
    }
    BASE_DIMENSIONS.push(baseName);

    // Push 0 onto existing base units
    for(var b in BASE_UNITS) {
      if(BASE_UNITS.hasOwnProperty(b)) {
        BASE_UNITS[b].dimensions[BASE_DIMENSIONS.length-1] = 0;
      }
    }

    // Add the new base unit
    var newBaseUnit = { dimensions: [] };
    for(var i=0; i<BASE_DIMENSIONS.length; i++) {
      newBaseUnit.dimensions[i] = 0;
    }
    newBaseUnit.dimensions[BASE_DIMENSIONS.length-1] = 1;
    newBaseUnit.key = baseName;
    BASE_UNITS[baseName] = newBaseUnit;

    newUnit = {
      name: name,
      value: 1,
      dimensions: BASE_UNITS[baseName].dimensions.slice(0),
      prefixes: prefixes,
      offset: offset,
      base: baseName
    };

    currentUnitSystem[baseName] = {
      unit: newUnit,
      prefix: PREFIXES.NONE['']
    };

  }
  else {

    newUnit = {
      name: name,
      value: defUnit.value,
      dimensions: defUnit.dimensions.slice(0),
      prefixes: prefixes,
      offset: offset,
    };

    // Create a new base if no matching base exists
    var anyMatch = false;
    for(var i in BASE_UNITS) {
      if(BASE_UNITS.hasOwnProperty(i)) {
        var match = true;
        for(var j=0; j<BASE_DIMENSIONS.length; j++) {
          if (Math.abs((newUnit.dimensions[j] || 0) - (BASE_UNITS[i].dimensions[j] || 0)) > 1e-12) {
            match = false;
            break;
          }
        }
        if(match) {
          anyMatch = true;
          break;
        }
      }
    }
    if(!anyMatch) {
      var baseName = name + "_STUFF"; ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.deleteUnit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>deleteUnit (name)](#apidoc.element.mathjs.type.Unit.deleteUnit)
- description and source-code
```javascript
deleteUnit = function (name) {
  delete Unit.UNITS[name];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.fromJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>fromJSON (json)](#apidoc.element.mathjs.type.Unit.fromJSON)
- description and source-code
```javascript
fromJSON = function (json) {
  var unit = new Unit(json.value, json.unit);
  unit.fixPrefix = json.fixPrefix || false;
  return unit;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.getUnitSystem"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>getUnitSystem ()](#apidoc.element.mathjs.type.Unit.getUnitSystem)
- description and source-code
```javascript
getUnitSystem = function () {
  for(var key in UNIT_SYSTEMS) {
    if(UNIT_SYSTEMS[key] === currentUnitSystem) {
      return key;
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.isValuelessUnit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>isValuelessUnit (name)](#apidoc.element.mathjs.type.Unit.isValuelessUnit)
- description and source-code
```javascript
isValuelessUnit = function (name) {
  return (_findUnit(name) != null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.parse"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>parse (str, options)](#apidoc.element.mathjs.type.Unit.parse)
- description and source-code
```javascript
parse = function (str, options) {
  options = options || {};
  text = str;
  index = -1;
  c = '';

  if (typeof text !== 'string') {
    throw new TypeError('Invalid argument in Unit.parse, string expected');
  }

  var unit = new Unit();
  unit.units = [];

  // A unit should follow this pattern:
  // [number]unit[^number] [unit[^number]]...[/unit[^number] [unit[^number]]]

  // Rules:
  // number is any floating point number.
  // unit is any alphanumeric string beginning with an alpha. Units with names like e3 should be avoided because they look like
the exponent of a floating point number!
  // The string may optionally begin with a number.
  // Each unit may optionally be followed by ^number.
  // Whitespace or a forward slash is recommended between consecutive units, although the following technically is parseable:
  //   2m^2kg/s^2
  // it is not good form. If a unit starts with e, then it could be confused as a floating point number:
  //   4erg

  next();
  skipWhitespace();
  // Optional number at the start of the string
  var valueStr = parseNumber();
  var value = null;
  if(valueStr) {
    if (config.number === 'BigNumber') {
      value = new type.BigNumber(valueStr);
    }
    else if (config.number === 'Fraction') {
      value = new type.Fraction(valueStr);
    }
    else { // number
      value = parseFloat(valueStr);
    }
  }
  skipWhitespace();    // Whitespace is not required here

  // Next, we read any number of unit[^number]
  var powerMultiplierCurrent = 1;
  var expectingUnit = false;

  // Stack to keep track of powerMultipliers applied to each parentheses group
  var powerMultiplierStack = [];

  // Running product of all elements in powerMultiplierStack
  var powerMultiplierStackProduct = 1;

  while (true) {
    skipWhitespace();

    // Check for and consume opening parentheses, pushing powerMultiplierCurrent to the stack
    // A '(' will always appear directly before a unit.
    while (c === '(') {
      powerMultiplierStack.push(powerMultiplierCurrent);
      powerMultiplierStackProduct *= powerMultiplierCurrent;
      powerMultiplierCurrent = 1;
      next();
      skipWhitespace();
    }

    // Is there something here?
    if(c) {
      var oldC = c;
      var uStr = parseUnit();
      if(uStr == null) {
        throw new SyntaxError('Unexpected "' + oldC + '" in "' + text + '" at index ' + index.toString());
      }
    }
    else {
      // End of input.
      break;
    }

    // Verify the unit exists and get the prefix (if any)
    var res = _findUnit(uStr);
    if(res == null) {
      // Unit not found.
      throw new SyntaxError('Unit "' + uStr + '" not found.');
    }

    var power = powerMultiplierCurrent * powerMultiplierStackProduct;
    // Is there a "^ number"?
    skipWhitespace();
    if (parseCharacter('^')) {
      skipWhitespace();
      var p = parseNumber();
      if(p == null) {
        // No valid number found for the power!
        throw new SyntaxError('In "' + str + '", "^" must be followed by a floating-point number');
      }
      power *= p;
    }

    // Add the unit to the list
    unit.units.push( {
      unit: res.unit,
      prefix: res.prefix,
      power: power
    });
    for(var i=0; i<BASE_DIMENSIONS.length; i++) {
      unit.dimensions[i] += (res.unit.dimensions[i] || 0) * power;
    }

    // Check for and consume closing parentheses, popping from the stack.
    // A ')' will always follow a unit.
    skipWhitespace();
    while (c === ')') {
      if(powerMultiplierStack.length === 0) {
        throw new SyntaxError('Unmatched ")" in "' + text + '" at index ' + index.toString());
      }
      powerMultiplierStackProduct /= powerMultiplierStack.pop();
      next();
      skipWhitespace();
    }

    // "*" and "/" should mean we are expecting something to come next.
    // Is there a forward slash? If so, negate powerMultiplierCurrent. The next unit or paren group is in the denominator.
    expectingUnit = false;

    if (parseCharacter('*')) {
      // explicit multiplication
      powerMultiplierCurrent = 1;
      expectingUnit = true;
    } ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.setUnitSystem"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.</span>setUnitSystem (name)](#apidoc.element.mathjs.type.Unit.setUnitSystem)
- description and source-code
```javascript
setUnitSystem = function (name) {
  if(UNIT_SYSTEMS.hasOwnProperty(name)) {
    currentUnitSystem = UNIT_SYSTEMS[name];
  }
  else {
    throw new Error('Unit system ' + name + ' does not exist. Choices are: ' + Object.keys(UNIT_SYSTEMS).join(', '));
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.type.Unit.prototype"></a>[module mathjs.type.Unit.prototype](#apidoc.module.mathjs.type.Unit.prototype)

#### <a name="apidoc.element.mathjs.type.Unit.prototype._bestPrefix"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_bestPrefix ()](#apidoc.element.mathjs.type.Unit.prototype._bestPrefix)
- description and source-code
```javascript
_bestPrefix = function () {
  if (this.units.length !== 1) {
    throw new Error("Can only compute the best prefix for single units with integer powers, like kg, s^2, N^-1, and so forth!");
  }
  if (Math.abs(this.units[0].power - Math.round(this.units[0].power)) >= 1e-14) {
    throw new Error("Can only compute the best prefix for single units with integer powers, like kg, s^2, N^-1, and so forth!");
  }

  // find the best prefix value (resulting in the value of which
  // the absolute value of the log10 is closest to zero,
  // though with a little offset of 1.2 for nicer values: you get a
  // sequence 1mm 100mm 500mm 0.6m 1m 10m 100m 500m 0.6km 1km ...

  // Note: the units value can be any numeric type, but to find the best
  // prefix it's enough to work with limited precision of a regular number
  // Update: using mathjs abs since we also allow complex numbers
  var absValue = abs(this.value);
  var absUnitValue = abs(this.units[0].unit.value);
  var bestPrefix = this.units[0].prefix;
  if (absValue === 0) {
    return bestPrefix;
  }
  var power = this.units[0].power;
  var bestDiff = Math.log(absValue / Math.pow(bestPrefix.value * absUnitValue, power)) / Math.LN10 - 1.2;
  if(bestDiff > -2.200001 && bestDiff < 1.800001) return bestPrefix;    // Allow the original prefix
  bestDiff = Math.abs(bestDiff);
  var prefixes = this.units[0].unit.prefixes;
  for (var p in prefixes) {
    if (prefixes.hasOwnProperty(p)) {
      var prefix = prefixes[p];
      if (prefix.scientific) {

        var diff = Math.abs(
            Math.log(absValue / Math.pow(prefix.value * absUnitValue, power)) / Math.LN10 - 1.2);

        if (diff < bestDiff
            || (diff === bestDiff && prefix.name.length < bestPrefix.name.length)) {
              // choose the prefix with the smallest diff, or if equal, choose the one
              // with the shortest name (can happen with SHORTLONG for example)
              bestPrefix = prefix;
              bestDiff = diff;
        }
      }
    }
  }

  return bestPrefix;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype._denormalize"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_denormalize (value, prefixValue)](#apidoc.element.mathjs.type.Unit.prototype._denormalize)
- description and source-code
```javascript
_denormalize = function (value, prefixValue) {
  var unitValue, unitOffset, unitPower, unitPrefixValue;
  var convert;

  if (value == null || this.units.length === 0) {
    return value;
  }
  else if (this._isDerived()) {
    // This is a derived unit, so do not apply offsets.
    // For example, with J kg^-1 degC^-1 you would NOT want to apply the offset.
    // Also, prefixValue is ignored--but we will still use the prefix value stored in each unit, since kg is usually preferable
to g unless the user decides otherwise.
    var res = value;
    convert = Unit._getNumberConverter(getTypeOf(value)); // convert to Fraction or BigNumber if needed

    for (var i = 0; i < this.units.length; i++) {
      unitValue       = convert(this.units[i].unit.value);
      unitPrefixValue = convert(this.units[i].prefix.value);
      unitPower       = convert(this.units[i].power);
      res = divide(res, pow(multiply(unitValue, unitPrefixValue), unitPower));
    }

    return res;
  }
  else {
    // This is a single unit of power 1, like kg or degC
    convert = Unit._getNumberConverter(getTypeOf(value)); // convert to Fraction or BigNumber if needed

    unitValue       = convert(this.units[0].unit.value);
    unitPrefixValue = convert(this.units[0].prefix.value);
    unitOffset      = convert(this.units[0].unit.offset);

    if (prefixValue == undefined) {
      return subtract(divide(divide(value, unitValue), unitPrefixValue), unitOffset);
    }
    else {
      return subtract(divide(divide(value, unitValue), prefixValue), unitOffset);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype._isDerived"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_isDerived ()](#apidoc.element.mathjs.type.Unit.prototype._isDerived)
- description and source-code
```javascript
_isDerived = function () {
  if(this.units.length === 0) {
    return false;
  }
  return this.units.length > 1 || Math.abs(this.units[0].power - 1.0) > 1e-15;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype._normalize"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>_normalize (value)](#apidoc.element.mathjs.type.Unit.prototype._normalize)
- description and source-code
```javascript
_normalize = function (value) {
  var unitValue, unitOffset, unitPower, unitPrefixValue;
  var convert;

  if (value == null || this.units.length === 0) {
    return value;
  }
  else if (this._isDerived()) {
    // This is a derived unit, so do not apply offsets.
    // For example, with J kg^-1 degC^-1 you would NOT want to apply the offset.
    var res = value;
    convert = Unit._getNumberConverter(getTypeOf(value)); // convert to Fraction or BigNumber if needed

    for(var i=0; i < this.units.length; i++) {
      unitValue       = convert(this.units[i].unit.value);
      unitPrefixValue = convert(this.units[i].prefix.value);
      unitPower       = convert(this.units[i].power);
      res = multiply(res, pow(multiply(unitValue, unitPrefixValue), unitPower));
    }

    return res;
  }
  else {
    // This is a single unit of power 1, like kg or degC
    convert = Unit._getNumberConverter(getTypeOf(value)); // convert to Fraction or BigNumber if needed

    unitValue       = convert(this.units[0].unit.value);
    unitOffset      = convert(this.units[0].unit.offset);
    unitPrefixValue = convert(this.units[0].prefix.value);

    return multiply(add(value, unitOffset), multiply(unitValue, unitPrefixValue));
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.abs"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>abs ()](#apidoc.element.mathjs.type.Unit.prototype.abs)
- description and source-code
```javascript
abs = function () {
  // This gives correct, but unexpected, results for units with an offset.
  // For example, abs(-283.15 degC) = -263.15 degC !!!
  var ret = this.clone();
  ret.value = abs(ret.value);

  for(var i in ret.units) {
    if(ret.units[i].unit.name === 'VA' || ret.units[i].unit.name === 'VAR') {
      ret.units[i].unit = UNITS["W"];
    }
  }

  return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.clone"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>clone ()](#apidoc.element.mathjs.type.Unit.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var unit = new Unit();

  unit.fixPrefix = this.fixPrefix;
  unit.isUnitListSimplified = this.isUnitListSimplified;

  unit.value = clone(this.value);
  unit.dimensions = this.dimensions.slice(0);
  unit.units = [];
  for(var i = 0; i < this.units.length; i++) {
    unit.units[i] = { };
    for (var p in this.units[i]) {
      if (this.units[i].hasOwnProperty(p)) {
        unit.units[i][p] = this.units[i][p];
      }
    }
  }

  return unit;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.divide"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>divide (other)](#apidoc.element.mathjs.type.Unit.prototype.divide)
- description and source-code
```javascript
divide = function (other) {
  var res = this.clone();

  for(var i=0; i<BASE_DIMENSIONS.length; i++) {
    // Dimensions arrays may be of different lengths. Default to 0.
    res.dimensions[i] = (this.dimensions[i] || 0) - (other.dimensions[i] || 0);
  }

  // Invert and append other's units list onto res (simplify later in Unit.prototype.format)
  for(var i=0; i<other.units.length; i++) {
    // Make a deep copy
    var inverted = {};
    for(var key in other.units[i]) {
      inverted[key] = other.units[i][key];
    }
    inverted.power = -inverted.power;
    res.units.push(inverted);
  }

  // If at least one operand has a value, the result should have a value
  if (this.value != null || other.value != null) {
    var valThis = this.value == null ? this._normalize(1) : this.value;
    var valOther = other.value == null ? other._normalize(1) : other.value;
    res.value = divide(valThis, valOther);
  }
  else {
    res.value = null;
  }

  // Trigger simplification of the unit list at some future time
  res.isUnitListSimplified = false;

  return getNumericIfUnitless(res);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.equalBase"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>equalBase (other)](#apidoc.element.mathjs.type.Unit.prototype.equalBase)
- description and source-code
```javascript
equalBase = function (other) {
  // All dimensions must be the same
  for(var i=0; i<BASE_DIMENSIONS.length; i++) {
    if (Math.abs((this.dimensions[i] || 0) - (other.dimensions[i] || 0)) > 1e-12) {
      return false;
    }
  }
  return true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.equals"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>equals (other)](#apidoc.element.mathjs.type.Unit.prototype.equals)
- description and source-code
```javascript
equals = function (other) {
  return (this.equalBase(other) && equal(this.value, other.value));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.format"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>format (options)](#apidoc.element.mathjs.type.Unit.prototype.format)
- description and source-code
```javascript
format = function (options) {

  // Simplfy the unit list, if necessary
  this.simplifyUnitListLazy();

  // Apply some custom logic for handling VA and VAR. The goal is to express the value of the unit as a real value, if possible.
Otherwise, use a real-valued unit instead of a complex-valued one.
  var isImaginary = false;
  var isReal = true;
  if(typeof(this.value) !== 'undefined' && this.value !== null && this.value.isComplex) {
    // TODO: Make this better, for example, use relative magnitude of re and im rather than absolute
    isImaginary = Math.abs(this.value.re) < 1e-14;
    isReal = Math.abs(this.value.im) < 1e-14;
  }

  for(var i in this.units) {
    if(this.units[i].unit) {
      if(this.units[i].unit.name === 'VA' && isImaginary) {
        this.units[i].unit = UNITS["VAR"];
      }
      else if(this.units[i].unit.name === 'VAR' && !isImaginary) {
        this.units[i].unit = UNITS["VA"];
      }
    }
  }


  // Now apply the best prefix
  // Units must have only one unit and not have the fixPrefix flag set
  if (this.units.length === 1 && !this.fixPrefix) {
    // Units must have integer powers, otherwise the prefix will change the
    // outputted value by not-an-integer-power-of-ten
    if (Math.abs(this.units[0].power - Math.round(this.units[0].power)) < 1e-14) {
      // Apply the best prefix
      this.units[0].prefix = this._bestPrefix();
    }
  }


  var value = this._denormalize(this.value);
  var str = (this.value !== null) ? format(value, options || {}) : '';
  var unitStr = this.formatUnits();
  if(this.value && this.value.isComplex) {
    str = "(" + str + ")";    // Surround complex values with ( ) to enable better parsing
  }
  if(unitStr.length > 0 && str.length > 0) {
    str += " ";
  }
  str += unitStr;

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.formatUnits"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>formatUnits ()](#apidoc.element.mathjs.type.Unit.prototype.formatUnits)
- description and source-code
```javascript
formatUnits = function () {

  // Lazy evaluation of the unit list
  this.simplifyUnitListLazy();

  var strNum = "";
  var strDen = "";
  var nNum = 0;
  var nDen = 0;

  for(var i=0; i<this.units.length; i++) {
    if(this.units[i].power > 0) {
      nNum++;
      strNum += " " + this.units[i].prefix.name + this.units[i].unit.name;
      if(Math.abs(this.units[i].power - 1.0) > 1e-15) {
        strNum += "^" + this.units[i].power;
      }
    }
    else if(this.units[i].power < 0) {
      nDen++;
    }
  }

  if(nDen > 0) {
    for(var i=0; i<this.units.length; i++) {
      if(this.units[i].power < 0) {
        if(nNum > 0) {
          strDen += " " + this.units[i].prefix.name + this.units[i].unit.name;
          if(Math.abs(this.units[i].power + 1.0) > 1e-15) {
            strDen += "^" + (-this.units[i].power);
          }
        }
        else {
          strDen += " " + this.units[i].prefix.name + this.units[i].unit.name;
          strDen += "^" + (this.units[i].power);
        }
      }
    }
  }
  // Remove leading " "
  strNum = strNum.substr(1);
  strDen = strDen.substr(1);

  // Add parans for better copy/paste back into the eval, for example, or for better pretty print formatting
  if(nNum > 1 && nDen > 0) {
    strNum = "(" + strNum + ")";
  }
  if(nDen > 1 && nNum > 0) {
    strDen = "(" + strDen + ")";
  }

  var str = strNum;
  if(nNum > 0 && nDen > 0) {
    str += " / ";
  }
  str += strDen;

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.hasBase"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>hasBase (base)](#apidoc.element.mathjs.type.Unit.prototype.hasBase)
- description and source-code
```javascript
hasBase = function (base) {

  if(typeof(base) === "string") {
    base = BASE_UNITS[base];
  }

  if(!base)
    return false;


  // All dimensions must be the same
  for(var i=0; i<BASE_DIMENSIONS.length; i++) {
    if (Math.abs((this.dimensions[i] || 0) - (base.dimensions[i] || 0)) > 1e-12) {
      return false;
    }
  }
  return true;

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.multiply"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>multiply (other)](#apidoc.element.mathjs.type.Unit.prototype.multiply)
- description and source-code
```javascript
multiply = function (other) {
  var res = this.clone();

  for(var i = 0; i<BASE_DIMENSIONS.length; i++) {
    // Dimensions arrays may be of different lengths. Default to 0.
    res.dimensions[i] = (this.dimensions[i] || 0) + (other.dimensions[i] || 0);
  }

  // Append other's units list onto res (simplify later in Unit.prototype.format)
  for(var i=0; i<other.units.length; i++) {
    // Make a deep copy
    var inverted = {};
    for(var key in other.units[i]) {
      inverted[key] = other.units[i][key];
    }
    res.units.push(inverted);
  }

  // If at least one operand has a value, then the result should also have a value
  if(this.value != null || other.value != null) {
    var valThis = this.value == null ? this._normalize(1) : this.value;
    var valOther = other.value == null ? other._normalize(1) : other.value;
    res.value = multiply(valThis, valOther);
  }
  else {
    res.value = null;
  }

  // Trigger simplification of the unit list at some future time
  res.isUnitListSimplified = false;

  return getNumericIfUnitless(res);
}
```
- example usage
```shell
...
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
math.eval('det([-1, 2; 3, 1])');  // -7

// chaining
math.chain(3)
    .add(4)
    .multiply(2)
    .done(); // 14
'''

## Browser support

Math.js works on any ES5 compatible JavaScript engine: node.js 0.10, and Internet Explorer 9 and newer, and all other browsers (
Chrome, Firefox, Safari). If support for old browsers like Internet Explorer 8 is required, the [es5-shim](https://github.com/kriskowal
/es5-shim) library has to be loaded.
...
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.pow"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>pow (p)](#apidoc.element.mathjs.type.Unit.prototype.pow)
- description and source-code
```javascript
pow = function (p) {
  var res = this.clone();

  for(var i=0; i<BASE_DIMENSIONS.length; i++) {
    // Dimensions arrays may be of different lengths. Default to 0.
    res.dimensions[i] = (this.dimensions[i] || 0) * p;
  }

  // Adjust the power of each unit in the list
  for(var i=0; i<res.units.length; i++) {
    res.units[i].power *= p;
  }

  if(res.value != null) {
    res.value = pow(res.value, p);

    // only allow numeric output, we don't want to return a Complex number
    //if (!isNumeric(res.value)) {
    //  res.value = NaN;
    //}
    // Update: Complex supported now
  }
  else {
    res.value = null;
  }

  // Trigger lazy evaluation of the unit list
  res.isUnitListSimplified = false;

  return getNumericIfUnitless(res);
}
```
- example usage
```shell
...
var math = require('mathjs');

// functions and constants
math.round(math.e, 3);            // 2.718
math.atan2(3, -3) / math.pi;      // 0.75
math.log(1000, 10);               // 3
math.sqrt(-4);                    // 2i
math.pow([[-1, 2], [3, 1]], 2);   // [[7, 0], [0, 7]]
math.derivative('x^2 + x', 'x');  // 2 * x + 1

// expressions
math.eval('12 / (2.3 + 0.7)');    // 4
math.eval('5.08 cm to inch');     // 2 inch
math.eval('sin(45 deg) ^ 2');     // 0.5
math.eval('9 / 3 + 2i');          // 3 + 2i
...
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.simplifyUnitListLazy"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>simplifyUnitListLazy ()](#apidoc.element.mathjs.type.Unit.prototype.simplifyUnitListLazy)
- description and source-code
```javascript
simplifyUnitListLazy = function () {

  if (this.isUnitListSimplified || this.value == null) {
    return;
  }

  var proposedUnitList = [];

  // Search for a matching base
  var matchingBase;
  for(var key in currentUnitSystem) {
    if(this.hasBase(BASE_UNITS[key])) {
      matchingBase = key;
      break;
    }
  }

  if(matchingBase === 'NONE')
  {
    this.units = [];
  }
  else {
    var matchingUnit;
    if(matchingBase) {
      // Does the unit system have a matching unit?
      if(currentUnitSystem.hasOwnProperty(matchingBase)) {
        matchingUnit = currentUnitSystem[matchingBase];
      }
    }
    var value;
    var str;
    if(matchingUnit) {
      this.units = [{
        unit: matchingUnit.unit,
        prefix: matchingUnit.prefix,
        power: 1.0
      }];
    }
    else {
      // Multiple units or units with powers are formatted like this:
      // 5 (kg m^2) / (s^3 mol)
      // Build an representation from the base units of the current unit system
      var missingBaseDim = false;
      for(var i=0; i<BASE_DIMENSIONS.length; i++) {
        var baseDim = BASE_DIMENSIONS[i];
        if(Math.abs(this.dimensions[i] || 0) > 1e-12) {
          if(currentUnitSystem.hasOwnProperty(baseDim)) {
            proposedUnitList.push({
              unit: currentUnitSystem[baseDim].unit,
              prefix: currentUnitSystem[baseDim].prefix,
              power: this.dimensions[i] || 0
            });
          }
          else {
            missingBaseDim = true;
          }
        }
      }

      // Is the proposed unit list "simpler" than the existing one?
      if(proposedUnitList.length < this.units.length && !missingBaseDim) {
        // Replace this unit list with the proposed list
        this.units = proposedUnitList;
      }
    }
  }

  this.isUnitListSimplified = true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.splitUnit"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>splitUnit (parts)](#apidoc.element.mathjs.type.Unit.prototype.splitUnit)
- description and source-code
```javascript
splitUnit = function (parts) {

  var x = this.clone();
  var ret = [];
  for(var i=0; i<parts.length; i++) {
    x = x.to(parts[i]);
    if(i==parts.length-1) break;
    // fix rounds a number towards 0
    var fixedVal = fix(x.toNumeric());
    var y = new Unit(fixedVal, parts[i].toString());
    ret.push(y);
    x = subtract(x, y);
  }
  ret.push(x);

  return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.to"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>to (valuelessUnit)](#apidoc.element.mathjs.type.Unit.prototype.to)
- description and source-code
```javascript
to = function (valuelessUnit) {
  var other;
  var value = this.value == null ? this._normalize(1) : this.value;
  if (typeof valuelessUnit === 'string') {
    //other = new Unit(null, valuelessUnit);
    other = Unit.parse(valuelessUnit);
    if (!this.equalBase(other)) {
      throw new Error('Units do not match');
    }
    if (other.value !== null) {
      throw new Error('Cannot convert to a unit with a value');
    }

    other.value = clone(value);
    other.fixPrefix = true;
    other.isUnitListSimplified = true;
    return other;
  }
  else if (valuelessUnit && valuelessUnit.isUnit) {
    if (!this.equalBase(valuelessUnit)) {
      throw new Error('Units do not match');
    }
    if (valuelessUnit.value !== null) {
      throw new Error('Cannot convert to a unit with a value');
    }
    other = valuelessUnit.clone();
    other.value = clone(value);
    other.fixPrefix = true;
    other.isUnitListSimplified = true;
    return other;
  }
  else {
    throw new Error('String or Unit expected as parameter');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toJSON ()](#apidoc.element.mathjs.type.Unit.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return {
    mathjs: 'Unit',
    value: this._denormalize(this.value),
    unit: this.formatUnits(),
    fixPrefix: this.fixPrefix
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.toNumber"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toNumber (valuelessUnit)](#apidoc.element.mathjs.type.Unit.prototype.toNumber)
- description and source-code
```javascript
toNumber = function (valuelessUnit) {
  return toNumber(this.toNumeric(valuelessUnit));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.toNumeric"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toNumeric (valuelessUnit)](#apidoc.element.mathjs.type.Unit.prototype.toNumeric)
- description and source-code
```javascript
toNumeric = function (valuelessUnit) {
  var other = this;
  if(valuelessUnit) {
    // Allow getting the numeric value without converting to a different unit
    other = this.to(valuelessUnit);
  }

  if(other._isDerived()) {
    return other._denormalize(other.value);
  }
  else {
    return other._denormalize(other.value, other.units[0].prefix.value);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.toSI"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toSI ()](#apidoc.element.mathjs.type.Unit.prototype.toSI)
- description and source-code
```javascript
toSI = function () {

  var ret = this.clone();

  var proposedUnitList = [];

  // Multiple units or units with powers are formatted like this:
  // 5 (kg m^2) / (s^3 mol)
  // Build an representation from the base units of the SI unit system
  var missingBaseDim = false;
  for(var i=0; i<BASE_DIMENSIONS.length; i++) {
    var baseDim = BASE_DIMENSIONS[i];
    if(Math.abs(ret.dimensions[i] || 0) > 1e-12) {
      if(UNIT_SYSTEMS["si"].hasOwnProperty(baseDim)) {
        proposedUnitList.push({
          unit: UNIT_SYSTEMS["si"][baseDim].unit,
          prefix: UNIT_SYSTEMS["si"][baseDim].prefix,
          power: ret.dimensions[i] || 0
        });
      }
      else {
        throw new Error("Cannot express custom unit " + baseDim + " in SI units");
      }
    }
  }

  // Replace this unit list with the proposed list
  ret.units = proposedUnitList;

  ret.isUnitListSimplified = true;

  return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.toString"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>toString ()](#apidoc.element.mathjs.type.Unit.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return this.format();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.type.Unit.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">mathjs.type.Unit.prototype.</span>valueOf ()](#apidoc.element.mathjs.type.Unit.prototype.valueOf)
- description and source-code
```javascript
valueOf = function () {
  return this.format();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.typed"></a>[module mathjs.typed](#apidoc.module.mathjs.typed)

#### <a name="apidoc.element.mathjs.typed.typed"></a>[function <span class="apidocSignatureSpan">mathjs.</span>typed (arg0, arg1)](#apidoc.element.mathjs.typed.typed)
- description and source-code
```javascript
function typed(arg0, arg1) {
  "use strict";
  var name = 'typed';
  if (test0(arg0)) { // type: string
    if (test1(arg1)) { // type: Object
      if (arguments.length === 2) {
        return signature0(arg0, arg1); // signature: string, Object
      }
      if (arguments.length > 2) {
        throw createError(name, arguments.length, 2, arguments[2]);
      }
    }
    throw createError(name, arguments.length, 1, arguments[1], 'Object');
  }
  if (test2(arg0)) { // type: ...Function
    var varArgs = [arg0];
    for (var i = 1; i < arguments.length; i++) {
      if (test2(arguments[i])) {
        varArgs.push(arguments[i]);
      } else {
        throw createError(name, arguments.length, i, arguments[i], 'Function');
      }
    }
    return signature1(varArgs); // signature: ...Function
  }
  if (test1(arg0)) { // type: Object
    if (arguments.length === 1) {
      return signature2(arg0); // signature: Object
    }
    if (arguments.length > 1) {
      throw createError(name, arguments.length, 1, arguments[1]);
    }
  }
  throw createError(name, arguments.length, 0, arguments[0], 'string,Function,Object');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.typed.addConversion"></a>[function <span class="apidocSignatureSpan">mathjs.typed.</span>addConversion (conversion)](#apidoc.element.mathjs.typed.addConversion)
- description and source-code
```javascript
addConversion = function (conversion) {
  if (!conversion
      || typeof conversion.from !== 'string'
      || typeof conversion.to !== 'string'
      || typeof conversion.convert !== 'function') {
    throw new TypeError('Object with properties {from: string, to: string, convert: function} expected');
  }

  typed.conversions.push(conversion);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.typed.addType"></a>[function <span class="apidocSignatureSpan">mathjs.typed.</span>addType (type)](#apidoc.element.mathjs.typed.addType)
- description and source-code
```javascript
addType = function (type) {
  if (!type || typeof type.name !== 'string' || typeof type.test !== 'function') {
    throw new TypeError('Object with properties {name: string, test: function} expected');
  }

  typed.types.push(type);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.typed.convert"></a>[function <span class="apidocSignatureSpan">mathjs.typed.</span>convert (value, type)](#apidoc.element.mathjs.typed.convert)
- description and source-code
```javascript
function convert(value, type) {
  var from = getTypeOf(value);

  // check conversion is needed
  if (type === from) {
    return value;
  }

  for (var i = 0; i < typed.conversions.length; i++) {
    var conversion = typed.conversions[i];
    if (conversion.from === from && conversion.to === type) {
      return conversion.convert(value);
    }
  }

  throw new Error('Cannot convert from ' + from + ' to ' + type);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.mathjs.typed.create"></a>[function <span class="apidocSignatureSpan">mathjs.typed.</span>create ()](#apidoc.element.mathjs.typed.create)
- description and source-code
```javascript
function create() {
<span class="apidocCodeCommentSpan">  /**
   * Get a type test function for a specific data type
   * @param {string} name                   Name of a data type like 'number' or 'string'
   * @returns {Function(obj: *) : boolean}  Returns a type testing function.
   *                                        Throws an error for an unknown type.
   */
</span>  function getTypeTest(name) {
    var test;
    for (var i = 0; i < typed.types.length; i++) {
      var entry = typed.types[i];
      if (entry.name === name) {
        test = entry.test;
        break;
      }
    }

    if (!test) {
      var hint;
      for (i = 0; i < typed.types.length; i++) {
        entry = typed.types[i];
        if (entry.name.toLowerCase() == name.toLowerCase()) {
          hint = entry.name;
          break;
        }
      }

      throw new Error('Unknown type "' + name + '"' +
          (hint ? ('. Did you mean "' + hint + '"?') : ''));
    }
    return test;
  }

  /**
   * Retrieve the function name from a set of functions, and check
   * whether the name of all functions match (if given)
   * @param {Array.<function>} fns
   */
  function getName (fns) {
    var name = '';

    for (var i = 0; i < fns.length; i++) {
      var fn = fns[i];

      // merge function name when this is a typed function
      if (fn.signatures && fn.name != '') {
        if (name == '') {
          name = fn.name;
        }
        else if (name != fn.name) {
          var err = new Error('Function names do not match (expected: ' + name + ', actual: ' + fn.name + ')');
          err.data = {
            actual: fn.name,
            expected: name
          };
          throw err;
        }
      }
    }

    return name;
  }

  /**
   * Create an ArgumentsError. Creates messages like:
   *
   *   Unexpected type of argument (expected: ..., actual: ..., index: ...)
   *   Too few arguments (expected: ..., index: ...)
   *   Too many arguments (expected: ..., actual: ...)
   *
   * @param {String} fn         Function name
   * @param {number} argCount   Number of arguments
   * @param {Number} index      Current argument index
   * @param {*} actual          Current argument
   * @param {string} [expected] An optional, comma separated string with
   *                            expected types on given index
   * @extends Error
   */
  function createError(fn, argCount, index, actual, expected) {
    var actualType = getTypeOf(actual);
    var _expected = expected ? expected.split(',') : null;
    var _fn = (fn || 'unnamed');
    var anyType = _expected && contains(_expected, 'any');
    var message;
    var data = {
      fn: fn,
      index: index,
      actual: actual,
      expected: _expected
    };

    if (_expected) {
      if (argCount > index && !anyType) {
        // unexpected type
        message = 'Unexpected type of argument in function ' + _fn +
            ' (expected: ' + _expected.join(' or ') + ', actual: ' + actualType + ', index: ' + index + ')';
      }
      else {
        // too few arguments
        message = 'Too few arguments in function ' + _fn +
            ' (expected: ' + _expected.join(' or ') + ', index: ' + index + ')';
      }
    }
    else {
      // too many arguments
      message = 'Too many arguments in function ' + _fn +
          ' (expected: ' + index + ', actual: ' + argCount + ')'
    }

    var err = new TypeError(message);
    err.data = data;
    return err;
  }

  /**
   * Collection with function references (local shortcuts to functions)
   * @constructor
   * @param {string} [name='refs']  Optional name for the refs, used to generate
   *                                JavaScript code
   */
  function Refs(name) {
    this.name = name || 'refs';
    this.categories = {};
  }

  /**
   * Add a function reference.
   * @param {Function} fn
   * @param {string} [category='fn']    A function category, like 'fn' or 'signature'
   * @returns {string} Returns the function name, for example 'fn0' or 'signature2'
   */
  Refs.prototype.add = function (fn, category) {
    var cat = category || 'fn';
    if (!this.categories[cat]) this.categ ...
```
- example usage
```shell
...
 *                              false (default), output type can vary depending
 *                              on input values. For example 'math.sqrt(-4)'
 *                              returns 'complex('2i')' when predictable is false, and
 *                              returns 'NaN' when true.
 */
function create (config) {
  // create a new math.js instance
  var math = core.create(config);
  math.create = create;

  // import data types, functions, constants, expression parser, etc.
  math['import'](require('./lib'));

  return math;
}
...
```

#### <a name="apidoc.element.mathjs.typed.find"></a>[function <span class="apidocSignatureSpan">mathjs.typed.</span>find (fn, signature)](#apidoc.element.mathjs.typed.find)
- description and source-code
```javascript
function find(fn, signature) {
  if (!fn.signatures) {
    throw new TypeError('Function is no typed-function');
  }

  // normalize input
  var arr;
  if (typeof signature === 'string') {
    arr = signature.split(',');
    for (var i = 0; i < arr.length; i++) {
      arr[i] = arr[i].trim();
    }
  }
  else if (Array.isArray(signature)) {
    arr = signature;
  }
  else {
    throw new TypeError('String array or a comma separated string expected');
  }

  var str = arr.join(',');

  // find an exact match
  var match = fn.signatures[str];
  if (match) {
    return match;
  }

  // TODO: extend find to match non-exact signatures

  throw new TypeError('Signature not found (signature: ' + (fn.name || 'unnamed') + '(' + arr.join(', ') + '))');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.mathjs.typed.signatures"></a>[module mathjs.typed.signatures](#apidoc.module.mathjs.typed.signatures)

#### <a name="apidoc.element.mathjs.typed.signatures.Object"></a>[function <span class="apidocSignatureSpan">mathjs.typed.signatures.</span>Object (signatures)](#apidoc.element.mathjs.typed.signatures.Object)
- description and source-code
```javascript
Object = function (signatures) {
  var fns = [];
  for (var signature in signatures) {
    if (signatures.hasOwnProperty(signature)) {
      fns.push(signatures[signature]);
    }
  }
  var name = getName(fns);

  return _typed(name, signatures);
}
```
- example usage
```shell
n/a
```



# misc
- this document was created with [utility2](https://github.com/kaizhu256/node-utility2)
